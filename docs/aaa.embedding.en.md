# AAA External Runtime Embedding Guide

> The canonical package-owned embedding contract moved to [`template/pallets/aaa/EMBEDDING.md`](../template/pallets/aaa/EMBEDDING.md).

`pallet-aaa` carries its integration guide beside the public adapter API so the document ships in the Cargo archive and remains owned by the reusable package. The [AAA Specification](./aaa.specification.en.md) continues to own portable semantics, while [AAA Architecture](./aaa.architecture.en.md) describes the current DEOS runtime binding.
