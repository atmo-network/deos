# DEOS Backlog

> Open framework work only. Specifications own normative behavior; code and tests own implementation and regressions; generated artifacts own resource bindings; Experiment Records own decision evidence. `BACKLOG.md` owns remaining work, priorities and release gates. Remove completed work from the active list without deleting its evidence. Do not grow a chronological implementation diary inside a task.
>
> Pre-`1.0`: no DEOS network launches before `1.0`. The `0.7.x` line is fresh-genesis. Published tags and historical observations remain immutable. Breaking authoring, storage, fee and scheduling changes must be explicit. This plan authorizes continuation of `0.7.27`, not publication, force-push, tag replacement or a second permanent execution engine.

## DEOS 0.7.27 — First Canonical Resident Actor and Parked-Balance Activation

**Planning basis:** branch `0.7.27`, reconciled at commit `5d771736e166ee623858ed910767df58d715c3b8`, tree `cf4b6973b665aa0431cf27b05fe4ed7443caa2db`. The published historical baseline remains `v0.7.26` at `c16675edf03e3af0d6dcf3080a31c64b49765a5b`; historical Experiment evidence remains immutable. [R1–R3]

**Replacement scope:** replace the active `0.7.27` backlog with this continuation plan. Retain existing N0–N7 identities and their applicable evidence; add only **N3.9 — Parked-Balance Mode Selection and Delivery**. An unchecked item below names remaining, changed or integration work, not a request to repeat its already proved subclaims. No historical experiment is renumbered or bulk-invalidated.

**Primary near-term outcome:** one ordinary User Actor, created and funded through supported public runtime paths, actually executes a multi-Step Contract through the canonical persistent service ring, retains membership across adjacent-round Steps and retry, reads fresh balances, preserves committed effects, and uses no legacy placement authority.

**Next outcome:** a recurring Actor captures its final balance, parks, ignores subthreshold changes, and wakes through a bounded runtime-owned path only after a qualifying parked-period change. The default candidate observes net balance movement from the fixed parking baseline with a floor of **at least 100 times the watched asset's existential/minimum balance**. If that trigger cannot meet its correctness, coverage and cost contract, select the explicitly scoped alternative of verified incoming credits to parked Actors, including a source whitelist. Do not substitute generic polling-on-any-spendability-change for the requested trigger.

**Release outcome:** finish the supported current-state/ring/wake/lifecycle machine; close every known applicable correctness and resource blocker; remove legacy service authority; bind and measure the actual implementation. A first working Actor is the next decisive milestone, not the whole release.

---

## 0. Current Reality and Immediate Blockers

### 0.1 Reuse what exists; do not count it as a complete engine

| Surface | Current evidence classification | Required next connection |
| --- | --- | --- |
| Current-state contract and round oracle | **Decision accepted; implemented and tested in their current scopes; final release evidence incomplete.** Amounts are `Fixed` / `Percent`; parked-balance semantics and next-encounter insertion are ratified. | Extend the independent model through the complete production V1/V2 traces and retain exact-tree evidence. |
| Canonical semantic/process/Service authority | **Implemented locally and production-reachable for Manual, AddressEvent, ObservationChange, AtTime and Cadenced publication; scalar and homogeneous-pair ObservationCrossing fire/rearm/coalesced/placed publication; initial/resumed publication; successful post-attempt retention; and canonical zero-Step/effectful terminal retirement. Generated owners exist for those covered boundaries; no complete end-to-end V1 proof.** | Retire the legacy FIFO consumer atomically and close the remaining canonical lifecycle/deadline/park paths. |
| Mandatory execution and retry | **Canonical Service executes bounded zero-Step, effectful completion and Block-deadline retry paths in production; the mandatory hook now proves one public User Actor across a committed prefix, Q1, genuine `FundingUnavailable`, exact Block deadline recovery and exactly-once completion without legacy control recreation, plus a current `Percent` resolution that changes with an inter-Step external credit and a recovered temporary-failure Attempt that re-resolves `Percent` against a credited balance, and a retained-Idle no-work encounter that advances the bounded cursor without executing a Step or recording an attempt, but the hook still retains the legacy FIFO drain.** | Make Service the sole ordinary drain and extend the public witness through the remaining V1 resource assertions. |
| Deadline/Park/Pending | **Implemented locally and production-reachable in bounded deadline-frontier and retry slices; generated-resource-bound only for the covered branches; V2 round trips absent.** | Close complete Service/Deadline/Park destinations and prove V1 retry plus V2-B/V2-O. |
| Oracle publication | **Producer, ObservationChange and all covered ObservationCrossing fire/rearm/coalesced/placed-fire shapes are production-reachable with canonical generated owners; final Oracle profile evidence remains incomplete.** | Close one complete revision-safe producer-to-consumer profile. |
| Lifecycle and cleanup | **Canonical Service terminal outcomes atomically finalize semantic authority and retire their exact generation-bound ring/process residence; focused rollback and cleanup coverage exists. Other lifecycle paths and final generated-resource evidence remain incomplete.** | Convert the remaining lifecycle publications and prove generation-safe cleanup/debt bounds after V1. |
| Performance | **Workloads decided; standalone carriers measured; whole-service result absent.** | Instrument V1 first; defer W1–W12 and exact-Wasm comparison until the hard cut. |

This table reconciles repository reality at the planning basis above; focused tests cover the later normative round/oracle amendment without upgrading unchanged implementation claims. `Accepted` remains decision evidence only. The active critical path is V1's single atomic semantic-writer/process/ring cutover; the exact immediate blocker is that every supported Publish/Replace/Remove caller and its full Weight owner must switch together before legacy placement can be removed. [R2–R8]

### 0.2 Blocking obligations

| ID | Exact blocker | Closure owner | Required exit evidence |
| --- | --- | --- | --- |
| B1 | ObservationCrossing and several lifecycle/publication destinations can still recreate legacy control/FIFO authority; mandatory service still drains FIFO after Service. | N2.4, N3.5, N5.1 | Every supported producer and destination publishes canonical residence; Service is the sole ordinary drain; absence checks reject legacy recreation. |
| B3 | No single production-shaped User Actor witness covers public creation, funding, resident adjacent Steps, genuine typed temporary failure, deadline retry and completion. | N2.1–N2.4, N3.1–N3.6, N5.2 | One V1 integration witness with exact economic outcome, residence assertions and operation ledger. |
| B4 | The ratified parked-balance contract is not yet implemented through a bounded complete source/review path. | N2.5, N3.9 | One bounded complete source/review implementation of the fixed final baseline and `100 × ED` floor; no busy tracking. |
| B5 | Net balance cannot identify real incoming credits or whitelisted senders. | N3.9 | Conditional verified-credit implementation if the default mode fails its frozen criteria; never infer provenance from net balance. |
| B6 | Oracle producers are connected ahead of scan/check/service consumers. | N3.1, N3.4, N3.7 | Bounded publication-to-consumption round trip, or explicit coherent gating before any shipping state. |
| B7 | Generation-safe cleanup and its resource/debt limits are not yet fully executable. | N4.1–N4.3, N5.2–N5.3 | Mutation/sweep/capacity traces and measured bounded maintenance; no reliance on idle-only cleanup. |
| B8 | Retained heaps, new selectors and callbacks still carry deep/full/rollback coverage obligations. | N5.2–N5.3, N7.2 | Reachable-domain coverage and complete composed charging, or verified elimination of the path and coverage of its replacement. |

Do not turn these eight blockers into eight new programmes. Each needs its smallest connected closure. Preserve the existing `cancel_run` close-allowance correction and requalify it against the retained lifecycle rather than reopening its old omission by accident. [R9]

### 0.3 Work order

```text
Intake + narrow semantic/round correction + known safety triage
                         |
                         v
V1: public User creation -> canonical semantic owner -> actual ring service
    -> several committed Steps -> real retry -> completion
                         |
                         v
V2-B: completed recurring Actor -> fixed balance baseline -> Park
      -> qualified parked change -> one check -> Live -> new Cycle
                         |
              +----------+----------+
              |                     |
              v                     v
V2-O: existing Oracle          lifecycle/reclaim
producer -> consumer          and full supported cutover
              |                     |
              +----------+----------+
                         v
whole-service workloads -> final resource/artifact closure -> review -> release
```

V1 must not wait for universal balance notification coverage, a new Oracle framework, exhaustive cleanup tuning, an alternate heap or a history-wide experiment migration. Its supported lifecycle and failure suffixes must nevertheless be correct and funded. A new helper is progress only when it discharges a named blocker on this path.

The semantic-authority prerequisite of the first V1 cohort is complete: `ActorSemanticStates` now owns dormant identity or active Identity/Hot/admission, while physical control placement is independently derived and strictly cross-validated. `ActorProcesses` still owns only generation, attempt guard, status and residence, and no production caller publishes it or enters `ServiceNodes`; ordinary execution still drains the legacy paged FIFO. Therefore the next retained N2.1/N3.5 slice must connect process/ring publication and mandatory service together with its composed resource and Weight owners; do not invent a second semantic reader, weaken physical validation or claim direct-state fixtures as ordinary V1 closure.

The cutover inventory has one semantic writer cohort, not one storage declaration. Every row below is part of the first retained caller/resource boundary; helpers or tests that bypass these composition roots do not discharge it.

| Semantic transition | Supported composition roots | Generated Weight owner |
| --- | --- | --- |
| Publish dormant | Dormant user/System creation | `create_user_actor*`, `create_dormant_system_actor` |
| Publish active | Active user/System creation, including explicit owner-slot and sovereign-ID variants | `create_user_actor*`, `create_system_actor*`, Crossing creation branch where applicable |
| Replace dormant → active | `activate_actor` through `do_activate_actor` | `activate_actor` plus the applicable trigger-index branch |
| Replace active → active | Contract replacement; lifecycle, Trigger, run, retry, latch, wakeup and service mutations in dispatch, detector, prepass and mandatory service paths | Owning dispatch Weight plus the generated trigger/prepass/service unit that performs the mutation; `update_contract` and close-substitution composition are included |
| Replace active → dormant | `deactivate_actor` through cancellation, queue/wakeup cleanup and contract removal | `deactivate_actor` plus its complete cleanup suffix |
| Remove active | Owner close, sweep, expiry, completion, apoptosis and every terminal execution path converging on `finalize_actor_loaded_inner` | `close_dispatch_weight_upper` and the initiating dispatch/prepass/service owner |
| Remove dormant | Dormant close through `close_inactive_actor` | `close_dispatch_weight_upper` |

The first physical-authority cut cannot be scoped to Manual or to initial publication. The production call graph has three convergence owners which must change in one retained cohort: `commit_activation_plan_inner` owns a latched dormant-to-active admission, `publish_active_state_on_legacy_fifo` owns initial and resumed placement (including `schedule_next_work_with_authority` capacity fallback), and `finalize_next_residence_on_legacy_fifo` owns every post-attempt retained, deferred or terminal destination. `prime_frame_actor_schedule`, `cancel_run_internal_loaded`, wakeup/deadline return, lifecycle mutation and trigger coalescing either enter those owners or mutate their currently published authority. Cutting only create/Manual would leave update, cancellation or reactivation able to recreate `ActorControlLocators`; cutting only the mandatory consumer would strand legacy publications. The smallest coherent implementation boundary is therefore all active-authority Publish/Replace/Remove callers plus the mandatory consumer, not one trigger profile.

The retained cohort has four ordered acceptance units inside that single atomic boundary. First, replace active publication with generation-bound `ActorProcesses` + `ServiceNodes` or the existing canonical Deadline/Park residence, including B+1 eligibility and destination-capacity preflight. Second, make activation, cancellation, lifecycle and wake return mutate `ActorSemanticStates` and that residence without reconstructing a control cell. Third, make `on_idle` run canonical Service as the sole Actor execution drain and remove the FIFO cutoff/drain branch. Fourth, delete active-cell restoration/projection and regenerate the composed create/activate/update/deactivate/close/service Weight owners. Tests must reject any successful active transition that leaves `ActorControlLocators`, `ActorUnsignaledControlCells`, Ready/Waiting frame membership or a FIFO ticket. These units may be developed sequentially in one working tree, but no publish-only intermediate may be committed or advertised as production authority.

The semantic map, compare-and-replace writer, lifecycle callers, loader and try-state guards have crossed over, but active physical cells deliberately retain mirrored Identity/Hot/admission until the service carrier and generated resource owners replace that projection coherently. Effectful Step, zero-Step and opening/running StopCycle execution select one typed carrier-neutral close-or-publish residence intent containing only authoritative state plus current-versus-cursor-zero resources. Staged canonical transactions now execute zero-Step retain/close, effectful completion/retain, adjacent-round retry and later block-deadline retry outcomes with semantic or terminal commit before ring advance/unlink. The later-retry path preflights its exact generation-bound deadline slot before effects, atomically transfers Service residence after suspended-state commit, and returns genuinely due work to Service exactly once; unsupported continuation and tick-deadline destinations still fail closed under whole-attempt rollback. Parking now has generation/plan-bound Service-to-Park transfer that preplans and publishes timed reviews into the shared C32 deadline index in the same transaction, complete retained source plans, exact indexed due-review traversal/publication, and one bounded Oracle-availability interpreter that reads every retained typed source once before atomically selecting indexed complete-plan re-arm or canonical wake; ambiguous source state, revision races and destination refusal preserve deadline membership, Pending review and Park residence. These paths remain staged behind the weighted consumer cutover. Production initial activation, resumed publication and mandatory service still use the legacy physical boundary with their explicit unsignaled restoration, scheduling continuation, exhaustion closure, events, Weight and settlement semantics. The mandatory hook currently gives canonical Service one bounded head attempt and then drains the legacy FIFO in the same block; this is deliberate staging, not an accepted dual-scheduler design. The cutover must therefore replace the `execute_cycle_to_cutoff{,_with_resources}` suffix and its `ActorReadyTail` cutoff, queue cleanup and starvation accounting together with initial/resumed/post-attempt publication; retaining that suffix after canonical publication would execute one Actor through two authorities. Deadline-frontier service already returns canonical members before either drain and remains part of the retained housekeeping prefix. The remaining executable cut is complete process/ring publication and residence mutation → mandatory service → legacy scheduler retirement → physical-field removal → benchmarks and generated bindings. Intermediate helper-only states do not close N2.1 or N3.5.

The first atomic carrier cut has one implementation seam, not a publish-only precursor. Replace the `ActorReadyTail` prepass snapshot and `live_queue_head` discovery loop with `begin_service_round` plus bounded `consider_service_head` encounters; adapt mandatory service to consume the captured ring head; route its `NextResidence` result to process/ring retention, deadline transfer or retirement; and switch initial/resumed publication through the same transaction before deleting the FIFO path. The retained FIFO adapter now loads an active generation-bound canonical `ActorRef` and compiles the validated legacy Identity/Hot/admission plus temporary eligibility/resource fields into one `LoadedServiceEntry`; `service_live_queue_entry` no longer receives an `ActorControlCell`. This narrows the replacement seam without publishing a second authority, but queue discovery/consumption and those temporary fields remain legacy-owned, so changing publication alone would still create inert ring members and lose executable resource ownership. The mandatory hook now settles External-phase housekeeping before entering FreshDrain, then pre-admits the generated round selector plus the selected zero-Step or effectful control/effect and maximum retained/removal suffix against the same `BlockResourceState` used by legacy drain. One eligible zero-Step or successful effectful head executes through the carrier-neutral transaction and settles actual Actor Control and Actor Drain Effect usage before ring advance/retirement; Weight or resource refusal restores both storage and local resource state without consuming the encounter or effect. Effectful later-retry/deadline placement remains deliberately unconsumed until its exact destination and settlement suffix joins this same boundary. Generated Weight must still cover every retained publication/removal/deadline suffix before the hook can advertise the ring as general service authority.

The cutover field map is now explicit. `ServiceNode::eligible_from` replaces FIFO `eligible_at`: initial publication and every reentry set B+1, while a retained adjacent-round continuation keeps its node and therefore needs no new eligibility timestamp. Identity/Hot/admission load from `ActorSemanticStates`; current execution resources derive from the admitted current Step, while zero-Step uses its generated completion envelope. A Weight, block-resource, fee-collection or invariant refusal leaves the same ring head, node, process and round frontier untouched. Only a committed service transaction may consume the encounter: retained Live/Pending work updates the process and advances to the captured successor without unlink/reinsert, longer deferral first secures Deadline residence then unlinks, and close first completes terminal cleanup then unlinks/retires. Ring advance may not precede any destination or terminal commit.

The first code prerequisite is complete: `plan_next_work_loaded` selects typed `Service(kind)`, exact-block `Wakeup` or `Disabled` process intent from current state, supplied Run authority, time and cutoff without touching storage. `plan_process_destination` pairs that intent with the active generation-bound `ActorRef`, preflights an absent/capacious structurally linked Service destination or the exact canonical Deadline slot, and constructs a complete valid `ActorProcess` plus its concrete insertion authority. `plan_actor_publication` composes that exclusive process residence with the independent optional Tick-trigger deadline, updated semantic Hot pointer and generated current-Step resources. Process/Park and Trigger deadlines have distinct reverse owners, and Trigger insertion validates its exact semantic Tick pointer without requiring process Deadline residence. The composite preflight rejects zero/stale generation, semantic/Contract/admission disagreement, mismatched Run presence, stale generated current-Step resources and any partial canonical process, Service or deadline publication. Its transactional committer atomically publishes semantic Hot, `ActorProcess`, the exclusive process carrier and optional Trigger deadline; one suspended Cadenced regression proves simultaneous Block-process and Tick-trigger membership. Paused-without-window retains exact Owner/OwnerPaused basis; active-unlatched retains Protocol/Idle disablement rather than an invalid residence-less Serving process. One inert transactional handoff now verifies the exact legacy primary, Run and generated resources, preflights the complete destination, removes legacy authority and invokes the composite publisher under one rollback boundary; zero-Step Service and simultaneous process/Trigger deadline regressions prove successful rehoming and exact legacy-root restoration after a post-removal insertion failure. The same rollback boundary accepts a separately validated lifecycle successor, and a canonical-to-canonical transition removes exact process/Trigger residence before republishing it: one regression now proves Deadline-plus-Cadenced pause to Disabled, late-refusal whole-root restoration, and Active-plus-latched resume to one Service process with an independent Trigger deadline. The handoff and lifecycle transition remain unreachable from production, while `schedule_next_work_loaded` preserves existing behavior. Canonical terminal removal now validates exact generation, semantic/Contract/admission/Run authority, independently removes Trigger deadline plus Disabled/Service/Deadline process residence, and converges on ordinary custody-neutral finalization under whole-root rollback; Park retirement remains owned by its complete-plan cleanup path. Canonical fee-bearing activation now validates useful-readiness capacity and exact Actor identity, atomically replaces Disabled publication with its latched Service successor, charges the Trigger Fee only inside the same transaction, emits the occurrence event only after payment and restores the complete storage root after late collection refusal. All shared publication, lifecycle, activation and terminal mutation seams are therefore executable but inert. Cutover reassessment found two concrete omitted integration invariants: the three publication adapters are not the whole writer cohort, and the nominal canonical loader still rejects every active semantic record without `ActorControlLocators` before it can inspect `ActorProcesses`. Manual, AddressEvent, ObservationChange, ObservationCrossing, AtTime and Cadenced occurrence paths still load or mutate legacy primary cells directly, and their busy deferred-latch branches must preserve the current canonical process residence while replacing semantic Hot state; otherwise initial publication plus canonical service would leave Trigger writers dependent on deleted FIFO authority. The next retained cohort must therefore switch `load_actor_semantic_state` / `load_actor_state_with_admission` and all downstream control/service loaders to generation-bound semantic/process/carrier validation while cutting these six Trigger families together with initial/resumed/post-attempt publication and mandatory service. Current-Step resources derive from canonical Contract/admission/Run authority rather than a removed control cell. The mandatory selector now loads the generation-bound semantic/process/node tuple and dispatches the node's actual `ServiceResidenceKind`; a Pending zero-Step regression proves it no longer rejects useful-readiness membership as hardcoded Live. One staged all-residence loader rejects legacy authority and validates the exact generation-bound Service, Deadline, Parked or carrier-free Disabled tuple. One storage-neutral occurrence planner now fixes the shared six-family transition: Idle carrier-free Disabled or Deadline/Park readiness becomes Serving Pending at B+1, duplicate latches are no-ops, and Running/Suspended Service/Deadline work mutates only the deferred semantic latch while preserving residence. Canonical Manual occurrence admission now reaches public mandatory Service execution for effectful completion, non-adjacent retry and zero-Step auto-close; focused regressions prove refusal preserves authority, Deadline reentry occurs exactly once, and terminal cleanup precedes singleton Service unlink and retirement without manual ring publication or low-level execution scaffolding. AddressEvent ingress now commits generation-bound semantic/process authority directly: first occurrence publishes one Pending Service at B+1, duplicate latches remain singleton and uncharged, Running/Suspended ingress preserves the live residence, insufficient or failed collection leaves Disabled authority intact, and the benchmark plus broad AddressEvent regression family use canonical Service timing rather than FIFO tickets. ObservationChange now uses the same authority for every ordinary and terminal fanout occurrence, advances bounded dirty pages without FIFO/wakeup cohorts, preserves absent scalar control and fee/detector semantics, and benchmarks Pending Service rather than legacy tickets or wakeups. Production-shaped suspended-process tests now advance through B+1 canonical Service independently of the retained pre-cutover publication fixture, and rollback-only CurrentRun simulation selects exact generation-bound Service/Block-deadline authority without reconstructing a legacy control cell. The canonical deadline frontier now classifies generation-bound temporal Trigger ownership independently from process deadlines, consumes due Cadenced sources, rearms them canonically, and publishes one paid Pending Service occurrence without reconstructing legacy control authority. The remaining retained cut must finish ObservationCrossing plus initial/resumed/post-attempt publication and legacy scheduler retirement; no further helper-only prerequisite is accepted.

---

## 1. Continuation Contract

### 1.1 Preserved decisions

- Runtime-owned autonomy; no keepers, external intent executors or required bots.
- Bounded multi-block Contracts; at most one committed Step per Actor per block; earlier committed effects survive a later Step failure.
- Current predicates and exactly `Fixed(value)` / `Percent(perbill)` on valid Task surfaces. `Percent(100%)` means all current Available capacity, respecting current fees, ledger restrictions and protected minima.
- `AbortCycle`, `ContinueNextStep` and bounded `RetryLater`. Retry retains the same generation/Cycle/cursor and reads fresh dynamic inputs. Admission refusal is not an executed failure.
- One open Cycle and one logical current service obligation. No queued future Cycle, busy funding history or retry-counter reset caused by external hints.
- Persistent cyclic service, generation-bound membership and one block-round identity. No cost/class priority and no bypass of an eligible resource-blocked head.
- Parked is nonterminal; Paused/Disabled is separately authorized; Retired cannot be revived. Contract mutation and old-generation sweeping preserve custody and new-generation state.
- One canonical semantic source plus derived residence indexes. Parking does not move the Contract or custody into a second store.

The current-state section of the specification remains the starting owner; reconcile superseded later sections before release rather than asking implementers to interpret two competing contracts. [R3]

### 1.2 Partial-round ordering

The ratified rule is **append behind all current residents as viewed from the persistent next-encounter cursor**. The normative specification, independent list model and selected carrier's insertion-before-cursor behavior agree on this outcome.

Required distinguishing trace:

```text
initial order A -> B -> C; A receives an admitted turn; next cursor = B
D is admitted before the next block; no more turns occur in this block
next block expected order: B -> C -> A -> D
```

The inspected model's fixed-array `push_back` instead yields `B -> C -> D -> A`. Add this counterexample before changing either side. If the owning specification deliberately selects the latter policy, change the carrier accordingly and record that choice; the two cannot remain different. [R4–R5]

New or reentered memberships in B stay ineligible until B+1. Deletion cannot donate a turn. Reentry or generation replacement cannot reset the same-block guard. Resource refusal preserves the same candidate through a later pass and the next block. Do not use initial ring length, a removable sentinel, or production-wide membership copying as the proof.

### 1.3 Parked-balance monitoring is not busy-state tracking

Balance activation exists only for a configured, recurring, **idle parked** Actor. A timed review of an idle parked balance watch is logically still this parking obligation even if represented by the deadline carrier.

No balance-trigger work may create a future Cycle while the Actor is Live, Running, retrying or Sleeping for an open Cycle. Current Steps continue to read real balances normally. A balance change during a retry can make that same Step executable, but does not wake it ahead of its retry policy or create another Cycle.

A cheap producer-side lookup may be necessary to discover that no parked registration exists. This bounded lookup still has a resource owner. “No busy tracking” means no Actor-specific baseline updates, credit accumulation, membership changes, full activation evaluation or Trigger fee for the busy Actor; it does not falsely promise zero cost to the underlying ledger operation.

Owner-paused/disabled and retired generations ignore ordinary balance activation. Other explicitly authored activation modes retain their own rules; an internal review timer must not secretly bypass the balance threshold and become an unconditional periodic trigger.

---

## 2. Parked-Balance Trigger: Required Semantic Amendment

The following requirements implement the task owner's clarification and the ratified N0.2 choices. They freeze one default and one conditional fallback before decision measurements; do not keep additional undocumented interpretations in code.

### 2.1 Fixed final baseline

For each explicitly watched asset/surface `a`:

```text
B_anchor[a] = authoritative final watched balance
T[a]        >= 100 * ED[a]
```

Capture `B_anchor` after the last committed Step effect and actual fee settlement, after releasing transient Attempt reservations, and after every balance-affecting finalization operation covered by the transition. Capture only when the configured recurrence requires another balance-activated Cycle. Do not enumerate all sovereign assets. The watched set comes from a bounded authored/derived plan.

Bind the anchor to Actor id, semantic generation, plan/configuration identity, a monotone parking episode and the exact asset/surface. This is wake metadata, **not** an Opening snapshot, a funding accumulator or a basis for future Task amounts.

If a completed Actor remains Live for one final permitted turn before parking, retain the completion anchor. A qualifying change between completion and installation of the parked registration must remain detectable; do not silently replace the anchor with the later balance and absorb the change. Alternatively, complete baseline capture and park publication atomically in the completion transition when its full cost is admitted.

Initial activation of a newly created or explicitly reconfigured Contract has no preceding completed Cycle. Define one explicit initial arm operation using the committed initialization balance. It must not mistake the initial balance for an incoming credit or retroactively replay deposits made while no parked registration existed.

Routine polls, subthreshold notifications, duplicate hints and negative checks must not move `B_anchor`. A new anchor is normally established by completion of the next admitted Cycle, or by an explicit authorized contract/rearm transition. Record any exceptional reset policy and its economic consequence.

### 2.2 Exact observed quantity and threshold

**Ratified default:** use absolute net change of the declared sovereign asset total-owned balance:

```text
delta[a] = abs_diff(B_now[a], B_anchor[a])
qualified = delta[a] >= T[a]
T[a] = max(authored_min_delta[a], checked_mul(100, ED[a]))
```

This interprets “balance change” as movement in either direction and “at least 100 times ED” as an inclusive boundary. The task owner did not separately choose increase-only behavior or per-credit versus cumulative incoming thresholds; freeze those distinctions explicitly. Do not silently convert the default to positive credits.

`B_now` must be bound to one named, authoritative host balance surface. Prefer the actual asset balance rather than transient Actor fee availability for this trigger; decide whether the host's free or total-owned balance best implements that contract and document it. **Do not equate balance, total-owned balance and spendable Available.** Effect amount resolution continues to use current Available independently. A hold/freeze change without a watched balance change is not automatically a balance trigger; a separately promised spendability/observation/time wake retains its own coverage obligation. [R10]

Use `ED[a]` in the same asset units as `B_now[a]`: the host's native existential balance for native currency and the authoritative asset minimum for another asset. Never multiply native ED and compare it with an unrelated asset balance. No price oracle is needed to interpret the threshold unless a separate explicit value-based policy is approved.

Unknown, zero, unrepresentable or changed asset minima need an explicit bounded admission/recertification outcome. Reject unsupported zero-floor profiles or require an explicitly governed positive floor; never silently admit a dust-sized threshold. Use checked/widened arithmetic, no unsigned underflow, silent saturation or overflow-to-zero. Bind configuration identity and define requalification when the minimum changes. On-chain configuration must never admit a threshold below the applicable floor.

The floor is a **wake magnitude**, not a fee, burned amount, storage deposit, proof of sender expenditure or guaranteed economic anti-spam cost. A party might recover transferred funds through an authored policy. Negative work, callbacks, notices and oscillation still need resource bounds and a payer.

### 2.3 Net change is not gross incoming value

The default compares against the fixed anchor, not against the most recent sample and not against a sum of absolute movements.

Examples at `T = 100 * ED`:

| Parked-period history | Default net-balance interpretation |
| --- | --- |
| `+60 ED`, then `+40 ED`, with no offsetting change | Qualifies at `+100 ED`; the first small update must not reset the baseline. |
| `+60 ED`, then `-60 ED` before a sampled check | Net zero; no required activation. |
| `+100 ED`, then a return to the anchor before any permitted observation | A transient may be missed in a declared sampled-current-state profile; do not claim historical-event detection. |
| A qualifying observation already retained as Pending, then balance falls back | Keep the check obligation; revalidate current applicability before starting a Cycle. A stale positive does not authorize an effect. |
| External funding during Running/retry | The Step sees current funds; no new balance activation, credit history or next-Cycle promise is created. |

With several watched assets, the first implementation should use bounded exact independent thresholds, with a declared `any qualified surface` rule unless the Contract explicitly requests a different bounded condition. Do not add quantities across unrelated assets. Shared hints must not activate unregistered assets/accounts.

### 2.4 Consume qualifying checks without oscillation or lost growth

A qualifying change creates **one** generation/episode-bound activation check. Repeated notices coalesce. The full current start condition is checked at its authoritative boundary; before Cycle start, revalidate it again or prove the carried facts remain fresh.

The first implementation must choose and test one negative-check/rearm rule. Required properties:

- An unchanged already-evaluated balance must not continuously enqueue checks merely because it remains outside the anchor threshold.
- A failed check must not silently erase a later change or rebase the final-cycle anchor.
- A further material balance movement that can enable the policy, or a separately watched condition becoming applicable, must get the promised reevaluation.
- Any last-evaluated revision/watermark is distinct from `B_anchor`, bounded, and explicitly priced.
- A transient threshold followed by a negative start result does not admit a second Cycle or reset retry history.

Choose between bounded notification invalidation and paid timed reevaluation according to the declared profile. A blanket `last_checked = now` or `dirty = false` is not a lost-wakeup proof. Avoid adding a general historical event log to answer this one question.

### 2.5 Timing and completeness under the revised contract

Do not require invalidation on every change to every possible execution predicate merely to implement the parked-balance delta trigger. Prove completeness for the **chosen wake contract** and separately for any other authored wake obligations.

A notification-driven implementation needs coverage of every supported mutation that changes the watched quantity. This may include direct transfers, protocol/Actor credits, XCM, issuance/burn, slashing, reaping/recreation or another asset-specific path. The list is closed by the actual host, not by a generic claim that every credit uses the same helper. No raw storage write may bypass a promised notification path.

A bounded timed review may implement the **same fixed-anchor threshold** when hooks are incomplete. It can miss intermediate net states and adds latency and polling cost; those are declared properties, not silent equivalence to complete event delivery. It is acceptable only if it meets the frozen correctness and whole-cost criteria. It must not become whole-population per-block polling or reset the baseline on each review.

### 2.6 Conditional fallback: verified parked-period credits with whitelist

If fixed-anchor net tracking cannot provide useful, affordable and adequately reliable activation on the intended host, N3.9 selects verified incoming-credit activation. This alternative is expressly authorized by the task owner; it does **not** authorize restoring the old future-Cycle machine or `PercentageOfLastFunding`.

For the fallback:

1. Install a registration only for the idle parked generation/episode. A dormant, busy, retrying, disabled or retired Actor does not accumulate activation credits.
2. Observe positive committed credits through certified host paths, including declared internal and cross-chain cases. A current net-balance difference cannot establish their source.
3. Apply an exact asset and bounded source whitelist at that boundary. An absent source, spoofable payload or unknown upstream sender never satisfies a concrete whitelist entry. Declare whether identity means the immediate certified payer/producer, not an inferred original user through intermediaries.
4. Reject duplicate/replayed callbacks and rollback all tentative activation evidence with the economic transaction. A self-transfer, charge/refund pair or balance-neutral ledger rearrangement must not become invented funding.
5. Use a bounded, threshold-capped sum of verified permitted credits during the current parked episode, with the same `100 × ED[a]` floor. Per-credit thresholding is not selected.
6. Keep only the bounded eligibility accumulator/covered authority necessary for one coalesced check, never an unbounded credit history. No credits from an open Cycle leak into the next parked episode.
7. Evaluate current conditions and spend current Available at execution. The whitelist governs **what wakes the Actor**, not ownership or earmarking of every token already at its sovereign account. A restricted-spending policy would be a separate feature.

Whitelist fallback may be the only supported balance-activation mode for a host if net tracking fails the gate. Keep only the selected production mechanism unless distinct supported use cases justify both. If neither meets the declared contract, report the exact blocking choice to the task owner; do not ship a silent generic-polling substitute.

---

## 3. Delivery Milestones and Non-Negotiable Evidence

### V1 — First ordinary canonical resident Actor (top priority)

V1 is closed only when a production-shaped runtime fixture does all of the following:

- Creates an ordinary **User** Actor through the real supported public dispatch, not direct insertion of `ActorProcesses`, ring nodes or semantic records.
- Funds custody and service costs through supported ledger paths. Recipients and asset accounts satisfy their real minimum-balance rules; no failed Transfer is counted as useful completion.
- Uses the real mandatory Actor service entrypoint with normal block/inherent context and generated path owners; no second test executor or `Weight::MAX` shortcut substitutes for admission.
- Runs a bounded multi-Step Contract with current `Percent` inputs. A normal external balance/market change between Steps changes a later resolution as declared.
- Produces a genuine supported temporary failure and then recovery through authoritative runtime state. Retry preserves cursor/Cycle/attempt counts and committed prefix. A mock-only synthetic success/failure switch is not sufficient for this integration claim.
- Demonstrates at least one adjacent-round retry remaining resident and one successful interior-Step progression without node allocation, unlink/reinsert or successor publication. Long retry Sleep/return receives its own V1 extension before claiming that branch complete.
- Completes the intended economic workflow with exact effects, fee outcomes and Q1/order trace; all lingering residence and cleanup work is reported.
- Leaves no legacy locator/control-cell/Ready-ticket owner for the Actor and invokes no legacy scheduling fallback. The candidate build must not run a second scheduler beside it to complete the path.

Choose the smallest existing Task/market fixture that supplies a real typed temporary failure; preserve its canonical adapter. Manual authorization or a positive supported current start check may start V1. **V1 does not depend on solving the parked-balance design first.**

A narrowly supported candidate checkpoint may explicitly reject not-yet-connected configurations, but cannot claim them implemented or route them through the old engine. All promised release profiles still have to be connected before N7.

### V2-B — Parked-balance round trip

Extend the actual V1 machinery, not a separate demo:

```text
last successful Step and fees
-> fixed final B_anchor and valid parking episode
-> subthreshold changes do not activate
-> cumulative net change reaches the declared threshold
-> one bounded check
-> current condition admitted
-> Live from the permitted next round
-> successful new Cycle
-> new final baseline
```

Include a deposit while busy, initial arming, a below-threshold negative review, a later qualifying change, Pending saturation, completion-to-park interleaving and generation replacement. If verified-credit fallback is selected, replace only the signal qualification step and add allowed/disallowed/unknown-source cases.

### V2-O — Existing Oracle producer-to-consumer path

Close one actual supported Oracle feed through registration, unchanged-value freshness update, value/lifecycle change, source scan, Pending, current check and Live return. Include false results and time/age expiry. Improving other Oracle cases may proceed after V1/V2-B, but every attached production callback must already preserve boundedness, transactionality and durable work.

Do not attach a producer to an indefinitely undrained new source list. Either finish the necessary consumer or explicitly gate the new publication coherently while that path is unavailable. Capacity behavior must not silently lose checks or poison unrelated work; any intentional upstream refusal is named and tested.

### Final release closure

V1, V2-B and V2-O are scoped milestones. The remaining supported modes, full domain resource coverage, cleanup, client/API bindings, W1–W12 and final review still gate release. One passing vertical slice does not certify maximum population, general host completeness or full semantic conformance.

---

## N2 — Complete the Actual Current-State Execution Path

- [ ] **N2.1 / Minimal Canonical Continuation.** Execute as the continuation slice of the joint N2.1/N2.4/N3.5 atomic cutover. Connect the current Run/Cycle representation to ordinary new-engine execution only when create/activate publish the canonical semantic/process/ring authority and the same change binds their composed resources. Prove Idle/open-run consistency, exact generation/Contract binding, cursor advancement, completion, zero-Step distinction and one current obligation. Keep independent semantic tests already passed; fill the missing production entrypoints without weakening the pre-cutover absence guard or adding a temporary dual reader. **Exit:** the V1 ordinary Actor advances and completes without direct test-state construction or legacy placement.

- [ ] **N2.2 / Live Amount and Predicate Evaluation.** Retain the implemented current-only evaluator. Verify `Percent` and `Fixed` through V1 public execution, including balance changes between Attempts, fee-native reservations, protected minima, shares and non-native assets. The public mandatory hook now resolves two adjacent `Percent` Steps against current tracked-balance authority, with one Step committing a running cursor and an inter-Step external credit raising the later resolution, and it re-resolves a `Percent` swap on a genuinely recovered temporary-failure Attempt against the credited current balance; those subclaims are covered, and the public `Unstake` path now witnesses `Percent`-of-current-shares resolution against the current share balance across adjacent canonical rounds; the migrated `tests/market_tasks` witnesses additionally prove `Fixed` resolution and asset-minimum preservation (`preserve_spend_keeps_sufficient_asset_minimum`) through the same public `manual_trigger` execution, while fee-native reservations and the remaining non-native surfaces stay open. Keep the parked balance anchor entirely outside Task amount resolution. Reject removed authoring forms rather than reinterpreting them; do not reintroduce Opening or LastFunding state. **Exit:** current economic results and boundary outcomes agree with the declared surfaces and independent expected arithmetic.

- [ ] **N2.3 / Error Policy and Retry Continuity.** Connect all retained error policies to canonical residence transitions. Canonical mandatory Service now derives the exact Block destination for a genuine non-adjacent `RetryLater`, preflights it before execution, and atomically commits the suspended Run into the deadline carrier; Weight, resource or destination refusal preserves the prior ring/process/effect state. Its generation-bound current-Step authority no longer carries or requires a legacy queue ticket, and the production hook proves exact Block recovery, fresh funding, shared FreshDrain resource settlement and exactly-once completion without a legacy queue entry. Adjacent-round retries retain the member. Canonical `AbortCycle` now commits the failed Idle semantic state, exact attempt evidence and resource settlement before advancing the retained live Service member; refusal remains atomic. A completed or aborted member is retained in the ring as Idle `Service(Pending)`, and a later round that revisits it with no pending occurrence now performs an explicit no-work encounter: the bounded cursor advances to the retained successor without opening a pipeline, executing a Step, or recording an attempt, so a subsequently inserted independent actor becomes serviceable; `advance_idle_service_head` owns the cursor-only advance and the focused `canonical_service_idle_no_work_head_advances_without_an_attempt` regression pins it. Canonical `ContinueNextStep` now commits failed Step evidence into the Running Run, advances exactly one cursor without repeating the failed prefix, retains live Service residence under shared resource settlement, and completes the following Step in a later eligible round. A permanent adapter failure under `RetryLater` now bypasses deadline parking, commits its failed evidence and Idle semantic state, preserves the failure streak, settles shared resources and advances the retained live member; acceptance is explicitly gated by permanent failure evidence. Local `RetryAttemptsExhausted` now accepts only the carrier-neutral terminal residence, preserves the prior committed retry evidence, atomically removes semantic, Run, control, deadline and Service topology, retains terminal query evidence, settles shared resources and rolls every owner back on resource refusal. Global `ConsecutiveFailures` now closes through the same exact canonical terminal residence even when the retry policy had preflighted a later deadline, preserves the prior attempt and streak until terminal commit, removes all live topology, retains terminal query evidence, settles shared resources and rolls every owner back on resource refusal. Neither busy credits, Manual requests, source updates nor parking metadata reset attempts. Admission/resource refusal does not consume an execution attempt. Preserve prefix effects and exact local/global exhaustion semantics. Public create/Manual plus the unmodified mandatory hook now proves a committed prefix, same-round Q1 refusal, genuine `FundingUnavailable`, exact Block deadline recovery after fresh tracked-asset funding, exactly-once completion and persistent absence of legacy control authority. The hook still drains the legacy FIFO after canonical Service, so sole-drain retirement remains coupled to the joint N2.1/N2.4/N3.5 cutover. **Exit:** after public create/activate publish canonical authority, V1 retry succeeds after genuine recovery without repeating earlier effects; long-delay, abort, continue and permanent-error paths have their own executable closures.

- [ ] **N2.4 / Loaded Transition Context and Economic Owners.** Continue the joint N2.1/N2.4/N3.5 atomic cutover from the implemented stable semantic and Contract-generation owner. Retain generation one for initial Contract publication, checked increment for every authorized replacement, last-generation continuity through dormancy, unchanged generation across ordinary Hot updates, and the active `ActorRef` loader. Publish that reference into every process, ring, deadline, park and cleanup carrier; never derive generation from cycle nonce, admission identity or placement. Use the implemented canonical all-residence semantic loader, Service wrapper, whole semantic-state mutation seam and retained-attempt transaction. The staged loader rejects legacy authority, binds semantic/process generation, and validates exact Service node, Deadline slot, Park owner/review, or carrier-free Disabled residence; the Service wrapper additionally returns the actual Pending/Live kind. These seams commit identity/Hot before ring advance, preserve all owners on refusal and leave the legacy-primary path unchanged. The carrier-neutral zero-Step and effectful completion cores now have staged Service adapters for retained and terminal results: retained completion commits semantic state before ring advance, while terminal completion cleans lifecycle semantics before atomic unlink/process retirement; effectful service attempts also preflight the exact eligible head before invoking their external effect. An adjacent-round temporary retry now retains canonical Service membership, commits its suspended Run/Hot semantics before advancing the current frontier, and is eligible again in the next round after genuine funding recovery. Its carrier-neutral due-retry constructor binds generation, semantic admission, stored Run, current Step/resources, exact Service residence and due round without legacy FIFO authority; genuine recovery completes exactly once without resetting attempts. Later retry now derives the first free slot from the bounded canonical deadline bucket, atomically transfers after preflight, and returns the first member of the earliest genuinely due block bucket without caller-selected Actor or page/slot coordinates. Canonical parking now atomically installs one complete generation-bound dependency plan and process Park evidence before releasing exact Service membership; stale owner or destination refusal preserves semantic state, Run/attempt state and topology. Its exact Actor/generation/plan/Park-evidence wake removes the complete registration plan before returning to canonical Service, refuses stale or occupied Pending authority non-consumingly and cannot wake twice. Negative scan and due-review results now revalidate the same Park episode and exact Pending authority before atomically replacing the complete plan while retaining Park residence; a positive due review additionally revalidates every retained source revision before the same exact canonical wake. One bounded due-review interpreter snapshots the complete retained source plan once and selects those exact negative or positive consumers; ambiguous interpretation, stale evidence, stale review/deadline input, revision races or newer Pending work leave current work and registrations untouched. One smallest weighted Oracle-review worker now owns a generated one-source complete-attempt runtime Weight and transactionally joins exact due publication to typed interpretation. Its first bounded traversal atom selects the earliest due block bucket from the shared C32 index, removes only inside the attempt, restores exact indexed/Pending/Park/Service authority on refusal, re-indexes negative re-arm, and wakes positive results; insufficient multidimensional Weight performs no reads or mutation. Mixed retry/review dispatch and broader clock traversal remain staged. Process/Park and temporal Trigger deadlines now have distinct generation-bound reverse owners, so one Actor can retain both exact handles without collision. The temporal owner can also be removed independently under a caller transaction: it requires exact generation, Tick coordinates and semantic pointer authority, preserves process residence, and restores the complete root after a later refusal. One inert composite publisher preflights and atomically commits semantic Hot state, process residence, Service/deadline membership and an optional independent Trigger deadline; it rehomes an already scheduled temporal Trigger tick into a newly planned canonical slot and rewrites semantic pointer coordinates, while malformed Service topology plus process- and Trigger-index failures prove exact whole-state rollback after each mutation boundary. Its atomic handoff now also accepts a separately validated lifecycle successor for resume/update while requiring unchanged Contract, admission, Run, generated resources and generation; invalid successor state restores the exact legacy root. A separate inert canonical lifecycle transition now removes the exact temporal Trigger and process residence, updates the validated semantic successor and republishes one complete destination atomically; a deadline-plus-Trigger pause proves Disabled convergence and whole-root restoration after a late generated-resource refusal. The fee-bearing canonical occurrence committer now consumes its pure occurrence plan against exact semantic/process authority: idle Disabled, Deadline and Park sources publish Pending at B+1, busy Service/Deadline residence remains byte-for-byte stable, independent temporal Trigger authority is consumed, semantic Hot is compare-and-replaced, and late fee refusal restores Park plans, registrations, reviews and every carrier. ObservationCrossing idle classification now loads the canonical semantic/Contract owner, verifies its carrier-free Disabled publication without a control cell, and selects Pending/coalesced occurrence intent from the canonical process rather than legacy readiness or FIFO capacity. The scalar fire path moves detector phase through generation-bound semantic authority and atomically charges/publishes Pending Service through the canonical occurrence committer; its generated occurrence benchmark rejects legacy control recreation. Initial activation, pause/resume republishing and successful zero-Step/effectful post-attempt retention now consume canonical process/Service authority in production. ObservationCrossing rearm and a coalesced busy fire now preserve the exact suspended canonical Deadline process while leaving legacy control absent. Cohort batching and final legacy FIFO retirement remain open; canonical terminal cleanup is covered for the locally exhausted and global failure-limit paths. Canonical Contract replacement is now connected for the Idle path: `store_actor_contract` selects the canonical replacement transition when no legacy control authority exists, releasing the old process/temporal residence, writing the new geometry, rotating semantic admission and generation, and republishing one generation-bound process/residence; the update caller suppresses the legacy prime when that canonical republish has already run. Inventory every semantic/process Publish/Replace/Remove owner and convert create, activate, update, deactivate, cancel, completion and terminal paths together with their resource composition. Derive cursor/resources from their selected owners rather than adding synchronized copies. Carry validated loaded authority until a relevant callback/mutation invalidates it. **Exit:** ordinary callers no longer recover semantic state from legacy placement; rollback restores the entire owned transition; every canonical carrier is bound to the current semantic generation; no copied authority, unowned selector or stale cached balance exists.

- [ ] **N2.5 / Certified Step and Wake Plans.** Reuse existing Contract/admission plans and add only the bounded information required for parked-balance activation: exact watched surfaces, mode, per-asset floor, direction, source capability and generation/config identity. Separate static plan, final parking baseline, acknowledged check metadata and current Step values. Do not require a general dependency solver or new VM. **Exit:** only configured surfaces are observed; the plan is not rebuilt on every Step and stale plans cannot authorize wakes/effects.

- [ ] **N2.6 / Co-Access and Stable State Geometry.** Confirm the selected stable semantic/process, sparse Run and ring layout against actual V1 reads/writes. Close the existing writer split before considering alternative layouts. Bind parking metadata to the stable generation, not to a mutable queue position. Track the actual cost of semantic record plus process plus ring node; “one stable owner” is not automatically one read. **Exit:** one implemented layout, no Contract/custody copying on transfers, and an operation ledger adequate to detect duplicated loads or writes.

## N3 — Connected Service, Parking and Wakeup

- [ ] **N3.1 / Autonomous Discovery and Activation Checks.** Connect the minimum paid positive current check needed for V1 and then the negative/return paths for V2. Use actual runtime callers and ordinary mandatory service; no test-only alternative executor. Coalesce one generation-bound check, retain it under refusal, and distinguish applicability from permission to inspect. **Exit:** supported work is discovered and processed without external executors; every producer has a bounded reachable consumer or is explicitly gated.

- [ ] **N3.2 / One Current Obligation and Residence Policy.** Implement Live/Sleep/Park/Pending transitions on the canonical process. A continuation due by the next permitted round remains resident; longer retry/deadline work Sleeps. An open Cycle never enters idle balance parking. Balance-mode completion captures the fixed anchor and ends busy monitoring before parking; no new Cycle is queued by its own effects. **Exit:** at every committed boundary exactly one current residence/terminal authority exists and no per-Step successor publication is needed for ordinary resident progress.

- [ ] **N3.3 / Certified Parking and Wake Completeness.** Requalify completeness against the selected trigger, not every hypothetical mutation of Available. For net-balance mode prove notifications for the named balance or declare bounded threshold-preserving review. For credit mode prove committed-credit and source coverage. Independently preserve authored time/Oracle validity obligations. Registration and final-baseline installation cannot miss a post-completion change. **Exit:** no busy tracking, indefinite stale park or silent switch from balance delta to generic predicate polling.

- [ ] **N3.4 / Negative Work, Fairness and Griefing Budget.** Price nonmatching, unparked, subthreshold, duplicate, negative, refused and cleanup paths as well as positive activation. Keep tiny updates on the smallest bounded lookup path; they must not decode the full Contract, append Pending work or charge another full activation fee. Preserve complete one-check paid authority where already selected, but review the actual payer and retention after the new threshold gate. Test refundable/circular deposits and repeated threshold oscillation: `100 × ED` is not a spam-proof fee. **Exit:** no free unbounded notices/polling, counter reset, starvation hidden by censoring or busy-state bookkeeping proportional to incoming events.

- [ ] **N3.5 / One Concrete Ring/Sleep/Index Carrier.** Complete the coherent semantic writer + process/ring cutover on the selected doubly linked carrier and retained C32 deadline substrate. In one supported candidate build, no Actor executes through a parallel old scheduler. Convert the lifecycle closure, conditional initial service publication and generated callers as one coherent change; inert helpers are not its exit. Early unconnected profiles must reject explicitly and cannot be advertised as delivered. **Exit:** public-created V1 actors use canonical semantic/process/ring authority; empty/singleton/interior/full/deep/fragmented and rollback paths are bounded and priced.

- [ ] **N3.6 / Block-Round Frontier and Persistent Ring Service.** Connect begin/probe/admit/advance to actual Prepass/Drain, sharing one immutable block-round identity. Preserve the ratified next-encounter order, Q1, non-consuming refusal and B+1 admission/reentry. Differentially compare mutable runtime traces to the corrected independent oracle, not just the existing fixed three-Actor trace. **Exit:** V1 Steps and adjacent retries retain membership; no duplicate, donated, skipped or recaptured turn; no production full-ring snapshot or unbounded wrap.

- [ ] **N3.7 / Dependency-Keyed Parking and Lost-Wakeup Protocol.** Finish the existing revision/scan/Pending consumers, using exact source/generation/plan/parking-episode authority. Bound scan target and append horizon; retain newer work; acknowledge only after durable destination or exact stale proof. Complete one current Oracle path, including equal-value refresh, lifecycle and age expiry. Avoid introducing unneeded per-credit source registries when actor-local certified hooks suffice. **Exit:** V2-O and source/Pending saturation cannot lose work, grow undrained sources, duplicate residence or revive old generations; unrelated source updates remain isolated and charged.

- [ ] **N3.8 / Membership Transfers, Arbitrary Control and Saturation.** Connect and prove arbitrary unlink, source-preserving refusal and atomic residence exchange. Secure a destination or bounded retained pending authority before removing the source; advance source/review cursors only after that commit. Mandatory revocation must remain possible at legal capacity. Include callbacks, partially served rounds, interior cancellation, replaced plans, event/time coincidence and exhausted identifiers. **Exit:** exactly one valid obligation survives success/refusal/rollback; full capacity is a defined backpressure case, not unexplained worker poisoning.

- [ ] **N3.9 / Parked-Balance Mode Selection and Delivery.** Implement and measure the smallest fixed-anchor candidate of §2 on the intended native and one supported non-native surface. Freeze hook/review coverage, cost/latency criteria, all boundary semantics and inclusion rules first. Complete V2-B with public credits, no busy bookkeeping and a floor of at least `100 × ED[a]`. If it fails, record the exact blocking reason and activate the single verified-credit/whitelist fallback; no repeated polling redesign loop. Keep only the accepted mechanism unless distinct supported profiles justify both. **Exit:** one trustworthy, affordable parked-balance activation product is fully integrated, or the exact unresolvable tradeoff is returned to the task owner before release; neither mode restores LastFunding amounts or a busy future-Cycle latch.

## N4 — Safe Revocation, Mutation and Asynchronous Cleanup

- [ ] **N4.1 / Revocation and Authorized Revival.** Connect Serving/Disabled/Retired semantics to ordinary controls. Parked may be mutated rather than deleted. Explicit replacement binds a new semantic generation and invalidates the old baseline/registrations atomically; resume of the same generation preserves the declared retry and anchor rules instead of guessing from absence. Ordinary credits cannot revive owner-disabled/retired state. **Exit:** control rights, committed prefix and custody survive replacement, pause/resume and saturation without old-generation execution.

- [ ] **N4.2 / Generation-Safe Owner and Protocol Cleanup.** Execute the selected sealed exact-handle reclamation plan in bounded quanta. Old Park anchors, credit accumulators if selected, event/time handles and Pending entries are included. Sweeps cannot delete a new generation or enumerate sovereign assets. Epoch/era timing can make work eligible, but cannot create an unbounded single-block purge. **Exit:** interrupted cleanup coexists with public replacement; all deleted keys are old-generation qualified; mandatory progress has a real budget and never depends solely on spare idle time.

- [ ] **N4.3 / Retained State, Holds and Reclamation Debt.** Bind actual retained-byte and terminal-capacity costs, including baseline surfaces, whitelist/credit metadata, reverse handles and outstanding generations. A collateral deposit does not reserve future CPU/PoV. Secure mandatory revocation capacity at admission; cap debt and apply backpressure before ordinary creation/replacement can outgrow maintenance. **Exit:** no free cold state, unbounded old-generation growth or premature hold/slot release; reported performance includes debt arrival, service and drain time.

## N5 — Removal and Complete Resource Closure

- [ ] **N5.1 / Old-Path Reachability and Removal.** Hard-cut the supported build from legacy placement, tickets, future-Cycle latches, historical Crossing duties, Opening snapshots and LastFunding accumulation. Preserve effect/custody/credit-authentication boundaries. A newly selected parked-credit adapter may reuse a verified producer boundary, but not its old scheduling authority. Inspect dispatch, genesis, hooks, simulation, recovery, embedding and feature builds; no compatibility reader or dormant fallback may recreate legacy service. The canonical B+1 cutover left most pre-cutover scheduler tests on legacy same-block timing: the `pallet-deos-actors` lib suite now reports 532 passed / 362 failed / 5 ignored of `899`, with production-effect tests such as `tests/market_tasks.rs` needing canonical timing migration and legacy-substrate tests needing deletion rather than preservation. A shared test-harness defect blocked a broad ordinary-path cohort: `run_idle` continued only while a legacy `ActorControlLocators` member was `Running`, so canonical semantic residents — whose hot state lives in `ActorSemanticStates` and whose authority is `ActorProcesses`/`ServiceNodes` — were never advanced to their B+1 eligible block. `run_idle` now also continues when an active canonical semantic record with `ActorProcesses`/`ServiceNodes` retains `pending_signal` at `Idle`, and that single helper correction moved 57 ordinary-path witnesses (balance conditions, `Percent`/`Fixed` resolution, action/pipeline fee separation, zero-Step close, retry/close composition) to passing with zero regressions. `tests/market_tasks` is now `44 passed / 0 failed`: 18 single- and multi-Step effect witnesses, the retry/deadline path, the four-Step dispatch witness, the liquidity adapter-outcome loop and the user swap-plus-transfer witness run on canonical B+1 Service timing, and the retained-Idle no-work encounter resolved the two ring-blocked witnesses. The former `cadence_rearm_capacity_failure_rolls_back_pipeline_opening` was deleted as a legacy-substrate test because it drove `WakeupCursorLen`/`drain_overdue_wakeups_cursor`; its opening-refusal invariant is covered canonically by `canonical_service_round_preserves_markers_cursor_and_blocked_head` and `mandatory_service_frontier_pre_admits_and_executes_one_effectful_head`, which pin `ServiceRoundError::InsufficientWeight`, zero meter consumption, and byte-stable Service header/`ActorProcesses` on refusal. The former `manual_trigger_is_preserved_on_weight_defer` was deleted because its premise — a subblock `on_idle` grant losing an occurrence — is unreachable once canonical publication makes the occurrence ineligible until B+1 and the next block's full-budget mandatory prepass owns admission; bounded refusal of an eligible canonical head remains covered by `canonical_service_round_preserves_markers_cursor_and_blocked_head` and the starvation witnesses. A second shared-fixture cohort reflected the completed creation cutover: public active creation already publishes canonical semantic/process authority (`ActorProcesses`, no `ActorControlLocators`/unsignaled cell), so three test helpers were corrected to match reality without touching production code — `mutate_actor_hot_coherent` now mutates the semantic owner when no legacy cell exists, `starvation_blocked_budget` reads the current-Step envelope from canonical service state, and a new `publish_test_service_member` fixture clears the creation-time Disabled process before publishing an inert Service member; this fixed 14 ordinary-path and ring fixtures (`storage_and_api`, `fees_and_funding`, `execution`) with zero regressions. A third fixture cohort corrected the canonical mandatory-service witnesses in `storage_and_api`: six `mandatory_service_*` tests and `on_idle_recovers_later_retry_and_completes_once_in_fresh_drain` had latched readiness with `manual_trigger` and then re-published an inert `Live` Service member at an earlier round, which `insert_service_member` rejected as `CorruptRing`; they now rely on the canonical `Pending` member published by `manual_trigger` (latch at block B, service at B+1), with retained Idle residence asserted as the documented `Service(Pending)` rather than the old `Live` label, and `frontier_dispatches_the_selected_zero_step_kind` now latches a real occurrence instead of expecting a zero-Step cycle with no pending signal. This fixed 7 canonical witnesses with zero regressions. A fourth cohort migrated the six `frame_only_*` ordinary-path witnesses that still drove the pre-B+1 same-block timing and asserted the legacy scalar mirror after canonical creation: they now advance one block through the shared `run_next_idle` helper before service, assert the canonical `ActiveActorState`/`ActorSemanticStates` owner, and require `ActorControlLocators` and the legacy `actor_control_cell` to remain absent while `actor_hot` reads the canonical owner, while the retained Idle resident still carries no queue ticket. `frame_only_productive_completion_closes_without_scalar_hot`, `frame_only_abort_cycle_removes_transaction_local_scalar_bridges`, `frame_only_abort_cycle_closes_at_failure_threshold_without_scalar_hot`, `frame_only_manual_zero_step_uses_only_canonical_control`, `frame_only_opening_stop_cycle_uses_only_canonical_control` and `frame_only_paused_ready_pop_uses_only_canonical_control` now pass with zero regressions. A fifth cohort migrated the five remaining single-failure `frame_only_*` execution witnesses to canonical B+1 timing: `frame_only_running_abort_cycle_failure_never_materializes_scalar_hot`, `frame_only_opening_continue_next_step_failure_never_materializes_scalar_hot`, `frame_only_stake_continue_next_step_rolls_back_without_scalar_hot`, `frame_only_running_continue_next_step_failure_never_materializes_scalar_hot` and `frame_only_stake_retry_later_permanent_failure_terminates_without_scalar_hot` now advance one block through `run_next_idle` before the first service and one further block for the aborting/advancing Step, then assert the canonical `ActiveActorState` owner and require `ActorControlLocators`/`actor_control_cell` to remain absent instead of reading the removed legacy scalar authority. These moved the lib suite to 454 passed / 437 failed / 5 ignored of `896` with zero regressions. A sixth cohort closed the B1/N3.5 success-retry continuation path rather than migrating fixtures around it. The production defect was in `execute_effectful_step_on_service_with_deadline`: a retry destination precomputed from the pre-attempt Run state (the `suspension_eligible_at` for the next possible failure) was still passed when the attempt instead succeeded, and the transition match rejected `Completed`/`Continued` with a non-`None` deadline as `AttemptTransactionError::Invariant`, so the whole attempt rolled back and the Actor stayed suspended with no attempt recorded. The match now treats any adjacent-round `Completed`/`Continued` outcome as canonical Service retention and ignores the now-irrelevant precomputed deadline. `retry_later_resumes_same_cursor_without_replaying_committed_prefix`, `frame_only_retry_attempt_exhaustion_closes_from_consumed_authority` and `frame_only_running_swap_retry_and_resume_never_materialize_scalar_hot` were migrated to canonical B+1 timing (latch at B, first service at B+1, later retry via the shared Block deadline, then adjacent-round resume) and the property test `tests::proptest_actor::swap_add_liquidity_transfer_pipeline_retries_only_the_unresolved_suffix` was unblocked by the same fix. This moved the lib suite to 458 passed / 433 failed / 5 ignored of `896` with zero regressions, verified by failure-name set difference against the parent commit (four fixed, none newly failing). A seventh cohort corrected a production read surface rather than a fixture: the public `actor_hot` query still projected the legacy primary cell and therefore returned `None` for every canonically published Actor. It now reads `ActorSemanticStates` first, falls back to the legacy primary only when no semantic record exists, and returns `None` for a dormant record, matching the already-canonical `active_actor_state`/`actor_identity` readers. Canonical in-place Hot mutation through `try_store_control_hot_with_authority` was extended to the same authority rule: when no legacy locator exists it compare-and-replaces the semantic record and rejects a leftover unsignaled cell, while a live legacy primary keeps its physical mirror. The focused `canonical_in_place_hot_mutation_requires_no_legacy_primary` regression pins that seam. This fixed 35 witnesses across `wakeups`, `scheduling`, `lifecycle`, `crossing`, `observations`, `fees_and_funding` and `execution`, corrected the now-invalid `actor_hot().is_none()` assertions in the three canonical `frame_only_*` scheduling witnesses, and added one test, moving the lib suite to 503 passed / 390 failed / 5 ignored of `898` with zero regressions by failure-name set difference. An eighth cohort converted the production Contract-replacement boundary itself: `store_actor_contract` now detects a canonically published Actor (no `ActorControlLocators` and no unsignaled cell) and routes the replacement through a new canonical transition that releases the old process residence and independent temporal Trigger deadline, writes the new Contract geometry, rotates the semantic admission and generation, and republishes one complete generation-bound process/residence through `publish_actor_publication`; `update_contract` skips the legacy `prime_frame_actor_schedule` when that canonical republish already owns the successor. This fixed the Idle-actor Contract update (previously `ActorInvariant`), the lifecycle policy-replacement, Crossing replacement, observation fanout and stop-cycle-after-summary witnesses, and added the focused `canonical_idle_contract_update_republishes_generation_bound_residence` regression. A ninth cohort canonicalized run cancellation for open runs rather than stopping at the Idle replacement: `cancel_run_internal_loaded` now detects a canonically published Actor (no `ActorControlLocators`/unsignaled cell) and routes the non-close reasons through the new `cancel_actor_publication` transition, which releases the generation-bound Service or Deadline residence and any independent temporal Trigger deadline, clears the open Run, and republishes one Idle successor through `publish_actor_publication` — never mirroring a physical control cell. For canonical Actors the legacy wakeup invalidation and the schedule-change `trigger_wakeup_substrate_invalidate_loaded` path are superseded by that transition, and `prime_frame_actor_schedule` now no-ops when a canonical process already owns the residence. This fixed `completion_policy_only_replacement_cancels_contract_bound_run`, `step_replacement_cancels_retry_and_resets_bounds_before_fresh_execution`, `cancelled_run_returns_idle_without_a_deferred_manual_cycle`, `cancellation_does_not_requeue_an_ignored_busy_manual_signal`, `semantic_control_origins_share_one_queue_churn_clock`, `actor_run_state_hold_is_reserved_for_the_active_installed_lifetime` and `active_trigger_replacement_matrix_preserves_one_canonical_family`. The former `cadence_update_rolls_back_exactly_when_existing_wakeup_cursor_is_corrupt` was deleted as a legacy-substrate test because it corrupted `ActorWaitingCursorIndices`, a carrier the canonical Trigger path no longer reads; canonical whole-transition rollback remains covered by the observation-replacement and atomicity witnesses. A tenth cohort closed the canonical owner-close path rather than a fixture: the public `close_actor`/sweep/window-expiry finalizer `finalize_actor` still finalized a canonically published Actor through `finalize_actor_from_retained_state`, which removes the semantic/lifecycle/contract surfaces but leaves the generation-bound `ActorProcesses` publication and its Service/Deadline residence behind, because only the service terminal path separately called `retire_service_member`. `finalize_actor` now detects a canonically published Actor (no `ActorControlLocators`/unsignaled cell) and routes it through the previously inert `remove_actor_publication_and_finalize`, which atomically releases the Trigger/Service/Deadline residence, removes the process publication, and converges on `finalize_actor_from_consumed_state`; the legacy retained-frame path is preserved for pre-cutover Actors. The focused `canonical_owner_close_releases_process_and_deadline_residence` regression pins that a closed suspended canonical Actor retains no `ActorProcesses`, no `DeadlineHandles`, and no legacy authority. This fixed `governance_can_manage_system_actor_control_surface`, `canonical_close_removes_future_tick_and_releases_state_hold`, `close_before_future_wakeup_removes_exact_waiting_membership`, `repeated_timer_close_churn_leaves_no_wakeup_tombstones` and `temporal_membership_try_state_accepts_exact_close_cleanup`, moving the lib suite to 515 passed / 378 failed / 5 ignored of `898` with zero regressions by failure-name set difference. An eleventh cohort closed the canonical owner-deactivation path rather than a fixture: `do_deactivate_actor` still called `remove_actor_from_queues_with_authority`, which rejects any Actor without a legacy `ActorControlLocators` entry, so deactivating a canonically published Actor failed with `ActorInvariant` and, even if tolerated, would have left its generation-bound `ActorProcesses`/Service/Deadline publication behind under a Dormant semantic record. The release logic was extracted into one `detach_actor_publication` owner that releases the Trigger/Service/Deadline residence, removes `ActorProcesses`, and rewrites the semantic Hot pointers without finalizing the durable identity; `remove_actor_publication_and_finalize` now reuses the same detach owner before its custody-neutral finalizer. `do_deactivate_actor` detects a canonically published Actor and detaches its publication, while the legacy queue/wakeup cleanup remains for pre-cutover Actors. This fixed 16 witnesses across `core`, `crossing`, `execution`, `fees_and_funding`, `lifecycle`, `observations`, `scheduling` and `storage_and_api` (`deactivate_activate_preserves_nonce_but_resets_active_epoch_state_for_both_classes`, `deactivation_uses_frame_authority_with_canonical_control`, `crossing_deactivation_removes_membership_with_canonical_control`, four observation-cleanup witnesses, `dormant_identity_owns_no_scheduler_state_and_round_trips_activation` and others), and added the focused `canonical_owner_deactivation_releases_process_and_deadline_residence` regression, moving the lib suite to 532 passed / 362 failed / 5 ignored of `899` with zero regressions by failure-name set difference. A twelfth cohort fixed the mandatory-prepass resource accounting rather than a fixture: `execute_cycle_to_cutoff_inner` reserved the whole `ActorControl` pass envelope from `BlockResourceState` and then the canonical Service round reserved its selector plus Step control from the same state a second time, so every effectful canonical head in the `PrepassExecuting` phase failed with `ResourceUnavailable` and only zero-Step heads could run. The Service round now takes an explicit effect domain plus a `control_owned_by_caller` flag; the prepass calls the new `service_canonical_round_head_with_reserved_control`, which reserves only effect capacity and leaves ActorControl to the enclosing `CyclePass` reconciliation, while direct service-round callers keep reserving both lanes. This fixed `on_initialize_freezes_cutoff_and_executes_the_mandatory_base_pass`, `cycle_summary_fee_fairness_property_matrix` and `exact_update_noops_preserve_all_actor_state_and_emit_nothing`, and added the focused `mandatory_prepass_pass_admits_effectful_service_without_double_reserving_control` regression, moving the lib suite to 536 passed / 359 failed / 5 ignored of `900` with zero regressions by failure-name set difference. A thirteenth cohort closed the canonical Service terminal-classification gap rather than a fixture: `service_canonical_round_head_inner` admitted an eligible head and then classified an Idle resident with no pending signal as "idle no work" and advanced the ring cursor, so a due schedule window or exhausted cycle nonce that reached Service through a canonical deadline (`return_due_deadline_member_to_service`) was silently discarded and the Actor stayed active past its window. The round now derives the canonical classification before deciding, and when `terminal_reason` is set it closes the Actor through the existing atomic `finalize_actor` owner and reports `ServiceRoundEncounter::Closed` instead of advancing; the branch reserves the selector plus `close_dispatch_weight_upper` and settles the same envelope. This fixed `window_expiry_wakeup_closes_inactive_actor_without_identity_scan`, `paused_actor_retains_direct_window_expiry_wakeup`, `cycle_nonce_exhaustion_closes_system_actor` and `reached_global_failure_cutoff_closes_before_another_attempt`, moving the lib suite to 541 passed / 354 failed / 5 ignored of `900` with zero regressions by failure-name set difference. A fourteenth cohort closed the canonical Crossing fire fee-refusal path rather than a fixture: `crossing_work_unit` forwarded a fee-transfer failure from `commit_canonical_trigger_occurrence_with_authority` as `InsufficientFee`, aborting the whole work unit, whereas the automatic-trigger contract requires a detected occurrence whose collection cannot complete to advance the certified traversal without readiness. The occurrence commit already restores its own pre-activation root, so the worker now swallows exactly `InsufficientFee` (mirroring `try_charge_automatic_trigger_occurrence`) and leaves the member `WaitingForRearm` with no readiness, occurrence fee, event, placement, or detection-disable latch, while any other error still propagates. This fixed `crossing_fire_collection_failure_advances_without_readiness`, `crossing_batch_falls_back_to_scalar_progress_for_an_underfunded_member`, `crossing_opening_executes_effectful_step_and_rearms_detector`, `falling_crossing_fires_and_rearms_without_duplicate_activation` and `repeated_latched_crossing_fires_charge_only_the_useful_transition`, deleted the legacy-authority duplicate `crossing_fire_collection_failure_uses_frame_authority_without_scalar_control` in favor of the canonical refusal test, and strengthened that test with canonical-residence assertions, moving the lib suite to 546 passed / 348 failed / 5 ignored of `899` with zero regressions by failure-name set difference. A fifteenth cohort closed the canonical completed-Cycle re-latch path rather than a fixture: a Manual occurrence for an Actor whose `pending_signal` was already true reached `commit_canonical_trigger_occurrence_with_authority`, whose leading `ensure!(!state.hot.pending_signal)` rejected the coalescing duplicate as `ActorInvariant`, and an Actor whose Cycle had just completed retained an Idle `Service(Pending)` residence that `plan_canonical_occurrence` rejected as `InvalidResidence`, so it could never be triggered again. `manual_trigger` now treats an already-latched `pending_signal` exactly like an open Cycle and returns before fee admission, `plan_canonical_occurrence` accepts a retained Idle Service resident as a valid occurrence source, and the occurrence committer re-admits that exact slot as a fresh B+1 Pending member instead of failing. This fixed `repeated_pending_manual_occurrence_is_latched_without_trigger_fee`, `stop_cycle_fee_failure_rolls_back_before_step_policy_or_stop`, `auto_close_threshold_reached_closes_actor_after_successful_cycle`, `unsuccessful_attempt_streak_closes_actor_at_inclusive_threshold` and `percentage_of_current_reloads_available_at_each_cycle`, and added the focused `completed_idle_service_resident_relatches_at_the_next_block` regression, moving the lib suite to 552 passed / 343 failed / 5 ignored of `900` with zero regressions by failure-name set difference. A sixteenth cohort migrated the canonical publication rollback and lifecycle-transition witnesses whose only legacy dependency was reading `actor_control_cell(...).resources` for the current-Step envelope: public creation already publishes canonical semantic/process authority, so each test now reads its envelope from the canonical loaded Step through the new `fixture_step_resource_envelope` helper and reaches the pre-publication state by detaching that exact canonical publication (`detach_actor_publication`) instead of removing a nonexistent legacy locator, with the malformed-Service rollback case now staged at an eligible canonical block so the Service destination is actually planned. This fixed `composite_publication_preflight_rejects_stale_resources_and_partial_canonical_authority`, `composite_publication_rehomes_an_existing_temporal_trigger_pointer`, `composite_publication_commits_process_and_temporal_trigger_deadlines_atomically`, `composite_publication_rolls_back_every_owner_after_carrier_mutation_failures`, `temporal_trigger_deadline_removal_is_independent_and_transactional`, `canonical_lifecycle_transition_atomically_pauses_and_resumes_service_authority` and `canonical_terminal_removal_closes_service_authority_transactionally`, moving the lib suite to 559 passed / 336 failed / 5 ignored of `900` with zero regressions by failure-name set difference. A seventeenth cohort closed the canonical generic-activation preflight reader seam rather than a fixture: `preflight_activation_loaded` unconditionally read the legacy `load_frame_control_authority` (the primary control cell created only by pre-cutover placement), so generically activating a canonically published Actor — the Crossing `FireCohortClosed` production path and the deferred observation fallback — failed with `ActorInvariant` before any classification. The preflight now branches on `ActorControlLocators`: a pre-cutover Actor keeps the legacy primary check, while a canonically published Actor derives its frame admission and identity from the generation-bound semantic owner through `load_control_authority_with_authority`, failing closed when that owner is absent or mismatched. This fixed `activation_preflight_selects_exact_block_wakeup_without_mutating_state`, `activation_preflight_selects_prime_schedule_without_mutating_state`, `cadenced_activation_preflight_carries_queue_authority_without_mutating_state`, `ready_activation_plan_commits_its_frozen_destination_from_canonical_authority`, `dormant_activation_and_trigger_replacements_preserve_strict_frame_loading`, `observation_activation_uses_primary_pending_authority` and `observation_change_execution_preserves_absent_scalar_control`, and added the focused `canonical_activation_preflight_derives_admission_without_a_legacy_primary` regression, moving the lib suite to 567 passed / 329 failed / 5 ignored of `901` with zero regressions by failure-name set difference. An eighteenth cohort migrated four ObservationChange fanout witnesses off legacy ready-frame queue-ticket membership to the canonical `Service(Pending)` ring owner: `one_fanout_page_sets_existing_latches_and_scheduler_membership`, `multiple_dense_dirty_feeds_receive_round_robin_service`, `maximum_density_fanout_converges_without_duplicate_queue_membership` and `latest_revision_fanout_model_converges_across_seeded_races` now assert one canonical `Service(Pending)` member per subscriber through the shared `assert_canonical_pending_member` fixture and the `ServiceHeader` count instead of unique `queue_ticket`s or `test_queue_append_commits`, moving the lib suite to 571 passed / 325 failed / 5 ignored of `901` with zero regressions by failure-name set difference. A nineteenth cohort migrated five ObservationChange classification/activation witnesses to the canonical Oracle fanout entrypoint instead of the generic `request_activation` path, which for a canonically published Actor still creates legacy control authority and corrupts the semantic owner: `compact_observation_classification_matches_idle_and_running_authority`, `compact_observation_classification_matches_suspension_and_retry_exhaustion`, `compact_observation_classification_matches_failure_auto_close_pause_and_breaker`, `compact_observation_activation_loads_only_current_authority_tiers` now latch through a shared `latch_canonical_observation_change` helper (`note_observation_changed` plus one fanout page) and run on canonical B+1 Service timing, while the obsolete `compact_observation_placement_matches_generic_ready_future_coalesced_and_terminal_state` witness and its `observation_activation_placement_snapshot` helper were deleted because they pinned parity between two test-only fallback entrypoints whose repeated-activation semantics are already covered by the canonical fanout coalescing and terminal-close witnesses, moving the lib suite to 575 passed / 320 failed / 5 ignored of `900` with zero regressions by failure-name set difference. A twentieth cohort migrated three observation witnesses to canonical B+1 Service semantics: `observation_subscriptions_follow_schedule_lifecycle_exactly` now reads the ObservationChange admission certificate from the canonical semantic owner through `load_control_authority_with_authority` instead of the legacy `actor_control_cell`; `observation_change_activation_requires_its_certified_feed_selector` stages its certified-feed-selector mismatch by rewriting the canonical `ActorSemanticStates` admission plus the `ActorContractHeads`/`ActorActivationAuthorities` admission identities instead of an `ActorUnsignaledControlCells` cell and asserts the fail-closed latch on the canonical Hot owner; and `on_idle_fanout_feeds_the_existing_scheduler_without_direct_execution` now expects canonical B+1 fanout to publish a `Service(Pending)` occurrence and runs the productive cycle at the next block rather than asserting same-block execution. The obsolete `same_block_wakeup_precedes_fanout_in_ticket_order` witness was deleted as legacy tie-breaking on removed ready-frame queue tickets. This moved the lib suite to 578 passed / 316 failed / 5 ignored of `899` with zero regressions by failure-name set difference. A twenty-first cohort finished the observation module: six legacy-substrate witnesses were deleted as unreachable under canonical publication — `mixed_observation_page_commits_each_contiguous_queue_cohort_once` (legacy queue-ticket cohort commits, covered by the canonical fanout membership witnesses), `observation_future_activation_moves_primary_to_waiting_without_scalar_bridge` (legacy `ActorControlLocators::Waiting` primary), `saturated_queue_materializes_fanout_through_the_canonical_deferred_wakeup` (legacy queue-saturation fallback), `fanout_terminal_branch_is_durable_and_executes_under_a_later_scalar_turn` (the compact deferred-terminal branch is unreachable because `process_observation_change_occurrence` commits a canonical occurrence before classifying, while window-expiry close is owned by the Service round), and `fanout_fault_captures_exact_page_position_actor_c6_authority_and_branch`/`fanout_position_cursor_does_not_replay_a_committed_page_prefix` (canonical occurrence commit never returns a Temporary `SchedulerExhausted`/wakeup-placement failure, so the worker retry class is legacy; canonical Invariant fault detection and repair-before-resume remain covered by `fanout_structural_fault_is_bounded_and_requires_repair_before_resume`). The one migrated witness `invalid_fresh_observation_fails_permanently_and_applies_step_policy` now stages a still-future `observed_at` beyond the B+1 service block and advances one further block for the `ContinueNextStep` successor, moving the lib suite to 579 passed / 309 failed / 5 ignored of `893` with zero regressions by failure-name set difference (the observations module is now 45 passed / 0 failed). A twenty-second cohort migrated the canonical temporal scheduling witnesses off the legacy `drain_overdue_wakeups_cursor` waiting-substrate drain, which never observes a canonically published Actor because its temporal Trigger lives in the shared `Deadline*` carrier served by `service_due_deadline_frontiers`: a new `service_canonical_temporal_frontiers` fixture mirrors the production `on_idle` housekeeping call, `underfunded_at_time_occurrence_selects_prepaid_custody_neutral_apoptosis`, `underfunded_cadenced_occurrence_advances_without_fee_readiness_or_apoptosis`, `cadenced_collection_failure_advances_deadline_without_readiness`, `at_time_collection_failure_rolls_back_consumption_and_retains_wakeup`, `at_time_occurrence_charges_once_consumes_deadline_and_latches_readiness`, `at_time_occurrence_uses_primary_pending_authority`, `cadenced_occurrence_uses_primary_pending_authority`, `frame_only_underfunded_at_time_closes_from_consumed_wakeup_authority`, `frame_only_zero_step_at_time_uses_only_canonical_control` and `immutable_zero_step_at_time_closes_at_authored_cycle_nonce` now drive the canonical deadline frontier and assert canonical `Service(Pending)`/`ActorSemanticStates` residence instead of the legacy ready-frame primary, moving the lib suite to 588 passed / 300 failed / 5 ignored of `893` with zero regressions by failure-name set difference. A twenty-third cohort continued the canonical temporal migration and exposed a production defect on the cadence rearm path: `cadenced_latch_disables_detection_until_pipeline_opening` and `pipeline_opening_rearms_cadence_from_current_tick` now drive `service_canonical_temporal_frontiers` and assert canonical `Service(Pending)`/`ActorSemanticStates` residence rather than the legacy ready-frame ticket, while `busy_cadenced_occurrence_preserves_frame_service_with_canonical_control` and `paused_temporal_occurrence_restores_unsignaled_frame_with_canonical_control` were deleted as legacy-authority duplicates (canonical pause moves the process to `Disabled(OwnerPaused)` with no residence and releases the temporal deadline, so the legacy `Unsignaled` primary cannot be recreated). This moved the lib suite to 590 passed / 296 failed / 5 ignored of `891` with zero regressions by failure-name set difference. The same cohort confirmed that `prepare_cadenced_rearm_hot` still registers the opening-time cadence rearm through the legacy `schedule_fresh_wakeup_reference`/`ActorWaitingEntry` waiting substrate instead of the canonical `TriggerDeadlineHandles` carrier; because production `on_idle` now services temporal deadlines only through `service_due_deadline_frontiers`, a canonically published Cadenced Actor whose first cycle opens sets `hot.trigger_wakeup_pointer` with no matching `TriggerDeadlineHandles` member and never fires again. Making that opening rearm use the canonical `plan_deadline_destination` plus `insert_trigger_deadline_member` owner while preserving the semantic-pointer/`commit_retained_service_attempt` write ordering is the next focused production task, and `busy_cadenced_occurrence_advances_deadline_without_future_cycle`, `cadenced_rearm_uses_frozen_opening_authority`, `next_block_cadence_rearms_after_each_deferred_opening_without_late_fifo_tickets` and `paused_timer_waits_for_resume_without_queue_churn_or_signal_loss` remain failing pending that fix. Migrate or delete the remaining legacy-substrate tests so ordinary-path tests agree before removal. A twenty-fourth cohort closed that canonical cadence rearm defect in production: `prepare_cadenced_rearm_hot` now detects a canonically published Actor (no `ActorControlLocators`/unsignaled cell with an `ActorProcesses` residence) and registers the opening-time rearm through `plan_deadline_destination` plus `insert_trigger_deadline_member` in the shared `TriggerDeadlineHandles` carrier, writing the semantic `trigger_wakeup_pointer` before the member insertion so the reverse handle validates; the legacy `schedule_fresh_wakeup_reference`/`ActorWaitingEntry` rearm remains for pre-cutover Actors only. `cadenced_rearm_uses_frozen_opening_authority` was migrated to canonical B+1 timing and canonical assertions (pointer tick 4, `TriggerDeadlineHandles` member, no legacy locator, zero legacy `ActorWaitingOccupancies`). This moved the lib suite to 594 passed / 292 failed / 5 ignored of `891` with exactly four fixed and zero newly failing by failure-name set difference: the migrated witness plus the previously legacy-timed end-to-end `tests::core::timer_always_executes_on_interval`, `tests::execution::user_dca_complete_lifecycle` and `tests::scheduling::global_fifo_eventually_services_system_actor_after_many_users`. A twenty-fifth cohort migrated the last three canonical cadence witnesses off `drain_overdue_wakeups_cursor`: `busy_cadenced_occurrence_advances_deadline_without_future_cycle` now opens a six-Step canonical Pipeline to Running, fires the next cadence at tick 4 through `service_canonical_temporal_frontiers`, and pins a deadline-only advance to tick 5 with no Trigger fee, event, cursor, or nonce change; `next_block_cadence_rearms_after_each_deferred_opening_without_late_fifo_tickets` asserts each occurrence publishes one `Service(Pending)` for B+1, the deferred opening re-arms exactly one period ahead with no legacy queue ticket or `ActorControlLocators`, and three cycles complete; `paused_timer_waits_for_resume_without_queue_churn_or_signal_loss` pins that pause releases the canonical `TriggerDeadlineHandles` deadline with no queue churn and resume re-arms the next cadence from the current tick. This moved the lib suite to 597 passed / 289 failed / 5 ignored of `891` with exactly three fixed and zero newly failing by failure-name set difference. A twenty-sixth cohort retired twelve legacy paged-FIFO/tombstone topology witnesses (eleven tests plus the `consume_structural_ready_head` helper) because canonical publication no longer populates `ActorControlLocators`/`ActorReadyFrameChunks`, so `manual_trigger`/`create_system_with` cannot establish the legacy queue state they assume: `paged_enqueue_coalesces_without_a_per_block_insertion_cap`, `tombstone_drain_rolls_back_on_live_occupancy_exceeding_span`, `tombstone_drain_rolls_back_on_cross_page_corruption`, `tombstone_drain_missing_current_page_is_blocked_without_mutation`, `live_occupancy_and_tombstone_span_are_independently_bounded`, `paged_queue_uses_one_live_actor_ticket_and_lazy_invalidation`, `paged_queue_crosses_and_reclaims_page_boundaries_without_prefix_rewrites`, `paged_queue_replacement_ticket_leaves_old_entry_as_tombstone`, `paged_tombstone_drain_is_scan_bounded_and_reclaims_multiple_pages`, `paged_tombstone_drain_stops_at_live_head_and_honors_cutoff` and `paged_scheduler_preserves_the_unexecuted_fifo_suffix` all relied on the test/benchmark-only `paged_enqueue` or on `enqueue_latched_actor`'s direct `preflight_paged_enqueue_cohort_with_authority`/`commit_paged_enqueue` construction. Canonical Service-ring topology coverage remains in `service_ring_oracle_covers_empty_singleton_interior_cursor_and_wrap`, `canonical_service_ring_is_transactional_generation_bound_and_structurally_complete`, `canonical_service_round_handles_empty_removal_interruption_and_faults` and the `service_member_*` benchmarks, and the architecture-cited `mixed_fifo_stops_at_corrupt_actor_without_touching_valid_suffix` was retained. This moved the lib suite to 597 passed / 278 failed / 5 ignored of `880` with zero newly failing. A twenty-seventh cohort closed the canonical Service round's missing admission-time insolvency close in production: `service_canonical_round_head_inner` already owned terminal-reason close and idle/no-work advance but never applied the legacy `apply_admission_loaded` User-Idle-latched pipeline-capacity test, so an insolvent User whose sovereign balance could not cover `MinUserBalance + Pipeline Machine total` stayed resident and deferred forever behind the protected floor. The round now derives `admission_insufficient` for a User `Idle` `pending_signal` Actor before any effectful or zero-Step decision and, when `pipeline_capacity_sufficient` is false, closes through the existing atomic `finalize_actor` owner with `CloseReason::CycleAdmissionInsufficient`; terminal reasons still take precedence and System or non-Idle states keep unlimited admission. `user_pipeline_insolvency_closes_before_effect_capacity_deferral` was migrated to canonical B+1 direct-pass timing, now asserts the unchanged canonical `ActorProcesses` residence for the solvent deferred case instead of the removed legacy paged head, and closes the insolvent case with the exact `CycleAdmissionInsufficient` event; `queue_progress_matrix_keeps_progress_and_coverage`, `queue_progress_handles_adjacent_removal` and `scheduler_continues_after_in_loop_close_and_executes_following_ready_actors` converge on the funded prefix with the insolvent tail closed, and the adjacent-removal witness asserts the canonical close event. This moved the lib suite to 604 passed / 271 failed / 5 ignored of `880` with seven fixed (the four scheduling witnesses plus the three admission-boundary `fees_and_funding` witnesses `paid_readiness_closes_only_when_pipeline_charge_cannot_fit_above_floor`, `user_fee_admission_boundary_is_exact_floor_plus_envelope` and `user_pipeline_admission_combines_adapter_lock_with_machine_charge`) and zero newly failing by failure-name set difference. A twenty-eighth cohort migrated the gate-anchored single-FIFO witness to canonical ring membership and retired four further legacy queue-topology witnesses whose carriers canonical publication never populates. `canonical_fifo_uses_one_physical_ticket_sequence` no longer reads `ActorReadyFrameChunks`/`next_queue_ticket`; it now asserts four latched Actors each hold exactly one generation-bound `Service(Pending)` membership with `queue_ticket` absent and reads the single circular `ServiceNodes` sequence in publication order. `canonical_tombstones_cannot_bypass_the_oldest_live_head` drove the removed `ActorReadyFrameChunks` primary and the test-only `paged_invalidate`/`test_head_discovery` oracles; `queue_ticket_exhaustion_closes_through_the_unified_sink` and `stale_close_entry_drains_as_tombstone_before_recreated_slot_runs` pinned the legacy `ActorReadyHead`/`ActorReadyTail` ticket namespace and `queue_ticket` field; and `mixed_materialization_ticket_order_is_reproducible_from_cursor_and_block_state` asserted a legacy ready-frame trace. The now-unused `restore_structural_queue_tombstone` and `mixed_materialization_ticket_trace` fixtures were deleted with them; canonical Service-ring topology, cursor removal, and generation-bound ring integrity remain covered by `canonical_service_ring_is_transactional_generation_bound_and_structurally_complete`, `canonical_service_round_handles_empty_removal_interruption_and_faults` and the `service_member_*` benchmarks. This moved the lib suite to 605 passed / 266 failed / 5 ignored of `876` with zero newly failing (one migrated plus four removals). A twenty-ninth cohort closed the canonical Service round's circuit-breaker gap in production: the round executed eligible heads and performed ordinary automatic terminal close even while `GlobalCircuitBreaker` was active, because only the legacy paged drain checked the breaker and the canonical round replaced that drain on the ordinary path. The round now derives the loaded classification once and returns a new `ServiceRoundEncounter::BreakerRefused` before any effect or terminal/admission close when the phase is `GlobalCircuitBreaker`, leaving exact ring placement and the semantic Hot owner unchanged and charging only the selector/probe envelope; the paged cutoff drain and head discovery treat that encounter as a clean pass stop rather than starvation. `frame_only_circuit_breaker_skip_uses_only_canonical_control` was migrated to canonical B+1 timing and canonical residence (one `Service(Pending)` member retained, no legacy `actor_control_cell`/`ActorControlLocators`, selector-only Weight). The already-canonical `breaker_defers_scheduler_owned_window_expiry_close` and `breaker_defers_pipeline_admission_apoptosis_without_partial_events` now pass unchanged, and the legacy-substrate `breaker_preserves_lower_level_unlatched_terminal_ready_authority` (which drove `wakeup_substrate_drain_key`, `preflight_paged_enqueue_authority`, and the removed legacy primary) was retired. This moved the lib suite to 608 passed / 262 failed / 5 ignored of `875` with exactly four failures cleared (three fixed plus one removal) and zero newly failing by failure-name set difference against `8a2129d1`. A thirtieth cohort migrated the starvation-telemetry witnesses to their canonical Drain-phase premise: the mandatory prepass runs with the full block reserve and services every conforming eligible Step, so a weight-blocked live head cannot persist across blocks through `run_idle`'s prepass continuation; the four witnesses now drive one Actors Drain pass per block through a new `run_drain_only` helper (no prepass, no multi-block continuation) and start at the B+1 eligible block, since the canonical occurrence published by `manual_trigger` is not eligible in its own block. `starvation_emits_observability_event_once_without_control_effects`, `proof_size_exhaustion_counts_as_idle_starvation`, `starvation_recovery_is_observable_once_and_healthy_idle_stays_sparse` and `breaker_freezes_starvation_count_without_recovery_event` now pass while `starvation_requires_live_fifo_work_and_clears_without_work` continues to pin the empty-queue negative. This moved the lib suite to 612 passed / 258 failed / 5 ignored of `875` with exactly four fixed and zero newly failing by failure-name set difference against `29f6780d`. A thirty-first cohort retired the explicitly inert legacy carrier-handoff seam and its three unreachable witnesses. Fresh-genesis `0.7.27` publishes only canonical semantic/process authority, so `create_system_with`/`manual_trigger` never populate a legacy primary control cell; the three `legacy_handoff_*` tests panicked at their `actor_control_cell(...)` lookup, and the dead `handoff_actor_publication`/`handoff_actor_publication_to_successor` production functions (each carrying an `#[allow(dead_code, reason = "...inert until all production lifecycle paths cut over together")]`) had no callers outside their `test_handoff_*` wrappers. The two functions, their two `#[cfg(test)]` wrappers and the three tests were removed together; the canonical resume/update successor path keeps its own `transition_actor_publication_to_successor` owner and `plan_actor_publication`/`preflight_actor_publication`/`publish_actor_publication` remain in production service. This moved the lib suite to 612 passed / 255 failed / 5 ignored of `872` with exactly three removed and zero newly failing by failure-name set difference against `22afad43`. A thirty-second cohort migrated four canonical ordinary-path timing witnesses off same-block legacy ticket premises: `canonical_fifo_executes_global_ticket_order_across_actor_types` now triggers four mixed User/System Actors at B, drives one `execute_cycle` pass at B+1 through the canonical Service ring (asserting exactly the `MaxExecutionsPerBlock` bounded prefix `[user_a, system_a, user_b]` in publication order and the deferred `system_b` at B+2), `repeated_trigger_same_block_yields_one_ticket_and_one_execution` asserts two same-block manual triggers coalesce into one `Service(Pending)` member with `queue_ticket` absent and one `CycleStarted`, `repeated_scheduler_pass_same_block_preserves_one_committed_step_turn` asserts the committed prefix publishes exactly one successor `Service(Pending)` and that a second same-block pass leaves that successor untouched, and `zero_step_opening_completes_without_step_or_run_state` asserts one canonical `Service(Pending)` member and runs its direct resource-accounting pass at the B+1 eligible block with a matching `BlockResourceState` instead of reading `queue_occupancy`. This moved the lib suite to 616 passed / 251 failed / 5 ignored of `872` with exactly four fixed and zero newly failing by failure-name set difference against `71ceba01`. A thirty-third cohort migrated three ordinary-path scheduling witnesses off legacy primary/FIFO premises: `eligibility_projection_uses_primary_authority_and_rejects_missing_primary` became `eligibility_projection_rejects_missing_canonical_contract_authority`, staging fail-closed classification by removing `ActorContractHeads` (canonical contract geometry) rather than a legacy primary control cell; `eligibility_projection_reports_exact_at_time_gate_and_consumption` now latches the AtTime occurrence through `service_canonical_temporal_frontiers(21)` and runs the cycle at B+1 through `run_next_idle` instead of the legacy `drain_overdue_wakeups_cursor` waiting substrate; and `manual_middle_step_preserves_canonical_control` now executes the first Step at B+1 through `Actors::execute_cycle`, asserts the canonical `ActorRunState`/`actor_hot` owner, and requires `ActorControlLocators` to remain absent instead of reading the removed legacy control cell. This moved the lib suite to 619 passed / 248 failed / 5 ignored of `872` (scheduling 97 passed / 27 failed) with exactly three fixed and zero newly failing by failure-name set difference against `bc7fa940`. A thirty-fourth cohort migrated three ordinary-path scheduling witnesses to canonical B+1/Service premises: `suspended_expiry_and_breaker_precede_liability_and_effect_deferral` now expects the B+1-served retry to be eligible at block 3, asserts canonical `ActorProcesses` residence plus `ActorControlLocators` absence instead of a legacy paged head, and services the paused window-expiry wakeup through `service_canonical_temporal_frontiers` before the drain pass; `scheduler_retries_manual_continuation_after_cooldown_without_new_signal` expects the two-block cooldown retry at block 4 and reaches it through the canonical `on_initialize`/prepass pattern, with `scheduled_wakeup_block` reading the canonical `DeadlineHandles`; and `run_retry_preserves_independent_external_timer_cadence` replaces the legacy `queue_ticket` assertion with canonical `ActorProcesses`/`ServiceNodes`/`TriggerDeadlineHandles` residence. This moved the lib suite to 622 passed / 245 failed / 5 ignored of `872` (scheduling 100 passed / 24 failed) with exactly three fixed and zero newly failing by failure-name set difference against `c8f558cb`. A thirty-fifth cohort closed the canonical Service round's simulation-agreement gap and migrated two canonical timing witnesses to their eligible-block premise. The canonical round already computed and applied a terminal close through the atomic `finalize_actor` owner, but it collapsed that outcome into the same `ServiceRoundEncounter::Closed` used for a benign already-considered head, so the canonical simulation branch of `simulate_actor_service` saw a non-`Eligible` encounter and returned `SimulationError::NotReady` instead of projecting the applied close. The encounter now carries a distinct `TerminallyClosed(CloseReason)` after the finalizer, the paged drain and head-discovery matches treat it as a clean pass stop/close, and the canonical simulation projects it as `AttemptDisposition::Closed(reason)`. This fixed `simulation_and_scheduler_reject_the_same_protected_fee_floor_boundary` (whose viability projection must also be taken at the B+1 eligible block) and `tests::lifecycle::classifier_projections_agree_on_breaker_terminal_and_paused_products`. `pause_and_breaker_gate_scheduler_owned_retry` now drives exactly one `on_initialize`/`run_prepass`/`on_idle` block at the breaker-gated and recovery blocks so the canonical breaker refusal no longer lets the shared helper advance into future rounds, and `scheduler_ignores_sparse_id_gaps` now runs its single service block through `run_next_idle` at the canonical B+1 eligible block. This moved the lib suite to 626 passed / 241 failed / 5 ignored of `872` (scheduling 103 passed / 21 failed) with exactly four fixed and zero newly failing by failure-name set difference against `c67ccc7b`. A thirty-sixth cohort retired five legacy-carrier scheduling witnesses whose premise canonical publication never establishes. Fresh-genesis `0.7.27` publishes only canonical semantic/process authority, so `ActorControlLocators`, `ActorWaitingFrameChunks`, `ActorReadyFrameChunks`, and `queue_ticket` are never populated for a `manual_trigger`/`create_system_with` Actor: `temporal_wakeup_drain_refuses_mismatched_schedule_qualification` panicked at its legacy `actor_control_cell` waiting-authority lookup and duplicated the canonical `temporal_occurrence_refuses_selector_and_schedule_mismatched_wake_qualification`; `retained_wakeup_deferral_preserves_capacity_rollback_and_rejects_corruption` read the removed legacy primary and the deleted `test_schedule_next_work_source` capacity seam; `retained_ingress_rejects_incomplete_authority_without_writes` drove `try_paged_enqueue`/`try_wakeup_substrate_schedule_inner`; `scheduler_fails_closed_without_consuming_a_corrupt_live_head` relied on `try_paged_enqueue`/`try_paged_invalidate` and a retained `actor_control_cell`; and `scheduler_close_rolls_back_on_fifo_topology_corruption` staged corruption through the removed `test_corrupt_queue_before_close_consume` legacy-FIFO seam, so the canonical Actor closed normally instead of rolling back. Canonical fail-closed classification, atomic publication preflight/rollback, and service-ring structural fault handling remain pinned by `eligibility_projection_rejects_missing_canonical_contract_authority`, `composite_publication_preflight_rejects_stale_resources_and_partial_canonical_authority`, `canonical_service_round_handles_empty_removal_interruption_and_faults` and `canonical_service_ring_is_transactional_generation_bound_and_structurally_complete`. The now-orphaned `test_corrupt_queue_before_close_consume`/`apply_test_close_queue_corruption` test seam and the unused `try_paged_invalidate` wrapper were removed with them. This moved the lib suite to 626 passed / 236 failed / 5 ignored of `867` (scheduling 103 passed / 16 failed) with zero newly failing. A thirty-seventh cohort retired seven legacy paged-FIFO/tombstone scheduling witnesses whose carriers canonical publication never establishes and closed one production defect exposed while migrating the last canonical temporal scheduling witness. Fresh-genesis `0.7.27` publishes only canonical semantic/process/Service authority, so `prepass_materialization_stays_behind_the_frozen_cutoff_until_next_block` and `hook_order_fixture_exposes_external_ticket_after_prepass_cutoff` (legacy `next_queue_ticket`/`queue_ticket` cutoff), `queued_actor_is_preserved_when_proof_budget_cannot_admit_probe` (legacy `execute_cycle_to_cutoff_with_resources` paged drain), `mandatory_prepass_reclaims_saturated_fifo_without_domain_spill` and `saturated_tombstone_queue_reclaims_head_before_ingress_and_recovers_deferred_work` (legacy `seed_saturated_tombstone_queue`/`ActorReadyFrameChunks`/`ActorReadyHead`/`ActorReadyTail`/`ActorReadyOccupancy` topology), and `admission_time_close_reasons_require_complete_queue_and_cleanup_budget` together with its now-orphaned `assert_scheduler_close_requires_atomic_budget` helper (legacy `execute_cycle` paged close budget) were retired. Canonical Service-ring structural, cursor, refusal, and close coverage remains in `canonical_service_ring_is_transactional_generation_bound_and_structurally_complete`, `canonical_service_round_handles_empty_removal_interruption_and_faults`, `service_ring_oracle_covers_empty_singleton_interior_cursor_and_wrap`, `canonical_service_round_preserves_markers_cursor_and_blocked_head`, and the starvation witnesses; `mixed_fifo_stops_at_corrupt_actor_without_touching_valid_suffix` was retired because its legacy paged primary is never created, and the architecture invariant-failure citation now points at `canonical_service_round_handles_empty_removal_interruption_and_faults`. The same cohort migrated `latched_at_time_replacement_does_not_retrigger_running_pipeline` to canonical B+1/`service_canonical_temporal_frontiers` timing and exposed a production defect: `prepare_opening_rearm_hot`'s `AtTime` branch set `consumed = true` but left a one-shot `TriggerDeadlineHandles` member and `trigger_wakeup_pointer` armed when a canonical schedule replacement had preserved a paid latch from a different Trigger family, so the shared `service_due_deadline_frontiers` frontier later observed a consumed occurrence and its processing transaction could never commit. The canonical `AtTime` opening branch now releases the stale Trigger member and clears the pointer before consuming the latch, and the migrated witness pins that no `TriggerDeadlineHandles` member or `trigger_wakeup_pointer` survives the opening. This moved the lib suite to 627 passed / 228 failed / 5 ignored of `860` (scheduling 104 passed / 8 failed) with exactly eight failures cleared (seven retired plus one migrated) and zero newly failing by failure-name set difference against `8d086ec4`. A thirty-eighth cohort migrated two canonical scheduling-planner witnesses and retired three legacy failure-injection witnesses. `next_work_plan_types_unsignaled_process_authority_without_writes` now plans the pending case at the real current block and detaches the canonical publication before pure destination planning (the planner's single-publication guard rejects an already-published Actor), reading its current-Step envelope from the canonical service authority; `supplied_run_is_the_only_consumed_scheduling_authority` advances its non-suspended fixture one block through `run_idle` and loads its admission/resources from `load_frame_actor_service_state`/`fixture_step_resource_envelope` instead of the removed legacy primary. `initial_placement_preserves_creation_and_reactivation_authority`, `temporal_replacement_publishes_exact_primary_and_preserves_failed_source` and `deferred_activation_preserves_source_on_failure_and_publishes_exact_waiting` were retired because their failure premise `test_fail_wakeup_placement_with_capacity` (and `seed_saturated_tombstone_queue`) affects only the legacy waiting-substrate placement path and primary control cell that canonical publication never establishes; canonical creation/activation/update publication completeness and atomic rollback remain pinned by `canonical_activation_preflight_derives_admission_without_a_legacy_primary`, `canonical_cadenced_deadline_publishes_pending_service`, `composite_publication_preflight_rejects_stale_resources_and_partial_canonical_authority` and `composite_publication_rolls_back_every_owner_after_carrier_mutation_failures`. This moved the lib suite to 629 passed / 223 failed / 5 ignored of `857` (scheduling 106 passed / 3 failed) with exactly five failures cleared (two migrated plus three retired) and zero newly failing by failure-name set difference against `34aa5e23`. A thirty-ninth cohort retired the three remaining legacy paged-drain cold-read weight-refusal scheduling witnesses and replaced them with one canonical Service-round cold-read witness. `idle_weight_refusal_reads_only_header_and_user_fee_prerequisite`, `suspended_weight_refusal_avoids_run_payload_and_funding_reads` and `running_fifo_head_avoids_cold_reads_before_eligibility_and_weight_admission` all staged their refusal through `execute_cycle_to_cutoff` on the legacy paged drain and `paged_head_entry`, which canonical publication never establishes, so under fresh genesis they read nothing and failed only their positive assertions. The new `canonical_weight_refusal_reads_no_actor_cold_state` drives `Actors::service_canonical_round_head` with a below-complete and a below-selector `WeightMeter`, asserts `ServiceRoundError::InsufficientWeight` with zero consumed and an unchanged storage root, records zero reads of `ActorProcesses`/`ServiceNodes`/`ActorContractHeads`/`ActorRunPayloads`, and then pins the positive control that an admitted round reads the ring and process authority and advances the cycle nonce. This moved the lib suite to 630 passed / 220 failed / 5 ignored of 855 (scheduling 107 passed / 0 failed) with exactly three failures cleared and one focused test added, zero newly failing by failure-name set difference against `89422623`. A fortieth cohort closed the ordinary-path execution timing cluster and the last legacy-premised test fixture. The shared `run_contract_authority` fixture now derives the admission from the canonical semantic owner through `load_control_authority_with_authority` and falls back to the legacy primary control cell only for pre-cutover Actors, fixing `retry_target_uses_only_cursor_local_count_and_last_attempt_block`. Six further ordinary-path timing witnesses were migrated to canonical one-Step-per-round / B+1 semantics: `running_state_rechecks_current_predicates_after_progress` and `manual_signal_during_running_is_ignored_without_a_future_cycle` now drive adjacent rounds through `run_next_idle` instead of same-block `on_idle`; `frame_only_effectful_auto_close_uses_terminal_nonce_without_scalar_hot` runs its terminal nonce close through `run_next_idle`; `continuation_can_complete_at_stop_cycle_without_replaying_prefix` now stages a genuine retry by committing the prefix in one round, failing the retry Step in the next, and resuming at the canonical `eligible_at` (replacing the removed legacy `ActorControlLocators::Ready` successor assertion with a canonical absence check); and `close_after_productive_cycle_waits_for_retry_completion` and `close_after_productive_cycle_keeps_retry_exhaustion_as_failure_terminal` resume their single-Step retry at the canonical suspension `eligible_at` rather than the legacy same-block premise. This moved the lib suite to 637 passed / 213 failed / 5 ignored of 855 (execution 47 failed) with exactly seven failures cleared and zero newly failing by failure-name set difference against `266be3c2`. A forty-first cohort closed a canonical retry-suspension production defect exposed while migrating the last legacy-premised execution window witness: `suspension_eligible_at` clamped a retry's `eligible_at` only upward to `window.start`, while `plan_process_destination` already clamped the precomputed deadline destination to the window terminal (`end + 1`). For a suspension whose cooldown reached past the window terminal, the persisted `ActorRunState.eligible_at` stayed unclamped (for example 202) while the destination key was the terminal (102), and the canonical service-round transition's `AttemptDisposition::Suspended` arm — which requires `destination.key == WakeupKey::Block(eligible_at)` — otherwise returned `AttemptTransactionError::Invariant`, rolling the whole attempt back. `suspension_eligible_at` now clamps `eligible_at` to `Self::window_terminal_at(&window)` as well, so the single semantic owner, the persisted Run eligibility, and the deadline destination agree; because `retry_eligible_at_loaded` re-derives the expected value from the same owner, the clamped value remains coherent for the classification read. `suspended_retry_wakes_for_window_expiry_before_cooldown` was migrated to canonical assertions (block-102 `DeadlineHandles` member and `ActorRunState.eligible_at`, `ActorControlLocators` absent, no legacy waiting authority) and now resets the stale `CurrentBlockResourceState` with `run_prepass()` before the block-102 `run_idle` drain pass, because the prior block's resource state otherwise made `on_idle` return early without servicing the deadline. This moved the lib suite to 638 passed / 212 failed / 5 ignored of 855 (execution 46 failed) with exactly one fixed and zero newly failing by failure-name set difference against `8985a384`. A forty-second cohort migrated the ordinary-path execution witnesses that still relied on a single same-block `run_idle` pass: canonical execution commits at most one Step per round and `run_idle` stops as soon as the committed prefix leaves the Actor `Running`, so `authored_abort_preserves_earlier_provisional_task_commit`, `continue_next_step_error_policy_proceeds_after_failure`, `cycle_summary_tracks_step_outcomes`, `percentage_of_current_uses_each_steps_available_balance`, `predicates_observe_current_step_state`, `skipped_stop_cycle_advances_to_the_suffix`, `staking_adapter_late_failure_rolls_back_partial_mutation`, `stop_cycle_commits_prefix_and_completes_before_unreachable_suffix` and `temporary_failure_keeps_continue_next_step_semantics` now drive the bounded `run_next_idle_to_completion` helper, which advances one explicit canonical round per Step until no Run is open and no occurrence is pending. This moved the lib suite to 647 passed / 203 failed / 5 ignored of 855 (execution 37 failed) with exactly nine fixed and zero newly failing by failure-name set difference against `2df8c50c`. A forty-third cohort migrated the ordinary-path retry timing cluster to canonical per-round execution and added one bounded absolute-block round helper. Canonical execution commits at most one Step per round and a manual or percentage occurrence is published for B+1, so a witness that staged its first attempt in the publication block or assumed several Steps in one same-block pass observed no attempt at all: `retry_later_local_attempt_cutoff_closes_without_prefix_replay`, `retry_later_resets_local_attempt_count_after_cursor_advancement`, `predicate_is_rechecked_from_current_state_after_retry`, `retry_re_evaluates_live_any_conditions_at_the_same_cursor`, `retried_percentage_steps_resolve_against_current_attempt_balances`, `user_retry_admits_and_charges_only_each_current_step` and `temporary_retry_backoff_is_one_two_four_eight_then_capped` now advance explicit canonical rounds through the new `run_canonical_round_at(block, weight)` helper (set block, `on_initialize`, mandatory prepass, `on_idle`), resume retries at the persisted `ActorRunState.eligible_at`, and split `queue_ticket`/legacy-hot premises into canonical `ActorProcesses`/`DeadlineHandles`/`scheduled_wakeup_block` assertions. Two block-number-sensitive premises were rebased one block later so the first canonical attempt still satisfies the precondition (`retry_re_evaluates_live_any_conditions_at_the_same_cursor` threshold 2 to 3) and the first charged round reports the committed prefix only (`user_retry_admits_and_charges_only_each_current_step` expects `pipeline_fee + prefix_fee` before the first retry round). This moved the lib suite to 654 passed / 196 failed / 5 ignored of 855 (execution 78 passed / 30 failed) with exactly seven fixed and zero newly failing by failure-name set difference against `79969851`. A forty-fourth cohort closed the canonical simulation budget gap and migrated its timing witnesses. The canonical branch of `simulate_actor_service` reserved the whole `budget.actor_control` from its local `BlockResourceState` and then called `service_canonical_round_head_inner` with no resource authority and a combined control-plus-shared meter, so the round's selector, terminal-close, zero-Step, and effectful control envelopes were never charged against the caller's `actor_control` budget: `simulate_current_contract` reported a successful projection (including `Closed(WindowExpired)`) even with `actor_control = Weight::zero()`. The canonical branch now passes its resource authority to the round with `control_owned_by_caller = false`, so the round reserves control from the `ActorControl` domain exactly as production does and insufficient control capacity maps to `ResourceDeferred`; the blanket reservation moves to the legacy branch that consumes it. `simulation_non_ready_terminal_cleanup_is_budgeted_and_custody_neutral` now passes unchanged. `simulation_zero_step_user_auto_close_preserves_budget_and_rollback_boundaries` and `simulation_projects_first_retry_suspension_and_rolls_back_actor_mutation` were migrated to the canonical B+1 eligible block, and the legacy-FIFO `simulation_terminal_ready_rejects_corrupt_fifo_span_without_cleanup` (which required an `ActorControlLocators::Ready` primary that fresh-genesis publication never creates) was retired; canonical simulation and production admission parity remain pinned by `simulation_and_scheduler_reject_the_same_protected_fee_floor_boundary` and `canonical_step_transition_matrix_has_production_simulation_parity`. This moved the lib suite to 657 passed / 192 failed / 5 ignored of 855 (execution 81 passed / 26 failed) with exactly four failures cleared (three fixed plus one retired) and zero newly failing by failure-name set difference against `7bd7d1a3`. A forty-fifth cohort closed the canonical simulation terminal-projection gap, migrated the per-block execution-bound witness, and exposed a further canonical close defect. `simulate_actor_service` returned `NotReady` for a terminal Idle Actor with no Service/Deadline residence (for example an exhausted cycle nonce) before it could classify, so `simulate_current_contract` could not project `Closed(CycleNonceExhausted)`; the canonical branch now takes the caller's terminal classification and, when the Actor has no residence, charges the identical selector plus close envelope against the resource authority and cycle meter before projecting `AttemptDisposition::Closed(reason)`, so an insufficient control budget defers instead of silently projecting a close. `execute_cycle_respects_max_executions_per_block` now drives the direct canonical drain pass (`Actors::execute_cycle`) instead of `on_idle`, whose extra leading Service head legitimately services up to `MaxExecutionsPerBlock` heads plus one per block. This moved the lib suite to 659 passed / 190 failed / 5 ignored of 855 (execution 83 passed / 24 failed) with exactly two fixed and zero newly failing by failure-name set difference against `1d109273`. The same cohort exposed the next production defect: a Cadenced Actor whose effectful Step reaches `ConsecutiveFailures` takes the `NextResidence::Close` branch, but `finalize_actor_from_consumed_state` returns `ActorInvariant`, so the attempt rolls back and the Actor stays latched and `Service(Pending)` in the ring forever; `system_immutable_actor_closes_internally_at_failure_threshold_without_tasks` and `close_after_productive_cycle_ignores_false_cycles_then_closes_immutable_actor` pin that missing close once the failure-limit finalizer is fixed. A forty-sixth cohort closed that canonical consumed-state close defect in production: `finalize_actor_loaded_inner` released every temporal Trigger residence through the legacy waiting-page `invalidate_wakeup_reference`, but a canonically published Actor stores its one-shot or cadence Trigger in the generation-bound `TriggerDeadlineHandles` carrier, so any canonical Actor with an armed temporal Trigger — the Cadenced failure-limit close, the `CloseAfterProductiveCycle` close, the `SchedulerIndexExhausted` close, and temporal apoptosis — failed pointer invalidation, rolled the whole attempt back, and stayed latched with a `Service(Pending)` member in the ring forever. The close now releases a resident `TriggerDeadlineHandles` member through the canonical `remove_trigger_deadline_member` owner (reading the generation from the semantic record) and clears the semantic pointer, keeping the legacy `invalidate_wakeup_reference` path only for a pre-cutover pointer. `system_immutable_actor_closes_internally_at_failure_threshold_without_tasks` and `close_after_productive_cycle_ignores_false_cycles_then_closes_immutable_actor` were migrated off the misaligned bare same-block `run_idle` onto a new `run_scheduled_cadence_occurrence` fixture that services the Actor's scheduled temporal frontier and advances one canonical B+1 round, and `wakeup_materialization_index_exhaustion_closes_without_an_attempt` now closes through the same canonical path. This moved the lib suite to 662 passed / 187 failed / 5 ignored of 855 (execution 85 passed / 22 failed) A forty-seventh cohort migrated the last two non-canonical execution cadence witnesses. `cycle_success_predicate_drives_failure_reset_auto_close_and_event_order` now drives each multi-Step cycle through the bounded `run_next_idle_to_completion` helper, because canonical execution commits at most one Step per round and the first `run_idle` pass stops once the committed prefix leaves the Actor `Running`; its first cycle still resets the inherited failure streak to zero and its second reaches the auto-close nonce target with the same seven-event order. `deferred_cycle_does_not_consume_auto_close_nonce_target` now observes a weight-blocked `run_drain_only` pass at the B+1 eligible block and then completes the successful cycle through one full canonical round, replacing its `run_idle`/`starvation_blocked_budget` premise, which could never block: the mandatory prepass always services the head under the full block reserve, so the fixture closed the Actor instead of deferring it. This moved the lib suite to 664 passed / 185 failed / 5 ignored of `854` (execution 87 passed / 20 failed) with exactly two fixed and zero newly failing by failure-name set difference against `9d3eb53d`. A forty-eighth cohort closed the canonical Run-state staging seam. `write_run_state`, `persist_run_progress` and `persist_run_suspension` staged their successor by reaching the pre-cutover physical control cell through `load_primary_control_cell`, so a canonically published Actor — which owns no legacy primary — returned `ActorRunInvariant` and rolled the staging back. The helpers now branch on `ActorControlLocators`: a canonically published Actor mutates its identity and Hot state through the one semantic owner (atomic compare-and-replace), inserts or removes the `ActorRunStateStore` successor, and reconciles the state hold exactly as the legacy placement path did, while a pre-cutover Actor keeps the full legacy primary/waiting/Ready publication. `actor_run_schema_round_trips_retry_position_and_typed_snapshot_surfaces` and `normal_running_progress_persists_one_causal_successor_and_rejects_stale_service_authority` now stage canonical Run state, and `canonical_loader_requires_run_state_exactly_for_suspended_state` was migrated off the legacy primary partitions to the canonical semantic hot, Run store, and admitted Contract geometry partitions. This moved the lib suite to 667 passed / 182 failed / 5 ignored of `854` (execution 88 passed / 19 failed) with exactly three fixed and zero newly failing by failure-name set difference against `ed92bf86`. A forty-ninth cohort closed the canonical Service round's User-retry insolvency gap and migrated the last legacy-premised execution retry/hold witnesses, implementing the behavior the Actors specification (§5.2/§9.4) and package architecture already required. The canonical round applied the legacy `apply_admission_loaded` User-Idle-latched `pipeline_capacity_sufficient` close but never the User-Suspended `action_capacity_sufficient` close, so a retry continuation whose sovereign could no longer cover `MinUserBalance` plus its current maximum Action fee re-suspended or deferred behind the protected floor instead of terminating. `service_canonical_round_head_inner` now derives `retry_action_insufficient` for a User `Suspended` Actor with a loaded current Step and closes through the same atomic `finalize_actor` owner with `CloseReason::CycleAdmissionInsufficient`, keeping terminal reasons and System or non-Suspended admission unchanged. Four `frame_only_*` retry/hold/expiry witnesses were migrated off bare same-block `on_idle` and legacy `Ready`/`Waiting`/`Unsignaled` locator premises to canonical B+1 timing and `ActorProcesses`/`ServiceNodes`/`DeadlineHandles` residence — `frame_only_exact_next_retry_completes_without_scalar_rehydration`, `frame_only_user_retry_preserves_installed_hold_without_scalar_rehydration`, `frame_only_user_later_retry_preserves_hold_through_block_waiting` and `frame_only_user_window_expiry_releases_hold_from_consumed_waiting_authority` — preserving the installed-hold-survives-suspension and hold-release-on-window-expiry assertions; `frame_only_underfunded_retry_apoptosis_closes_from_retained_ready_authority` and the canonical `user_retry_insolvency_closes_before_effect_capacity_deferral` now close the insolvent retry with `CycleAdmissionInsufficient` while asserting canonical Service-ring retention for the solvent deferred case instead of the removed legacy paged head. The now-unused `canonical_scheduled_wakeup_block` helper was removed. This moved the lib suite to 673 passed / 176 failed / 5 ignored of `854` (execution 96 passed / 11 failed) with exactly six fixed and zero newly failing by failure-name set difference against `d01867ce`. A fiftieth cohort closed the last legacy-premised execution cancellation and finalizer witnesses. `loaded_cancellation_missing_run_is_idle_only_and_never_resurrects_backing` and `loaded_cancellation_uses_supplied_run_despite_missing_or_stale_backing` no longer read the pre-cutover physical primary cell through `actor_control_cell`; both load the canonical semantic/Service admission through `load_frame_actor_service_state`, so the cancelled supplied Run is authoritative even when the `ActorRunStateStore` backing is absent or diverging, and the missing-Run Idle-only path still fails closed without mutation. `consumed_frame_finalizer_rejects_a_retained_primary` now stages a retained legacy primary with a direct `ActorControlLocators` insert and asserts the consumed-frame finalizer refuses with `ActorInvariant` before any mutation, then removes the injected locator and confirms the Actor remains active with no identity registry row. `run_attempt_missing_state_fails_without_panicking_or_mutating` drives the canonical `service_canonical_round_head` at the suspended retry's eligible block with the persisted Run backing removed, asserting the round fails closed with unchanged Hot state, zero events, and an unchanged storage root. This moved the lib suite to 677 passed / 172 failed / 5 ignored of `854` (execution 100 passed / 7 failed) with exactly four fixed and zero newly failing by failure-name set difference against `9f49d4ae`. A fifty-first cohort closed the legacy-premised execution timing/completion cluster and one canonical retry terminal-placement defect. `running_simulation_obeys_q1_and_returns_only_the_current_step` now runs its opening round at the canonical B+1 eligible block instead of in the trigger block, `eligibility_projection_reports_suspended_retry_then_ready_at_attempt_block` expects the B+1-served opening attempt to place retry eligibility at block 3 rather than 2, `maximal_current_amount_contract_needs_no_opening_snapshot` advances explicit canonical rounds to completion through `run_next_idle_to_completion` because canonical execution commits one Step per round, `run_attempts_have_unique_chain_coordinates_without_the_stored_ordinal` resumes the second retry through `run_next_idle` and the completion through `run_canonical_round_at` at the persisted two-block-backoff `eligible_at` (block 5), and `control_invocation_receipts_are_independent_of_actor_fee_exemption` drives every attempt at block 2 or the persisted `eligible_at` and returns a block-deadline retry to Service through the canonical deadline frontier instead of the retired `drain_overdue_wakeups_cursor`. The same cohort exposed a production defect on the effectful-step placement owner: `execute_effectful_step_on_service_with_deadline` precomputes a retry deadline for a possible temporary Suspended outcome and probes it before the attempt, but `later_retry_destination` matched a terminal permanent failure or authored abort only when `deadline == None`; a Suspended retry whose task then failed permanently with a precomputed future deadline fell through to `AttemptTransactionError::Invariant`, so the whole service round rolled back as `ServiceRoundError::ProcessResidenceMismatch` and the member never retired. The permanent-failure/abort arm now matches any deadline, consistent with the `exhaustion_reason` arm and the `Completed` arm, because a terminal failure removes the Run and leaves the precomputed deadline irrelevant. This moved the lib suite to 682 passed / 167 failed / 5 ignored of `854` (execution 105 passed / 2 failed) with exactly five fixed and zero newly failing by failure-name set difference against `5d60da9a`. A fifty-second cohort closed the last two legacy-premised execution witnesses and exposed a canonical Trigger-occurrence production defect. `loaded_step_returns_exact_persisted_successor_run` no longer builds its plan from the legacy `hot.queue_ticket`/`build_current_step_plan`/`paged_consume_head_at` seam; it uses `build_canonical_current_step_plan` and asserts the returned authority against the canonical `ActorRunStateStore`, and its Idle-orphan corruption case inserts a canonical Run-store orphan. `execute_loaded_single_step_core`'s Idle guard now also rejects a resident `ActorRunStateStore` entry so that direct seam fails closed on the canonical orphan partition instead of only the legacy `ActorRunHeads`/`ActorRunPayloads`. `canonical_step_transition_matrix_has_production_simulation_parity` was migrated off `enqueue_latched_actor`, `paged_head_entry`, `drain_overdue_wakeups_cursor`, and same-block `execute_cycle_to_cutoff` onto a new `latch_canonical_occurrence` fixture plus canonical Service rounds: manual and observation rows latch through the canonical Trigger owner, the Cadenced row services its creation-armed due temporal frontier, both Fresh and persisted-Run rows advance to the canonical occurrence block, and a local suspension is returned to Service through `service_canonical_temporal_frontiers`. While proving the Cadenced row the cohort exposed a production defect in `commit_canonical_trigger_occurrence_with_authority`: the function rebuilt the expected semantic admission from the caller's Contract, but `load_actor_service_state_with_control` deliberately reconstructs a bounded current-Step Contract whose body commitment differs from the stored body for any multi-Step Actor, so every AtTime/Cadenced occurrence on a multi-Step Contract failed the semantic CAS with `ActorInvariant`, rolled the temporal transaction back, and never latched. The commit now treats the stored semantic record as the authority, requiring the caller's generation, identity, Hot state, Run store, legacy-authority absence, and stored-admission trigger authorization to agree before latching that stored record, which restores multi-Step temporal publication without weakening the guarded identity boundary. This moved the lib suite to `684 passed / 165 failed / 5 ignored` of `854` (execution 107 passed / 0 failed, module green) with exactly two fixed and zero newly failing by failure-name set difference against `d1b6c6f5`. A fifty-third cohort retired the `wakeups` module's legacy waiting-substrate cluster. Fresh-genesis publication owns no legacy primary control cell and never populates the `WakeupCursor*`/`ActorWaiting*` paged waiting substrate, so thirty-nine witnesses were unreachable: they either anchored on `schedule_latched_service_wakeup`/`actor_control_cell`, drove `wakeup_substrate_schedule`/`wakeup_substrate_drain_*`/`wakeup_cursor_*`/`drain_overdue_wakeups_cursor` directly, injected `seed_saturated_tombstone_queue` capacity, or asserted legacy `WakeupCursorLen`/`ActorWaitingTails`/`ActorWaitingOccupancies`/`queue_ticket` topology. The removed witnesses were `paged_wakeup_substrate_replaces_and_invalidates_exact_slots`, `wakeup_replacement_rolls_back_when_existing_cursor_is_corrupt`, `wakeup_pointer_corruption_matrix_fails_closed_and_is_detected`, `wakeup_replacement_rolls_back_when_existing_page_or_slot_is_missing`, `wakeup_replacement_rolls_back_on_live_count_underflow`, `wakeup_cursor_capacity_overflow_fails_closed_and_preserves_existing_path`, `wakeup_page_index_overflow_fails_closed_as_namespace_exhaustion`, `wakeup_bucket_corruption_fails_closed_as_corrupted_topology`, `paged_wakeup_substrate_invalidation_rolls_back_on_cursor_mismatch`, `paged_wakeup_substrate_links_and_unlinks_middle_pages`, `paged_wakeup_drain_preserves_partial_progress_and_crosses_page_boundaries`, `paged_wakeup_drain_discards_explicit_orphan_references`, `cursor_wakeup_drain_recovers_sparse_overdue_blocks_without_scanning_gaps`, `cursor_wakeup_drain_halts_and_resumes_between_slot_units`, `cursor_wakeup_drain_stops_independently_on_reftime_and_proof_size`, `on_idle_wakeup_worker_respects_remaining_weight_in_each_dimension`, `wakeup_materialization_closes_when_queue_ticket_is_exhausted`, `waiting_removal_repairs_upward_and_reclaims_heap_tail_page`, `paged_wakeup_cursor_orders_sparse_blocks_across_page_boundaries`, `wakeup_drain_respects_max_wakeups_per_block`, `wakeup_worker_stops_at_its_own_weight_envelope_without_lending`, `wakeup_drain_preserves_bucket_when_proof_budget_cannot_admit_it`, `wakeup_drain_stops_at_the_sparse_future_minimum`, `wakeup_ownership_fails_closed_for_corrupt_actor_partitions`, `mixed_wakeup_bucket_rolls_back_valid_neighbors_around_corruption`, `saturated_enqueue_defers_latched_actor_and_preserves_failed_placement`, `saturated_enqueue_falls_back_to_exact_next_block_wakeup_not_silent_loss`, `saturated_enqueue_fails_closed_when_wakeup_fallback_also_fails`, `manual_trigger_fails_closed_when_wakeup_fallback_cannot_preserve_readiness`, `paged_wakeup_uses_the_exact_requested_block_without_spillover`, `defer_wakeup_deduplicates_repeated_manual_trigger_for_same_actor`, `typed_ingress_notify_classifies_wakeup_capacity_as_temporary`, `paged_wakeup_recovery_is_independent_of_sparse_actor_ids`, `canonical_waiting_publication_shares_and_releases_the_temporal_cursor_atomically`, `primary_service_membership_rejects_a_stale_pipeline_identity`, `temporal_service_preserves_exact_primary_wakeup_address`, `wakeup_materialization_fault_preserves_suspended_retry_until_repair`, `creation_wakeup_failures_roll_back_exactly` and `trigger_rearm_index_exhaustion_closes_before_the_due_attempt`. Canonical temporal deadline, close cleanup, cadence, window-expiry, Service-ring structural/refusal, and fault-recording coverage remains in `timer_wakeup_uses_exact_cadence_without_actor_phase`, `close_before_future_wakeup_removes_exact_waiting_membership`, `canonical_close_removes_future_tick_and_releases_state_hold`, `window_expiry_wakeup_closes_inactive_actor_without_identity_scan`, `paused_actor_retains_direct_window_expiry_wakeup`, `repeated_timer_close_churn_leaves_no_wakeup_tombstones`, `wakeup_materialization_index_exhaustion_closes_without_an_attempt`, `queue_saturation_at_block_max_cannot_create_same_block_wakeup`, `wakeup_fault_recording_admits_both_weight_dimensions_and_is_idempotent`, `creation_and_activation_before_cutoff_use_exact_next_block_wakeup` and the `scheduling`/`storage_and_api` canonical deadline witnesses. The now-unused `try_schedule_latched_service_wakeup` and `assert_on_idle_wakeup_insufficiency_preserves_state` fixtures and the test-only `test_fail_wakeup_placement_with_capacity`/`block_wakeup_cursor_drain_unit_weight_upper` seams were removed with them. This moved the lib suite to `684 passed / 126 failed / 5 ignored` of `815` with exactly thirty-nine removed and zero newly failing. A fifty-fourth cohort finished the `wakeups` module and closed the canonical schedule-replacement stale-deadline production defect exposed by its first remaining witness. `replace_canonical_contract_geometry` released the superseded `TriggerDeadlineHandles` member but left the semantic `trigger_wakeup_pointer` armed, so the successor publication re-used the replaced cadence/AtTime tick instead of re-deriving it; a schedule replacement now clears the pointer so the successor re-plans its temporal deadline from the replacement anchor, while an unchanged schedule keeps the exact outstanding tick. `cadence_update_replaces_live_future_wakeup_instead_of_accumulating` pins that `timer_schedule(20)` re-anchors to exactly one canonical `Tick(7)` member under `timer_schedule(5)`, and `cancelled_continuation_exactly_invalidates_its_wakeup_before_reprime` pins the canonical cooldown `DeadlineHandles` member and its exact release on cancellation. Four legacy waiting-substrate witnesses were retired because canonical publication never creates their premise: `ticket_and_terminal_window_wakeup_coexist_under_one_pointer` (legacy `queue_ticket`/`wakeup_pointer`/`WakeupCursorLen` coexistence), `terminal_window_wakeup_survives_queue_saturation_and_continuation` (legacy `seed_saturated_tombstone_queue`/`actor_control_cell` saturation), `continuation_attempt_rolls_back_when_retry_wakeup_topology_is_corrupt` (legacy `ActorWaitingOccupancies` corruption plus `queue_ticket` retention) and `pipeline_and_trigger_temporal_memberships_coexist_and_drain_independently` (legacy `ActorWaitingFrameChunks`/`wakeup_substrate_drain_key` topology). Canonical temporal/schedule-replacement, suspended-retry window clamp, busy-cadence, Service/Trigger membership and Service-round fault coverage remain in `busy_cadenced_occurrence_advances_deadline_without_future_cycle`, `next_block_cadence_rearms_after_each_deferred_opening_without_late_fifo_tickets`, `suspended_retry_wakes_for_window_expiry_before_cooldown` and `canonical_service_round_handles_empty_removal_interruption_and_faults`, and the protocol-coherence audit anchor for independent Pipeline/Trigger temporal topology moved to `next_block_cadence_rearms_after_each_deferred_opening_without_late_fifo_tickets`. This moved the lib suite to `686 passed / 120 failed / 5 ignored` of `811` (`wakeups` 16 passed / 0 failed, module green) with exactly six cleared (two migrated plus four retired) and zero newly failing by failure-name set difference. A fifty-fifth cohort migrated the `lifecycle` module's ordinary-path authority and timing witnesses off the legacy primary-control-cell substrate. Canonical semantic/process authority is now read through `load_frame_actor_state`/`load_control_authority_with_authority` and canonical residence is `ActorProcesses`/`DeadlineHandles`/`ActorSemanticStates`, so the contract-replacement/crossing-install/pause-resume cluster (`policy_only_contract_replacement_uses_frame_authority_with_canonical_control`, `user_contract_replacement_reconciles_state_hold_from_frame_authority`, `user_crossing_install_uses_frozen_class_with_canonical_control`, `crossing_schedule_replacement_preserves_frame_phase_with_canonical_control`, `temporal_schedule_replacement_invalidates_loaded_frame_reference_with_canonical_control`, `pause_and_resume_use_frame_authority_with_canonical_control`), the cancellation/close/deactivation cluster (`authored_cancellation_restores_frame_authority_with_canonical_control`, `authored_ready_close_removes_frame_authority_with_canonical_control`, `cadenced_waiting_deactivation_invalidates_frame_wakeup_with_canonical_control`, `ready_deactivation_consumes_frame_ticket_with_canonical_control`, `deactivation_removes_active_epoch_and_all_contract_fragments_without_orphans`), the hot-mutation/lockstep/creation/active-set cluster (`stable_hot_mutation_and_store_seams_update_primary_and_fail_closed_on_topology_fault`, `lifecycle_mutations_keep_frame_identity_and_hot_authority_in_lockstep`, `system_immutable_creation_rejects_manual_but_allows_internal_window_close`, `active_actors_set_maintains_integrity`), and the collision-orphan witness (`actor_id_collision_rejects_each_orphan_canonical_partition_without_writes`, whose first partition now stages an orphan `ActorRunStateStore`) assert canonical authority with legacy locators absent. The fail-closed cluster was renamed `*_fail_closed_without_canonical_authority` and now corrupts `ActorContractHeads` instead of removing a legacy primary (`pause_and_resume_...`, `permissionless_sweep_...`, `permissionless_sweep_many_rolls_back_an_earlier_close_on_missing_canonical_authority`, `close_and_deactivate_...`, `active_reactivation_...`, `contract_update_...`). Three ordinary-path timing witnesses now honor the canonical B+1 cycle anchor: `eligibility_projection_waits_for_cooldown_and_reports_next_block` waits for block 7, and `manual_trigger_waits_through_cooldown_without_second_signal`/`manual_trigger_waits_for_schedule_window_without_second_signal` assert the deferred eligibility (`WaitingBlock(7)`/`WaitingBlock(10)`) and serve it through `run_canonical_round_at` instead of an eager same-block `run_idle`. This moved the lib suite to `711 passed / 95 failed / 5 ignored` of `811` (`lifecycle` 94 passed / 11 failed) with exactly twenty-five fixed and zero newly failing by failure-name set difference. A fifty-sixth cohort finished the `lifecycle` module. Six witnesses migrated to canonical authority/residence/timing: `waiting_observation_replacement_late_failure_restores_source_and_old_feed_service` and `waiting_observation_replacement_moves_subscription_and_executes_only_new_feed` read `ActorSemanticStates`/`ActorProcesses`/`load_control_authority_with_authority` instead of the legacy primary control cell while keeping their observation feed/subscriber/fanout assertions; `eligibility_projection_reports_failure_limit_auto_close_and_nonce_exhaustion` wraps its direct `store_actor_contract` geometry rotation in a storage transaction (the layer an extrinsic dispatch establishes) so the canonical replacement seam can release ring residence; `auto_close_configuration_enforces_origin_mutability_and_target_rules` advances one block past the successful boundary mutation because canonical `run_idle` leaves the B+1 block with that mutation already recorded; `window_expiry_cancels_while_failure_cutoff_finalizes_before_close` serves the window-terminal expiry (block 102) and the retry/failure-cutoff blocks (105 and 107) through `run_canonical_round_at`; and `latched_contract_replacement_preserves_temporal_trigger_authority` advances one canonical B+1 round instead of a same-block `execute_cycle`, because the replacement republishes its retained latch for the following block. Five witnesses were retired as unreachable under canonical publication: `idle_close_removes_terminal_and_temporal_waiting_owners` and `idle_contract_replacement_rebinds_retained_waiting_admission_and_executes_due_work` (legacy `actor_control_cell`/`ActorWaiting*`/`WakeupCursorLen` close and retained-admission rebinding), `control_permanent_placement_exhaustion_closes_through_the_unified_sink` (legacy `ActorReadyHead`/`ActorReadyTail`/`WakeupCursorLen`; a canonical manual activation publishes no monotonic paged ticket, so the injected ticket namespace is inert), `manual_trigger_is_preserved_on_proof_size_defer` (legacy primary cell plus paged-tombstone weight envelope; canonical refusal and unchanged storage are pinned by `canonical_weight_refusal_reads_no_actor_cold_state`), and `typed_ingress_preflight_and_notify_close_permanent_exhaustion` (legacy `ActorReadyHead`/`ActorReadyTail` ingress exhaustion; canonical ingress close and balance-only behavior are pinned by `expired_ingress_remains_balance_only_and_closes_inline` and `typed_ingress_absent_destination_is_balance_only`). Canonical close/deadline release coverage remains in `canonical_owner_close_releases_process_and_deadline_residence` and `canonical_owner_deactivation_releases_process_and_deadline_residence`, schedule-replacement re-arm in `cadence_update_replaces_live_future_wakeup_instead_of_accumulating` and `temporal_schedule_replacement_invalidates_loaded_frame_reference_with_canonical_control`, pause/resume in `pause_and_resume_use_frame_authority_with_canonical_control`, and index-exhaustion close in `wakeup_materialization_index_exhaustion_closes_without_an_attempt`, the crossing index-exhaustion witness, and `queue_saturation_at_block_max_cannot_create_same_block_wakeup`. The now-unused `ActorControlLocation` import was removed. This moved the lib suite to `717 passed / 84 failed / 5 ignored` of `806` (`lifecycle` 100 passed / 0 failed, module green) with exactly eleven cleared (six migrated plus five retired) and zero newly failing by failure-name set difference. A fifty-seventh cohort finished the `fees_and_funding` module and closed a canonical simulation-classification gap. The canonical Service round mapped every attempt failure to `ServiceRoundError::ProcessResidenceMismatch`, so a canonical fee-collection refusal in `simulate_actor_service` projected as `Classification(ActorInvariant)` or `NotReady` instead of the interface-local `FeeCollectionFailed` the package architecture promises; the round now preserves `AttemptTransactionError::FeeCollection` as a distinct `ServiceRoundError::FeeCollection` (effectful and zero-Step opening paths) and the canonical simulation branch maps it to `SimulationError::FeeCollectionFailed`, while the paged drain and head-discovery consumers keep treating any round error as a clean pass stop. Thirty module witnesses were then moved off the legacy primary-cell/ticket/paged-weight substrate: canonical authority reads through `load_control_authority_with_authority` (`manual_trigger_uses_canonical_pending_authority`, `address_event_uses_canonical_pending_authority`, `window_expired_takes_precedence_over_balance_exhausted`, `busy_manual_occurrence_creates_no_future_cycle_latch_or_trigger_fee`) and canonical fail-closed staging by removing `ActorContractHeads` (`manual_trigger_fails_closed_without_canonical_authority`, `address_event_preflight_and_commit_fail_closed_without_canonical_authority`); the certified-selector ingress mismatch now mutates the `ActorSemanticStates` admission certificate plus its `ActorContractHeads` identity instead of `ActorUnsignaledControlCells`; `canonical_fee_bearing_activation_is_atomic_with_publication` takes its current-Step envelope from `fixture_step_resource_envelope` and detaches the canonical publication before republishing as Disabled; single-latch dedup asserts one canonical `ServiceNodes`/`Service(Pending)` member instead of `queue_ticket` (`one_ingress_matching_the_single_source_latches_one_ticket`, `identical_authoritative_transfers_retain_one_latched_ticket`); the direct-pass weight witnesses advance to the B+1 admission block (`non_invoked_task_releases_effect_weight_after_maximum_admission`, `valid_actual_control_replaces_the_maximum_in_pass_consumption`, `valid_zero_actual_control_charges_pipeline_but_no_action_fee`, `valid_zero_actual_effect_releases_effect_fee_after_invocation`, `missing_actual_control_weight_rolls_back_without_fee_collection`, `missing_actual_effect_weight_rolls_back_without_fee_collection`, `greater_than_reserved_actual_control_weight_rolls_back_the_complete_attempt`, `greater_than_reserved_actual_effect_weight_rolls_back_the_complete_attempt`) and pin canonical `service_round_begin_populated`/`service_round_probe_eligible`/`service_round_admit_eligible` envelopes, with `missing_actual_*` projecting `Some((0, 0))` rollback evidence and the `greater_than_reserved_*` round-bookkeeping warmed so the storage root isolates the attempt; fee-sink refusals run one canonical `execute_cycle` so the double-service `on_idle` hook cannot re-attempt (`condition_skip_pipeline_fee_failure_aborts_before_skip_event`, `pipeline_fee_route_failure_aborts_before_task_execution`); retry reloads resume at the persisted `ActorRunState.eligible_at` through `run_canonical_round_at` (`retry_later_funding_unavailable_resumes_without_new_logical_run`, `suspended_cycle_reloads_current_available_on_each_retry`); multi-Step reload witnesses advance one explicit round per Step through `run_next_idle_to_completion` (`percentage_of_current_reloads_after_prior_step_mutation`, `funding_unavailable_releases_action_fee_reservation_for_later_step_spend`, `preserve_spend_keeps_native_minimum_across_fixed_percentage_and_split_tasks`); and `user_portfolio_rebalancer_both_directions` drives two Cadenced occurrences through `run_scheduled_cadence_occurrence` plus per-round completion. This moved the lib suite to `747 passed / 54 failed / 5 ignored` of `806` (`fees_and_funding` 109 passed / 0 failed, module green) with exactly thirty cleared and zero newly failing by failure-name set difference. A fifty-eighth cohort finished the `crossing` module and closed three production defects exposed while migrating it. First, the canonical placed cohort had silently degraded to the scalar single: `build_placed_cohort_authority` still required a vestigial legacy paged-enqueue `queue_plan`/`queue_candidates` authority that the canonical `classify_fire_activation` no longer produces (its `PublishPending` arm returned `None`), so `preflight_current_placed_batch_authority` could never build a placed cohort and `FireCohortPlacedBatch` was unreachable in production while remaining reachable only through the direct benchmark entry. The vestigial `queue_plan` field, `CrossingCohortPreflight::queue_candidates`, the `queue_candidates.len() == candidates.len()` gate and the unused `preflight_paged_enqueue_cohort_with_authority` call were removed, and the canonical `PublishPending` placed fire is now the homogeneous `placed_immediate_fifo: Some(true)` branch, so a homogeneous tail or exact non-tail prefix again admits its full bounded cohort through the canonical `commit_canonical_trigger_occurrence_with_authority` owner. Second, `resume_actor` verified its successor publication with `successor != process`, but a paused Actor carrying a latched occurrence is published as `Serving`/`Service(Pending)`, exactly the shape resume republishes, so the equality refused a legitimate resume with `ActorInvariant`; the check now asserts the applied semantic lifecycle left the paused state instead of requiring a different process value. Third, `sync_crossing_compiled_authority` refreshed the compiled membership `admission_identity` and `counterpart_threshold` after a canonical Contract replacement but left the rotated generation-bound process identity stale, so a later homogeneous cohort commit mutated the stale `ActorRef` and failed closed as `Stale`; the compiled member and its `CrossingMembershipLocator` now follow the authoritative semantic generation, and `crossing_schedule_replacement_preserves_frame_phase_with_canonical_control` asserts phase/geometry preservation with the generation advance instead of exact locator equality. The module was then migrated to canonical observables: the pair/admission/fallback ladder and exact-component-capacity witnesses assert `ServiceNodes`/`ServiceHeader` residence and canonical `Service(Pending)` members instead of legacy `ActorReadyOccupancy`, `queue_ticket` and `test_queue_append_commits`; the herd/jump/sparse range witnesses assert one canonical `Service(Pending)` member per latched crossing; the non-tail tail-suffix witness latches its noise cohort through `latch_canonical_occurrence` and asserts the canonical non-tail cohort residence; and the replacement/rearm/zero-Step witnesses drive the canonical B+1 round instead of the legacy paged `execute_cycle`. Two witnesses were retired as unreachable under canonical publication with cited canonical coverage: `crossing_activation_under_saturated_fifo_latches_and_defers_exactly_once` (legacy `seed_saturated_tombstone_queue`/`ActorWaiting*` saturation and a block-wakeup pointer) and `crossing_ticket_exhaustion_closes_and_cleans_detection_membership` (legacy `ActorReadyHead`/`ActorReadyTail` ticket-exhaustion close), both superseded by `queue_saturation_at_block_max_cannot_create_same_block_wakeup` and `wakeup_materialization_index_exhaustion_closes_without_an_attempt`; `crossing_activation_requires_its_certified_threshold_selector` was retargeted to the canonical fast-path fallback (a stale `ActorActivationAuthorities` mirror disables only the read-only compact path, while the authoritative semantic admission still latches) and renamed `crossing_activation_falls_back_to_authoritative_admission_on_stale_compact_authority`; `crossing_same_tail_page_places_two_candidates_in_one_admitted_cohort` dropped its legacy `ActorReadyTail` capacity/corruption premise while keeping the pair placement and `SkipPostInstallationPair` rearm coverage; and `ready_crossing_replacement_rebinds_ticket_to_current_membership_and_contract` became `ready_crossing_replacement_retains_pending_service_for_replacement_contract`. This moved the lib suite to `767 passed / 32 failed / 5 ignored` of `804` (`crossing` 59 passed / 0 failed, module green) with exactly twenty-two cleared (twenty migrated plus two retired) and zero newly failing. A fifty-ninth cohort finished the `storage_and_api` module. It replaced the pre-cutover `canonical_service_cutover_waits_for_a_nonplacement_semantic_authority_owner` source scan with `canonical_semantic_publication_is_the_sole_actor_authority`, which pins the post-cutover creation/activation owner (`insert_active_actor` through `mutate_actor_semantic_state` to `publish_actor_publication`), the single semantic owner, each terminally-finalizing dispatch's shared `close_dispatch_weight_upper()` pricing and the generated pallet/runtime Weight bindings, and it dropped the two stale inventory rows for the removed `insert_unsignaled_control_authority` and `demote_ready_frame_to_unsignaled` raw-mutation owners. Eleven witnesses migrated to canonical authority/residence/timing: `process_publication_is_transaction_local_single_authority_and_rollback_safe` now expects `ProcessAlreadyExists` when a canonically created Actor already owns its process carrier; `canonical_service_ring_is_transactional_generation_bound_and_structurally_complete` asserts a mismatched Service residence instead of a legacy-authority refusal; `actor_identity_resolves_lifecycle_owner_and_fails_closed_on_missing_primary` removes the canonical `ActorSemanticStates` record; `production_contract_load_requires_its_certified_wake_qualification` mutates the semantic admission certificate plus its `ActorContractHeads` identity; `contract_replacement_updates_frame_admission_and_current_step_resources_in_place` reads the semantic admission and the `ActorContractHeads` inline Step resources; `admitted_contract_storage_loads_one_current_fragment_and_replaces_exact_tail` takes its certificate from the semantic owner and replaces geometry through `store_actor_contract`, asserting the process carrier survives geometry removal; `current_step_service_state_does_not_load_unreached_tail_chunks` pins that the canonical round fail-closes on incomplete geometry while the current-Step loader still serves the inline head fragment without the removed tail chunk; `run_head_and_immutable_payload_remain_coherent_across_progress` asserts canonical `ActorRunStateStore` cursor and contract-authority coherence across rounds; `running_execution_and_post_placement_ignore_unreached_tail_chunks` drives canonical B+1 rounds and `ServiceNodes` residence; and `canonical_loader_classifies_every_primary_partition_presence_mask` classifies the canonical `ActorSemanticStates`/`ActorContractHeads`/`ActorProcesses` presence domain. Four witnesses were retired as unreachable under canonical publication: `legacy_control_adapter_derives_ready_kind_and_rejects_malformed_or_ambiguous_cells` (legacy `actor_control_cell`/`compile_legacy_control_process` adapter; fresh genesis never creates a primary cell), `current_step_plan_builds_only_from_coherent_opening_authority` (legacy `queue_ticket`/`ActorReadyHead` paged-FIFO planning; `candidate_geometry` and the canonical Service-round witnesses own Opening-authority coverage), and `temporal_membership_try_state_rejects_unconsumed_at_time_without_pointer`/`temporal_membership_try_state_rejects_page_slot_pointing_at_different_actor` (legacy `trigger_wakeup_substrate_invalidate_inner`/`ActorWaitingFrameChunks` waiting topology; canonical temporal membership is pinned by `canonical_deadline_index_covers_order_pages_stale_state_and_rollback` and the scheduling/wakeups deadline witnesses). The now-unreachable legacy paged-FIFO planner and waiting-substrate invalidation helpers received explicit `#[allow(dead_code)]` markers because their only remaining callers are runtime-benchmark fixtures. This moved the lib suite to `779 passed / 16 failed / 5 ignored` of `800` (`storage_and_api` 112 passed / 0 failed, module green) with exactly sixteen cleared and four retired, zero newly failing by failure-name set difference against `8eef4067`. A sixtieth cohort finished the `core` module and closed a canonical cancellation production defect. `cancel_actor_publication` rebuilt its admission certificate from the caller's `source.contract`, but the canonical control loader (`load_actor_service_state_with_authority`) deliberately reconstructs a bounded current-Step Contract whose body commitment differs from the stored full body for a multi-Step Actor, so cancelling a multi-Step suspended Actor failed closed with `SchedulerIndexExhausted` and left the Run unresolved; the owner now reads the durable admitted geometry through `load_actor_contract` and uses that full Contract as the publication authority for the pre-cancel source and the Idle successor, which also cleared the `proptest_actor::burn_before_temporary_failure_remains_committed_after_cancellation` property. The remaining `core` witnesses were migrated to canonical timing and residence: `explicit_cancellation_preserves_committed_effects_and_emits_terminal_summary` runs the retrying swap Step in the following eligible round because canonical execution commits one Step per round; `reactivation_with_positive_nonce_uses_schedule_anchor_for_cooldown` observes the cooldown-gated occurrence with one Drain pass at the trigger block and the completed run at block 18; the four simulation witnesses (`single_step_simulation_respects_both_resource_lanes_and_preserves_non_head_state`, `matching_runtime_simulation_reports_stop_and_rolls_everything_back`, `zero_step_simulation_completes_without_step_records_or_state_mutation`, `fresh_current_plan_simulation_returns_runtime_trace_and_rolls_back_every_write`) simulate at the canonical B+1 admitted block, with the first targeting the Service-ring head and asserting the non-head follower's residence is untouched; `strict_head_of_line_heavy_head_deferral_preserves_follower_order` asserts canonical `ServiceNodes` residence and drives the constrained deferral through one `run_drain_only` pass before completing the heavy head over its remaining rounds; `weight_deferral_is_silent_when_both_dimensions_are_exhausted` uses the canonical service-round selector envelope; `system_immutable_indefinite_commitment_survives_breaker_mitigation` reads `fixture_step_resource_envelope`; `control_transition_fails_closed_at_transaction_depth_limit` expects the framework `Transactional(LimitReached)` because the canonical commit owns its own bounded transaction instead of mapping a transaction-nesting limit to `SchedulerIndexExhausted`; and `cancellation_fails_closed_when_the_run_outlives_its_process_residence` removes the canonical `ActorProcesses` carrier and asserts `ActorInvariant` with unchanged Hot state, Run store and events. `canonical_head_discovery_distinguishes_empty_head_and_blocked` and its now-unused `test_head_discovery` helper were retired because they drove the legacy paged-FIFO `live_queue_head`; canonical head discovery is pinned by `canonical_service_round_preserves_markers_cursor_and_blocked_head`, `canonical_service_idle_no_work_head_advances_without_an_attempt` and `canonical_service_round_handles_empty_removal_interruption_and_faults`. This moved the lib suite to `791 passed / 3 failed / 5 ignored` of `799` (`core` 52 passed / 0 failed, module green) with exactly thirteen cleared (twelve core plus the cancellation-fixed proptest property) and one retired, zero newly failing by failure-name set difference against `0c8e972a`. A sixty-first cohort finished the `proptest_actor` module, so the `pallet-deos-actors` lib suite is fully green. `assert_model_invariants` was migrated off the legacy `ActorControlLocators`/primary-cell/FIFO observable onto the canonical partition: the `ActorSemanticStates` active/dormant split must agree with the `ActorIdentities` dormant registry, `ActorContractHeads` and `ActorProcesses` must cover exactly the active set, the `ActorRunStateStore` head and payload tiers must agree, `active_actor_count`/`actor_identity_count` must match the partition, no canonical publication may recreate a legacy control locator, and each active Actor must load coherent canonical process/residence authority through `load_canonical_actor_semantic_state`. The run-state-machine property now reads canonical activity instead of a primary locator, so its lifecycle/observation operations actually execute, and `active_actors_count_invariant` counts `active_actor_count`. `seeded_scheduler_corruption_transitions_preserve_exact_pre_state` was retired as unreachable under canonical publication: every corruption variant targeted a pre-cutover carrier (`ActorWaitingCursorIndices`, a primary-cell `wakeup_pointer`, `ActorWaitingOccupancies`, `ActorReadyOccupancy`, `ActorReadyFrameChunks`) and a legacy mutation owner (`try_wakeup_substrate_schedule_inner`, `paged_invalidate`/`paged_drain_tombstones`), none of which fresh-genesis publication creates; canonical fail-closed rollback remains pinned by `canonical_deadline_carrier_covers_fragmentation_full_pages_move_and_rollback`, `canonical_deadline_index_covers_order_pages_stale_state_and_rollback`, `temporal_trigger_deadline_removal_is_independent_and_transactional`, `canonical_service_ring_is_transactional_generation_bound_and_structurally_complete` and `canonical_service_round_handles_empty_removal_interruption_and_faults`. The now-unreachable `schedule_latched_service_wakeup` fixture and the test-only `try_wakeup_substrate_schedule_inner` wrapper were deleted, and the `try-runtime`/`runtime-benchmarks`-only `mutate_primary_control_cell` fixture received an explicit `#[allow(dead_code)]` reason. This moved the lib suite to `793 passed / 0 failed / 5 ignored` of `798` (`proptest_actor` 6 passed / 0 failed) with exactly three fixed and one retired, zero newly failing by failure-name set difference against `3c94067f`. A sixty-second cohort made the canonical persistent Service ring the sole ordinary block drain and removed the legacy paged-FIFO fallback from that loop. `execute_cycle_to_cutoff_inner` serviced the canonical head first but, on `Empty`, fell through to `live_queue_head`, which probed and consumed the pre-cutover paged Ready FIFO over `ActorReadyHead`/`ActorReadyTail`; the loop now stops on `Empty`, so a system with no canonical Service resident has no ordinary drain and the legacy FIFO is never consulted. The now-unreachable `live_queue_head`, `classify_current_queue` and `head_blocked_by_weight` helpers, the `HeadDiscovery` and `ReadyHeadOwner` enums, the `ActorServiceSource::FifoHead` variant and its `paged_consume_loaded_head_at`/`paged_consume_closed_head_at` wrappers were deleted; `paged_consume_head_at_inner` is now a test/benchmark-only head consume and the two `scheduler_paged_consume_*` benchmarks use `paged_consume_head`. The vestigial cutoff and control-meter plumbing was dropped from the drain interior without changing the public test seam. One witness was migrated: `observation_change_execution_preserves_absent_scalar_control` now latches through the production Oracle fanout (`latch_canonical_observation_change`) instead of the test-only legacy compact activation, and `empty_materialization_families_charge_only_their_measured_probes_and_yield` dropped the retired legacy queue-scan probe. This moved the lib suite to `793 passed / 0 failed / 5 ignored` of `798` with zero newly failing by failure-name set difference against `04444065`; clippy, the default and `runtime-benchmarks` checks, and the runtime `actors_integration_tests` failure set (95/113/21) are unchanged. A sixty-third cohort retired the legacy compact ObservationChange activation producer. `process_observation_change_occurrence` reached `request_observation_activation_compact_with_cause`/`request_observation_activation_ordinary_with_cause` only when `load_observation_activation_state` returned `None`, but those wrappers re-loaded the same authority and therefore always returned either `ActorInvariant` for a canonical active record or `IgnoredStale`; the reaction owner now fails closed inline with exactly that decision and no legacy call site. The now-unreachable `ObservationTerminalHandling` enum, the `ObservationActivationOutcome::TerminalDeferred` variant and its two match arms, the `#[cfg(test)]` `request_observation_activation_compact` wrapper, `request_observation_activation_compact_with_cause`, `request_observation_activation_ordinary_with_cause`, `request_observation_activation_compact_inner`, `place_observation_wakeup`, and the observation-only paged-enqueue helpers `prepare_observation_ready_cell`, `preflight_paged_enqueue_observation_loaded` and `reserve_following_paged_enqueue_observation` were deleted; the test-only `classify_observation_activation_compact` parity helper is now `#[cfg(test)]`. The two `observations` witnesses were migrated: `observation_activation_uses_primary_pending_authority` latches through the production Oracle fanout (`latch_canonical_observation_change`) and asserts canonical `Service(Pending)` residence, and `observation_change_activation_requires_its_certified_feed_selector` asserts the canonical loader rejects the corrupted admission before the fanout fails closed. The `actors` package architecture now states the inline fail-closed reaction contract instead of a deferred observation fallback. This moved the lib suite to `793 passed / 0 failed / 5 ignored` of `798` (unchanged counts, no witness added or removed) with zero newly failing by failure-name set difference against `0c254fc0`; clippy, the default and `runtime-benchmarks` checks, and the runtime `actors_integration_tests` failure set are unchanged. A sixty-fourth cohort retired the unreachable `FireCohortClosed` Crossing work plan and the last production `request_activation` call site. `classify_fire_activation` can classify only `FireCohortPlaced` or `FireCohortCoalesced`, so the terminal-cohort branch of the Crossing transition owner that called the legacy paged-enqueue `request_activation` producer, together with its separate final-member membership cleanup and `with_activation(true)` return, never executed; the branch, the `FireCohortClosed` variant, its `crossing_plan_components`/`crossing_plan_weight` arms, the `crossing_fire_probe` match arm and the `crossing_terminal_cohort_preflight` benchmark were removed, the terminal weight arm now belongs only to `StructuralFault`, and that weight method stays bound for the runtime monotonicity witness. `request_activation` now remains only as a benchmark fixture. This left the lib suite at `793 passed / 0 failed / 5 ignored` of `798` (unchanged counts) with zero newly failing by failure-name set difference against `394ff179`; clippy, the default and `runtime-benchmarks` checks, and the runtime `actors_integration_tests` failure set are unchanged. A sixty-fifth cohort retired the legacy frame prime. `prime_frame_actor_schedule` was one of the last non-benchmark producers of the pre-cutover paged Ready FIFO: both remaining production call sites were unreachable for canonically published Actors — the Contract-replacement site ran only when `!canonical_republished`, and the function's canonical short-circuit returned `Ok(())` whenever the Actor already owned a process without a legacy control locator or unsignaled cell, while the `cancel_run` site ran after `cancel_run_internal` had already republished the canonical Idle successor — so both call sites and the function were removed together with its `PrimeSchedulePlan` dispatch, its `preflight_activation_enqueue`/`commit_paged_enqueue_transactional` paged placement and its `defer_wakeup_with_authority`/`try_wakeup_substrate_schedule_transition_with_authority` temporal fallbacks. `preflight_prime_schedule_loaded` and `initial_trigger_wakeup_tick` remain because the legacy activation-plan preflight and the canonical occurrence placement still share them. This left the lib suite at `793 passed / 0 failed / 5 ignored` of `798` (unchanged counts) with zero newly failing by failure-name set difference against `5d6dc018`; clippy, the default and `runtime-benchmarks` checks, and the runtime `actors_integration_tests` failure set are unchanged. A sixty-sixth cohort made canonical publication the sole temporal occurrence placement owner. `process_due_temporal_occurrence_loaded` routed a non-canonical Cadenced occurrence through the legacy `try_charge_automatic_trigger_occurrence`/`request_activation` paged-FIFO latch, but fresh-genesis publication never creates a legacy primary control cell, so the branch now fails closed as an incoherent pre-cutover carrier. `request_activation`, `request_activation_inner` and `preflight_activation_loaded` are now `#[cfg(any(test, feature = "runtime-benchmarks"))]` fixtures with no production caller; the now-uncalled `invalidate_ready_to_unsignaled_with_authority` was deleted and `invalidate_ready_to_unsignaled_inner`/`try_invalidate_ready_to_unsignaled` were gated to the same fixture-only scope. This left the lib suite at `793 passed / 0 failed / 5 ignored` of `798` (unchanged counts) with zero newly failing by failure-name set difference against `9c9e28a8`; clippy, the default and `runtime-benchmarks` checks, and the runtime `actors_integration_tests` failure set (95/113/21) are unchanged. A sixty-seventh cohort retired the last production consumer of the pre-cutover paged waiting substrate. The materialization coordinator's family-0 arm still drained the legacy `WakeupCursor*` heaps through `drain_overdue_wakeups_cursor_resuming`, but fresh-genesis canonical publication never populates that substrate, so family 0 now reports no work (`materialization_family_has_work(0, ..)` is false) and returns zero Weight while the retained drain primitive remains a pre-cutover worker only tests and benchmarks drive directly. The family budget still reserves family 0's maximum-unit minimum, and because the retired slot consumes nothing that quantum flows back into the shared lendable remainder for families 1 and 2, so the pallet `on_idle` weight witness and the proof-limited starvation budget were updated to the retired composition. This left the lib suite at `793 passed / 0 failed / 5 ignored` of `798` (unchanged counts) with zero newly failing by failure-name set difference against `db6735cf`; clippy, the default and `runtime-benchmarks` checks, and the runtime `actors_integration_tests` failure set (95/113/21) are unchanged.A sixty-eighth cohort retired the fixture-only legacy activation producers. `request_activation`, `request_activation_inner`, `preflight_activation_loaded` and `test_activation_plan_kind` were deleted together with the five `scheduling` witnesses and the single `storage_and_api` plan-kind assertion that exercised them, so the canonical Service-ring latch is the sole activation fixture. Five benchmark fixtures were migrated: `pipeline_admission_apoptosis`, `scheduler_paged_execute_cheap` and `scheduler_paged_execute_cheap_mixed` latch through a new `benchmark_latch_canonical_occurrence` helper (trigger-accurate family, generic trigger fee probe), `crossing_coalesced_cohort_preflight` latches through `benchmark_latch_canonical_crossing`, and `crossing_fire_pair_probe` dropped its vestigial 31-entry legacy queue-boundary cohort and occupancy assertion because canonical pair placement no longer depends on global ticket adjacency. `commit_activation_plan` and `preflight_activation_from_authority` remain because the pre-cutover temporal terminal branch still consumes them. This moved the lib suite to `788 passed / 0 failed / 5 ignored` of `793` (five retired witnesses, zero failures) with zero newly failing by failure-name set difference against `c97e011d`; clippy, the default and `runtime-benchmarks` checks, and the runtime `actors_integration_tests` failure set (95/113/21) are unchanged. A sixty-ninth cohort retired the generic activation plan. `process_due_temporal_occurrence_loaded` now classifies the loaded canonical Actor before publication: a terminal classification closes through the retained-state finalizer, and a nonterminal occurrence publishes through `commit_canonical_trigger_occurrence_with_authority`, so the legacy paged-enqueue ticket-exhaustion close (a saturated `ActorReadyHead`/`ActorReadyTail`) no longer blocks or closes a canonically published temporal Actor. `ActivationPlan`, `ActivationAction`, `PrimeSchedulePlan`, `preflight_activation_from_authority`, `commit_activation_plan`/`_inner`, `preflight_activation_enqueue`, `preflight_prime_schedule_loaded`, `defer_activation_wakeup`, `commit_paged_enqueue_transactional`, and the now-unconstructed `ActivationOutcome::{Coalesced,Closed}` and `ActivationFailure::Temporary` variants were deleted. `wakeup_materialization_index_exhaustion_closes_without_an_attempt` became `canonical_temporal_occurrence_ignores_the_legacy_ticket_namespace`, asserting the canonical occurrence still executes under a saturated pre-cutover ticket namespace. This left the lib suite at `788 passed / 0 failed / 5 ignored` of `793` (counts unchanged) with zero newly failing by failure-name set difference against `41ef9f73`; clippy, the default and `runtime-benchmarks` checks, and the runtime `actors_integration_tests` failure set (95/113/21) are unchanged. A seventieth cohort canonicalized the genesis temporal-anchor bootstrap and fixed a real genesis temporal liveness defect. Genesis installation records an uninitialized temporal anchor (`temporal_anchor_tick == None`) because no consensus timestamp exists during construction and publishes a tick-zero bootstrap deadline, but `process_due_temporal_occurrence_loaded` resolved that bootstrap by writing Hot through the legacy control seam and placing the full-delay deadline through `try_wakeup_substrate_schedule_transition_with_authority`, which for a canonically published Actor left no canonical `trigger_wakeup_pointer` or `TriggerDeadlineHandles` member (the retired production family-0 drain no longer serviced the legacy carrier). A canonically published Actor now initializes its anchor in `ActorSemanticStates` and re-places the deadline through `plan_deadline_destination` plus `insert_trigger_deadline_member` with the semantic `trigger_wakeup_pointer`, while a pre-cutover carrier keeps the legacy waiting-substrate transition and its index-exhaustion close. `uninitialized_genesis_cadence_reanchors_from_frame_authority` became `uninitialized_genesis_cadence_reanchors_canonically`, asserting the canonical `TriggerDeadlineHandles` `Tick(101)` member, semantic pointer and absent legacy locator/cell instead of the legacy primary. This moved the runtime `actors_integration_tests` failure set to `96 passed / 112 failed / 21 ignored` with exactly one fixed (`fee_sink_allocation_respects_security_phase_and_epoch_readiness`, which arms the genesis Fee Sink cadence from the first consensus timestamp through canonical `actor_hot` and the canonical deadline frontier) and zero newly failing by failure-name set difference against `8ef6eae3`, while the lib suite held at `788 passed / 0 failed / 5 ignored` of `793` (counts unchanged); clippy, the default and `runtime-benchmarks` checks are unchanged. A seventy-first cohort retired the last truly-dead staged helpers and legacy read accessors. `build_due_retry_service_plan` (the superseded canonical retry planner, now that canonical retries flow through `service_canonical_temporal_frontiers`) and `compile_legacy_control_process` (the storage-free legacy control-owner adapter whose production callers never materialized) were deleted, together with the unused legacy waiting-substrate read accessors `wakeup_pages` and `wakeup_cursor_pages`; a repo-wide stripped-comment reachability scan now reports no remaining pallet function with a single definition reference. This held the lib suite at `788 passed / 0 failed / 5 ignored` of `793` (counts unchanged) with zero newly failing by failure-name set difference against `ba74efc7`; clippy, the default and `runtime-benchmarks` checks are unchanged. The cohort also established the exact remaining legacy reachability: the `finalize_*_on_legacy_fifo` cluster, `publish_active_state_on_legacy_fifo`, `schedule_next_work_with_authority`, `enqueue_authority_loaded` and the paged `commit_paged_enqueue` writers are reachable only from the pre-cutover simulation branch of `simulate_actor_service` and from fixtures, so no fresh-genesis path can populate `ActorReadyHead`/`ActorReadyTail` and `paged_drain_tombstones`/`ready_work_exists` can never fire in production; the migration blocker for deleting that cluster is the `scheduler_inner_*` benchmark family, whose shared `execute_reachable_step_inner` driver still measures per-Step control through `execute_current_step_and_place` (legacy finalizer) instead of the canonical `execute_effectful_step_on_service_with_deadline`. A seventy-second cohort restored the `runtime-benchmarks` test target, which no longer compiled. `tests.rs`'s `mutate_actor_hot_coherent` still called the test-only `try_mutate_actor_hot_semantic`, gated `#[cfg(all(test, not(feature = "runtime-benchmarks")))]`; the helper now routes through `try_mutate_control_hot`, which follows the semantic owner for a canonically published Actor and the mirrored primary for a pre-cutover fixture, and the now-redundant `try_mutate_actor_hot_semantic` was deleted. `candidate_geometry` (compiled only under `runtime-benchmarks`) qualified its two `ActorReadyOccupancy` reads as `crate::ActorReadyOccupancy`. `cargo test -p pallet-deos-actors --features runtime-benchmarks --no-run` now builds, so the runtime-benchmark fixtures gained a runnable falsifier for the first time; the first run reports `893 passed / 195 failed / 5 ignored`, with the failures confined to the pre-cutover `candidate_geometry`/`waiting_integrity` fixture modules and the `scheduler_inner_*` benchmark tests. The default `pallet-deos-actors` lib suite stayed green at `788 passed / 0 failed / 5 ignored` of `793`, and the default/all-target and `runtime-benchmarks`/all-target Clippy `-D warnings`, the default and `runtime-benchmarks` checks and the workspace check all pass. **Exit:** V1/V2 and every promised release profile are served by one engine; absence assertions and ordinary-path tests agree.

- [ ] **N5.2 / Complete Weight Owners and Reachable State Domain.** Generate complete composed owners alongside caller conversion. Cover semantic publication/removal, ring rounds, actual Step/retry, all notice/check outcomes, final anchor capture, threshold arithmetic, whitelists/credits if selected, deadline extraction, scans, transfer refusal and cleanup. Reuse sound unchanged owners only with exact applicability. Include first/middle/last/full/fragmented/deep, cold/hit, selector/executor and rollback suffixes. Measure pure control flow and producer-side no-match work. **Exit:** every reachable production segment is owned; `actual <= reserved` is backed by a sound model, not two matching underestimates; no provisional Weight enters a release performance claim.

- [ ] **N5.3 / Known Hazard and New-Model Soundness Closure.** Close B1–B8 and all applicable inherited hazards with executable evidence or proven elimination. Add fixed-anchor drift, below-floor wakes, per-asset unit confusion, credit duplication/source spoofing, lost completion-to-park changes, stale episode acknowledgment and busy-state accumulation. Complete the round/generation differential suite and full-capacity recovery. **Exit:** no known supported-domain correctness/resource blocker remains; a design-level Accepted record or green source-string test alone cannot close a behavioral claim.

- [ ] **N5.4 / Physical Architecture Closure Gate.** Compose the actual retained semantic/process/ring/wake/deadline/cleanup implementation and its generated owners. Replace design-only closure labels with actual caller, test, binding and whole-operation evidence. Qualify physical choices if measured replacement costs contradict their purpose. **Exit:** one complete production-capable model; ordinary resident progress avoids the targeted republishing work, and no essential wake, capacity, cleanup or Weight proof is deferred to `0.7.28`.

## N6 — Useful Measurements and Finite Design Choice

- [ ] **N6.1 / Economic Goals, Workloads and Materiality Freeze.** Retain EXP-0136 W1–W12 where their premises survive. Before new decision runs, amend only the rows changed by parked-only `100 × ED` activation: recurrence, true/false churn, sparse Park, threshold/credit storms and balance review latency. Classify previously affordable subthreshold starts as intentionally outside the new promise, not missing successes. Preserve populations/horizons unless the accepted semantic change truly requires a documented revision. Freeze the V1/V2 fixture contracts before collecting their decision outputs. **Exit:** no post-result target adjustment; every sacrificed capability and cost transfer is visible.

- [ ] **N6.2 / Minimal Necessary Physical Choices.** Continue EXP-0137's selected baseline and its latest S/P amendment. Safety defects require correction regardless of experiment budget. At most one qualified performance-driven physical alternative campaign with one predeclared refinement is admitted after a complete representative slice identifies a material owner. The explicitly authorized net-balance versus parked-credit decision is a bounded semantic-mode choice under N3.9, not permission for endless carrier alternatives. **Exit:** one retained implementation; no ritual second prototype, arbitrary coefficient trigger or premature physical freeze against contradictory evidence.

- [ ] **N6.3 / Final Whole-Service Production Comparison.** Confirm final workloads on exact production Wasm with matched semantic/resource identities. Equal-new-semantics comparison is CS1/CS0 only; compare H/L separately by economic goal and declared changes. Include User fees, effects, rejection/censoring, notifications, review timers, residency, source lag and cleanup. Do not compare small deposits that intentionally no longer qualify as if the new engine had lost valid promised work. **Exit:** frozen useful materiality and protected limits hold, or a finite, honest negative result names the remaining decision; no fabricated universal speedup.

- [ ] **N6.4 / Design, Sacrifice and Release Freeze.** Record the chosen balance quantity, direction, floor, baseline lifetime, mode and whitelist semantics; recurrence and round order; retained retries; missed transient states; fees; and generation cleanup. Distinguish free/total balance detection from Available-based spending. Freeze one design only after V1/V2 and resource/evidence closure. **Exit:** reviewers can state what this Actor guarantees, what it no longer promises and why the measured tradeoff is acceptable.

- [ ] **N6.5 / Residency Benefit and Parking-Thrash Accounting.** Start instrumentation on V1, not another isolated helper. Count membership allocation/unlink, successor publication, semantic/process/node/header writes, input reads, retry transitions, effects, fees, time and remaining work. V2 adds final-baseline writes, per-credit no-match/subthreshold cost, negative checks, registration/acknowledgment changes, source scans, review timers and cleanup. Measure busy-credit storms and repeated unchanged reviews separately. **Exit:** saved republishing and avoided checks exceed their replacement costs on the claimed scope; no fictitious “one header write per Step” or free parked population.

## N7 — Bind, Independently Review and Publish

- [ ] **N7.1 / Final Cutover and Artifact Identity.** Bind the final source tree, benchmark Wasm, generated Actors and dependent Oracle/Router/effect weights, production Wasm, metadata, ABI/PAPI, bounds, cost/fee vectors and clients. Retain unchanged artifacts only with applicability evidence; incompatible authoring must be explicitly rejected/versioned. Ordinary public paths, independent embedding and all supported feature builds share the declared contract. **Exit:** one reproducible artifact set and no legacy/provisional numerical authority masquerading as final binding.

- [ ] **N7.2 / Independent Round, Wake and Resource Review.** Reviewers attack B2's distinguishing order, source/consumer saturation, watched-balance definition, baseline races, tiny-credit griefing, fallback source identity, callback rollback, full/deep heaps and old-generation sweep. Derive expected traces independently of implementation. Fix findings in their owners and rerun affected proofs, not the entire corpus by reflex. **Exit:** every material finding is resolved or removed from the enforced supported scope with explicit authorization; no claim relies solely on tests generated from the same algorithm.

- [ ] **N7.3 / Exact-Tree Assurance and Durable Evidence.** Run the repository's required full validation on the frozen final tree, plus the complete V1/V2 and supported-domain matrix. Retain decision-bearing raw outputs, commands, environment facts, benchmark and production identities, source manifests and regression seeds. Use the interactive-workstation reassessment protocol, not a quiet-host requirement. **Exit:** exact-tree assurance is reproducible, local versus external CI evidence is distinguished, and host noise is not used to excuse an unowned path.

- [ ] **N7.4 / Public Truth and Guarded Publication.** Reconcile specification, architecture, embedding, Wiki EN/RU, authoring and release notes. Explain the parked-only threshold and selected fallback without advertising it as a sender-spending restriction or universal notification guarantee. Publish measured benefits and limitations, unsupported forms and remaining noncritical research. Use the existing release procedure only after explicit task-owner authorization. **Exit:** tagged code, claims, accepted decisions and final evidence describe the same system.

---

## 4. Required Proof Matrix

Tests below are bounded witnesses, not mandatory new EXP IDs. Use the existing test/runtime infrastructure; retain current passing proofs only within their unchanged domain.

### 4.1 Balance, baseline and fallback

| Witness | Required result |
| --- | --- |
| Deposit during Step execution or retry | Current economic reads see it; no balance-trigger state/credit sum/next-Cycle promise changes. |
| Periodic Actor active between due points | Its own schedule remains authoritative; parked-balance notifications do not replace retry/cadence logic. |
| Last effect + actual fee + released fee reservation | Anchor equals the declared final authoritative balance, not a provisional value. |
| External credit between completion and Park installation | It is either included by the defined atomic snapshot boundary or remains visible relative to the retained completion anchor; never swallowed by a later reset. |
| Delta `T-1`, `T`, `T+1` | Inclusive qualification at T under the ratified default; exactly one owed check, not one per callback. |
| `+60 ED` then `+40 ED` with an intermediate negative review | Qualifies at the fixed-anchor total; review cannot drift the baseline. |
| `+60 ED` then `-60 ED` before sampled observation | No gross-volume qualification in net mode. |
| Decrease by T | Matches the explicitly frozen direction rule; no unsigned underflow. |
| Same observed balance after a failed start check | No perpetual duplicate activation; later material growth or another promised dependency change still gets its check. |
| Native and non-native assets | Correct minimum and units for each; no cross-asset sum or native-ED substitution. |
| Zero/unknown ED, threshold overflow or config revision | Explicit supported-domain refusal/requalification; no zero/dust gate or stale policy. |
| Hold/freeze/spendability change without watched-balance change | Does not pretend to be a deposit/balance movement; any independently promised wake remains covered. |
| Tiny unrelated credits and unregistered assets | Only the priced minimal producer path; no full Actor/Contract evaluation, baseline rewrite or Pending growth. |
| Pending already exists; newer change; destination full | Exactly one durable obligation survives; no blind acknowledgment or source loss. |
| Pause/retire/update/recreate before callback | No unauthorized revival; new generation has fresh declared arm state; old callbacks and sweep cannot affect it. |
| Fallback: allowed/disallowed/unknown source | Only certified whitelisted positive credits qualify; inference from transaction labels or net balance is forbidden. |
| Fallback: repeated callback, outer rollback, self-transfer, refund | No duplicate/counterfeit credit and no state retained from rollback; qualification follows the exact certified policy. |
| Fallback: several small permitted parked credits | Matches the selected aggregate/per-credit rule; no busy-period accumulation, unbounded history or LastFunding amount basis. |

### 4.2 Ring, continuation and ownership

| Witness | Required result |
| --- | --- |
| A served; cursor B; new D; next block | Exact ratified distinguishing order; model and carrier agree. |
| Empty/singleton/pair/interior/cursor removal and reinsertion | Bounded exact neighbor updates, no missing member or donated turn. |
| Resource refusal in each Weight component | Same eligible head retains priority across passes/blocks; no execution attempt counted. |
| Resident Step and adjacent retry | Same membership survives; current values are reloaded; cursor/attempt and prefix are correct. |
| Long retry Sleep/return | One obligation, no early service, no generation loss, no future Cycle. |
| Current start becomes false after first Step | The existing Cycle obeys Step conditions/error policies; it is not parked by reevaluating its original start gate. |
| Public create/update/deactivate/close | One semantic owner and one residence/terminal authority; no legacy fallback or direct-fixture-only success. |
| Atomic callback failure after tentative state/fee/index changes | Correct rollback of all owned state; the next Attempt cannot use abandoned cached authority. |
| Oracle update during scan/negative acknowledgment | Covered revision only is consumed; newer promised work and bounded backpressure persist. |
| New generation while old sweep runs | New code, custody, wake registration and same-block guards remain isolated. |

### 4.3 Resource and empirical distinctions

Keep separate: raw benchmark samples, fitted RefTime, generated ProofSize, logical accesses, distinct keys, recorded root-inclusive proof, requested/outstanding reservation, settled Weight, token fees, state holds and cleanup debt. A lower settled charge does not prove a smaller physical proof. An additional shared key may invalidate a claimed bound even if a simple coefficient comparison looks favorable.

Measure selection before mutation, failed preflight, producer callbacks and rollback suffixes. Reuse is allowed only within the recorded scope. No new selector is free because it has no DB reads. No old coefficient is sound merely because a new workstation run is noisy.

---

## 5. Evidence, Comparisons and Stopping

### 5.1 Existing results

- Preserve EXP-0120–0137 and earlier sealed observations. Add dated applicability amendments or a newly earned bounded claim only where this request changes meaning.
- Keep the existing correction for `cancel_run`; qualify its resource composition when lifecycle changes.
- The reported partial-round discrepancy is an open model/carrier consistency finding until the executable trace closes it. Do not describe it as an exploited released-runtime bug.
- Retained deadline heaps keep deep/full/fragmentation obligations. Dropping old Cadenced semantics alone does not remove them.
- The old due-frontier experiment already showed isolation from added non-due identities in its scope. Do not sell parking as though legacy execution necessarily scanned every Actor.

### 5.2 W1–W12 amendment discipline

EXP-0136 remains the starting twelve-workload contract, not a claimed set of completed current-engine runs. Amend the exact affected semantic premises before measurements. [R7]

Add parked-balance witnesses as explicit extensions to recurrence/churn/sparse/wake rows, not as a second independent mega-suite. A sparse event-Parked population must use a domain with actually proved notification coverage; do not manufacture 9,872 event-complete balance Actors if the host only supports timed review. Change a semantically impossible setup through an explicit pre-measurement amendment, preserving its economic purpose and publishing the distinction.

V1/V2 may establish scoped early results before the entire matrix. They cannot make a release claim without final binding and protected mixed-service/maintenance checks. Benchmark setup may create large fixtures through controlled helpers when clearly labelled, but the first ordinary-Actor integration witness must use normal public paths.

### 5.3 Success and finite fallback

- V1 success is real canonical execution with full resource ownership, not a throughput target.
- Net-balance mode success requires correct final-anchor semantics, supported coverage or explicit sampled review, bounded producer/check costs, declared acceptable latency and no busy-state activation work.
- If net mode fails, record the exact failure and run the one authorized verified-credit/whitelist alternative. Do not endlessly tune generic polling or expand all Oracle coverage before deciding this mode.
- Performance acceptance remains the predeclared whole-service materiality, with explicit tradeoffs. No mandatory `100 Actors/block`, `2×` or universal percentage is introduced.
- Safety correction never expires with a research allowance. Conversely, an attractive unrelated optimization does not extend the release automatically.
- If neither activation mode or the complete engine meets the agreed contract, return the exact unresolved decision. Do not silently weaken thresholds, drop unfinished Actors, count failed effects or publish design-only success.

### 5.4 Compact milestone report

For each V1/V2 handoff report only:

```text
exact commit/tree and runtime identities
public entrypoint and supported Actor profile
connected path and remaining blockers
actual executed tests and commands (not merely test source presence)
semantic/economic outcomes and chronological trace
complete resource owners plus remaining qualified domains
operation ledger, pending work and maintenance debt
next smallest connected deliverable
```

There is no additional report per helper. The next report should demonstrate a closed behavior, not another inventory of inert types.

---

## 6. Deferred Work and Retained Programmes

| Programme | Disposition |
| --- | --- |
| Broad page/fanout/bitmap/hash/key-layout/cache sweeps, shared immutable programs and speculative fast paths | Proposed `0.7.28` portfolio after one sound, measured current engine. Local redundant work can still be removed when equivalence and coverage are clear. |
| Timing-wheel/radix replacement by preference alone | Not active. The selected carrier gets one implementation; a bounded qualified S/P route can reopen it. |
| Full Local Causal Introspection / Network Physiology | Deferred; new diagnostics do not instantiate either programme. |
| General future-Cycle event history, LastFunding amount mode and a second scheduler | Excluded. The permitted parked-credit fallback is a narrow activation qualifier only. |
| Whole-program atomic recipes, Q1 relaxation and cost-priority bypass | Not selected; require separate semantic approval. |
| Universal event-complete balance/spendability interface for every host | Not required. Ship the explicitly supported net/review or verified-credit profile and do not overstate its guarantees. |
| Further Oracle feature expansion | Improve after the connected required paths; existing supported reachable callbacks still need full correctness and pricing. |
| Complete migration/renumbering of historical EXP files | Not required; qualify consumed claims only. |
| External keepers or intents | Excluded as a requirement for ordinary Actor progress. |
| `$BLDR` capital bridge, builder invoice settlement, User creation gate, vacant-slot custody claim, obligation identity and capacity-release scheduling | Preserve the historical task contracts and entry conditions; no automatic import into this release. [R11] |
| Broader emergency-breaker work | Separate scope, but actual reachable breaker/revocation/custody behavior remains in release coverage. |
| New signing/distribution platform | Not an invented release prerequisite; exact artifacts and essential durable evidence remain required. |

No deferred heading may absorb a supported-domain safety or resource blocker. Historical remediation for an old release is distinct from eliminating the path in this fresh-genesis release.

## 7. Final Release Statement

Before publication, the final Synthesis must answer:

1. Does an ordinary publicly created User Actor run entirely on the new semantic/process/ring owners, including retry and completion?
2. Which per-Step membership operations actually disappeared, and what operations replaced them?
3. What exact parked-balance quantity, direction, baseline boundary and `100 × ED` rule shipped?
4. Was fixed-anchor net mode retained, or was verified-credit/whitelist mode selected, and why?
5. How are busy periods, subthreshold changes, negative checks, late credits and configuration changes handled without lost wakes or baseline drift?
6. Are all attached Oracle/balance publishers matched by bounded paid consumers or explicit support gating?
7. Are Sleep, mutation, arbitrary unlink, full capacity and old-generation cleanup correct under the final resource model?
8. Which end-to-end economic result improved, under which changed guarantees, and with what latency, state and maintenance tradeoffs?
9. Which exact artifact/test/review identities support each of those claims?

The required conclusion is a working, measured machine with a precise activation product. It is not “all design experiments are Accepted.”

---

## Sources and Provenance

These links identify the inspected source basis, not files to overwrite. Read the working tree first. The task owner's latest parked-balance clarification supersedes conflicting planning premises, but does not assert that the new behavior already exists.

- **R1 — inspected branch/commit:** `0.7.27` at `6d44e92d75e7334b5763a6684c62e5279c39725f`, tree `7807ddb705ef885d9ee02d2c83714a412fb9e1f1`. [Commit](https://github.com/atmo-network/deos/commit/6d44e92d75e7334b5763a6684c62e5279c39725f).
- **R2 — checkpoint backlog and blocker map:** [BACKLOG.md at the inspected commit](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/BACKLOG.md).
- **R3 — current normative amendment:** [Actors specification §2.3 at the inspected commit](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/template/pallets/actors/docs/specification.en.md).
- **R4 — independent round oracle:** [current_state_semantic_oracle.rs](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/template/pallets/actors/tests/current_state_semantic_oracle.rs).
- **R5 — selected carrier and semantic loading code:** [actors/src/lib.rs](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/template/pallets/actors/src/lib.rs); [scheduler types](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/template/pallets/actors/src/types/scheduler.rs).
- **R6 — current lineage and hazard routes:** [Actors experiments index](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/.agents/skills/architecture-experiments/tracks/actors/experiments.md); [EXP-0135](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/.agents/skills/architecture-experiments/tracks/actors/EXP-0135.md).
- **R7 — existing workload/materiality contract:** [EXP-0136](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/.agents/skills/architecture-experiments/tracks/actors/EXP-0136.md).
- **R8 — physical-choice baseline and S/P amendment:** [EXP-0137](https://github.com/atmo-network/deos/blob/6d44e92d75e7334b5763a6684c62e5279c39725f/.agents/skills/architecture-experiments/tracks/actors/EXP-0137.md).
- **R9 — lifecycle close-allowance correction:** [commit 2d7b6f96](https://github.com/atmo-network/deos/commit/2d7b6f96e28d5a17dbf395ca4bcb7c459f1f37f0).
- **R10 — SDK background only:** official [`fungibles::Inspect`](https://paritytech.github.io/substrate/master/frame_support/traits/tokens/fungibles/trait.Inspect.html) distinguishes per-asset minimum, balance, total balance and reducible balance; official [`with_transaction`](https://paritytech.github.io/polkadot-sdk/master/frame_support/storage/transactional/fn.with_transaction.html) documents rollback and parent-transaction commit. These general APIs motivate the distinctions above; verify the actual pinned SDK and host adapter rather than importing a different version's behavior.
- **R11 — historical outstanding programmes:** [published v0.7.26 backlog](https://github.com/atmo-network/deos/blob/v0.7.26/BACKLOG.md), including its retained-programme references to earlier task contracts.
- **Planning provenance:** the previously supplied `DEOS_BACKLOG_0.7.27_LIVE_RING_FINAL.md` supplied the N-task identities. The working branch's already selected `Fixed`/`Percent` contract, completed narrow claims and current blockers take precedence over stale statements in that planning file.
