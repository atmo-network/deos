# DEOS Backlog

> Open framework work only. Completed delivery history belongs in `CHANGELOG.md`; normative semantics remain owned by specifications, architecture documents, code, generated metadata, and production Weight.
>
> Pre-`1.0` boundary: no DEOS network will launch before `1.0`. The `0.7.x` line remains fresh-genesis and may change storage, metadata, runtime APIs, validation topology, and release mechanics without deployed-lineage migration or live-network ceremony.

## DEOS 0.7.26 — Local Causal Introspection

Implementation starts only after 0.7.25 Geometry Freeze. The accepted [causal-introspection specification](./docs/actors-causal-introspection.specification.en.md) remains prepared future architecture, not a 0.7.25 implementation gate.

Architectural progression: frozen execution physics → exact local cause/effect/resource truth → later whole-network Network Physiology. Causal Introspection is its epistemic prerequisite, not a renaming of physiology. Global SCCs, feedback loops, cascade analysis, centrality, capital-circulation interpretation and homeostasis/pathology classification remain Network Physiology concerns, outside this local implementation.

Dependency: fix shared cause/descriptor/API types and binding identities first; topology extraction, host descriptors and System-role catalog may proceed under disjoint ownership. Coordinate cause propagation and resource/receipt hot paths. Stable runtime API/event/schema fixtures unlock parallel materializer/client work; integrated semantic/threat and scale assurance then precede correction, exact-tree convergence and architecture/Wiki closure.

- [ ] `Actors Introspection / Cause Identity and Propagation`: Define a typed domain-separated `CauseRef` for Manual, certified AddressEvent, Observation, Temporal, ActorEffect, ExternalProtocol, Host, and explicit gaps; bind exactly one cause to each useful `pending_signal: false -> true`, retain at most one distinct deferred next-Cycle cause, consume the right cause at Opening, preserve deterministic consequence ordinals/finality, and never alter readiness, FIFO, fees, or scheduler authority.
- [ ] `Actors Introspection / Local Topology and Dependencies`: Implement one bounded mutation-free extractor for stored and proposed Contracts that separates activation inputs, ordered Step precondition/amount/host dependencies, effect bundles, and error policy; preserve Opening versus Current timing, DNF, funding, completion, and `MaxContractSteps`, use typed dependency targets, and leave unresolved dynamic routes explicitly `Unknown`.
- [ ] `Actors Introspection / Host Effect Descriptors`: Add pure identity-bound descriptors for AssetOps, DexOps, LiquidityOps, StakingOps, FeeCollector, certified ingress, Oracle, TMC, and Router that report implicit dependencies, possible atomic effect legs, induced causes, atomicity/Weight/fee owners, and unknown dynamic surfaces, failing closed on unsupported capability without becoming a second host-semantic owner.
- [ ] `Actors Introspection / Atomic Effect Bundles`: Project at most one atomic `EffectBundle` per Step with Task kind, structural legs, amount truth, induced cause classes, rollback and native-fee owners, explicit mint/burn issuance delta, no bundle for `StopCycle` or rolled-back Tasks, finalized identity for resolved/committed truth, and no cross-asset value aggregation.
- [ ] `Actors Introspection / Resource Attribution and Receipt Decision`: Separate source, shared detector, Actor-specific control, Task effect, transaction/Trigger/Pipeline/Action/Task-native fees, state holds, and subsidy; compare event reconstruction, compact receipt event, and bounded receipt state by exactness and production cost, retain no state unless justified, label maximum versus actual, and never canonically divide shared work among Actors.
- [ ] `Actors Introspection / System Role Catalog`: Add stable typed role-to-sovereign-to-current-Actor bindings and catalog identity for active, dormant, custody-only, and vacant System roles, independent of display name, Actor ID, locator reuse, and Contract identity; preserve the current Activation DAG unchanged and validate genesis/TryRuntime cardinality and host binding.
- [ ] `Actors Introspection / Binding Identity and Runtime APIs`: Derive an introspection identity from the Actor semantic model, host descriptor identities, System-role catalog, and relevant runtime metadata, then expose bounded local methods for Actor topology, proposed Contract topology, current cause, System-role bindings, and binding identity without population scans, execution-block prediction, or raw physical storage geometry.
- [ ] `Actors Introspection / Causal Materializer`: Build a reference `Cause -> TriggerOccurrence -> Cycle -> Step -> ProducedCause` artifact from finalized canonical state/events, binding genesis, range, runtime code, metadata, introspection identity, Contract and role-at-event; support deterministic full/incremental equivalence, pre-finality rollback, explicit gaps, stable artifact hashes, and CLI/library access without protocol dependence or temporal-proximity inference.
- [ ] `Actors Introspection / Local Explainability Client`: Show role, activation input, current Pipeline/cursor and cause, Step dependencies, atomic effect bundle, possible produced causes, resource maxima, observed actual evidence, truth class, and runtime/introspection identity; remain local-only, distinguish structural/current/observed truth, expose stale materialization, and degrade to bounded runtime truth during provider failure.
- [ ] `Actors Introspection / Semantic and Threat Assurance`: Exhaust every Trigger, Predicate, amount/funding mode, Task, completion/error policy, cause family, descriptor, effect bundle, resource owner, System role, API, reorg/gap, alias/collision/staleness, unknown route, and scheduler-influence threat; prove stored/proposed parity, no duplicate lineage, exact rollback, no-double-charge, bounded APIs, no new hot-path read, and unchanged FIFO/Q1/Economic Zipper/class neutrality.
- [ ] `Actors Introspection / Materializer Scale Assurance`: Test full versus incremental deterministic output, input reordering, long Pipelines, high fanout, finalized reorgs, peak memory and rebuild time at 10,000, 100,000 synthetic, and 1,000,000 mostly dormant historical identities, while keeping off-chain scale evidence separate from runtime capacity claims.
- [ ] `Actors Introspection / Correction and Final Convergence`: Classify failures as specification, implementation, test, descriptor, evidence, or materializer defects; reopen semantics rather than weakening tests, remove unjustified hot-path or receipt state, correct cause/topology/resource ownership, and finish with package/runtime/materializer/client, no-std, Clippy, TryRuntime, embedding, security-review, production-Weight, Wasm, metadata, PAPI, and ABI evidence green on one frozen tree.
- [ ] `Actors Introspection / Architecture and Wiki Closure`: Publish causal-introspection architecture and update Actors integration, host-domain descriptors, read-model/client boundaries, and bilingual Wiki with local cause/topology/effect/resource/System-role truth, canonical-chain versus materialized provenance, provider degradation, and an explicit boundary deferring Network Physiology and global graph interpretation beyond Local Causal Introspection.

## DEOS 0.7.26 — Conditional Candidates

These two candidates remain conditional, not commitments created by the release heading or prerequisites for 0.7.25. Same-looking Task parameters do not establish the same economic intent. No universal Obligation registry, generic primary obligation references, coalesced economic intent, WaitingCapacity, second retry lane or capacity-priority scheduler is admitted without operation-specific evidence and an accepted owning specification. Do not reopen FIFO/Q1 geometry to fit new vocabulary.

- [ ] `Actors 0.7.26 / Operation-Specific Obligation Identity`: Start only from a concrete duplicate-commitment risk observed in production-shaped evidence. Classify the owning operation as idempotent, deduplicable, joinable, non-deduplicable, or uniqueness-critical; derive exact consensus-visible identity without duplicating Actor continuation; compare `N` independent requests against one obligation plus bounded dependents for state, ProofSize, RefTime, adapter invocations, external commitments, retry, and cleanup; specify and implement only an accepted operation-local owner rather than a universal registry.
- [ ] `Actors 0.7.26 / Capacity-Release Scheduling`: Consider a distinct capacity state only if `0.7.25` evidence proves materially different scheduler semantics from ordinary retry, such as event-driven release, no exponential backoff, or a separately bounded fairness/resource domain. Specify maximum live obligations, pending resolutions, external intents, retained retry state, fanout, and queue contribution before implementation; preserve one FIFO, Actor-owned continuation, Q1, cutoff, causal floor, and Step durability.

## Unscheduled Framework Convergence

Outside the Geometry Freeze dependency and without a release assignment. Preserve these contracts unchanged; only mechanically necessary frozen-C1 correctness and binding closure belongs to G3–G5. Shared lifecycle/runtime changes must not enter the 0.7.25 critical path by implication.

- [ ] `Runtime / $BLDR Capital Bridge`: Align the Treasury B runtime plan with the Builder Economy capital bridge by gradually consuming both LP reserve assets through DEOS Router, splitting recipient `$BLDR` equally between burn and BLDR Treasury, preserving retained-prefix custody and bounded retry semantics, and regenerating runtime tests, benchmarks, Weight, metadata, integration truth, and client liveness projection.
- [ ] `Runtime / Builder Invoice Settlement`: Replace the fixed BLDR Treasury funding-source payload with a validated bounded CIDv1 invoice identity and explicit governance-approved Mutable System Actor treasury account, implement `BaseFloorCapped` payout with typed below-floor `InsufficientTreasuryCapacity` failure, expose target/actual/capacity-limited receipts, and regenerate tests, benchmarks, Weight, metadata, integration truth, and client projections.
- [ ] `Actors / User Creation Gate`: Add a governance-owned `UserActorCreationEnabled` flag that is false at genesis, gates only signed User Actor creation and exact-slot recreation, can be enabled or disabled through the Root-governed pallet surface, and leaves existing Active/Dormant User Actors, owner controls, execution, close, and custody recovery unchanged; expose canonical status and regenerate tests, benchmarks, Weight, metadata, and client authoring state.
- [ ] `Actors / Vacant-Slot Custody Claim`: Add a signed immediate non-FIFO call that derives the caller's vacant User sovereign slot, rejects any occupied identity/index binding, accepts a bounded duplicate-free list of `AssetKind`, reads current balances on-chain, and atomically transfers every selected available balance to the owner under ordinary transaction Weight/fee only, independent of User creation enablement; cover stale UI input, zero balances, ledger minima, transfer failure rollback, weights, metadata, and client checkbox flow.
- [ ] `Actors / Emergency Breaker Closure`: Specify and prove the exact stop matrix of the existing Root-controlled `GlobalCircuitBreaker` instead of adding a second overlapping switch: halt User/System creation, dormant activation, Pipeline/Step execution, and economic Tasks while preserving custody, bounded topology housekeeping, breaker recovery, and explicitly admitted owner recovery/control paths; test every Trigger/materialization/lifecycle path, expose canonical breaker status to clients, and harden any path that can still advance forbidden Actor semantics while active.
