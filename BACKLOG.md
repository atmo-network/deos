# DEOS Backlog

> Open framework work only. Completed delivery history belongs in `CHANGELOG.md`; specifications own normative behavior and resource policy, code and tests own implementation, generated artifacts own their exact bindings, and Experiment Records own decision evidence. `BACKLOG.md` alone owns release scope and remaining work.
>
> Pre-`1.0` boundary: no DEOS network will launch before `1.0`. The `0.7.x` line remains fresh-genesis and may change storage, metadata, runtime APIs, validation topology, physical execution geometry and release mechanics without deployed-lineage migration or live-network ceremony.

## Status — `0.7.26` Release Evidence Open

Release readiness remains open after local PR #32 remediation. Continue corrected-candidate preparation; the pre-repair attestation and green CI on the old PR head do not cover this change.

- [ ] `Release / Evidence`: Run `./scripts/validate-local.sh full` on the prepared corrected candidate and bind passing evidence to its current source and artifact identities before release acceptance.
- [ ] `Release / PR`: Deliver the validated correction to PR #32 through a history-preserving route permitted by `AGENTS.md`, require fresh CI on the corrected head and resolve its review gate before any guarded merge, tag or publication.

Delivery history remains in `CHANGELOG.md`, campaign decisions and recorded next-release starting evidence remain in the Actors experiment track index, and artifact bindings remain in the release-assurance candidate attestation. Deferred programmes remain unselected.

Recorded release-assurance outlook items beyond the guarded flow — separate artifact packaging, a published `SHA256SUMS`, and platform or signing attestation — have no repository convention or tooling today. The flow publishes the tag and release notes while artifact identities stay recorded and verified in the candidate attestation, and adopting any of these capabilities requires a future explicit scope decision.

## Deferred / Not Currently Selected

Keep inactive unless explicitly selected by a future scope decision:

```text
CB2 / count-parameterized Crossing admitted-batch owner

CG / demand-aware family minimum policy

full Local Causal Introspection

Network Physiology

operation-specific obligation identity

capacity-release scheduling

unrelated framework work
```

Local Causal Introspection remains prepared design only; harness-only diagnostic tooling does not activate that programme.
