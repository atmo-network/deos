# DEOS Backlog

> Canonical open backlog for the framework.
> Pair with `AGENTS.md` for durable protocol/context rules and `CHANGELOG.md` for completed delivery history.

## Open Backlog

> This roadmap now tracks only remaining open work.
> Fully delivered sections were rotated out of this file so the backlog stays focused on unresolved implementation, rollout, policy, and dependency items.

> Current baseline assumptions:
>
> - the local `pallet-vrf` commit/reveal line is retired
> - native `$NTVE`-weighted collator security remains, but only across an explicitly trusted, team-operated invulnerable collator set for the current line
> - same-block randomness is not required
> - until relay randomness exists, the accepted fallback is deterministic previous-block-hash sampling plus that trusted team-operated collator set
> - the preferred future randomness path is **relay-beacon replacement** only if Safrole/Sassafras or another upstream line actually delivers a new parachain-consumable **per-block** protocol beacon
> - permissionless collators stay deferred until that kind of relay-beacon path exists
>
> Current shipped baseline already includes:
>
> - multi-asset share-vault staking with `stXXX` receipts
> - native `$NTVE -> stNTVE` liquid staking plus locked `NTVE/stNTVE` LP nomination inside one `pallet-staking`
> - AAA staking automation through portable generic `Stake { asset, amount }` / `Unstake { asset, shares }`, with native liquid staking routed by the runtime adapter, nomination security moved to locked `NTVE/stNTVE` LP, and the System AAA donation actor
> - bounded governance reward-memory + proposal lifecycle in `pallet-governance`, including exported GovXP participation/authorship counters
> - current launch governance policy frozen to a domain-scoped `primary + protection` hierarchy (`Native + $VETO` for protocol governance, `$BLDR + Native` for the canonical tactical domain), narrow admin recovery, bounded recent finalized-outcome retention, and the public ordinary cadence/enactment schedule now shipped on the current line
> - reserve-aware Zap slippage intentionally frozen to execution-plan build time for the current launch line
> - same-asset auto-compound reward settlement
> - mirror implementation-architecture docs for staking and governance
> - Polkadot SDK `2603` / node `1.22.1` runtime line with `system_version >= 3` and staged `:pending_code -> :code` runtime-upgrade behavior

## Open Runtime / Security Hardening

### 0. Harden template runtime weight and launch-readiness surfaces

- [x] `Generate and wire production XCM weights:` runtime benchmark registration now uses `pallet_xcm::benchmarking::Pallet::<Runtime>` plus a minimal runtime benchmark config, `./scripts/benchmarks.sh pallet_xcm` generates `template/runtime/src/weights/pallet_xcm.rs` with the standard `steps=50` / `repeat=20`, runtime config wires `pallet_xcm` to the generated `SubstrateWeight<Runtime>`, targeted XCM tests pass, and `scripts/audit-template-readiness.sh` passes without `--warn`.
- [x] `Classify and reduce placeholder runtime WeightInfo bindings:` timestamp, transaction-payment, parachain-system, message-queue, XCMP queue, session, and collator-selection now use concrete upstream `SubstrateWeight<Runtime>` implementations. The remaining `cumulus_pallet_weight_reclaim::Config` placeholder is documented near the config because SDK 2603 keeps its generated `SubstrateWeight` type private and the public `()` implementation returns the same measured constant weight.

### 1. Refresh template workspace documentation and navigation hygiene

- [x] `Repair stale staking wording in template workspace docs:` `template/README.md` and `template/pallets/README.md` now describe staking as liquid `stXXX` receipts, native `$NTVE -> stNTVE`, locked `NTVE/stNTVE` LP nomination, governance custody, and reward settlement instead of live native-binding security.
- [x] `Normalize asset-conversion test naming:` runtime integration test module/file spelling now uses `asset_conversion_integration_tests`, and the targeted runtime test set passes.

### 2. Progressively harden template review gates

- [x] `Add a lightweight template readiness audit script or documented command set:` `scripts/audit-template-readiness.sh` now checks production XCM fallback weights, unclassified runtime weight placeholders, stale template staking aliases, and asset-conversion test naming drift without building the workspace; `scripts/README.md` documents the entrypoint.
- [x] `Turn template review findings into regression coverage where practical:` `scripts/audit-template-readiness.sh` now catches fallback XCM weights, unclassified runtime weight placeholders, stale live-contract staking aliases, and asset-conversion naming drift; generated XCM weights and targeted XCM/runtime tests cover the fixed production-weight surface.

### 3. Migrate staking to the new `NTVE/stNTVE` liquidity-backed nomination contract

> Target spec: [`docs/staking.specification.en.md`](./docs/staking.specification.en.md).
> Architecture docs should be updated only after implementation truth lands in code.

- [x] `Resolve 0.3.0 audit follow-ups before treating the staking rework as fully stabilized:` a focused audit of `e859615..4eb290e` found no failing targeted tests, and the identified launch-line regressions/hardening debt are now closed inside this migration epic.
  - [x] `Close the native nomination reward settlement escape hatch:` generic `claim_reward(asset_id = NTVE, epoch)` and `claim_reward_batch` now reject the native staking asset, `claim_nomination_reward_batch(epochs)` is implemented for liquid native nomination reward settlement, and pallet regressions cover generic rejection plus native batch claiming.
  - [x] `Regenerate staking runtime weights from the expanded benchmark set:` production `pallet_staking` benchmarks have been rerun with `steps=50` / `repeat=20`, and `template/runtime/src/weights/pallet_staking.rs` now reflects the expanded collator LP custody, governance custody, and native nomination reward benchmark set.
  - [x] `Make epoch reward rollover genuinely bounded and resumable:` epoch close now persists `PendingRewardEpochRollover`, processes touched asset buckets up to `MaxRewardRolloverAssetsPerBlock` per `on_initialize`, resumes unfinished rollovers across blocks, and only advances `LastProcessedRewardEpoch` / native reconciliation after the cursor completes.
  - [x] `Keep AAA staking portable instead of DEOS-native-specific:` AAA now exposes only generic `Task::Stake { asset, amount }` plus `StakingOps::stake`, with native/liquid staking routing handled inside the runtime adapter; the separate `StakeNative` task, `stake_native` adapter method, and `StakeNativeExecuted` event are removed from the AAA contract.
  - [x] `Repair stale documentation introduced by the 0.3.0 cutover:` docs hub plus AAA specification/architecture wording now describe liquid `stNTVE`, locked `NTVE/stNTVE` LP nomination, and native nomination rewards instead of presenting native binding or operator-aware native staking as live truth.
  - [x] `Regenerate the generated wiki projection from the refreshed documents:` affected wiki pages now reflect the native staking / AAA portability baseline, native staking bootstrap call-preparation tooling, and closed staking migration state across English and Russian mirrors.
  - [x] `Finish web-client staking surface hardening before productizing it:` `StakingWidget` now gates availability/action writes on the canonical pool view, parses token amounts as exact 12-decimal strings, surfaces wallet NTVE/stNTVE/LP balances via the bounded known-asset projection, provides safe-max fill helpers for LP/native custody forms, and loads bounded native nomination reward claimability before claim/compound actions.
  - [x] `Fix machine-readable output for the local bootstrap readiness probe:` `scripts/bootstrap-native-staking-local.sh check --json` now suppresses shell banners/log lines before invoking the node query path, preserving human output for the default mode while keeping JSON mode parseable for automation.
- [x] `Implement the native staking/liquidity/governance migration to the new launch contract:` the current launch contract now replaces native `stNTVE` balance-binding security with locked `NTVE/stNTVE` LP nomination, keeps `stNTVE` as the yield-bearing native staking receipt, moves selective nomination rewards to epoch accounting, and keeps transfer events out of staking security/reward truth. Future swap/mixed-route donation acquisition remains explicitly policy-gated below rather than blocking this migration baseline.
  - [x] `Rewrite the staking specification around the new target contract:` `docs/staking.specification.en.md` now defines yield-bearing `stNTVE`, zero-fee `NTVE/stNTVE` liquidity, AAA balanced LP donations, locked-LP collator nomination, aggregate `NativeVotePower` governance locks, frozen vote power, and epoch-based nomination rewards.
  - [x] `Design the migration boundary from shipped NativeBindings to locked LP nominations:` because this repo is still a pre-fork framework line, the new implementation should reset the pallet schema rather than preserve in-repo upgrade archaeology; old `NativeBindings`, cached native-delegation storage, repair cursors, reward touch/snapshot storage, truncated-epoch markers, and same-asset reward liabilities are retired/reset in the reference code, while downstream live forks must own any explicit migration from their deployed state. Existing `stNTVE` receipt identity and staking-pool accounting remain the economic baseline, but old bindings do not silently become locked-LP backing because users must explicitly acquire and lock `NTVE/stNTVE` LP under the new contract.
  - [x] `Map implementation ownership across staking/governance/router/AAA before code surgery:` `pallet-staking` owns native pool accounting, `stNTVE`, locked `NTVE/stNTVE` LP nomination, LP-value snapshots, and nomination reward liabilities/claims; `pallet-governance` owns aggregate `NativeVotePower` locks, frozen per-referendum vote power, enactment-horizon lock extension, and governance-coefficient export; Axial Router / runtime adapters own the zap quote/execution route used by donation and compound paths; System AAA owns the recurring LP-farming donation actor that calls the bounded donation helper.
  - [x] `Refactor native staking entry/exit around liquid stNTVE only:` `stake_native(amount)` now mints liquid yield-bearing `stNTVE` without choosing or updating an operator; the transitional operator-aware `stake_native_with_operator` compatibility surface has now been removed after locked-LP nomination became the authoritative security path.
  - [x] `Create and bootstrap the canonical NTVE/stNTVE AMM pool:` local-dev bootstrap, production/operator sequencing, readiness probing, and plan-only launch call preparation are now in place; `bootstrap-native-staking-local.sh prepare-calls` emits the next Root/governance staking-admin or signed operator call data needed to register/initialize staking, create the canonical pool, and seed liquidity without relying on dev presets.
    - [x] `Reserve the LP asset namespace before runtime-mediated pool creation:` `AssetConversionAdapter` now seeds `NextPoolAssetId` into the `TYPE_LP` namespace before creating pools so canonical router/runtime-created pools do not collide with local, staked, or foreign asset ids; runtime test helpers reuse that policy instead of ad hoc per-test storage patches.
    - [x] `Bootstrap native staking assets and pool seeding for local dev:` dev/local genesis now registers the local native-staking asset, initializes the `stNTVE` receipt, and seeds the LP asset-id namespace through staking genesis; `07-seed-web-client-state.sh` now stakes Alice's local NTVE and creates/funds the local `NTVE/stNTVE` pool when empty.
    - [x] `Specify production/operator pool bootstrap sequencing:` `docs/staking.architecture.en.md` now records the required production sequence: staking asset registration, LP namespace seeding, canonical pool creation, balanced bootstrap liquidity, readiness checks, guarded AAA activation, and fail-safe remediation if launch preparation is incomplete.
    - [x] `Add plan-only operator readiness probe:` `scripts/bootstrap-native-staking-local.sh check` now checks live local NTVE/stNTVE asset readiness, staking exchange rate, canonical pool liquidity, and Native Staking LP Farmer AAA presence, then emits a human or JSON bootstrap phase plus recommended action without submitting transactions.
  - [x] `Ship deterministic LP-donation acquisition for the current policy:` the current LP-farming donation path can start from `$NTVE`, compute the stake-vs-donate split from current AMM reserves plus the `stNTVE` exchange rate, mint the required `stNTVE` side, and donate balanced liquidity without minting LP. Swap/mixed-route acquisition is now tracked only as a conditional future policy slice below.
    - [x] `Pin the current pallet-asset-conversion donation invariant in runtime tests:` direct balanced transfer into the `NTVE/stNTVE` pool account increases both reserves while LP total issuance stays fixed, proving the intended donation primitive can be built on top of the current pool accounting.
    - [x] `Add a bounded runtime donation helper for already-balanced assets:` `AssetConversionAdapter::donate_balanced_liquidity` now checks pool existence, non-zero reserves, ratio tolerance, and transfers both sides directly into the pool account without minting LP; the runtime regression covers both ratio rejection and successful zero-tolerance donation.
    - [x] `Add the deterministic stake-acquisition path:` `AssetConversionAdapter::donate_native_staking_liquidity_from_ntve` now starts from an `$NTVE` budget, computes the stake-vs-donate split from current AMM reserves plus the `stNTVE` exchange rate, stakes the calculated amount to mint `stNTVE`, and atomically donates both sides through the balanced helper without minting LP.
  - [x] `Introduce the AAA LP-farming donation actor:` the actor id, skeleton, activation policy, deterministic `$NTVE -> stNTVE` acquisition path, generic `DonateLiquidity` execution primitive, and donation event surface are now in place so LP farming can increase `NTVE/stNTVE` reserves and LP token value without changing LP supply.
    - [x] `Add the generic AAA execution primitive and runtime actor skeleton:` `Task::DonateLiquidity` now resolves a declared pool input budget through the AAA amount-resolution pipeline, calls a generic runtime donation adapter with `(asset_a, asset_b)`, emits `LiquidityDonated`, and the Native Staking LP Farmer System AAA (`aaa_id = 14`) is reserved at genesis as a timer/noop skeleton until pool activation.
    - [x] `Add the first actor execution regression:` runtime coverage now updates a System AAA to the native staking LP-farming plan, funds it with `$NTVE`, executes the donation task, verifies AMM reserves increase while LP supply is unchanged, and confirms the donation event.
    - [x] `Wire activation policy for the canonical actor:` `TmctolGenesisSystemAaas::activate_native_staking_lp_farming` now refuses activation until the native staked receipt asset, native staking pool, Native Staking LP Farmer actor, and non-empty `NTVE/stNTVE` AMM all exist, then replaces the genesis noop plan with `build_native_staking_lp_farming_execution_plan` through the canonical AAA control path.
  - [x] `Replace native collator backing with locked NTVE/stNTVE LP:` lock/unlock/redelegate storage and calls now exist, conservative native-equivalent locked-LP value is the authoritative session-ranking backing, and live `stNTVE` balance / transfer-driven cache repair no longer influence collator backing.
    - [x] `Add the first locked-LP nomination intake surface:` `pallet-staking::lock_native_lp_for_collator(lp_asset_id, amount, operator)` now validates the canonical `NTVE/stNTVE` LP asset through runtime policy, validates the collator target, transfers LP into the staking lock account, records account/operator lock storage, updates account/operator/total aggregate locked LP, and emits `NativeLpLocked` without yet changing session ranking.
    - [x] `Add unlock/redelegate lifecycle for locked LP:` `request_unlock_native_lp`, `withdraw_unlocked_native_lp`, and `redelegate_native_lp` now move locked LP through explicit pending-unlock and redelegation states while updating operator aggregates; requested unlocks stop counting as operator backing immediately and become withdrawable only after `NativeLpUnlockDelay`.
    - [x] `Add conservative locked-LP backing valuation to session ranking:` runtime candidate ranking now computes conservative native-equivalent value for locked `NTVE/stNTVE` LP using `2 * min(reserve_NTVE, reserve_stNTVE * staking_exchange_rate) * locked_lp / lp_supply`, preferring locked-LP backing when present while retaining the old native-binding fallback during the migration window.
    - [x] `Retire native-binding ranking fallback and cache repair:` locked-LP nomination is now authoritative for session ranking, native-binding fallback has been removed, `on_idle` no longer runs transfer-driven native-delegation refresh/repair, obsolete native-delegation cache storage plus ingress hooks have been deleted, and the old native-binding compatibility entrypoints/storage are gone.
      - [x] `Remove native-binding fallback from session ranking:` `DelegationWeightedCollatorSessionManager` now ranks only by conservative locked-LP native-equivalent backing, then deposit/account tie-breakers; legacy native bindings no longer affect candidate order.
      - [x] `Retire transfer-driven native-delegation maintenance from on_idle:` staking `on_idle` now spends its budget only on reward snapshot ingress; stale native-binding/cache state is no longer repaired from `stNTVE` transfer events.
      - [x] `Delete obsolete native-delegation cache and ingress surfaces:` removed `CachedNativeDelegations`, `CachedOperatorNativeShares`, dirty/repair cursor storage, native-delegation event ingress traits/adapters, and cache-specific tests/benchmark setup.
      - [x] `Delete native-binding compatibility surfaces:` removed `stake_native_with_operator`, `bind_native`, `clear_native_binding`, `NativeBindings`, native-binding events, binding benchmarks/weights, and runtime/AAA expectations that native staking binds an operator.
  - [x] `Implement aggregate NativeVotePower governance locks:` locked `$NTVE`, locked `stNTVE`, standalone locked `NTVE/stNTVE` LP, and already-collator-locked LP now feed frozen vote power at vote time, with one account-level `lock_until = max(enactment horizons)` and no per-referendum source-specific unlock ledger.
    - [x] `Freeze proposal ballot weights at cast time:` `pallet-governance` now stores the computed ordinary/protection vote weight plus raw protection power in each ballot, so later staking exchange-rate, LP-reserve, or balance-provider changes cannot rewrite already-cast proposal tallies.
    - [x] `Add account-level governance lock horizon:` accepted ballots now extend `GovernanceLocks[account].lock_until` to the proposal's effective primary close plus enactment delay, preserving the single aggregate `max(lock_until)` model without per-referendum unlock ledgers.
    - [x] `Wire collator-locked LP into tactical NativeVotePower:` `pallet-staking` now maintains account-level and total locked native LP aggregates, and the runtime `DecliningNativeStake` protection profile for the `$BLDR` domain converts those frozen aggregates through the same conservative `NTVE/stNTVE` native-equivalent LP valuation used for session backing.
    - [x] `Enforce governance-lock custody for collator-locked LP:` `pallet-staking::request_unlock_native_lp` now consults the runtime governance lock horizon and refuses to remove locked LP from account-level NativeVotePower while an accepted ballot's enactment horizon is still active.
    - [x] `Add standalone NTVE/stNTVE LP governance locks:` `lock_native_lp_for_governance`, `request_unlock_native_lp_for_governance`, and `withdraw_unlocked_native_lp_for_governance` let accounts lock canonical LP for NativeVotePower without nominating a collator, update account/total native LP aggregates, and respect the same governance lock horizon before reducing vote-power custody.
    - [x] `Add locked $NTVE and stNTVE governance sources:` `lock_native_asset_for_governance`, `request_unlock_native_asset_for_governance`, and `withdraw_unlocked_native_asset_for_governance` now custody native `$NTVE` or its `stNTVE` receipt in the staking lock account, expose per-account/total locked aggregates, convert `stNTVE` through the live staking exchange rate in runtime NativeVotePower, and block unlock while the account governance lock horizon is active.
  - [x] `Retarget governance voting and coefficient export to the full frozen-power model:` collator-locked LP, standalone locked LP, locked `$NTVE`, and locked `stNTVE` now feed frozen tactical protection ballots with custody enforcement, while nomination rewards use the separate collator-locked LP base so governance-only LP cannot earn nomination rewards.
    - [x] `Separate nomination-reward LP base from governance-only LP:` `pallet-staking` now maintains `account_native_collator_lp_locked` separately from the broader account-level NativeVotePower LP aggregate, and the runtime exposes `native_nomination_reward_base_weight(account)` from conservative collator-locked LP value so standalone governance LP cannot accidentally earn nomination rewards.
  - [x] `Replace reward ingress with epoch-based nomination reward accounting:` native `$NTVE` reward funding is now recognized by epoch balance reconciliation, locked-LP reward weights drive native snapshots, bounded liabilities/claims settle rewards, and native reward-account / receipt-transfer / governance-event scanning has been retired.
    - [x] `Add the balance-reconciliation primitive:` `pallet-staking::reconcile_reward_inflow(asset_id)` now records only the unaccounted positive delta between `reward_account(asset_id)` live balance and existing reward liability, freezes the current epoch denominator once, and is idempotent when called repeatedly without new funding.
    - [x] `Retarget native reward snapshots to collator-locked LP base weight:` `pallet-staking` now accepts a runtime `RewardBaseWeightProvider`; the reference runtime overrides the `$NTVE` reward base to conservative collator-locked `NTVE/stNTVE` LP value while leaving non-native staking rewards on their existing share-balance path.
    - [x] `Wire native reward reconciliation into epoch rollover:` staking epoch rollover now calls `reconcile_reward_inflow(NativeStakingAssetId)` after reward-touch snapshot rollover when the native pool exists, so unaccounted `$NTVE` reward-account funding is recognized without relying on reward-account transfer events.
    - [x] `Retire native reward-account event ingress:` runtime reward-event scanning now ignores `$NTVE` reward-account inflows, relying on epoch reconciliation instead, while explicit collator LP lock/unlock state transitions touch native reward snapshots for the affected account.
    - [x] `Retire native governance-event reward scan dependency:` `pallet-governance` now exposes a runtime hook for winning-vote record/eviction reward touches; the reference runtime uses it to touch native reward snapshots directly, and reward-event scanning filters native `$NTVE` out of governance-event domain expansion.
    - [x] `Retire native receipt-transfer reward scan dependency:` runtime reward-event scanning now ignores native `stNTVE` receipt transfers, keeping native reward snapshots driven by explicit native staking, collator-LP lock/unlock, governance-touch hooks, and epoch reward-account reconciliation rather than transferable receipt movement.
    - [x] `Scope remaining reward event scanning as legacy non-native support:` the runtime event-ingress adapter is now named `RuntimeLegacyRewardSnapshotEventIngress`, with tests renamed around legacy reward-event ingress so remaining event scanning is explicit compatibility support for non-native share-vault rewards rather than the native nomination-reward path.
  - [x] `Implement nomination reward settlement and compound extension:` bounded native nomination reward settlement is now split between liquid payout and one-call compound settlement.
    - [x] `Ship the baseline liquid nomination claim:` `claim_nomination_reward(epoch)` now claims native `$NTVE` nomination rewards from epoch-reconciled funding using the native collator-LP reward snapshot path, pays liquid `$NTVE` rather than minting `stNTVE`, decrements reward liability, and shares the existing per-epoch claim guard.
    - [x] `Add the runtime compound path:` `claim_and_compound_nomination_reward(epoch, operator)` now claims native `$NTVE`, splits it through the runtime `AssetConversionAdapter::compound_native_nomination_reward_to_locked_lp(account, operator, amount)` helper into stake/liquidity legs, mints proportional `NTVE/stNTVE` LP, and locks the resulting LP to the chosen collator transactionally.
  - [x] `Delete obsolete native staking maintenance paths after replacement:` native-binding entrypoints/storage, native-delegation cache repair, native reward-account event scanning, native receipt-transfer reward scanning, and native governance-event reward scanning are now deleted or bypassed for the native path; remaining non-native event scanning is explicitly scoped as legacy share-vault compatibility rather than native staking truth.
  - [x] `Update staking architecture docs after code truth lands:` `docs/staking.architecture.en.md` now reflects the shipped native staking implementation: liquid `stNTVE`, locked `NTVE/stNTVE` LP nomination, explicit NativeVotePower custody, epoch-reconciled native nomination rewards, AAA donation/compound helpers, bounded read-model views, runtime bindings, and operational watchpoints.
  - [x] `Add runtime/client read-model support for the new surfaces:` bounded runtime/client reads now cover staking exchange rate, `NTVE/stNTVE` reserves, LP native-equivalent value, locked LP backing/detail, governance lock state, frozen vote power, and known-epoch nomination reward claimability.
    - [x] `Expose native staking bounded read-model views:` `pallet-staking` now provides view functions for native staking exchange rate, canonical `NTVE/stNTVE` liquidity pool reserves/LP issuance, account native locked-LP position with conservative native value, and known-epoch native nomination reward claimability.
    - [x] `Expose account governance power read-model views:` `pallet-governance` now provides a bounded account/proposal view with governance lock horizon, live ordinary/protection power, current protection raw power, and frozen per-track ballot facts for client display.
    - [x] `Surface native staking read models in the web client:` `StatisticsWidget` now consumes the bounded staking runtime views through the blockchain adapter and displays `stNTVE` exchange rate, canonical `NTVE/stNTVE` reserves, selected-account locked LP, collator LP, and conservative native-equivalent value with explicit canonical-chain provenance.
    - [x] `Surface governance account-power read models in the web client:` `GovernanceWidget` now consumes `account_governance_power_view(domain, item_id, account)` and shows the selected account's governance lock horizon, current primary/protection power, protection raw power, and frozen primary/protection ballot facts on active proposal cards.
    - [x] `Refresh typed PAPI metadata for the new views:` the committed `deos.scale` snapshot and generated descriptors now include the new staking/governance view functions, letting the web-client use typed view access for native staking and account governance power instead of temporary defensive dynamic lookup.
    - [x] `Add first browser write path for native nomination rewards:` `StatisticsWidget` now exposes signed `claim_nomination_reward(epoch)` and `claim_and_compound_nomination_reward(epoch, operator)` actions through the live blockchain adapter, preserving visible signer gating and shared transaction-log progress.
    - [x] `Add first browser write path for collator LP nomination lifecycle:` `StatisticsWidget` now exposes signed `lock_native_lp_for_collator`, `request_unlock_native_lp`, `withdraw_unlocked_native_lp`, and `redelegate_native_lp` actions for the canonical `NTVE/stNTVE` LP through the live blockchain adapter with signer gating and shared transaction-log progress.
    - [x] `Add first browser write path for governance-only NativeVotePower custody:` `StatisticsWidget` now exposes signed governance-only lock/unlock/withdraw actions for `NTVE`, `stNTVE`, and standalone canonical `NTVE/stNTVE` LP, keeping those custody paths distinct from collator backing and nomination reward base.
    - [x] `Expose pending native staking position details:` `pallet-staking` now provides bounded `native_collator_lp_position(account, operator)` and `native_governance_custody_position(account, asset_id)` views, the committed PAPI metadata/descriptors include them, and `StakingWidget` can load per-operator locked/pending LP plus governance LP/asset custody and pending unlock details.
    - [x] `Make PAPI metadata refresh reproducible:` `deos-runtime` now includes an `export_metadata` example and `/scripts/export-papi-metadata.sh` exports runtime metadata v16 plus optionally regenerates the web-client PAPI descriptors, replacing ad hoc temporary-test metadata export.
  - [x] `Validate the migration across runtime and economic layers:` pallet/runtime coverage now exercises donation invariants, locked-LP backing, frozen governance power, epoch reward recognition, no-transfer-tracking behavior, AAA donation execution, native reward claim/compound, and bounded runtime/client read-model surfaces.

## Open Product / Client Work

### 1. Governance v1 contract rollout (current launch-line baseline shipped)

- [x] `Implement the ordinary public referendum cadence in pallet-governance:` the reference runtime now uses the public ordinary cadence contract directly: protection opens at submission, ordinary classes wait through a `3 day` lead-in before primary voting, ordinary protection stays open for `7 days`, ordinary primary runs for `7 days`, the canonical public Declining Power curve decays `7x -> 1x` by the end of day 6 with day 7 flat at `1x`, and successful ordinary referenda now enter a default `3 day` enactment delay with bounded query visibility.
  - [x] `Add proposal timing scaffolding:` runtime config plus bounded storage/query state for lead-in, protection-window close, primary open/close, urgent-open override, and enactment scheduling are now present through additive config hooks, `ProposalUrgentAuthorizedAt` / `ProposalPendingEnactmentAt` storage, and the new canonical `proposal_timing(domain, item_id)` runtime view.
  - [x] `Retarget ordinary lifecycle/resolution to the new timing model:` the generic plumbing for enactment scheduling, lead-in-gated primary admission, and protection-window admission is now matched by runtime policy too, so the reference line now runs the target public cadence rather than the older tiny launch-line timings.
    - [x] `Implement enactment-delay finalization/status handling:` successful proposals now schedule bounded pending enactment, expose that state through `proposal_status`, and clear the additive timing markers when finalized-outcome retention expires.
    - [x] `Implement lead-in-gated primary voting:` primary ballots now stay closed until the configured lead-in ends, while protection-track ballots remain admissible during that earlier window.
    - [x] `Open protection from submission and remove the old first-\`2/3\` activation rule:` protection votes are now simply admissible until the configured protection close instead of relying on late first-touch activation semantics.
  - [x] `Retarget ordinary vote weighting to the target public Declining Power curve:` ordinary tracks now use `7x -> 1x` by end of day 6 with day 7 flat at `1x`, while urgent primary handling stays flat `1x`.
    - [x] `Split ordinary vs protection weighting windows:` the pallet now feeds ordinary and protection ballots through separate timing windows so future lead-in / protection-window divergence can be enabled honestly.
    - [x] `Retarget the shipped declining-power formula to the canonical 7x curve:` the runtime now uses the agreed `7x -> 1x` public temporal weighting shape instead of the older `10x -> 1x` line.
- [x] `Implement the minimal domain + cadence + payload-kind surface needed for governance v1 rollout:` the current line now carries explicit `GovernanceDomain + CadenceMode + ProposalPayloadKind`, ordinary and urgent cadence, bounded preimage-backed execution, advisory finalization, and the minimal payload vocabulary (`L1RootAction`, `L2TreasurySpend`, `L2ParameterChange`, `Intent`, `L2SignalToL1`).
  - [x] `Add proposal metadata scaffolding:` active proposals now persist additive `CadenceMode + PayloadKind + payload hash` metadata through `ProposalMetadataByItem`, the pallet exposes proposal metadata, derived execution authority, and payload-preimage availability query surfaces, the runtime now includes canonical `pallet_preimage` support, and submission now requires explicit cadence mode, payload kind, and payload hash at the pallet boundary.
  - [x] `Bind domain + cadence + payload kind to execution authority:` each proposal item now has one domain, one cadence mode, one executable payload identity, and one declared execution origin/authority.
    - [x] `Expose additive execution-authority scaffolding:` proposals now expose a derived `proposal_execution_authority(domain, item_id)` query surface based on payload kind.
    - [x] `Define preimage admission policy per payload kind:` the governance contract now fixes the v1 rule that executable payload kinds may be submitted before their full preimage is noted as long as readiness stays queryable through the canonical preimage surface, while advisory payload kinds may remain hash-only and actual enactment still requires runtime-visible payload availability.
    - [x] `Bind payload hashes to deterministic dispatch semantics:` the canonical bounded executor now maps `(domain, payload_kind, payload_hash)` into runtime behavior instead of stopping at metadata/readiness scaffolding.
      - [x] `Add the canonical payload-executor scaffold:` the governance pallet now has one bounded enactment scaffold that consumes proposal metadata plus payload readiness and can decide `dispatch`, `non-executable advisory finalization`, or explicit execution failure, even though the reference runtime still wires a no-op executor for executable payload kinds.
      - [x] `Add bounded enactment servicing for due pending proposals:` due pending items now flow through an epoch-keyed bounded enactment service path instead of remaining pure status scaffolding until retention expiry.
      - [x] `Wire executable payload kinds into real enactment behavior:` each executable payload kind now has at least one bounded runtime path on the reference line: `L1RootAction -> RuntimeCall::System(authorize_upgrade { code_hash })` in the strategic domain, `L2ParameterChange -> RuntimeCall::AxialRouter(update_router_fee { new_fee })` through a governance-owned internal setter, and `L2TreasurySpend -> RuntimeCall::Assets(transfer { id = $BLDR, ... })` executed as the designated BLDR Treasury sovereign with non-Root transactional transfer semantics.
      - [x] `Wire advisory payload kinds into explicit non-dispatch finalization:` `Intent` and `L2SignalToL1` now finalize as honest non-dispatch advisory outcomes instead of pretending to be missing execution work.
      - [x] `Classify executable payload expansion beyond the first bounded slices as future-gated:` `L2ParameterChange` now has two truthful runtime paths (`AxialRouter.add_tracked_asset` and `AxialRouter.update_router_fee`), while `L2TreasurySpend` now supports bounded asset transfers from the designated BLDR treasury sovereign for any asset class held there. The remaining wider call-matrix work is conditional expansion, not a blocker for the current v1 baseline.
        - [x] `Publish the current rejected-surface inventory for L2 parameter delegation:` the current line now records that TMC launch-physics mutation, staking onboarding/recovery/admin reward bootstrap, AAA global-control surfaces, and asset-registry registration/migration remain L1/system-owned rather than truthful domain-owned `L2ParameterChange` targets.
        - [x] `Define the next truthful L2 treasury authority topology before widening payout semantics further:` the current line now states the explicit bounded topology — tactical `L2TreasurySpend` executes only from the tactical domain's single declared `BldrTreasury` funding source, with asset scope limited to balances actually held there, and any wider source family or native payout topology remains future opt-in work rather than an implied right of the current payload.
      - [x] `Ship bounded execution-side observability for the first slices:` generic success/failure/advisory-finalization events now exist and each names the bounded payload kind directly, execution failures expose bounded failure-reason categories (including invoice-side `MissingWinningPrimaryOption`), the runtime retains bounded `proposal_execution_detail(domain, item_id)` query projections for recent enacted/failed/advisory items, and treasury-spend execution details carry scalar-aware `base_amount + scalar + final_amount` invoice truth. Richer per-kind reporting is future-gated by new payload families or failure-state slices.
    - [x] `Publish one canonical payload-kind matrix across docs/runtime/events:` the docs, runtime enum, query surfaces, tests, and submission/vote events now consistently use only `L1RootAction`, `L2TreasurySpend`, `L2ParameterChange`, `Intent`, and `L2SignalToL1` as the active payload-kind vocabulary.
  - [x] `Define advisory payload semantics:` the governance specification now fixes bounded `Intent` / `L2SignalToL1` payload shape (`summary`, optional `doc_cid`, optional referenced payload hash), explicit non-executable handling, and the default no winner-memory / GovXP credit contract.
  - [x] `Define invoice execution payload semantics:` the governance specification now fixes beneficiary, payout asset, base amount, funding source, discrete scalar application, caps, and transactional failure handling for `L2TreasurySpend` invoice payloads.
  - [x] `Define and implement the minimum governance event contract:` submission, vote/replacement, fast-track authorization, finalized outcomes, enactment scheduling, execution success/failure, invoice execution, and runtime-upgrade execution now all have explicit bounded event coverage on the current launch line.
    - [x] `Enrich proposal submission events with payload semantics:` `ProposalSubmitted` now carries cadence mode, payload kind, and payload hash so indexers no longer need to infer proposal meaning from storage alone.
    - [x] `Enrich vote-cast events with replacement/time semantics:` `ProposalVoteCast` now carries `vote_epoch` plus the replaced protection-side vote, so `Pass -> Veto` / `Veto -> Pass` rewrites are externally visible.
    - [x] `Emit first payload-execution observability events:` the runtime now emits generic execution success/failure/advisory-finalization plus typed `ProposalRuntimeUpgradeAuthorized`, `ProposalParameterChangeExecuted`, and `ProposalTreasurySpendExecuted` events for the first bounded executable slices.
    - [x] `Emit urgent-authorization observability for the fast-track path:` the pallet now emits `ProposalUrgentAuthorized` when an expeditable proposal crosses the raw protection-track `Pass` threshold and records its authorization epoch.
- [x] `Implement first-class tactical invoice voting:` canonical `$BLDR` governance now ships `L2TreasurySpend` payloads with live `Amplify / Approve / Reduce / Nay` primary evaluation, `Veto / Pass` protection, discrete payout-scalar resolution, scalar-aware treasury execution receipts, and browser-visible invoice-family voting/execution truth on the current launch line.
  - [x] `Split primary-track vote shape by proposal family:` the kernel no longer hardcodes primary voting as storage-level `Aye / Nay` only: `ProposalVoteKind` / `ProposalVotesByItem` can now represent invoice-family `Amplify / Approve / Reduce / Nay` alongside protection `Veto / Pass`, binary families reject invoice-only vote kinds, invoice families reject binary-only `Aye`, and the current launch line still truthfully reports `Binary` for all runtime combinations until a later policy/resolution rollout opts invoice behavior in.
  - [x] `Retarget tally and query surfaces for invoice-family primary options:` `proposal_vote_tally(domain, item_id)` now carries the widened invoice-family primary weights, and the new canonical `proposal_primary_track_tally(domain, item_id)` view now exposes a family-aware primary-lane summary with deterministic lowest-scalar tie-breaking for the leading positive invoice option so clients/executors no longer have to reconstruct that identity off-chain.
  - [x] `Activate the discrete invoice resolution rule on the launch line:` the reference runtime now marks tactical `L2TreasurySpend` proposals in the canonical `$BLDR` domain as `Invoice`, so the spec's invoice rule is no longer kernel-only: live proposals now use `Amplify / Approve / Reduce / Nay` primary semantics, explicit `PassingAmplify / PassingApprove / PassingReduce` states, and the existing `Veto / Pass` protection lane.
  - [x] `Retarget treasury execution receipts/events/detail from transfer-only to scalar-aware invoice execution:` tactical treasury enactment now decodes a dedicated bounded invoice payload (`beneficiary`, `payout_asset`, `base_amount`, explicit funding source), applies the winning scalar on-chain, executes the final transfer from the designated BLDR treasury sovereign, and carries `base_amount + scalar + final_amount + funding_source` through bounded treasury execution events/detail as `InvoiceScalarTransfer` truth.
- [x] `Retarget GovXP and public submission to the v1 contract:` counters-first GovXP, signed advisory submission, opening-fee/preimage quoting, the next public payload-kind opt-in, and the first signed tactical treasury browser composition slice are now all landed on the current line.
- [x] `Add governance product UX for proposal semantics and execution state:` active/retained proposal semantics, invoice/runtime-upgrade execution detail, bounded advisory submission review, tactical treasury invoice composition, and the explicit materialized/archive boundary are broadly landed for the current launch-line governance surface. Further active/retained execution-state UX or composition growth is conditional on a new payload family, failure-state slice, or signed composition target actually shipping.
  - [x] `Define the current active/retained execution-state UX baseline around shipped payload families and failure states`
  - [x] `Ship the current richer browser composition baseline beyond advisory-only review through signed tactical treasury invoice composition`
  - [x] `Define a separate materialized/archive governance UX instead of stretching bounded retention cards`

### 2. Web-client wallet and execution UX hardening

- [x] `Finish transaction-grade wallet UX in web-client:` account/signer ownership, bounded asset projection, send-surface provenance, native safe-max honesty, tracked-asset transfer coverage, and draft-keyed in-flight send behavior across watch-only/signer transitions are now landed on the current line.
- [~] `Extend the Wallet widget beyond the bounded tracked-asset contract:` the wallet now discovers the bounded tracked-asset set with live balances for the selected account and the send surface can already transfer Native plus those bounded tracked assets; any expansion to a full portfolio UX is blocked until a materialized / indexed asset-discovery surface exists beyond live chain storage.
- [x] `Harden the Swap widget execution path:` signer guidance, safe-max, minimum-buy, quote gating, submit self-validation, slippage-aligned quote eligibility, and buy/sell execution log wording are now all aligned on the current line.
- [x] `Keep web-client bundle growth honest after local dev signing landed:` the first lazy-loading, bootstrap deferral, and bundle-report slices are already in place. The remaining shared `deos` / metadata bootstrap cost is now treated as consciously accepted startup weight under the current on-chain-first/eager product contract rather than as hidden optimization debt.
  - [x] `Trace the current bootstrap importer graph and choose the next concrete boundary to cut:` the current bundle report now points to governance advisory payload hashing as the next honest cut, because `GovernanceWidget` still statically pulls the heavy `@polkadot/util-crypto` path through advisory-payload derivation.
  - [x] `Move governance advisory payload hashing behind an existing action-path lazy edge:` the governance advisory review/submit path now computes the payload hash through an on-demand dynamic `@polkadot/util-crypto` import instead of pinning that crypto chunk on the base governance viewer path.
  - [x] `Choose the next bootstrap fan-out boundary after the advisory hashing cut:` the next honest choice is `(a)` for the current line: accept that startup still pays for bounded live chain bootstrap while the canonical default workspace remains on-chain-first/eager, and defer any further typed-API/metadata lazy boundary until that product contract changes.

### 3. Web-client UI architecture simplification

- [ ] `Continue evolving the reserved edge-lane layout model:` the first header/footer/sidebar lane-spec line is already landed. Remaining work:
  - [ ] `Define the first concrete left/right lane growth slice if product pressure creates another reserved lane`
  - [ ] `Extend RESERVED_LANE_SPECS to that next lane without reintroducing user-reorderable edge-lane state`
- [ ] `Continue organic customization passes across the web-client:` the shared pane chrome and the current highest-traffic widgets already adapt much better across widths. Remaining work:
  - [x] `Identify the next highest-pain widget under extreme width/height constraints and split it into a concrete adaptation slice:` the next clear slices were `WikiWidget` after the metadata expansion, the reserved sidebar lane widgets, the dense `StatisticsWidget` route-mix surface, and the narrow `AutomationWidget` actor-card header.
  - [x] `Run the next focused extreme-pane audit instead of reopening one umbrella layout-polish pass:` the current passes narrowed that audit first to `WikiWidget` reader mode, then to the reserved sidebar lane widgets, then to the dense `StatisticsWidget` route-mix block, and then to the narrow `AutomationWidget` actor cards.
  - [x] `Keep WikiWidget content-first in narrow reader panes after the metadata expansion:` in non-dual reader mode the widget now moves secondary discovery/provenance cards behind the main markdown body so content stays first when space is tight.
  - [x] `Make AccountWidget adapt more honestly in narrow sidebar widths:` the sidebar account surface now shortens the selected address with full-detail fallback in dense lanes, uses earlier two-column preset/injected-account grids once width supports them, and keeps the custom-account action row tighter without wasting vertical space.
  - [x] `Make SettingsWidget adapt more honestly in narrow sidebar widths:` the sidebar settings surface now keeps the endpoint field full-width, reuses a two-column row for the smaller controls once the lane is wide enough, and lets the apply action stop wasting vertical space in wider sidebars while still collapsing back to full width in tight lanes.
  - [x] `Let StatisticsWidget route-mix cards stack in the densest panes:` the route-mix detail cards now drop back to one column before those labels/details become cramped mini-columns.
  - [x] `Let AutomationWidget actor-card headers stack earlier in narrow panes:` dense panes now stop forcing the actor label/role and status badge to compete in one horizontal header row, and the header actor-count badge also shortens in compact panes.
- [ ] `Continue decomposing oversized web-client slices:` log, market, portfolio, system, and the current governance viewer boundaries are already much cleaner than before. Remaining work:
  - [ ] `Watch governance state for the next real separation boundary as proposal composition or archive work grows`
  - [x] `Extract native staking actions out of the overloaded statistics pane:` the native staking read/write surface now lives in a dedicated `StakingWidget` component, while `StatisticsWidget` returns to protocol/liquidity/trace metrics orchestration and passes only responsive pane state into the staking surface.
  - [x] `Keep native staking operations readable after the write-surface expansion:` `StakingWidget` now groups nomination rewards, collator LP nomination, and governance-only NativeVotePower custody through an inline collapsible action-section snippet instead of a separate one-off component or one long uninterrupted action card stack.
  - [ ] `If another frontend store or widget regrows into a hotspot, extract one named sub-slice with explicit ownership instead of reopening a generic refactor umbrella`

### Collator Economics & Fee Routing — two-phase launch configuration

> Reference block-reward distribution design for launching the network in two phases.
> Phase 1 uses trusted, permissioned collators and only pool-level reward flows.
> Phase 2 introduces permissionless collators, LP nomination, and claimable LP-staking nomination rewards.

- [x] `Define the two-phase reward contract before code changes:` all rewards and fees follow one outer split: 20% to the block author / trusted collator, 80% to Fee Sink (AAA #1), then Fee Sink redistributes by the active phase.
  - [x] `Phase 1 — trusted-collator launch:` no permissionless collator nomination, no LP-point nomination, no claimable nomination reward path; Fee Sink routes only into the staking-pool yield flow and the `NTVE/stNTVE` liquidity donation flow.
  - [x] `Phase 2 — permissionless collators:` enable LP nomination to specific collators and introduce the claimable LP-staking nomination reward path where GovXP affects each nominator's reward share.
  - [x] `Freeze the phase-switch contract:` Phase 2 is an explicit runtime-upgrade boundary, not a launch-time governance parameter; LP nomination and claimable nomination rewards stay inactive until a later runtime line enables permissionless collators.
- [x] `Implement the unified 20/80 collection rule:` route 20% of block rewards and eligible fees to the collator and 80% to Fee Sink (AAA #1).
  - [x] `Transaction fee routing:` runtime transaction fees now use `RuntimeFeeSplit`, sending 20% to the current author when resolvable and routing the remainder to Fee Sink, with no-author fallback routing all fees to Fee Sink.
  - [x] `AAA fee routing:` user-AAA creation/evaluation/execution fees now route through an author-aware `FeeRouter`; when the current author is resolvable it applies the same 20% collator / 80% Fee Sink split, otherwise it falls back to routing fees to Fee Sink.
  - [ ] `Block reward routing:` blocked on explicit block reward source/amount design decision; the split infrastructure is ready when the economic parameters land.
- [x] `Implement Phase 1 Fee Sink redistribution:` Fee Sink is now materialized as System AAA #1 and redistributes accumulated native fees/rewards into exactly two Phase 1 pool flows.
  - [x] `Staking pool flow:` Fee Sink sends 50% of native balance into the native staking pool account so later pool sync raises `stNTVE/NTVE` exchange rate.
  - [x] `Liquidity pool flow:` Fee Sink sends 50% of native balance into `lp_reward_account(NTVE)` as the dedicated native LP-donation ingress.
  - [x] `Keep collator-specific reward accounting disabled in Phase 1:` trusted collators remain permissioned operators; the Phase 1 Fee Sink execution plan fans out only to pool-level ingress accounts, not to per-account LP nomination reward snapshots.
- [~] `Prepare Phase 2 reward routing without activating it at launch:` `build_phase2_fee_sink_execution_plan` with 1:1:4 split target is defined alongside the Phase 1 plan in AAA config, but Fee Sink runtime activation remains on the Phase 1 50/50 plan; full Phase 2 activation gate is the permissionless-collator runtime-upgrade boundary.
  - [~] `Claimable LP nomination flow:` `reward_account(NTVE)` ingress is defined; the Phase 2 Fee Sink plan routes 4/6 into it; GovXP-weighted distribution is already partially wired through `pallet-governance` reward-touch hooks and `pallet-staking` nomination claim surfaces but needs explicit LP-nomination reward-weight provider activation when permissionless collators ship.
  - [ ] `LP nomination activation:` expose LP-point nomination to specific collators only when permissionless collator selection is enabled.
- [x] `Define staking pallet reward ingress accounts:` `pool_account(asset_id)`, `lp_reward_account(asset_id)`, and `reward_account(asset_id)` now separate staking-pool yield, liquidity-pool donation, and future claimable nomination rewards so Fee Sink distribution has explicit destinations instead of implicit balance conventions.
- [x] `Add a /simulator model for the two-phase distribution:` simulator helpers and regressions now verify the 20/80 collection split, Phase 1 two-pool Fee Sink distribution, and Phase 2 `1:1:4` distribution conservation.
- [x] `Update the canonical architecture docs:` `core.architecture.en.md`, `staking.architecture.en.md`, `staking.specification.en.md`, and `aaa.architecture.en.md` now reflect the two-phase fee/reward flow without adding a separate system-map document.

### Conditional / Externally Gated Work

### 4. Governance execution expansion policy

> Only actionable when a concrete domain-owned control surface, payload family, or failure-state slice is selected for governance beyond the current v1 baseline.

- [ ] `Only after a genuinely delegated/domain-owned parameter surface exists, add the next L2ParameterChange path beyond the Axial Router pair:` do not widen legacy Root setters that still belong to launch physics, system control, staking administration, or asset registration.
- [ ] `Only when a new payload family or failure-state slice ships, broaden per-kind execution observability beyond the current bounded detail/events.`
- [ ] `Only when runtime-signed submission authority expands beyond advisory plus tactical treasury invoices, add the next browser composition surface for that newly public payload kind.`
- [ ] `Only when a materialized/indexed governance backend is selected, replace the current archive-boundary placeholder with live archive search and ballot timelines.`

### 5. Native staking LP donation route policy

> Only actionable if AAA policy needs route choice beyond deterministic `$NTVE -> stNTVE` stake acquisition.

- [ ] `Only if pool-ratio divergence makes deterministic acquisition insufficient, add swap/mixed-route acquisition:` route selection beyond pure staking should include router quote comparison, slippage bounds, and fallback behavior when the `NTVE/stNTVE` pool ratio diverges materially from the staking exchange rate.

### 6. Relay-beacon replacement path

> Only actionable if a real parachain-consumable per-block beacon appears upstream.

- [ ] `Only if a new parachain-consumable per-block protocol beacon exists, define the relay-beacon replacement contract against that actual surface.`
- [ ] `Only if that future per-block beacon exists, design the runtime proof-ingestion path:` a weight-accounted `ConsensusHook` snapshot remains the leading pattern, but it should be finalized against the real upstream beacon surface rather than today's epoch-scale items.
- [ ] `Only if that future per-block beacon exists, wire AAA to it and measure the proof-size / weight impact.`
- [ ] `Only after a production-ready per-block relay/protocol beacon exists, design and prototype any permissionless-collator activation path instead of reviving a local threshold line.`

### 7. External dependency watch

> The simplified randomness/security roadmap depends on upstream delivery rather than local cryptographic ambition.

- [ ] `Track Safrole/Sassafras release readiness and parachain-consumable randomness availability in paritytech/polkadot-sdk.`
- [ ] `Treat the current Polkadot/JAM post-quantum roadmap as a directional beacon-over-VRF signal, not as proof of a shipped parachain-consumable API.`
- [ ] `Watch the current upstream signals only in the live polkadot-sdk monorepo:` Sassafras (`#41`, `#1336`, `#7669`) and BLS stabilization (`#10327`, `#11149`).
- [ ] `Only if the relay-beacon path stalls or proves unusable, reassess whether any local threshold runtime work should survive as fallback research.`

## Operator-local validation entrypoints

- `./scripts/aaa-release-gate.sh`
  Runs the documented AAA scheduler release gate (fairness matrix, topology matrix, sparse long-run liveness, 10k stress, occupancy profile).

- `./scripts/try-runtime-local.sh --prepare`
  Builds the runtime with `try-runtime`, prepares the local Zombienet dev chain, and runs the canonical `on-runtime-upgrade live` plus `execute-block live` dry-runs against the parachain RPC.
