[package]
name = "deos-runtime"
description = "DEOS reference parachain runtime"
version.workspace = true
license = "MIT-0"
authors.workspace = true
homepage.workspace = true
repository.workspace = true
edition.workspace = true
publish = false

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[build-dependencies]
docify = { workspace = true }
substrate-wasm-builder = { optional = true, workspace = true, default-features = true }

[dev-dependencies]
cumulus-primitives-parachain-inherent = "0.26.0"
cumulus-primitives-proof-size-hostfunction = "=0.20.0"
cumulus-test-relay-sproof-builder = "0.27.0"
sc-executor = "=0.50.0"

[dependencies]
codec = { workspace = true }
cumulus-pallet-parachain-system.workspace = true
docify = { workspace = true }
hex-literal = { optional = true, workspace = true, default-features = true }
log = { workspace = true }
pallet-deos-actors.workspace = true
pallet-asset-registry.workspace = true
pallet-deos-router.workspace = true
pallet-governance.workspace = true
pallet-oracle.workspace = true
pallet-session-rotation.workspace = true
pallet-staking.workspace = true
pallet-tmc.workspace = true
primitives.workspace = true

polkadot-sdk = { workspace = true, features = [
  "cumulus-pallet-aura-ext",
  "cumulus-pallet-parachain-system",
  "cumulus-pallet-session-benchmarking",
  "cumulus-pallet-weight-reclaim",
  "cumulus-pallet-xcm",
  "cumulus-pallet-xcmp-queue",
  "cumulus-primitives-aura",
  "cumulus-primitives-core",
  "cumulus-primitives-utility",
  "pallet-asset-conversion",
  "pallet-assets",
  "pallet-aura",
  "pallet-authorship",
  "pallet-balances",
  "pallet-collator-selection",
  "pallet-message-queue",
  "pallet-preimage",
  "pallet-session",
  "pallet-timestamp",
  "pallet-transaction-payment",
  "pallet-transaction-payment-rpc-runtime-api",
  "pallet-xcm",
  "parachains-common",
  "polkadot-parachain-primitives",
  "polkadot-runtime-common",
  "runtime",
  "staging-parachain-info",
  "staging-xcm",
  "staging-xcm-builder",
  "staging-xcm-executor",
] }
scale-info.workspace = true
serde_json = { workspace = true, default-features = false, features = [
  "alloc",
] }
smallvec = { workspace = true, default-features = true }

[features]
default = ["std"]
std = [
  "codec/std",
  "cumulus-pallet-parachain-system/std",
  "log/std",
  "pallet-deos-actors/std",
  "pallet-asset-registry/std",
  "pallet-deos-router/std",
  "pallet-governance/std",
  "pallet-oracle/std",
  "pallet-session-rotation/std",
  "pallet-staking/std",
  "pallet-tmc/std",
  "primitives/std",

  "polkadot-sdk/std",
  "scale-info/std",
  "serde_json/std",
  "substrate-wasm-builder",
]

runtime-benchmarks = [
  "cumulus-pallet-parachain-system/runtime-benchmarks",
  "hex-literal",
  "pallet-deos-actors/runtime-benchmarks",
  "pallet-asset-registry/runtime-benchmarks",
  "pallet-deos-router/runtime-benchmarks",
  "pallet-governance/runtime-benchmarks",
  "pallet-oracle/runtime-benchmarks",
  "pallet-session-rotation/runtime-benchmarks",
  "pallet-staking/runtime-benchmarks",
  "pallet-tmc/runtime-benchmarks",
  "primitives/runtime-benchmarks",

  "polkadot-sdk/runtime-benchmarks",
]

try-runtime = [
  "cumulus-pallet-parachain-system/try-runtime",
  "pallet-deos-actors/try-runtime",
  "pallet-asset-registry/try-runtime",
  "pallet-deos-router/try-runtime",
  "pallet-governance/try-runtime",
  "pallet-oracle/try-runtime",
  "pallet-session-rotation/try-runtime",
  "pallet-staking/try-runtime",
  "pallet-tmc/try-runtime",

  "polkadot-sdk/try-runtime",
]

# Enable the metadata hash generation.
#
# This is hidden behind a feature because it increases the compile time.
# The wasm binary needs to be compiled twice, once to fetch the metadata,
# generate the metadata hash and then a second time with the
# `RUNTIME_METADATA_HASH` environment variable set for the `CheckMetadataHash`
# extension.
metadata-hash = ["substrate-wasm-builder/metadata-hash"]

# A convenience feature for enabling things when doing a build
# for an on-chain release.
on-chain-release-build = ["metadata-hash"]
