[package]
name = "pallet-deos-actors"
version = "0.7.13"
description = "DEOS Actors bounded economic actor runtime for FRAME"
authors = ["DEOS Contributors"]
license = "MIT-0"
homepage = "https://github.com/atmo-network/deos"
repository = "https://github.com/atmo-network/deos"
documentation = "https://docs.rs/pallet-deos-actors"
readme = "README.md"
keywords = ["substrate", "frame", "automation", "actors"]
categories = ["cryptography::cryptocurrencies"]
edition.workspace = true
publish = false
include = ["Cargo.toml", "README.md", "docs/**/*.md", "src/**/*.rs", "examples/**/*.rs"]

[lib]
name = "pallet_deos_actors"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { workspace = true }
scale-info = { workspace = true }
sp-api = { workspace = true }
frame = { workspace = true }
polkadot-sdk = { workspace = true, features = [
  "frame-support",
  "frame-system",
  "sp-runtime",
  "sp-weights",
] }

[dev-dependencies]
proptest = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
polkadot-sdk = { workspace = true, features = [
  "frame-support",
  "frame-system",
  "pallet-balances",
  "sp-core",
  "sp-io",
  "sp-runtime",
] }

[features]
default = ["std"]
std = [
  "codec/std",
  "polkadot-sdk/std",
  "scale-info/std",
  "sp-api/std",
]
runtime-benchmarks = [
  "polkadot-sdk/runtime-benchmarks",
  "frame/runtime-benchmarks",
]
try-runtime = ["polkadot-sdk/try-runtime"]
