# EXP-0048 — Run and Contract Head-Tail Reachability

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0029](./EXP-0029.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | Public lifecycle constructors jointly realize the declared Run, head, tail, cursor and zero-Step encoded geometry. |
| Physical mechanism | Run and Contract Head-Tail Reachability |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0029](./EXP-0029.md) |


## Decision Question

Public lifecycle constructors jointly realize the declared Run, head, tail, cursor and zero-Step encoded geometry.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns run and contract head-tail reachability. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0029 / “Retained Correction Evidence”, “Derived Metrics”, “Environment and Artifact Identity”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. No candidate is introduced by this migration.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Extraction baseline: `a60de7748132211d9e58cecd5be82b41f76ce230`. The migration manifest records exact original content/mode identities and source spans. Question-specific source/Wasm/output identities remain inline. Historical fragments remain historical; no new runtime or production artifact was built.

Use the identified environment attached to each extracted source. Where a shared control is needed, follow its explicit evidence source; absent identity requires refresh before acceptance.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Public lifecycle constructors jointly realize the declared Run, head, tail, cursor and zero-Step encoded geometry. | One terminal one-Step head incorrectly retaining a tail, or a proposed encoded maximum that public construction cannot attain | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Accepted for the admitted mock/reference host domain; rejected dense maxima remain explicit resource-admission boundaries |

## Measurements

<!-- extracted: EXP-0029:345-372:48 -->
### Pre-fission EXP-0029 lines 345–372

The six `scheduler_inner_suspended_head_{retry,complete,progress,opening_retry,opening_complete,opening_progress}` profiles now use `prepare_reachable_suspended_head_inner`: author before admission, certified OwnerOnly funding, real Manual Opening, actual failure, and due wakeup. Removed the unused synthetic Run installer and scalar-source consumer. The measured block still isolates the inner transition after source consumption; this is not a full scheduler or Executive measurement. `opening_snapshot_capture` remains an isolated capture benchmark, with positive `min_lp_out` so its two-leg Steps satisfy the Task shape contract.

| Head profile partition | Authored and retained domain | Real transition |
| --- | --- | --- |
| Retry / progress | `S = MaxContractSteps`, `L_tail = 2(S−1)`, `max(0, L_tail−MaxFundingTrackedAssets) ≤ n ≤ L_tail`; `O = n`, `F = R = L_tail−n` | Retry head is unfunded Fixed Transfer; progress head is current-balance AddLiquidity whose impossible output bound first fails transactionally |
| Completion | `S = 1`, `O = F = R = 0` for this current-balance Task family | A head cannot complete by reaching the end while retaining an authored tail; host account freeze changes spendability after real Temporary failure, so current resolution skips and completes without effects |
| Predicates | Tail `o_i = p_i = capacity`; head Current-only uses `0..capacity` Current occurrences, Opening variant uses one Opening plus `0..capacity−1` Current occurrences | Retained results total `(S−1) × capacity + o_head`; head predicates are true, tail predicates false; frozen head truth survives retry |
| Native domain controls | Terminal, minimum/interior/maximum coupled allocations and every `0 ≤ o_head ≤ p_head ≤ capacity` | `reachable_suspended_head_inner_allocation_boundaries` checks admission, immutable captures, actual retry/progress/completion, custody-preserving thaw and TryRuntime; rejects terminal tails, excessive amount allocation and excessive predicate count before Actor publication |

The changed benchmark signatures deliberately remove independent `r` and `f` axes. Existing generated Weight signatures and runtime selectors are still stale and must be reconciled by EXP-0028 / EXP-0064 before EXP-0065 regeneration; a slope along `O + F = L_tail` is a tradeoff, not an independent marginal coefficient for either field. These profiles do not prove an envelope for frozen-source heads with their additional one/two amount positions, all Opening/Current compositions, false-head branches, User holds, invoked effects or every storage layout.

The first reference-Wasm retry probe rejected the maximum authored Contract with `ContractStepsExceedOnIdleBudget` before measurement. Source `196e9fd7d33bfe7f98584f41eb18c33b3880ce6879362496e21acfdddb656bf9`, benchmark source SHA-256 `6dab431697abb79241ae819e78fd012934902967982a1cfb1a16849f2c33aa2f`, Wasm `e66b2aef40ed62222eaf0b157c725f77d81791e4b585315cd562339358500f72`; failed-log SHA-256 `299cf5524b8864d5c5709fab42816539344558ae2c0fdceb9822f41982e9d558`. Command: `./scripts/benchmarks.sh --extrinsic scheduler_inner_suspended_head_retry --output /tmp/deos-exp29-head-retry.rs pallet_deos_actors`, 50 × 20. This is evidence of the current resource-admission boundary, not structural Contract rejection or permission to reduce semantic bounds.

The same-Wasm terminal probe passed admission and real failure, then falsified the fixture assumption that exactly one Actor becomes Ready: host asset bootstrap also owns a due Actor. The corrected fixture preserves that topology, verifies its own Ready locator and asserts successor occupancy relative to the other live entries rather than deleting unrelated scheduler state. Native filtered `suspended_head` tests pass all nine tests, including the six generated benchmark tests and the allocation/timing control.

After that correction, both terminal profiles pass on the bound head Wasm at 50 steps × 20 repeats. Command family: `./scripts/benchmarks.sh --extrinsic scheduler_inner_suspended_head_complete --output /tmp/deos-exp29-head-complete.rs pallet_deos_actors`; subsequent profiles use the same command with `--skip-build`, matching exact profile/output names. No concurrent heavy work ran during these measurements. Hardware/cache/heap/map controls match the funding run above.

| Terminal profile | Domain | RefTime model, picoseconds before configured DB Weight | Estimated / measured proof bytes | DB operations |
| --- | --- | --- | --- | --- |
| Current-only completion | `p = 0..4`, one Step, no immutable amount/funding entries | `118,862,218 + 7,724,877p`; slope standard error `185,422`; minimum execution `98,338,000` | Estimated `6,007 + 2,698p`; measured `2,548 + 220p` | `6 + 2p` reads, `4` writes |
| One-Opening completion | `p = 0..3` Current plus one Opening occurrence, one Step | `119,176,095 + 8,972,528p`; slope standard error `226,071`; minimum execution `101,551,000` | Estimated `6,021 + 2,689p`; measured `2,560 + 207p` | `6 + 2p` reads, `4` writes |

The three remaining maximal-tail probes on that same Wasm (`progress`, `opening_retry`, `opening_progress`) all fail Contract admission before a model is produced, with the same `ContractStepsExceedOnIdleBudget` class as the first retry probe. Failed-log SHA-256 respectively: `4eb2168631637f008b81a55ab8d26d2aca596f14f6e9d145e8ac5ad92a87366c`, `845e02f444b6b9d6f6dab3ccd3af2ddc24ff2104c1e9d04c2ce64a7ff35ee321`, `716de0472518ff9d68efaa66b1b35048c4e20bf83734262dca051f2519e736ca`. These are four failed parameter-domain probes, not proof that every point or every twelve-Step Contract is inadmissible. The earlier unrelated-Ready counterexample log hash is `f665660b90a0424be1cde03b2bec55610fbf0ed413728d50c730595b257977ee`.

Retained head source validation: `./scripts/ci-local.sh --only tests --package pallet-deos-actors --all-features` passed, as did workspace/all-target/all-feature Clippy with `-D warnings`. Changed-scope completion passed with `--skip-cargo-check --skip-runtime-tests`: fresh Clippy supplies compilation, and the head changes are confined to the benchmark module, leaving the previously passed default-feature runtime inputs unchanged. This reuse does not turn the four benchmark admission failures into passing production evidence.

The [EXP-0028 reference-Wasm receipt audit](./EXP-0030.md#reference-wasm-inclusion-and-admission-boundary) additionally qualifies CompleteMax at 1 and 5 authored Steps and records ordinary admission refusal at 9 and 12 Steps. That record owns exact source/Wasm identities, commands and scoped measurements. This is another host resource boundary, not a reason to bypass admission, lower authored bounds for the fixture, or reopen C1; EXP-0064 must reconcile the complete envelope.
<!-- extracted:end -->

<!-- extracted: EXP-0029:466-469:48 -->
### Pre-fission EXP-0029 lines 466–469

Qualified zero-Step head certificate: `scheduler_inner_zero_step_user_max_head` ordinarily admits full AddressEvent source/asset whitelists and SignedAllowlist with a present window and terminal nonce. Native and fresh reference-Wasm assertions require the stored header's encoded length to equal its host MaxEncodedLen; first Step/resources are both absent. Thus the exact zero-Step head bound is the attained header maximum plus two Option discriminants, without compact resource widths or semantic predicate capacity. [EXP-0028](./EXP-0037.md#maximum-zero-step-head-and-window-reference) owns the complete User Opening measurement and exact source/artifact identities.

Under the reference AccountId32, five-byte Local AssetId and sixteen-entry lists, the derived SCALE field sizes are Trigger `597`, cooldown `4`, present window `9`, funding `514`, completion `1`, present nonce `9`, Step count `4`, three hashes `96` and machine envelope `32`: header `1,266` bytes and zero-Step head `1,268`. The mock's eight-byte AccountId reduces each of the two sixteen-account lists by 384 bytes, yielding `498/500`. These sizes are derived from the qualified host types; the executable equality certifies joint reachability rather than independently maximizing unreachable fields. This closes serialized zero-Step head size for these hosts, not CPU/decode-branch, complete Waiting/hold or nonempty-head dominance.
<!-- extracted:end -->

### Head and Tail Constraint

Let `p_i` count canonical timed-predicate occurrences in Step `i`, with Opening subset `o_i`; `S` is admitted Step count.

| Domain | Source-derived necessary constraint | Qualification / executable owner |
| --- | --- | --- |
<!-- partitioned: EXP-0029:339-339:48 -->
| Head/tail | One head; `ceil(max(S−1, 0)/4)` tail chunks | For cursor `c > 0`, chunk `k = floor((c−1)/4)` has `min(4, S−1−4k)` Steps; fragment length is not cursor or total remaining Steps |
<!-- extracted:end -->

<!-- partitioned: EXP-0029:67-83:48 -->
### Head Measurement Environment

| Artifact | Identity / required binding |
| --- | --- |
| Baseline source | Commit `5b46b19004b62a723d6c45a610dfe1e5c79982b0`; template tree `98af72ffbb55fe8840b5bb44b5b01d65c8e775e1` |
| Measured fixture source | SHA-256: pallet `src/lib.rs` `78d113fb1592c516fc29a713601af8edc0780c014f8653e26abeaabff0a3b44f`, `src/benchmarking.rs` `4adba7cc4918f8c22d0209592caf7f2db855099e818032d5f11e8d844e626e52`, runtime `configs/actor_config.rs` `7216c72353c9ce45abaf4392e26019e76fa3fdbe88b7f1dd271884191237e4d5` |
| Suspended-head source / Wasm | Source `83f327f5536e9401ddc50babb1e1a5cb0410c3dbd0edcdf0c64f8c889f69aecc`; benchmark source SHA-256 `1edf9c0b46e85c9a35bf8341e7c2343ff3660ba2aaa6aa8f063f5ca4116c8817`; benchmark Wasm `c79eaec1e59bd2f69a8912ddce1b59fa65bf37f784b7d2c7dd5eaac3c129b696`. Same reference host/toolchain, with unrelated host-created Ready Actors preserved |
| Terminal head fragments | Current-only `/tmp/deos-exp29-head-complete.rs` SHA-256 `f16fe85c58e6561031bec9a4f8b937f3f51affaf8ec218575ee7aca0c515d971`; one-Opening `/tmp/deos-exp29-head-opening-complete.rs` SHA-256 `44a31ca0bbba96fa0acec1b1db1580ca8f3edf1fa5225459a2aa5d6114dd8149`. Normalized models below; neither is installed as production Weight |
| Generated Weight / metadata / ABI | Not accepted by this experiment |
| Commands | Native commands and outcomes below; `scripts/benchmarks.sh --check` checks compilation/storage names only, not measured Weight or production capacity |
| Evidence owner | Baseline source and the question-specific facts below; new measurements must carry exact source/artifact identities in this record |
<!-- extracted:end -->

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

Public constructors attain the exact zero-Step maximum head, terminal one-Step/no-tail state, every host-feasible Opening tail count, all partial/full current tail fragments, committed-prefix cursors and the declared Running/Suspended head allocations. The four maximal-tail reference tuples remain ordinary `ContractStepsExceedOnIdleBudget` refusals rather than fabricated reachable maxima. Current-tree `cargo test -p pallet-deos-actors --all-features reachable_ -- --nocapture` passes all nine selected reachability controls.

## Interpretation

The reachable domain is the union of publicly constructed admitted profiles, not the Cartesian product of storage capacities. Tail chunk count follows authored Steps, a terminal one-Step head has no tail, and rejected dense reference tuples remain structurally legal but resource-inadmissible. Numeric transition ownership and exact artifact generation stay with EXP-0059, EXP-0064 and EXP-0065.

## Decision

Accepted for the admitted mock/reference host domain. Public lifecycle construction realizes zero-Step, Opening, Running and Suspended head/tail/cursor geometry at the declared boundaries; ordinary admission refusal excludes the four stale-binding maximal-tail points. This satisfies EXP-0029 O3 without reducing semantic bounds or accepting a production Weight envelope.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. Do not infer a new selection from record relocation or from aggregate coefficient subtraction.

## Validity

- `Establishes`: Publicly reachable zero-Step, head/tail chunk, committed-cursor and Run geometry for the declared admitted mock/reference domains, including explicit ordinary-admission exclusions.
- `Does not establish`: Complete production Weight, exact release artifact closure or integrated throughput.
- `Known limitations`: The four maximal-tail reference points are resource-inadmissible on the recorded binding; generated fragments are uninstalled and exact production Weight/artifact identity remains downstream.
- `Invalidation triggers`: A changed measured body, host domain, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Follow the owning physical boundary, not a benchmark name.

## Follow-Up / Next Gradient

Stop when the declared proof is satisfied. Transfer any independent owner, selector, parameter domain or artifact-identity question before investigating it beyond the smallest classifier. A hypothetical corner case alone is not a measurement mandate.

## Architecture Impact

No runtime, semantic, storage or production binding change. C1 and A0 remain frozen; this record owns only its declared proof evidence.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0029](./EXP-0029.md) O3
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0029](./EXP-0029.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
