# EXP-0052 — Joint Suspended and User Close Geometry

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0029](./EXP-0029.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | Ordinary admission, funding and actual suspension jointly realize the declared User close capture/detector/Run geometry. |
| Physical mechanism | Joint Suspended and User Close Geometry |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0029](./EXP-0029.md) |


## Decision Question

Ordinary admission, funding and actual suspension jointly realize the declared User close capture/detector/Run geometry.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns joint suspended and user close geometry. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0029 / “Joint Retry and Crossing Cleanup Qualification”, “Derived Metrics”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. No candidate is introduced by this migration.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Extraction baseline: `a60de7748132211d9e58cecd5be82b41f76ce230`. The migration manifest records exact original content/mode identities and source spans. Question-specific source/Wasm/output identities remain inline. Historical fragments remain historical; no new runtime or production artifact was built.

Use the identified environment attached to each extracted source. Where a shared control is needed, follow its explicit evidence source; absent identity requires refresh before acceptance.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Ordinary admission, funding and actual suspension jointly realize the declared User close capture/detector/Run geometry. | One admitted capture allocation that fails real suspension/public close, or whose retained detector/Run geometry differs from its certificate | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Accepted for the observed admitted reference subdomain; adjacent refused tuples remain excluded |

## Measurements

<!-- extracted: EXP-0029:181-233:52 -->
### Joint Retry and Crossing Cleanup Qualification

The next falsifier extends the existing `close_actor_opening_partition(o, p)` diagnostic from Manual suspension to a Crossing-triggered suspension with a retained deferred latch and a live detector cursor. Keep the same maximum-length authored Contract and independent capture axes; produce observations through the host publisher and the existing bounded worker. Append ordinary guards only after the Run exists, then test cursor repair, Waiting/Run removal, owner holds and custody neutrality through public close. The existing Manual measurements remain historical inputs, not a bound for this joint state. Start with the previously admitted small capture rectangle; a passing point must not replace the general close Weight or qualify the full 12/24/48 envelope. No admission bypass, Contract mutation or direct cursor insertion is allowed.

Preparation corrects the shared Crossing retry constructor to publish through the host and retain monotonic block time. The general `close_actor` fixture also replaces its inserted cursor with a real, not-yet-serviced queued transition; cursor repair belongs to the explicit diagnostics. Native close-family checks cover those changed siblings. The joint profile uses Rising threshold four/rearm one, a real initial failed liquidity attempt, then publications one/four to latch a second occurrence without changing Run bytes. Another publication of one lets the bounded worker skip the disabled target and rearm ordinary guards from two full source pages. Public close must rewind the live cursor and the next worker must visit the moved guard.

Require `run.eligible_at > current block` immediately before measurement. A preparation that advanced time past the retry deadline was rejected before sampling; its build was deliberately terminated. The final constructor tests future Waiting, avoiding an overdue primary that a normal wakeup pass would already consume. Source content/modes `f0bcb81f8aa6503afd0d5ac875a21cfdd5b2dc23a17b638f2adc2818ed5102e0`, benchmark SHA-256 `1fc65b1346f4a531e7f47eeb67a80ca2867fd05b6ff52e40ee80157050c3fc2e`, script SHA-256 `b7c4392bb6e4a41e5714bfa7bf22f386756e06e4f59e301c8168cc2bdf116e22`; production Weight remains `2d9c60ac7764d1e7dc366531dd4a409444daf66cf3e37ed7feb922bb614e8f1a`.

Command: `./scripts/benchmarks.sh --low 0,0 --high 6,12 --extrinsic close_actor_opening_partition --output /tmp/deos-exp29-joint-crossing-close.rs --json-file /tmp/deos-exp29-joint-crossing-close.json pallet_deos_actors`. The new paired component-bound options forward an explicitly selected rectangle to the bencher through the ordinary build/output route, requiring exact selection and explicit output. Keep the standard 50 × 20, compiled Wasm, heap 4096, database/cache and hardware controls; run no concurrent heavy work during sampling.

The first Wasm falsifier rejects preparation before measurement: publication of sample three produces current value zero, not three. Source above, Wasm `d04c8a9c961e611a9f44c9470f39692921a335bdbb6d8e5845d60743bda6d607`, log `/tmp/deos-generate-pallet-deos-actors-weights.YKf12N.log` SHA-256 `d61bfa4af621d8b671b64a5eeba4bdc0a09999e5406e6a5bc37be2c51e86c329`; build 101 seconds, refusal three seconds, no model or samples. This is a fixture defect: the reference Oracle uses EMA with a 100-block half-life and floors both weighted terms separately. The mock's direct-value publication cannot qualify that host behavior.

Correction reuses the existing zero-Step publication strategy: sample 1,000 crosses threshold four, and at most the host's per-block transition bound of monotonically decreasing publications reaches the rearm boundary. Bounded worker passes then drain earlier transitions until they reach the repairable source cursor; no pending queue or cursor is injected, and the future retry deadline remains an asserted prerequisite. Native close-family checks pass after this correction. The shared retry constructor now checks that the actual published value crosses its authored threshold; older near-`u128::MAX` Crossing fixtures retain their separate host-domain qualification obligation instead of gaining an implicit direct-value Oracle assumption.

Corrected source content/modes `3422b307de3279d68783ad0a4e769f05f280ec4903a8740a81261ee0982c6439`, benchmark SHA-256 `89bd0c18938a7a51cbfc2e4e36638cf9015f99cb8bb1ad314d525221c2dfb304`; script, production Weight and the selected rectangle are unchanged.

The corrected Wasm is `227d4e1f00a8b94c1d27d95e1e46329c1b685bcf4395fef524bbd8b01358a914`. Its build takes 100 seconds. The initial 0..6 / 0..12 run reaches real cleanup, then fails later creation with `ContractStepsExceedOnIdleBudget` after 113 seconds; no partial model or raw artifact is exported. Retain log `/tmp/deos-generate-pallet-deos-actors-weights.FCWLAu.log`, SHA-256 `2d2e511e4edaf0e603e7c93d31eeccf2e693f007d870867a60ad0b6acded50ff`.

A four-point `--steps 2 --repeat 1 --min-duration 0 --low 0,0 --high 4,12` probe passes through creation, real suspension, detector history and verified close at `(0,12)`, `(4,12)`, `(4,0)` and repeated `(4,12)`. A separate exact `(5,12)` probe rejects creation; this is an adjacent boundary at fixed P=12, not a global Opening limit. Exact `(0,0)` reaches verified cleanup and exports four timing/DB samples; the CLI then refuses slope generation because both components are constant. All use the same Wasm with `--skip-build`; the newly exposed duration flag changes sampling controls only, and omitted duration retains the bencher default.

| Probe artifact | SHA-256 |
| --- | --- |
| Four-point model `/tmp/deos-exp29-joint-crossing-probe.rs` | `7f149939d55e484c01a15008dc5edf9b0397a1aba477dc8bb7f75728da031a1a` |
| Four-point raw JSON, same stem | `b8d94a96e4a33a5e83bc102325f070a9645bf8650dd3bbe64a224160cf6339df` |
| `(5,12)` log `/tmp/deos-generate-pallet-deos-actors-weights.Zms0ae.log` | `bb86b5513c633292eecc5365c31d06afb2eea49ce55db97fc008e0705e3f03e0` |
| `(0,0)` raw `/tmp/deos-exp29-joint-crossing-zero.json` | `0c1a1797515175964315eb167b368f143b62b2c233df6c809873d5ce1d2ad845` |
| `(0,0)` log `/tmp/deos-generate-pallet-deos-actors-weights.NKZnrO.log` | `01ba8e86e4e7aa1268b0d30a6958ca886f9799eede3993e67eee177d673f1a1e` |

The final command repeats the ordinary 50 × 20 run with `--skip-build --low 0,0 --high 4,12`, keeping the original `joint-crossing-close` output paths. It passes in 271 seconds. Runner source content/modes is `14062564e34466603c88aca10dca74dd33aebefff299ce4c175ddcaf53d7269c`, script SHA-256 `8099f3a7e66400c08ac5d57f78918de7a1c7911cbfe98b0bd1b696282f70e3de`; benchmark/runtime build inputs are unchanged from the corrected Wasm. Model SHA-256 `8c3445edd428e0abbf7c46ee3c74ac0cead7c55211f12db31d8633c0654f1b40`, raw JSON `c008c4272a77ff0dc5adcb22b6f594f1d7f91cd78733883100028de5dbe2f7ec`.

FRAME CLI 58.0.0 samples each component while holding the others at their selected maxima (`pallet/command.rs`); these are axis faces, not every point of a Cartesian rectangle. The full run retains all 2,000 timing samples and 100 DB samples over 17 distinct tuples. Every tuple has 34 unique reads, 56 repeated reads, 32 writes and zero repeated writes. Reference state has a 12-Step Contract, three tail chunks, F=0, cursor-zero suspended Run, all captured predicates true, full User owner-slot occupancy before close, and a 256-member Crossing source cohort before the bounded worker. The target has one future retry service key; no full Waiting page or heap is constructed.

| Sampled tuple family | Recorded proof B |
| --- | --- |
| `(0,12)` | 29,594 |
| `(1,12)` | 29,613 |
| `(2,12)` | 29,636 |
| `(3,12)` | 29,658 |
| `(4,P)`, every P=0..12 | `29,656 + 2P` |
| Separate `(0,0)` witness | 29,569 |

Full-run timing is min 704,920, mean 744,983.9065, upper median 739,701, nearest-rank p95 782,584, p99 842,509 and max 1,731,602 ns. No sample is discarded. The generated model is `733,171,212 + 1,990,914 O + 155,329 P` ps plus 34 reads and 32 writes, estimated proof `37,984 + 22 O + 2 P` bytes. At `(4,12)` the reference DB-inclusive value is 4,792,998,816 ps and 38,096 bytes. This is a diagnostic model over those samples, not a replacement production allowance.

Residuals remain separate from admission: measured-proof regression `29,568 + 22 O + 2 P` differs from observed proof by −1..+2 bytes; emitted estimated proof exceeds every observed DB sample by at least 8,414 bytes. Raw timing minus the non-DB affine term ranges from −32,554,171 to +990,594,098 ps, with 963/2,000 positive residuals. Thus the raw timing fit is not a sample-wise upper bound. Neither DB additions nor estimated-proof headroom establish the unmeasured complete-state envelope or actual/reserved runtime dominance.

The trace includes three Contract tails, present Run head/payload, one Waiting frame, service-directory/heap removal, two Crossing pages, target/moved locators, cursor rewind, and ordinary owner `System::Account`/`Balances::Holds` each read and written once. No Oracle feed or observation is read during measured close; publication and aggregation belong to preparation. Postconditions prove custody/sink balance neutrality, removal of the target's process/hold state and resumed visitation of the moved guard. EXP-0028 may consume this joint terminal phase point and EXP-0064 its admission witnesses, but complete heap/page, funding, other Contract/predicate families and consumed-source apoptosis remain unqualified. Do not subtract this aggregate from the earlier zero-Step result or sum overlapping close owners.

Validation on the corrected benchmark source: all-feature native `close_actor` family passes (62 seconds), independent Opening-partition boundary test passes (12 seconds), and workspace/all-target/all-feature Clippy passes (17 seconds). The earlier preflight compiles all benchmark surfaces (54 seconds) then reports the existing G5 stale-storage-name failure; the corrected full Wasm build and execution refresh the changed benchmark compilation evidence. Script syntax, help, malformed/partial-bound refusals and actual forwarding are checked through the public entrypoint. Runtime semantics, host policy, production Weight and generated metadata are unchanged.

Dependent-caller review finds that `prepare_reachable_update` still fabricates a second Rising occurrence without rearming after the now-real first publication; native `bench_update_contract` rejects its missing deferred latch. Correct only that constructor: use an actually crossable threshold four/rearm one, publish a real rearm then fire, and retain the replacement threshold at `u128::MAX` so replacement moves to a distinct Armed leaf. Native update-contract checks pass. Source content/modes `94f6904be510de7eafea8c380afae26527e97b2c8d84053474f0fac91fabdd88`, benchmark `9db8a3cf99bd19726600f3a6861f9ccf3d5abefbbbd7cc32ddaf8c23bf04b9b1`. Comparison against archived measured source `/tmp/deos-exp29-joint-measured-benchmarking.rs` (hash `89bd0c18938a7a51cbfc2e4e36638cf9015f99cb8bb1ad314d525221c2dfb304`) changes only this update constructor; joint-close construction and measured operation remain identical. The numeric close model retains its recorded Wasm identity, with no new numeric binding claimed.

The dependent host falsifier selects `update_contract_reachable_allocation` at exact `(o=0, t=0)` through the same public script with `--extra --steps 2 --repeat 1 --min-duration 0 --low 0,0 --high 0,0`, model path `/tmp/deos-exp29-real-update.rs` and JSON path `/tmp/deos-exp29-real-update.json`. The corrected-source Wasm build passes in 100 seconds, SHA-256 `0dc8bf1f3a959a614bd5dacda338ae2784921e6cace6cdd6576c4559f3124779`. The first invocation omitted `--extra` and therefore selected no benchmark (log `/tmp/deos-generate-pallet-deos-actors-weights.AxBGRk.log`, SHA-256 `6b2cf0bedc8cb101eabed7a810a4740b20622693d7498541c6d6cff3ed956695`). Reusing that Wasm with `--skip-build --extra` reaches the constructor but fails `ContractStepsExceedOnIdleBudget` at public creation in three seconds, before Run construction, rearm/fire or replacement (log `/tmp/deos-generate-pallet-deos-actors-weights.XM6CPW.log`, SHA-256 `83df4bd095c4f9cdedc374e9ec58d2b61e081c585711730444c16bc800f4c302`). No samples or model qualify this update state; production-host qualification remains open. Native update-contract checks and the independent allocation-boundary witness pass. This refusal supplies no reason to bypass admission, shrink the semantic limits or repeat the unchanged probe.
<!-- extracted:end -->

<!-- extracted: EXP-0029:405-465:52 -->
### Derived Metrics

Independent capture qualification extends the existing close diagnostic to `close_actor_opening_partition(o, p)`: `o` counts amount positions and `p` counts predicate occurrences, each assigned in authored order across the same maximum-length Contract. Current timing/resolution fills the remaining positions. The old coupled workload is the exact specialization `(2n, 4n)` on the reference host; the all-Opening retry constructor remains the maximum endpoint. First falsifiers are `(24, 0)` and `(0, 48)`, then accepted/rejected boundaries as needed. These probes decide which lifecycle fixtures can be measured; they cannot change production admission or install partial Weight.

The source admission owner reserves `MaxFundingTrackedAssets` at Opening even when this Contract has no LastFunding positions (`derive_step_resource_envelopes` and Idle `execution_step_control_weight_context`). The runtime also switches Suspended-head Weight families above four first-Step predicate evaluation units. Both inputs belong in interpretation: structural `F = 0` does not imply zero funding allowance, and independent capture counts do not establish selector continuity. EXP-0064 owns that envelope decision.

Source arithmetic rejects funding-reserve reduction as a sufficient repair for the all-Opening AddLiquidity maximum. The current generated Weight file (SHA-256 `6bad800cfbe4848b13f046b3598746e9a6163ec91a54ddbaa0f2e0b2fd5ba2ea`) contributes `58,613` proof bytes from `opening_snapshot_capture(24)`, `125,435` from `opening_predicate_capture(48)`, `81,886` from close and `34,255` from the AddLiquidity effect: `300,189` bytes before every other nonnegative owner. Guaranteed service is strictly below `(50 − 20 − 10 − 14)% × 5,000,000 = 300,000` bytes after scheduler/coordinator/drain deductions. Even zero funding cannot make this bound fit.

This is a lower-bound calculation on the currently bound, stale model, not a new empirical proof bound or a protocol impossibility. The runtime's component-wise maximum includes both capture components; removing a different positive allowance cannot remove their sum. [EXP-0028](./EXP-0033.md#opening-capture-component-ranges) qualifies the current component ranges next; full selector/funding derivation remains EXP-0064 work. No production selector, reserve, semantic limit or fixture was changed to test this hypothesis.

Independent-axis probes bind source content/modes `1f53c465c7db0844e73f2905b607b0287859cc6c2ea692c671be078f1fd4cf15`, benchmark source SHA-256 `171e68cf5e186b51d79cbedf709f937d2100181c3757163d41b331d6fa93b0e2`, and benchmark Wasm `068c40b5263e66ed0669fe12fee2569d57f549d200064fbf69fc20be229f50f3`. Reference host, compiler, bindings and measurement controls are unchanged; no concurrent build/test/benchmark runs during sampling. The public-script full-range probe (`--steps 2 --repeat 1`) builds in 113 seconds, then rejects creation before producing a model.

| Independent `(O, P)` probes | Observed result | Qualification |
| --- | --- | --- |
| `(12,0)`, `(18,0)`, `(21,0)`, `(22,0)` | Admission, actual suspension and verified public close reach analysis | Singleton execution witnesses; CLI subsequently rejects slope generation because a component has only one distinct value |
| `(23,0)`, `(24,0)` | `ContractStepsExceedOnIdleBudget` during creation | No measurement or close; `(22,0)/(23,0)` is an observed adjacent boundary |
| `(0,12)`, `(0,18)`, `(0,19)`, `(0,20)` | Admission, actual suspension and verified public close reach analysis | The same singleton analysis limitation |
| `(0,21)`, `(0,24)`, `(0,48)` | `ContractStepsExceedOnIdleBudget` during creation | No measurement or close; `(0,20)/(0,21)` is an observed adjacent boundary |

Reproduce each singleton with the earlier raw bencher command, changing both `--low` and `--high` to the comma-separated pair, retaining `--steps 2 --repeat 1 --min-duration 0`, and writing `/tmp/deos-exp29-independent-O-P.rs` with its log beside it. Boundary log SHA-256: `(22,0)` `26d7807b76fdb5194b62b49251d08846c1d5bdef7a3826a3c18bb5da581da2cf`; `(23,0)` `e4813d02a74bc3d8fa621e7ecaa16a6b6811e37b3cbf752e17f48dcce47e96e7`; `(0,20)` `87d1f96f64a7a21d4c45a02a326c41f4810b5d3690e8412dede47cce963a88f5`; `(0,21)` `12f97a99379792d72b13a85a2b008104fb2b2b5adb2be0c0d70c4fe98ffedc9e`.

These results falsify the hypothesis that only simultaneous saturation causes the maximum fixture's refusal. They do not prove all intermediate points, all predicate placements or a global maximum. In particular, `P > 0` puts Opening timing in Step 0 and selects a different Suspended-head allowance from `P = 0`; independent authored counts are not a proof of independent resource costs. Singleton regression output is discarded as numerical evidence.

The successful independent fit uses `--low 0,0 --high 6,12 --steps 50 --repeat 20 --heap-pages 4096`, with default minimum duration, output `/tmp/deos-exp29-independent-close-admitted.rs` and its `.log`. FRAME samples the two upper edges: every integer `(O,12)` for `O = 0..6` and `(6,P)` for `P = 0..12`, not the complete Cartesian interior. Every sampled fixture passes admission and postconditions. Fragment SHA-256 `691852c7532811d9ff7a1e9ba89a5acd46e30443b04ea7a1c56ca81410adf894`; raw-log SHA-256 `24403373a22db5049dd5d8183279b4f6712fa912bd482729da342122a10c65c8`. The fragment is not installed.

| Independent sampled-edge close model | Result |
| --- | --- |
| Generated RefTime before database charge | `586,611,692 + 132,411P` ps; `P` slope standard error `124,787` ps; no nonnegative fitted `O` slope |
| Minimum execution | `541,488,000` ps |
| Estimated ProofSize | `16,318 + 22O + 2P` bytes |
| Recorded proof | Least-squares `7,903 + 22O + 2P` bytes; median regression intercept `7,902` |
| Database | `27` reads, `24` writes, constant across both sampled edges |
| Timing dispersion | Per-point means `573.6..596.3` µs, sigma `5.869..17.31` µs (`1.0..2.9%`); no outlier removal |

The independent proof slopes reproduce the earlier coupled `52`-byte slope (`2 × 22 + 4 × 2`). This separates the two capture payload contributions for this close workload. The small CPU slope is comparable to its standard error and is not evidence of a performance improvement or cost-free captures. All three cold chunks and ordinary owner Account/Holds accesses remain included; detector/heap maxima, other predicate placements, the full interior, complete Opening/Step admission and Executive throughput remain outside the measurement.

Independent refinement validation: all eight native `bench_close_actor` tests pass (34 seconds), and `reachable_retry_opening_partition_boundaries` passes all nine zero/single/maximum amount/predicate combinations through real suspension, close and TryRuntime (25 seconds). Canonical formatting and Actors all-features Clippy pass. Preflight compilation passes (62 seconds), then correctly retains the known G5 stale-storage rejection. Production code and bindings are unchanged.

The earlier coupled `close_actor_opening_partition(o)` revision follows EXP-0028's failed User close setup. It preserves `S = MaxContractSteps`, all three reference cold chunks, four predicates and two AddLiquidity amount positions per Step. Only the first `o` Steps use Opening timing and amounts; the suffix uses Current, giving `O = 2o`, Opening predicate results `4o`, and no LastFunding entries. Ordinary User admission, paid Manual readiness and an actual output-bound failure construct Suspended Run state before measured public close. Its all-Opening constructor is the exact `o = S` specialization, preserved by the current independent maximum. No Actor Contract is shortened, mutated after admission or admitted by a bypass.

| Reference Opening partition | Result | Evidence class |
| --- | --- | --- |
| `o = 0, 1, 2, 3`; `O = 0, 2, 4, 6`; Opening results `0, 4, 8, 12` | Admission, paid Manual Opening, actual Temporary suspension and custody-neutral public close pass | Full reference-Wasm benchmark verification on the accepted subrange |
| `o = 4` | `ContractStepsExceedOnIdleBudget` before creation | Explicit singleton boundary probe on the same Wasm |
| Every `o = 5..11` | The same creation refusal | Separate singleton probes; no monotonic-selector assumption |
| `o = 12` | The same creation refusal | EXP-0028's original maximum User close setup; identical User Step body and budget inputs, independent of its Crossing Trigger |

The native benchmark passes its component tests including `o = 0` and the mock's full maximum. Reference qualification binds source content/modes `e40dbca3a854acaf86fe0afb8301cc9a793d834616a838842dc7c9d6f5ffac89`, benchmark source SHA-256 `cc56de02bc64c5c32b6baabaac16e95631b217114eb46fc5753d44b5b879c681`, and benchmark Wasm SHA-256 `eab00ae6738fb6950751fd9f8e767732055e24b5b9c293e6e62c19c087d50498`. Same pinned reference hardware/toolchain/cache/heap/map controls; no concurrent agent build/test/benchmark job during measurement. The full-range public-script probe builds in 110 seconds and rejects creation during sampling; it produces no accepted full-range coefficient.

Singleton rejection probes use `bin/frame-omni-bencher v1 benchmark pallet --runtime template/target/benchmarks/release/wbuild/deos-runtime/deos_runtime.compact.compressed.wasm --pallet pallet_deos_actors --extrinsic close_actor_opening_partition --low o --high o --steps 2 --repeat 1 --min-duration 0 --heap-pages 4096 --output /tmp/deos-exp29-close-opening-rejected-o.rs`, substituting each `o = 4..11`. These are constructive admission checks, not timing evidence; none reaches measurement or creates a fragment. The prior `o = 12` failure is reused by equality of the budget's User/Step inputs rather than repeated.

| Accepted `o = 0..3` close model | RefTime ps before DB additions | Estimated / recorded proof B | Reads / writes |
| --- | --- | --- | --- |
| `close_actor_opening_partition(o)` | 593,729,432; minimum observed 549,589,000 | `16,317 + 52o` / `7,903 + 51o` | 27 / 24 |

Measure that subrange with the same raw bencher command using `--low 0 --high 3 --steps 50 --repeat 20 --heap-pages 4096 --output /tmp/deos-exp29-close-opening-admitted.rs` and the default minimum-duration control. The temporary uninstalled fragment has SHA-256 `9e438454ecfbab1e445b05f407284fae076d913d54eea5f8e144abaa272fa23a`. Per-point mean/σ microseconds are `593.9/12.29`, `592.8/11.46`, `591.8/10.30`, `595.7/12.85`; the fitted nonnegative RefTime slope is zero, which does not prove captures cost nothing. Measured proof mode covers only this geometry.

The trace includes three cold chunks, Run head/payload, Waiting primary/key removal and ordinary owner `System::Account` plus `Balances::Holds`, each owner-ledger store at one read/write. Native TryRuntime and Wasm postconditions require no Actor/Run/hold/locator/tail residue and unchanged sovereign/Fee Sink balances. This direct public close is not full FIFO or Executive, nor a general Crossing/ObservationChange, maximum encoded-byte, independent Opening-amount/predicate or populated-heap bound. The measured coupled prefix domain cannot replace the original maximum fixture or become an unconditional 3-Step Opening limit; other allocations and final Weight identities remain EXP-0029 / EXP-0064 work.

Retained-state validation: the all-features native `bench_close_actor` family passes after constructor/assertion generalization (41 seconds); prior maximum assertions remain explicit in `assert_reachable_retry`, while the generic assertion derives the exact authored Opening count. Actors all-features Clippy (25 seconds), canonical formatting, workspace check, changed-scope completion (13 seconds), record normalization/dependency graph and diff checks pass. Production execution and host bindings are unchanged; completion uses `--skip-runtime-tests` and reuses prior runtime/embedding evidence. Final preflight compiles (58 seconds) but retains the known stale generated-storage rejection. Source content/modes remain identical after context synchronization. Successful subrange evidence does not erase either full-range admission failure or close the G5 artifact gate.
<!-- extracted:end -->

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

Ordinary User creation, paid Manual/Crossing readiness, host publication, actual Temporary suspension and public close realize the declared retained Contract/Run/capture/detector/Waiting/hold geometry throughout the measured admitted subdomain. The joint Crossing point `(O=4,P=12)` passes at maximum Contract length; `(5,12)` rejects ordinarily. Independent boundaries reach `(22,0)` and `(0,20)` while adjacent `(23,0)` and `(0,21)` reject. Current-tree all-feature `close_actor` validation passes all 14 selected tests.

## Interpretation

The reachable User-close domain is admission-constrained rather than the independent `12/24/48` storage product. Passing points preserve real suspension, deferred Crossing latch, live cursor repair, hold cleanup and custody neutrality; rejected adjacent points remain explicit exclusions rather than reasons to weaken bounds.

## Decision

Accepted for the observed admitted reference subdomain. Ordinary admission/funding and actual suspension jointly realize the certified User-close capture/detector/Run geometry, including the joint `(O=4,P=12)` Crossing point and independent admitted boundaries. This satisfies EXP-0029 O7 without accepting refused maximal tuples or a replacement production Weight.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. Do not infer a new selection from record relocation or from aggregate coefficient subtraction.

## Validity

- `Establishes`: The admitted reference User-close geometry, its real suspension/detector/hold/custody postconditions and the observed adjacent admission boundaries.
- `Does not establish`: Complete production Weight, exact release artifact closure or integrated throughput.
- `Known limitations`: The independent 12/24/48 maximum is not jointly admitted; sampled axis faces are not an interior Cartesian proof, and exact production Weight remains downstream.
- `Invalidation triggers`: A changed measured body, host domain, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Follow the owning physical boundary, not a benchmark name.

## Follow-Up / Next Gradient

Stop when the declared proof is satisfied. Transfer any independent owner, selector, parameter domain or artifact-identity question before investigating it beyond the smallest classifier. A hypothetical corner case alone is not a measurement mandate.

## Architecture Impact

No runtime, semantic, storage or production binding change. C1 and A0 remain frozen; this record owns only its declared proof evidence.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0029](./EXP-0029.md) O7
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0029](./EXP-0029.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
