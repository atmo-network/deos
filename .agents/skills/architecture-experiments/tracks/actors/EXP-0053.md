# EXP-0053 — V10 Admission Refusal Lower Bound

| Field | Value |
| --- | --- |
| Status | Measured |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0029](./EXP-0029.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | The V10 charged proof lower bound suffices to reject the five declared maximal lifecycle tuples through ordinary admission. |
| Physical mechanism | V10 Admission Refusal Lower Bound |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0029](./EXP-0029.md) |


## Decision Question

The V10 charged proof lower bound suffices to reject the five declared maximal lifecycle tuples through ordinary admission.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns v10 admission refusal lower bound. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0029 / “Admission Requalification After Opening Model Binding”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. No candidate is introduced by this migration.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Extraction baseline: `a60de7748132211d9e58cecd5be82b41f76ce230`. The migration manifest records exact original content/mode identities and source spans. Question-specific source/Wasm/output identities remain inline. Historical fragments remain historical; no new runtime or production artifact was built.

Use the identified environment attached to each extracted source. Where a shared control is needed, follow its explicit evidence source; absent identity requires refresh before acceptance.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | The V10 charged proof lower bound suffices to reject the five declared maximal lifecycle tuples through ordinary admission. | One of the five declared V10 maximum tuples whose charged proof lower bound does not exceed the strict service ceiling | Exact V10 charged-model arithmetic and reference-Wasm admission refusal | Self | Required | Named parent decision and declared downstream consumers | Satisfied with scope; original binding/host limitations retained; complete release binding remains open |

## Measurements

<!-- extracted: EXP-0029:373-404:53 -->
### Admission Requalification After Opening Model Binding

V10 changes admission's generated Opening inputs, so the earlier User-close and four maximal-tail Suspended refusals cannot certify the new numeric boundary. Reuse the unchanged legal constructors, actual OwnerOnly funding, Opening/failure/wakeup transitions and captured-state assertions. The smallest falsifier first retries maximal `close_actor` and the four nonterminal Suspended-head profiles under current reference admission. A creation refusal classifies that concrete tuple as unadmitted; it neither rejects authored semantics nor supplies a measured transition Weight.

Build one fresh benchmark Wasm from `e77b794` plus the Current provider-regression extension. Run the existing maximal User-close profile through `scripts/benchmarks.sh --steps 2 --repeat 1 --extrinsic close_actor --output /tmp/deos-exp29-v10-user-close.rs pallet_deos_actors`. Then use that exact Wasm for singleton CLI probes of `scheduler_inner_suspended_head_{retry,progress,opening_retry,opening_progress}` at `n = 22`, `p = 4` for Current-only and `p = 3` for one-Opening heads, with `--steps 2 --repeat 1 --min-duration 0`. These are setup/admission probes, not accepted timing fits. If any passes, retain its constructive result and measure only the accepted domain needed by the owning transition. If all fail, inspect the composed reservation's binding resource and transfer that numeric obstruction to EXP-0064 rather than shrinking the authored Contract or repeating the same rejection.

Prepared source content/modes `abad930ade82698f93b7352f4baf3604961efdcb5beae285cb46577f6c19d37d`; benchmark source `656def3ed440db18adcae3b63264d7458d48dec442237741be53fc5ea552c8c9`; provider `0858133d39eaabb1d2591def2454f1b72d7ee6746b3c1f5ecd720f49a37750f9`; staged Weight file `2d9c60ac7764d1e7dc366531dd4a409444daf66cf3e37ed7feb922bb614e8f1a`. Preflight compilation passes in 56 seconds, followed by the known G5 stale-storage-name rejection. Fresh benchmark Wasm `d8b5692148edf84b91ed4fd7d70180cae648a1083c86ba472006e19643e8123d` builds in 101 seconds. The User-close probe and all four singleton probes fail creation with `ContractStepsExceedOnIdleBudget`; no measurement model is produced.

| Profile | Authored `(S,O,P,F)` at probe | Failure-log SHA-256 |
| --- | --- | --- |
| User `close_actor` | `(12,24,48,0)` | `4566c70b04bcdc6711531844954523fbae945f2ee7b4583e20d2ab0395e48a5d` |
| Suspended Current head retry | `(12,22,44,0)` | `9a47462ebd9da54924bfa821c6ae658d539312e04a5098e1eaee71feeaff4cc0` |
| Suspended Current head progress | `(12,22,44,0)` | `49aa1cf56d4c101fcb5a6555834214f4ae3c26fa892552f63110c8854154da79` |
| Suspended one-Opening head retry | `(12,22,45,0)` | `a44a541ecaba44e754f52034f4eb7ac8b59b7b51aedff1f7a548cdd8ebb569b9` |
| Suspended one-Opening head progress | `(12,22,45,0)` | `a5479236ddc1370a7c7681e128cb1008ba95438e87ac9b5a84c6eccf80b6d84e` |

Singleton command: `bin/frame-omni-bencher v1 benchmark pallet --runtime template/target/benchmarks/release/wbuild/deos-runtime/deos_runtime.compact.compressed.wasm --pallet pallet_deos_actors --extrinsic scheduler_inner_suspended_head_PROFILE --low 22,P --high 22,P --steps 2 --repeat 1 --min-duration 0 --heap-pages 4096 --output /tmp/deos-exp29-v10-PROFILE.rs`, with `PROFILE` and parameter `P` defined above. Logs use the corresponding `.log` path; User close uses `/tmp/deos-generate-pallet-deos-actors-weights.8Z1wQu.log`. No concurrent heavy work ran during probes. These source-bound rejections supersede the old-binding rejections only for these exact points; they do not invalidate the already admitted smaller or terminal domains.

Source arithmetic independently identifies ProofSize as sufficient to reject every point. `guaranteed_actor_service_weight` is strictly below `(50−20−10−14)% × 5,000,000 = 300,000` bytes after fixed scheduler/coordinator/drain deductions. The first-Step composed reservation includes both captures, the maximum head plan (at least 8,058 bytes), Current component (12,697 bytes at these hints), and its own Task effect; `contract_steps_admission_weight_upper` additionally includes close (81,886 bytes). This is a lower bound on currently charged Weight, not recorded proof or a fundamental C1 limit.

| Profile | Amount / predicate capture B | Head plan / Current B | Head effect / close B | Charged proof lower bound B |
| --- | --- | --- | --- | --- |
| User close | 69,430 / 137,486 | 8,058 / 12,697 | 34,255 / 81,886 | 343,812 |
| Current retry | 63,758 / 126,142 | 8,058 / 12,697 | 18,280 / 81,886 | 310,821 |
| Current progress | 63,758 / 126,142 | 8,058 / 12,697 | 34,255 / 81,886 | 326,796 |
| One-Opening retry | 63,758 / 128,978 | 8,058 / 12,697 | 18,280 / 81,886 | 313,657 |
| One-Opening progress | 63,758 / 128,978 | 8,058 / 12,697 | 34,255 / 81,886 | 329,632 |

All omitted owners are nonnegative, including funding reservation, reconstruction, Run commit and placement. The effect is the first Step's Transfer or AddLiquidity, not a maximum taken from a different Step. Thus eliminating the current 40-entry funding allowance alone cannot admit these five Contracts. Repeating these full-range benchmark setups would add no decision evidence. EXP-0064 must reconcile complete charged owners and supported constraints; EXP-0028 / EXP-0063 must qualify cleanup and placement with admitted geometry before a smaller bound can replace the current one.

Final caller-corrected validation: `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes in 2 minutes 11 seconds, including its nested debug Wasm build. Experiment normalization validates all 28 records and their dependency DAG; `git diff --check` and `completion-gate.sh --skip-cargo-check --skip-runtime-tests` pass. The earlier canonical formatting check remains applicable: no Rust or script edits followed it. These checks close this fixture/script correction, not G4–G8 or production Weight acceptance.
<!-- extracted:end -->

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

The extracted observations and counterexamples above remain valid only within their original source-bound scope. No new observation was made during migration.

## Interpretation

A scoped witness may satisfy its declared obligation without accepting every production consumer. Existing rejected fits, admission refusals, missing host domains and uninstalled fragments remain explicit evidence boundaries.

## Decision

No new Accepted/Rejected decision during migration. The declared scoped proof is measured; stop repeating its satisfied witnesses and let the parent consume their stated limits.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. Do not infer a new selection from record relocation or from aggregate coefficient subtraction.

## Validity

- `Establishes`: Only the source-qualified observations and constraints above.
- `Does not establish`: Complete production Weight, exact release artifact closure or integrated throughput.
- `Known limitations`: Existing missing domains, diagnostic sample counts, stale bindings and host restrictions remain effective.
- `Invalidation triggers`: A changed measured body, host domain, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Follow the owning physical boundary, not a benchmark name.

## Follow-Up / Next Gradient

Stop when the declared proof is satisfied. Transfer any independent owner, selector, parameter domain or artifact-identity question before investigating it beyond the smallest classifier. A hypothetical corner case alone is not a measurement mandate.

## Architecture Impact

No runtime, semantic, storage or production binding change. C1 and A0 remain frozen; this record owns only its declared proof evidence.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0029](./EXP-0029.md) O8
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0032](./EXP-0032.md), [EXP-0033](./EXP-0033.md), [EXP-0034](./EXP-0034.md), [EXP-0035](./EXP-0035.md), [EXP-0036](./EXP-0036.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0029](./EXP-0029.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
