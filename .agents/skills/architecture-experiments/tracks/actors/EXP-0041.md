# EXP-0041 — Pipeline Admission Apoptosis Ownership

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0028](./EXP-0028.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | Pipeline admission insolvency has one custody-neutral minimal-apoptosis owner covering its admitted detector, cold-tail and window domain. |
| Physical mechanism | Pipeline Admission Apoptosis Ownership |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0028](./EXP-0028.md) |


## Decision Question

Pipeline admission insolvency has one custody-neutral minimal-apoptosis owner covering its admitted detector, cold-tail and window domain.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns pipeline admission apoptosis ownership. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0028 / “Insolvency with Cold Contract Tails and Future Window References”, “Paid Trigger and Pipeline-Admission Insolvency”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. The deciding reference binding selects the attained maximum-Contract Crossing plus future-window full-FIFO atom, which dominates every retained insolvency profile component-wise.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Pre-fission baseline `a60de7748132211d9e58cecd5be82b41f76ce230`. Each extracted block retains its own exact source/Wasm/output identity. Shared pinned hardware/toolchain/cache controls follow [EXP-0030](./EXP-0030.md#environment-and-artifact-identity) only where the original comparison explicitly reuses them. A shared environment never substitutes for the block’s own source or Wasm identity. No runtime artifact was rebuilt during migration.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Pipeline admission insolvency has one custody-neutral minimal-apoptosis owner covering its admitted detector, cold-tail and window domain. | One paid useful Trigger leaving the Actor one balance unit short of Pipeline admission with retained detector/window state | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Satisfied for the admitted reference domain: the maximum-Contract Crossing plus future-window full-FIFO witness attains the falsifier and dominates every retained insolvency profile |

## Measurements

<!-- extracted: EXP-0028:487-507:41 -->
### Insolvency with Cold Contract Tails and Future Window References

Question: qualify jointly reachable retained cold Contract chunks, Crossing topology, User holds and a future Block window reference at Pipeline-admission insolvency. `remove_admitted_contract_geometry_with_admission` loads and deletes the complete admitted geometry; `finalize_actor_loaded_inner` invalidates future references before process removal. The earlier zero-Step full-FIFO point contains neither cold tail chunks nor a future window reference. Those actual owners, not a hypothetical optimization, require this domain witness.

The shared ordinary paid-insolvency constructor now accepts authored Steps and an optional minimum-length window while preserving the former zero-Step inputs. Paired `scheduler_paged_user_crossing_insolvency_max_contract` and `_window` profiles hold a maximum-Step Transfer Contract constant and vary only the retained future window reference. Native `pipeline_insolvency_cleans_each_admitted_tail_and_future_window` checks every Step count through the host maximum with and without that reference, exact tail chunk counts and custody-neutral cleanup. New profiles remain excluded from default production generation. Step-count maximum is not maximum Contract bytes, maximum detector population or a complete Waiting/heap envelope; admission must succeed normally before any measured claim.

Native qualification passes all 26 combinations (`S = 0..12`, window absent/present). Fresh reference Wasm admits both 12-Step points, records real Trigger payment and a one-unit Pipeline shortfall, and verifies complete process/hold/tail/reference removal with unchanged custody/Fee Sink and no new Pipeline receipt. Twelve Steps are therefore not globally resource-inadmissible: the earlier 9/12-Step CompleteMax rejection is specific to that expensive authored geometry, not permission to reduce the semantic ceiling.

| Full FIFO insolvency profile | Generated RefTime / minimum ps | Estimated / recorded proof B | Reads / writes | Temporary fragment SHA-256 |
| --- | --- | --- | --- | --- |
| `scheduler_paged_user_crossing_insolvency_max_contract` | 577,317,000 / 525,703,000 | 86,387 / 6,197 | 63 / 58 | `50f5228b1be68e37b28dc540638415b989fc8f5fb8900dc0498c28b7882bf0f6` |
| `scheduler_paged_user_crossing_insolvency_max_contract_window` | 709,110,000 / 603,717,000 | 86,870 / 6,680 | 70 / 65 | `315aef46e499b267d838151de333f123334eb1f2a5668c947d6aef05352b691f` |

Source content/modes `692d88aa58c342f5fa0bf3cab929d822cb08871cbeac073fcc2fb0539a1d6178`; benchmark source SHA-256 `536d3908b423e3ca1abcb61b8d78116478e0397f7858b4b45f82340ff1abc2df`; benchmark Wasm SHA-256 `ef01d799221a721ceaaff52a2b8e74786d44b0f2b91f1cc4d0948994d02dbba4`. Same pinned reference toolchain/hardware/cache/heap/map controls as above, 50 steps × 20 repeats, no concurrent agent build/test/benchmark job during measurement. Fresh build took 110 seconds and each generation 14 seconds. Both profiles use Measured proof mode.

Reproduce with `scripts/benchmarks.sh --extrinsic scheduler_paged_user_crossing_insolvency_max_contract --output /tmp/deos-exp28-insolvency-max-contract.rs pallet_deos_actors`, then `scripts/benchmarks.sh --skip-build --extrinsic scheduler_paged_user_crossing_insolvency_max_contract_window --output /tmp/deos-exp28-insolvency-max-contract-window.rs pallet_deos_actors` on the same binary. Both fragments remain temporary and uninstalled.

Both traces include exactly three cold tail reads/writes. The window variant additionally reads/writes the exact Waiting chunk, occupancy, head, tail, cursor index, cursor page and cursor length. It removes the last entry of a singleton Block key; it does not qualify a populated heap's competing removal branches. The inclusive values already cover FIFO liability, source handling, Crossing and lifecycle/hold cleanup. Do not add those owners again or subtract the pair to fabricate a standalone general reference-removal coefficient. EXP-0029 owns joint reachability and EXP-0063 retains the complete heap envelope; G5 production binding remains open.

Validation: the focused all-length native property passes (33 seconds), followed by the complete native `insolvency` family (16 seconds) and the reused direct `pipeline_admission_apoptosis` family (2 seconds). Actors all-features Clippy (25 seconds), canonical formatting, workspace check, changed-scope completion (13 seconds), record normalization/dependency graph and diff checks pass. Only benchmark setup/assertions and private accounting names changed; former zero-Step constructors retain identical authored inputs and balances, and prior production/runtime evidence remains applicable. Completion uses `--skip-runtime-tests`. Preflight compilation passes (63 seconds), retaining the known G5 stale generated-storage rejection. Source content/modes remain equal after context synchronization; no production numeric envelope is accepted.
<!-- extracted:end -->

<!-- extracted: EXP-0028:530-555:41 -->
### Paid Trigger and Pipeline-Admission Insolvency

Two complete FIFO profiles ordinarily create a zero-Step User whose paid Manual or Crossing occurrence leaves exactly one balance unit below Pipeline capacity. Setup prefunds ordinary creation and adds Trigger fee minus one before the real source event; it never burns balances, mutates Contracts or injects latches. Crossing retains indexed membership after readiness. Measurement includes canonical FIFO discovery, liability check, source retirement and cleanup. Postconditions require no Run/Action/Pipeline collection, unchanged post-Trigger custody/Fee Sink balances and complete process removal. Singleton Ready and fresh Crossing geometry remain narrower than complete production envelopes.

| Complete FIFO profile (`Measured`) | RefTime before DB additions / minimum (ps) | Estimated / recorded proof (bytes) | Reads / writes | Generated fragment SHA-256 |
| --- | --- | --- | --- | --- |
| `scheduler_paged_zero_step_user_insolvency` | 261,490,000 / 242,423,000 | 8,693 / 2,753 | 22 / 17 | `8f6d5c667b2f85d1717c7dc793eee09b4efd078e71efad413ac42edd4e9b92a5` |
| `scheduler_paged_zero_step_user_crossing_insolvency` | 491,830,000 / 461,518,000 | 85,256 / 5,066 | 60 / 55 | `57d1160a04e731cdb45da888b2462223d242273afdc54c017f9b0aab09f0451a` |

Both pass reference-Wasm admission, measurement and postconditions on source `cad7d896f10545e5a7a85231e2b2d30ddb57eea0d7bbef2b3a271e2463755210`, benchmark source SHA-256 `a2c6a3d31b55bbd7ddc5c000f33fd48675127b63ab2eb94b81974550719f0ffe`, Wasm `28f1b05bb40519d62bbd35dbc910197fde268ca2d73885e481ee94a3ec0e09f4`. Reproduce with `scripts/benchmarks.sh --extrinsic PROFILE --output /tmp/deos-exp28-zero-step-KIND.rs pallet_deos_actors`, using the table order and KIND=`insolvency`, `crossing-insolvency`; add `--skip-build` only for the second. Fresh build took 105 seconds; each measurement took 14 seconds.

That full-FIFO comparison exposes retained Crossing cleanup, but cannot isolate a cleanup coefficient by subtraction. The next pair uses the same `finalize_actor` boundary as the existing `pipeline_admission_apoptosis` fixture and `MaxEncodedLen` for both profiles. Manual's owner becomes an ordinary benchmark account because scheduler-owned hold release must include its ledger access; the old `whitelisted_caller` fixture and generated trace omitted that System account. Source inspection also rejects the old maximum-Contract comment: `make_contract_steps` supplies one Transfer Step. The comparison therefore qualifies Manual one-Step versus Crossing zero-Step cleanup, not a maximum Contract.

| Direct cleanup profile (`MaxEncodedLen`) | RefTime before DB additions / minimum (ps) | Estimated / recorded proof (bytes) | Reads / writes | Generated fragment SHA-256 |
| --- | --- | --- | --- | --- |
| `pipeline_admission_apoptosis` | 206,174,000 / 191,787,000 | 15,106 / 2,677 | 19 / 16 | `64714694ebb0787d2e956e2ff7ebe21d0a5378f0fbde5a6b5ccfd6da6489ac58` |
| `pipeline_admission_apoptosis_crossing` | 440,426,000 / 400,615,000 | 81,886 / 4,850 | 57 / 54 | `1824d07afc73ef65b63fad9e0854767b4936e1519eff3d03b2c694dbbac87238` |

Both direct profiles pass on source `75509f5108862238d3e0e0566725d755ce6c4cfedbb4ad8d22fab649ed8db568`, benchmark source SHA-256 `98cce743e42983b0ba6279f591dca0094a2f746432857da57bebed2242cc94ea`, Wasm `3a4fe1243913423b6536a0d95dc3f4edb2ed90c063f7ee91e56a06603cd375df`. Reproduce with `scripts/benchmarks.sh --extrinsic PROFILE --output /tmp/deos-exp28-apoptosis-KIND.rs pallet_deos_actors`, table order and KIND=`manual`, `crossing`, adding `--skip-build` for the second. Fresh build took 103 seconds; measurements took 13/14 seconds. All four measurements use 50 steps × 20 repeats, the pinned environment above and no concurrent agent build/test/benchmark job during measurement. Fragments remain uninstalled; all three new diagnostic profiles are excluded from default production generation.

The two direct traces now include one System account read/write for owner hold release. Crossing additionally touches 32 radix nodes and its leaf/member/feed topology, despite having zero Steps. This rejects Manual-only qualification of `close_admission_decision`'s common `pipeline_admission_apoptosis` owner; merely regenerating the Manual fixture cannot establish the missing domain. The direct Crossing fixture retires the resulting FIFO tombstone only after measurement. Both inclusive boundaries already contain their declared source/cleanup work, so add neither twice. Neither pair bounds maximum Contract/header, populated detector/Waiting geometry or every insolvency state. EXP-0064 must reconcile the admitted cleanup domain before G5 binding; C1 and monetary rules are unchanged.

After measurement, only two benchmark comment lines changed to correct the one-Step domain; executable setup and measured bodies are unchanged. Retained source is `8ba757ba38b54c05a1092862e409c8f44240d5df546cb0641c2553b16ba12d61`, benchmark SHA-256 `dbece9d420c67edc9cb626c75f03fa3e1659731443f132638da1a66b3e3d3749`. Numerical evidence keeps its measured identities rather than claiming a new exact-tree Wasm binding.

Validation: focused native all-features Manual/Crossing FIFO tests pass (29/1 seconds); the final direct-apoptosis family passes after the owner/PoV correction (13 seconds). Workspace all-features Clippy passes (23 seconds), as do canonical formatting, workspace check, changed-scope completion (14 seconds), record normalization/dependency graph and diff checks. No production/runtime helper behavior changed, so unaffected behavioral evidence is reused and completion skips runtime tests. The final preflight compiles (59 seconds) and retains the known stale generated-storage rejection; G5 remains open. Context updates preserve the retained source identity above.
<!-- extracted:end -->

### Deciding Pipeline-Apoptosis Owner

The retained reference-Wasm profiles partition ordinary Manual and Crossing cleanup, complete FIFO service, maximum-Step cold Contract tails and an optional future Block reference. `scheduler_paged_user_crossing_insolvency_max_contract_window` directly attains the smallest falsifier after a real paid Trigger: a 12-Step User Crossing Actor remains one balance unit short of Pipeline admission while retaining three cold tail chunks and one future window reference, then closes without Pipeline collection, Action invocation, custody movement or residue.

Its attained vector, `709,110,000` pre-database RefTime, `86,870` estimated ProofSize, 70 reads and 65 writes, dominates every retained direct and full-FIFO insolvency profile in all four charged coordinates. Under the pinned reference RocksDB charge, the resulting RefTime is `8,959,110,000`. No cross-artifact subtraction or synthetic coordinate envelope is needed.

This is one inclusive reference branch owner: canonical Ready discovery/consumption, Crossing cleanup, complete admitted Contract-tail deletion, singleton Block-reference invalidation, lifecycle removal and applicable owner-hold release occur once. EXP-0058 still owns source/destination composition for other transitions, but its generic source owner must not be added again when this complete apoptosis atom is selected. EXP-0063 retains populated Waiting-page and competing heap-repair coverage; the attained singleton reference does not decide those envelopes. EXP-0065 owns the exact generated selector and must regenerate it from the complete accepted G4 composition rather than subtracting or manually patching fragments.

## Derived Metrics

The selected attained vector contains the maximum-Contract no-window point `577,317,000 / 86,387` plus 63 reads / 58 writes, zero-Step Crossing FIFO `491,830,000 / 85,256` plus 60 / 55, direct Crossing cleanup `440,426,000 / 81,886` plus 57 / 54, and both narrower Manual points component-wise. Its RocksDB total is `709,110,000 + 70 × 25,000,000 + 65 × 100,000,000 = 8,959,110,000` RefTime.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

The maximum-Contract Crossing plus future-window witness attains the declared detector/cold-tail/reference falsifier and contains every retained insolvency vector component-wise. Its postconditions establish custody-neutral complete process, hold, tail and reference removal with no Pipeline receipt.

## Interpretation

For the admitted reference domain, Pipeline-admission insolvency is one terminal FIFO atom rather than a direct cleanup fragment plus independently repeated source, detector, tail or reference charges. Populated Waiting/heap alternatives remain separate composition inputs, not reasons to keep this attained owner open.

## Decision

Accepted for the admitted reference Pipeline-apoptosis domain. Select the attained complete FIFO owner at `709,110,000` pre-database RefTime, `86,870` estimated ProofSize, 70 reads and 65 writes (`8,959,110,000` RefTime under the pinned RocksDB charge). Maximum and actual select the same custody-neutral atom. EXP-0065 owns exact regeneration; no diagnostic fragment, subtraction or manual coefficient repair is accepted.

## Rejected Alternatives and Tradeoffs

Reject the stale Manual-only generated owner; direct Crossing cleanup as a complete bound; the no-window maximum-Contract point; adding generic FIFO/source, Crossing, tail, hold or reference work to the selected inclusive atom; subtracting aggregate profiles to fabricate an inner coefficient; treating the singleton reference as a populated-heap envelope; and manually patching production Weight before EXP-0065.

## Validity

- `Establishes`: One attained reference full-FIFO apoptosis owner over admitted maximum-Step cold tails, Crossing topology, one future Block reference and applicable User hold cleanup.
- `Does not establish`: Populated Waiting-page/heap removal, every Contract byte/header or detector population combination, exact final production Weight identity or integrated throughput.
- `Known limitations`: The selected diagnostic profile remains uninstalled; EXP-0058/EXP-0063/EXP-0064 must preserve no-double-charge composition before EXP-0065 regeneration.
- `Invalidation triggers`: A changed insolvency detector, Contract-tail/reference/hold cleanup, source boundary, admitted host domain, source/Weight/Wasm identity or selector rule requires scoped requalification.
- `Residual qualitative insight`: An attained terminal atom can close its scoped owner while larger source/heap populations remain independently composed.

## Follow-Up / Next Gradient

Stop measuring this satisfied apoptosis owner. EXP-0058 owns other source/destination branches, EXP-0063 owns populated Waiting/heap envelopes, EXP-0064 composes admission and EXP-0065 regenerates exact artifacts. Reopen only for the declared falsifier or a changed relevant path, domain, selector or identity.

## Architecture Impact

The reference Pipeline-insolvency input now requires one inclusive full-FIFO owner covering source consumption, Crossing cleanup, maximum-Step cold tails, singleton future-reference invalidation and User hold release without duplicate charges. No runtime, semantic or storage change is introduced; C1 and A0 remain frozen, and complete production regeneration remains G5 work.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0028](./EXP-0028.md) O12
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0028](./EXP-0028.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
