# EXP-0098 — Crossing Executed-Path Resource Coverage

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0066](./EXP-0066.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-08 |
| Affected domain | ObservationCrossing resource composition |
| Physical mechanism | Crossing transition detection, latching and scheduled service |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0074](./EXP-0074.md) — accepted W6 generation chronology |
| Successor experiment | None |

## Decision Question

Does the selected current-generation ObservationCrossing path compose detector/latch and scheduled service owners exactly once while stale generation authority performs no Actor work?

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` §§4.2, 4.3 and 4.6; `docs/actors-resource-policy.specification.en.md` §§4 and 6.
- `Invariant`: One useful latch, generation authority and scheduler-only execution remain unchanged.
- `Reopen trigger`: A causal or fee semantic change belongs to specification.

## Context and Hypothesis

EXP-0089 preparation incorrectly treated Crossing and broad deferred fanout as one path. Crossing has its own transition worker and must not borrow broad-fanout owners.

## Baseline and Candidates

Use W6 Actor 27: replacement from generation 1 to 2, one post-replacement transition, materialization in block 5 and successful Transfer service in block 6. The pre-replacement transition is the stale-authority negative control.

## Controls

- `Controlled variables`: EXP-0095 binding, W6 fixture, generation, threshold, Task and exact-Wasm chronology.
- `Changed variables`: Source-bound Crossing detector/latch resource accounting only.
- `Confounders`: Broad observation fanout, temporal work and scheduled service are separate owners.

## Benchmark Class, Workloads, and Criteria

- `Class`: Source-bound native accounting linked to accepted exact-Wasm W6 states.
- `Workload`: One generation-2 User ObservationCrossing occurrence and one scheduled Transfer.
- `Metrics`: Detector/latch Control, generated I/O, service Control/effect and state/proof linkage.
- `Acceptance`: Complete nonduplicated equation; stale generation produces no Actor-specific charge or service.
- `Rejection`: Broad-fanout owner borrowed, missing detector/service owner or unmatched state.

## Environment and Artifact Identity

- `Current input`: EXP-0074 W6 evidence on EXP-0095 Weight/Wasm.
- `Capacity evidence`: `/tmp/deos-0725-exp0098-capacity.log`, SHA-256 `e4eb094b14420758af05c0c3a54d60749631591509abad86dd197bc3b44f18a4`.
- `Complete native ledger`: `/tmp/deos-0725-exp0098-complete.log`, SHA-256 `fac5c5c7f300e67af28f098144e9901a979d53778f1cf32751a2251fcf9406c3`.
- `Resource artifact`: `crossing_single_user_fire_binds_executed_detector_latch_control` binds one funded User Crossing occurrence, generated worker meter, next-block service Control and Transfer effect.
- `W6 exact-link evidence`: `/tmp/deos-0725-exp0098-w6-exact-link.log`, SHA-256 `a620760c2ee8cec559d7911ba4a0c71f0177b5304c3e4ae218f274480f72bcae`.

## Proof Obligations

- `Freeze`: Frozen for W6 Actor 27's generation-2 Crossing detector/latch plus scheduled Transfer service and the exact O1 falsifier.
- `Review triggers`: Crossing branch, generation state, fee owner, Task or evidence-linkage change.
- `Decomposition review`: One Crossing family path; broad deferred fanout remains EXP-0089.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Crossing detector/latch and scheduled service compose exactly once | One missing, duplicate or borrowed owner | Source-bound ledger linked to W6 exact states | Self | Required | EXP-0066 O26 Crossing coverage | Accepted |

## Measurements

The current runtime `crossing_single_user_fire_binds_executed_detector_latch_control` fixture executes one funded User Rising Crossing against generation-current membership. The complete worker invocation consumes `14,876,034,000 / 386,687`, emits exactly one ObservationCrossing occurrence, leaves exactly one pending signal and owns one ready ticket. This meter total includes worker base, probes, traversal and generated branch units exactly as admitted by `service_crossing_transitions`; it does not include subsequent scheduler Control or Transfer effect.

The current runtime `crossing_capacity_policy_is_bound_to_measured_minimum_progress_and_explicit_horizons` regression passes on generated EXP-0095 Weight. Component-wise admission permits four Crossing candidates per block under the configured worker limit, preserving explicit 9,000 User and 10,000 total members per feed and corresponding 2,250/2,500-block uncontended horizons. This is capacity-envelope evidence, not executed owner frequency.

EXP-0074 independently proves W6 Actor 27's stale generation does not execute, generation 2 materializes exactly once and its Transfer services exactly once under exact Wasm. The isolated funded path records detector/latch Control `14,876,034,000 / 386,687`, then next-block prepass plus one User service Control `12,954,769,748 / 141,918` and separate Transfer effect `2,293,433,000 / 29,222`. The recipient gains exactly one unit, the latch and ticket clear, and no broad-fanout owner enters the equation.

Detector/latch Control has an independent source-bound native ledger. The refreshed canonical W6 route asserts the generation-2 occurrence in block 5 and its sole Transfer service in block 6, preserving the `N -> N+1` causal floor. Both the native full-Executive fixture and exact EXP-0095 Wasm replay pass through the same assertions. The native ledger and exact-Wasm chronology remain separate evidence classes: the ledger decides resource ownership while W6 decides exact production-state chronology.

## Derived Metrics

None.

## Statistical / Variance Notes

Deterministic capacity regression plus bounded exact-Wasm chronology input; no timing distribution or resource equation inferred.

## Result

The selected Crossing detector/latch/service equation is complete without broad-fanout borrowing, and W6 binds the same generation-safe causal sequence to exact production Wasm.

## Interpretation

Capacity and chronology cannot substitute for detector/latch resource ownership. Broad fanout remains excluded.

## Decision

Accepted for the selected funded User Crossing-to-Transfer path. No broader reactive population, Task or fairness acceptance follows. No C1 reopening.

## Rejected Alternatives and Tradeoffs

- `Broad-fanout substitution`: Rejected because ObservationCrossing uses independent worker ownership.

## Validity

- `Establishes`: Independent ownership of the Crossing accounting question.
- `Does not establish`: Resource closure.
- `Invalidation triggers`: Binding, generation, worker or Task changes.

## Follow-Up / Next Gradient

Proceed to EXP-0090 heavy economic Task and EXP-0093 retry-placement resource obligations.

## Architecture Impact

None.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0066](./EXP-0066.md) O26.
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0074](./EXP-0074.md), [EXP-0095](./EXP-0095.md).
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: [EXP-0074](./EXP-0074.md) chronology only.
- `Transfers question to`: None.
- `Produces input for`: [EXP-0066](./EXP-0066.md) reactive coverage.
- `Reopen trigger`: Changed Crossing worker, generation semantics, binding or reproducible accounting mismatch; cost alone does not reopen C1.
