# EXP-0044 — User State-Hold Reconciliation Bound

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0028](./EXP-0028.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | User state-hold reconciliation is included exactly once and bounded over admitted live geometry and the host hold ledger. |
| Physical mechanism | User State-Hold Reconciliation Bound |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0028](./EXP-0028.md) |


## Decision Question

User state-hold reconciliation is included exactly once and bounded over admitted live geometry and the host hold ledger.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns user state-hold reconciliation bound. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0028 / “Remaining User Opening Owner”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. The deciding candidate reuses the attained complete `scheduler_paged_execute_opening_max` User profile: bind hold work only through the inclusive post-publication Opening owner rather than fabricating a disjoint coefficient from aggregate subtraction.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Pre-fission baseline `a60de7748132211d9e58cecd5be82b41f76ce230`. Each extracted block retains its own exact source/Wasm/output identity. Shared pinned hardware/toolchain/cache controls follow [EXP-0030](./EXP-0030.md#environment-and-artifact-identity) only where the original comparison explicitly reuses them. A shared environment never substitutes for the block’s own source or Wasm identity.

The deciding evidence reuses [EXP-0031](./EXP-0031.md)'s exact current reference-Wasm profile at code baseline `e92ab80` plus benchmark-only postcondition strengthening: `benchmarking.rs` SHA-256 `56178d1e89c600bed20dc737e9fe96376929c9d41c00cd0cd9586dea66d8bdba`, unchanged scheduler SHA-256 `bdedd82571a229f3609194547c6256d8ca454055640e407c2c9af03d2245fbd1`, benchmark script SHA-256 `303d94b87dd04514118764b96c8c064da758337cc044cd57ba59098cdf292632` and benchmark Wasm SHA-256 `cb901d049e328d2d1074fabf3f3583e6800526e7ba9b4188f8a258ee04e0a1a6`.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: EXP-0068 reuses the scoped hold-boundary evidence alongside EXP-0028; no new measurement is added here.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node. EXP-0068 owns completion-inner measurement separately from this attained full User progress owner.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | User state-hold reconciliation is included exactly once and bounded over admitted live geometry and the host hold ledger. | One admitted User Run transition changing live geometry and requiring host hold reconciliation after publication | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Satisfied for the admitted reference Opening domain by the attained maximum-Step User Run publication and funding-hold decrease |

## Measurements

<!-- extracted: EXP-0028:703-708:44 -->
### Remaining User Opening Owner

The large-head comparison uses an ordinary admitted one-Step User Contract with full AddressEvent source/asset whitelists, SignedAllowlist funding, optional window/terminal nonce, full SplitTransfer legs and the legal four-predicate DNF spread across four clauses. Reference observation identifiers use the widest encoded variant; Current predicates need no published observation for admission or this collection-only operation. Readiness comes from certified signed ingress, with no Contract/head injection. Assertions qualify Trigger, funding, feed, Predicate and Task widths against host type maxima. This is a large-head diagnostic, not an accepted universal maximum: structural DNF capacity is sixteen but admission permits four, and compact resource Weight fields retain admission-derived widths. A byte-size witness alone cannot prove decode-branch CPU dominance.

Constructive native qualification passes `ci-local.sh --only tests --package pallet-deos-actors --all-features --test-filter pipeline_large_head_opening_collection`. Admission requires distinct clauses, SCALE-byte ordering of Trigger whitelists, at least two RetryLater attempts and an auto-close nonce within the host horizon. The retained fixture uses four distinct scalar thresholds, canonical filters and the host horizon without changing field widths. It verifies stored predicate count, Task width, certified readiness, TryRuntime consistency, exact collection/receipt and unchanged cycle nonce. Benchmark preflight compiles but still rejects pre-existing stale generated storage names; this remains the G5 gate, not a new profile failure.
<!-- extracted:end -->

### Deciding Current Reference-Wasm Witness

The attained `scheduler_paged_execute_opening_max` profile is an ordinarily admitted 12-Step User Opening. It consumes the real Ready source, collects the Pipeline fee, publishes a Running successor and then executes `reconcile_actor_state_hold_with_authority` in the same complete owner. Exact postconditions preserve the hold owner plus identity, Contract-head, Contract-body, detector and active-lifetime maximum Run components, while requiring the funding component to decrease after accumulated ingress moves into the Run snapshot. The profile therefore exercises a real variable live-geometry delta and the owner-funded host hold ledger after publication rather than merely constructing a large encoded head.

| Profile | RefTime before DB additions / minimum (ps) | Estimated / recorded proof (bytes) | Reads / writes | Generated fragment SHA-256 | Raw JSON SHA-256 |
| --- | ---: | ---: | ---: | --- | --- |
| `scheduler_paged_execute_opening_max` | 536,459,000 / 479,678,000 | 14,158 / 5,743 | 24 / 13 | `2d824d36d9f1e9821c71a0df88cd02a3e868ea491a413bc0b689cbec022c9f12` | `21f268723a55ca53211ed0a2987b28609b91c14831595b6b0ebda87e23a98ad4` |

The generated storage trace includes one read/write each for `ActorStateHolds` and the host `Balances::Holds` ledger. It also includes the three maximum reference Contract-tail reads and every optional detector-key probe performed by generic reconciliation. Under the pinned RocksDB charge of 25,000,000 RefTime/read and 100,000,000 RefTime/write, the complete inclusive owner totals `2,436,459,000` RefTime. Those aggregate terms cannot be subtracted to invent a standalone hold coefficient: source consumption, Pipeline collection, first-Step evaluation, Running publication and hold reconciliation overlap in one attained atom.

Reproduce with `scripts/benchmarks.sh --extrinsic scheduler_paged_execute_opening_max --output /tmp/deos-exp31-user-paged-opening-complete.rs --json-file /tmp/deos-exp31-user-paged-opening-complete.json pallet_deos_actors`. The run used compiled Wasm, 4,096 heap pages, 1,024 MiB DB cache, map bound 1,000,000 and 50 steps × 20 repeats on the pinned AMD Ryzen 7 4800H reference host, with no concurrent heavy job. The artifact and raw JSON remain verified at the hashes above. Native benchmark construction, fresh reference-Wasm execution, Actors/runtime Clippy, workspace checks, canonical formatting, graph normalization and changed-scope completion passed in the source cohort recorded by EXP-0031.

The reference binding therefore selects exactly one post-publication hold reconciliation inside each applicable complete User transition owner. For this maximum-Step Opening branch, the attained inclusive value above is the bound; no additional hold allowance is added. Zero-Step, temporal, Crossing and terminal complete owners retain their separately accepted inclusive hold behavior. Arbitrary host implementations, populated outer queue/heap composition and exact generated production identity remain outside this decision and belong to EXP-0063 through EXP-0065.

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

The current reference Wasm attains an admitted maximum-Step User Opening whose real Running publication changes funding geometry and reconciles the owner-funded hold afterward. The complete owner is `536,459,000 / 14,158` before database additions with 24 reads and 13 writes; it contains the hold record and host ledger read/write exactly once.

## Interpretation

State-hold work is a post-publication part of the complete User transition, not an additive component inferred from a historical aggregate. The active-lifetime Run reservation remains constant by accepted EXP-0024 policy while the exact funding component decreases; this is still a real live-geometry reconciliation and ledger release. Other complete branches own their applicable reconciliation or terminal release once.

## Decision

Accepted for the admitted reference domain. Select one `reconcile_actor_state_hold_with_authority` operation after applicable User publication and include it only in the matching complete transition owner. For maximum-Step User Opening, use the attained inclusive `536,459,000` RefTime / `14,158` ProofSize plus 24 reads and 13 writes; do not add or subtract a standalone hold coefficient. EXP-0065 owns complete regeneration and exact final binding.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. Do not infer a new selection from record relocation or from aggregate coefficient subtraction.

## Validity

- `Establishes`: Exactly one post-publication state-hold reconciliation inside the attained maximum-Step User Opening owner, including one hold-record and one host-ledger read/write and an exact funding-component decrease.
- `Does not establish`: Arbitrary host implementations, complete production Weight, exact release artifact closure or integrated throughput.
- `Known limitations`: The accepted value is an inclusive complete-Opening owner, not an isolated hold coefficient; outer queue/heap composition and exact production binding remain open.
- `Invalidation triggers`: A changed measured body, host domain, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Follow the owning physical boundary, not a benchmark name.

## Follow-Up / Next Gradient

Stop measuring this satisfied owner. EXP-0058/EXP-0059 own non-hold source and Run composition, EXP-0063 owns outer Waiting/heap alternatives, and EXP-0065 owns complete generated-file regeneration. Reopen only for a legal admitted User geometry outside the declared inclusive owners, a changed hold algorithm/host ledger, or omitted/duplicate reconciliation.

## Architecture Impact

Benchmark-only postconditions expose the exact hold-component transition. Runtime semantics, production storage and bindings are unchanged; C1 and A0 remain frozen. The accepted inclusive owner is an input to later composition and regeneration.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0028](./EXP-0028.md) O15
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: [EXP-0031](./EXP-0031.md) exact complete User Opening profile.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0028](./EXP-0028.md), [EXP-0068](./EXP-0068.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
