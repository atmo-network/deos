# EXP-0034 — Opening Amount Capture Bound

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0028](./EXP-0028.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | The nonzero Opening amount capture bound dominates the declared typed amount allocation and host-ledger domain. |
| Physical mechanism | Opening Amount Capture Bound |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0028](./EXP-0028.md) |


## Decision Question

The nonzero Opening amount capture bound dominates the declared typed amount allocation and host-ledger domain.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns opening amount capture bound. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0028 / “Full-Contract Amount Capture Range”, “Native Amount Capture Reservation Qualification”, “Unfunded and Missing-Receipt Amount Capture Qualification”, “Mixed Amount Capture Allocation Qualification”, “Share-Mixed Count Model”, “Opening Capture Component Ranges”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. The deciding candidate is the staged component-wise maximum of the generated full-Contract PreservableAsset, TargetAsset and one-share/remaining-PreservableAsset inputs. `RuntimeStepControlWeight::opening_snapshot_weight` caps each input at its measured structural domain, selects their component-wise maximum for every nonzero result count, and selects only the independent full-Contract traversal at zero.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Pre-fission baseline `a60de7748132211d9e58cecd5be82b41f76ce230`. Each extracted block retains its own exact source/Wasm/output identity. Shared pinned hardware/toolchain/cache controls follow [EXP-0030](./EXP-0030.md#environment-and-artifact-identity) only where the original comparison explicitly reuses them. A shared environment never substitutes for the block’s own source or Wasm identity. No runtime artifact was rebuilt during migration.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: More than six proof subsections; multiple consumers reviewed.
- `Decomposition review`: All retained amount profiles challenge the same typed nonzero Opening amount capture bound: native/non-native preservable amounts, target assets, staking shares, funded/unfunded states and fixed-total mixtures. The zero-result traversal owner is EXP-0036; cardinality algebra is EXP-0045; final complete identity is EXP-0065. These bounded witnesses cannot justify adding a new charged operation or a new independent acceptance domain.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | The nonzero Opening amount capture bound dominates the declared typed amount allocation and host-ledger domain. | One admitted typed amount surface or fixed-total mixture whose host-ledger capture exceeds the selected amount bound | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Satisfied for the reference binding: generated component owners cover every structurally admitted typed allocation and qualified host-ledger state; maximum/actual share the selector without adding zero traversal or direct inclusive capture |

## Measurements

<!-- extracted: EXP-0028:1166-1378:34 -->
### Full-Contract Amount Capture Range

The remaining nonzero amount fixture on `2761bf1` builds only `ceil(e/2)` Steps and begins with Native; its installed historical model covers only `e = 1..16`, while the reference bound is 24. This cannot certify full-Contract traversal or maximum non-native account encoding. Strengthen the existing `opening_snapshot_capture(e)` to retain every configured Step, exactly `e` distinct PreservableAsset surfaces, and maximum encoded non-native accounts. Once all distinct surfaces are introduced, remaining AddLiquidity Steps repeat the final surfaces, retaining duplicate-detection work without adding snapshot entries. At one entry, a distinct second asset has a Fixed amount. Verify every captured value against its exact balance minus minimum, not only a nonzero count.

The revised fixture is a capture component, not an admitted full Actor or an executed liquidity plan. It preserves the independent zero-entry owner. Reference staking inspection disproves an assumed extra Pool read: `RuntimeStakedAssetIdResolver` computes the receipt ID, then `effective_share_balance` reads receipt Asset existence and Account balance. This identifies two store families but does not certify StakingShares timing/encoding or generic-host equivalence; TargetAsset, Native/reservation and staking coverage remain separate qualifications.

Prepared source content/modes `437679263d11b4525f821a27c887b51c306aec97d36a5aff4f0ca4ccfd5e1f03`; benchmark source SHA-256 `6feebf03f0753d00255cf020049ffa4e2280e3fc68434a1976b2891ee37e032c`. The native all-feature benchmark passes. Measure `scripts/benchmarks.sh --extrinsic opening_snapshot_capture --output /tmp/deos-exp28-opening-amount-full.rs --json-file /tmp/deos-exp28-opening-amount-full.json pallet_deos_actors` on fresh Wasm with ordinary 50 × 20/map/cache/heap controls and no competing build or measurement. Require all reference counts 1..24, exact distinct surfaces and tracked owners. Compare the generated count model with raw DB/proof and the old model only within its stated domain; a read mismatch or larger component rejects old complete-coverage claims. Do not infer generic-host bounds, whole-Actor admissibility or an isolated speed comparison across binaries.

The range passes on Wasm `d1771f189d9afa8136bc9c03c85709f34b39d5b62435013afb5d0417ed0cf541` (100-second build, 15-second run). All counts 1..24 are present among 9,000 timing/450 DB samples. Each count has exactly `2e` unique reads, `e` repeated reads and zero writes. The generated model is `8,919,121 + 9,720,116e ps` before DB, estimated proof `1,366 + 2,836e bytes`, and `2e` database reads. The slope error is `3,903 ps`; recorded proof is 785 bytes at one and 9,051 at 24. At 24, timing minimum/upper-median/p95/maximum are `235,579 / 242,004 / 260,932 / 312,475 ns`. Generated/raw SHA-256: `de7d44f5349c0ed7510a4efed4f01bd6b770cf65d5fb9a3a48abb4a1531a975a` / `1a703b8049e4010bc28a01e2a8600c2fe193795b87855dfe19d4240a31812ba1`.

Within the historical 1..16 domain, new estimated proof exceeds the old `4,373 + 2,260e` model from six entries; at 16 it is 46,742 versus 40,533 bytes. Recorded proof remains a distinct metric and does not by itself prove actual underweight. The changed state/encoding/traversal and larger estimated component reject retaining the old profile as complete maximum-state evidence. The new fragment remains uninstalled pending typed-surface qualification; its lower DB-adjusted time is not an isolated optimization result.

The next bounded falsifier compares full-Contract `opening_target_snapshot_capture(e)` and `opening_staking_snapshot_capture(e)`, each over `1..min(MaxOpeningSnapshotEntries, MaxContractSteps)` because each such Task contributes one surface. Remaining Steps repeat the last surface. Target uses the same maximum account preparation; staking uses ordinary registration and stake into distinct local positions, with receipt Asset/Account encoding checked before and after staking. The benchmark-only host helper defaults to explicit unsupported failure. Existing maximum account preparation is shared without changing the prior pure fixture's input values or measured capture body. Compare typed host models and tracked reads against the PreservableAsset candidate; do not treat a type ordinal as a count parameter, extrapolate staking count past the Step bound, or infer mixed/Native/reservation coverage from pure families.

Typed-profile preparation binds source content/modes `7be1c99a132f38f94f0854bf4949a98b1f5588a157003b2fd7a2f5bb7db1472d`, benchmark source `a4ef71d93807f2d445527737fde3fb45a6a90d2c041988e307037bd77d01953b`, and runtime helper `72d385a2f0bbfac7083e4130730563d28ba82bc74a6b686b1d9f0382c9440b3b`. Three native capture benchmark tests, workspace/all-target/all-feature Clippy and formatting pass. Run `scripts/benchmarks.sh --extra --extrinsic opening_target_snapshot_capture --output /tmp/deos-exp28-opening-target-full.rs --json-file /tmp/deos-exp28-opening-target-full.json pallet_deos_actors`, then reuse that Wasm with `--skip-build --extra --extrinsic opening_staking_snapshot_capture` and `/tmp/deos-exp28-opening-staking-full.{rs,json}`. Retain the preceding 50 × 20/map/cache/heap controls and raw per-count samples; no competing build or benchmark runs during measurement.

The two typed ranges pass on the same Wasm `a887a69199df13c3802b2a85ad782a0c74e51915b7dc27db5514c14166f78a9d`: Target takes 18 seconds after a 100-second build; staking takes 15 seconds without rebuilding. Every count 1..12 has exactly `2e` unique reads, no repeated reads and no writes. Target retains 15,000 timing/750 DB samples; staking retains 7,000/350. Ordinary staking setup and both maximum-encoding assertions execute successfully in Wasm, qualifying the reference helper beyond native compilation.

| Typed surface | Generated RefTime before DB (ps) | Estimated proof (bytes) | Slope error (ps) | Recorded proof at 1 / 12 | Timing at 12, min / upper-median / p95 / max (ns) |
| --- | --- | --- | --- | --- | --- |
| TargetAsset | `10,563,163 + 7,873,507e` | `1,376 + 2,834e` | 3,548 | 785 / 4,695 | 103,577 / 106,510 / 119,640 / 244,868 |
| StakingShares | `8,318,032 + 7,492,937e` | `1,361 + 2,716e` | 4,999 | 624 / 3,236 | 95,824 / 99,246 / 122,154 / 294,315 |

Both generated models add `2e` database reads. Target generated/raw SHA-256: `489f641aa1ad41fe86ccbee026a8b2ed0ce4a088477941137d6fd1c9fd1c89d4` / `e7dbc534c50158ee1d6905482f7f77f7e83f1d9d7ac01d1c65c49f8a4d3ddee6`; staking: `cedc0f4fba9fa8114cad6b19fca9224eb152d7e0b4c2c995200eacfc80693f17` / `c7d8dc8dea20477bb9c930d868f84d93b7e832ecb401759eb73b919a9a31a9db`. No samples or outliers are deleted. The pure PreservableAsset measurement used the preceding binary; its unchanged capture body and equivalent inputs support coverage comparison, not an isolated cross-binary speed claim.

Within 1..12, PreservableAsset's fitted RefTime exceeds both typed models and its estimated proof exceeds staking. Target estimated proof is larger by `10 - 2e` bytes at counts 1..4, equal at five, then smaller. This rejects selecting the PreservableAsset regression alone as a component-wise envelope even though the difference is small; it is not proof of actual undercharging. The typed profiles qualify present, funded, maximum-encoded non-native records only. Keep all three fragments uninstalled until mixed-type traversal, Native/reserved funding and unavailable host states are qualified. Native three-profile tests, all-feature workspace Clippy and these real-host Wasm executions validate the retained benchmark-only changes; no production algorithm, provider or Weight changes in this slice.

### Native Amount Capture Reservation Qualification

The next scoped falsifier tests whether Native amount capture adds a storage owner or exceeds the typed non-native candidates at the same captured count. `spendable_balance` reads the host balance once and subtracts Actors-local reservation only for FeeNative; `protected_minimum` selects the User floor or asset minimum without reading Actor state. Measure full-Contract Native PreservableAsset, TargetAsset and their same-asset typed combination for both classes and zero/half/full visible-balance reservation. Use ordinary native funding, a distinct fixed auxiliary asset, AddLiquidity and SwapOut shapes, and exact snapshot postconditions. This is a capture-component reservation boundary, not proof that every supplied reservation is admitted as a complete Pipeline.

Encode the 18 finite cases categorically, retaining each case's raw time/reads/proof rather than using an ordinal regression as a production count Weight. Existing pure non-native models remain the comparison inputs; no runtime algorithm or fee policy change is proposed. Missing-account/receipt and mixed three-family counts remain independent of this Native qualification.

Prepared source content/modes `9f48f9a5170fb9759c5d61f46c1f8b3418dcfe9bbc4d894adcf194fc67a2332c`, benchmark source SHA-256 `1e0179a675412d4926c92eac490b2c2be1ec2664b7f893dcf9d72cc3f26567d0`. Native benchmark test and canonical formatting pass. Measure `scripts/benchmarks.sh --extra --extrinsic opening_native_snapshot_capture --output /tmp/deos-exp28-opening-native.rs --json-file /tmp/deos-exp28-opening-native.json pallet_deos_actors`, retaining 50 × 20/map/cache/heap controls, checking that every ordinal 0..17 occurs, and excluding competing builds or measurement. Case `c` uses class `c / 9` (System/User), family `(c % 9) / 3` (Preservable/Target/both), and reservation `c % 3` (zero/half/full).

All 18 cases pass on Wasm `10a407d118a1ad2e470decbb589a91f586b59ec60572bda05b1cd7dceb1709e6` (100-second build, 52-second run), retaining 41,000 timing/2,050 DB samples. Each has one unique `System::Account` read, zero writes and 103 recorded proof bytes; the generated estimated proof is 3,568 bytes. Single surfaces have one repeated read and the pair has three: Balances' expendable reducible-balance path reads Account and calls `System::can_dec_provider`, which reads the same Account. Actors-local reservation and minimum selection add no storage owner. This matches the pinned host source, rather than treating the adapter invocation as exactly one physical read.

| c | Class | Family | Reserve | Min / upper-median / p95 / max (ns) |
| --- | --- | --- | --- | --- |
| 0 | System | Preservable | Zero | 6,286 / 7,683 / 9,079 / 24,724 |
| 1 | System | Preservable | Half | 6,216 / 7,752 / 9,010 / 29,334 |
| 2 | System | Preservable | Full | 6,216 / 7,752 / 9,080 / 22,978 |
| 3 | System | Target | Zero | 6,146 / 7,683 / 9,149 / 27,658 |
| 4 | System | Target | Half | 6,146 / 7,683 / 9,010 / 26,749 |
| 5 | System | Target | Full | 6,146 / 7,683 / 9,080 / 68,305 |
| 6 | System | Both | Zero | 9,359 / 10,965 / 12,781 / 68,725 |
| 7 | System | Both | Half | 9,150 / 10,896 / 12,502 / 47,423 |
| 8 | System | Both | Full | 9,428 / 10,965 / 12,571 / 27,657 |
| 9 | User | Preservable | Zero | 6,286 / 7,752 / 9,080 / 27,588 |
| 10 | User | Preservable | Half | 6,146 / 7,752 / 9,080 / 26,261 |
| 11 | User | Preservable | Full | 6,077 / 7,683 / 9,079 / 25,771 |
| 12 | User | Target | Zero | 6,565 / 7,683 / 9,079 / 41,905 |
| 13 | User | Target | Half | 6,216 / 7,683 / 9,009 / 30,102 |
| 14 | User | Target | Full | 6,286 / 7,683 / 9,080 / 36,738 |
| 15 | User | Both | Zero | 9,359 / 10,896 / 12,501 / 34,781 |
| 16 | User | Both | Half | 9,498 / 10,896 / 12,362 / 54,268 |
| 17 | User | Both | Full | 9,498 / 10,895 / 12,362 / 25,214 |

Generated/raw SHA-256: `313dd3862437168e5f5a19a0ac6834a8756844c27a571a33dceab7e42a505939` / `a731cafeb1d453b77869fd4ee336927cce48a2642b579574267932db0b3f1f3b`. Cases 8/16 retain 1,640 timing/82 DB samples, case 17 retains 820/41, all others retain 2,460/123. Preserve the reported outliers; no raw maximum is replaced by a fitted expectation. The categorical RefTime regression is not a count Weight and is not installed.

The 3,568-byte estimate is below every preceding non-native typed model at the matching one/two captured entries; Native medians and p95 are also below those models' pre-DB RefTime. This scoped comparison supports retaining the typed non-native envelope candidates, not claiming a deterministic wall-clock maximum or isolating a speed change across binaries. Native reservation arithmetic, User/System minimum selection and same-asset typed deduplication now have explicit reference-Wasm witnesses. Mixed non-native/Native/StakingShares allocations, other host states and whole-Actor reservation reachability remain separate acceptance inputs.

### Unfunded and Missing-Receipt Amount Capture Qualification

The next falsifier isolates zero-valued snapshots with absent host accounts, plus the early return after an absent staking receipt. An absent account does not imply an empty Opening snapshot: the typed key must still be inserted with zero. Use maximum-encoded non-native records owned by another account, leave the capture account unfunded, and retain full configured Contract traversal. Cover PreservableAsset counts through `min(MaxOpeningSnapshotEntries, 2 * MaxContractSteps)` and TargetAsset/StakingShares counts through `min(MaxOpeningSnapshotEntries, MaxContractSteps)`. Fund real reference staking positions on the other owner for present-receipt/absent-account cases. For the single missing-receipt witness, reuse ordinary empty receipt registration and destruction through the existing host helper; no fabricated storage deletion or new helper is needed.

The categorical profile contains all declared counts plus the one missing-receipt case (49 reference cases). Preserve per-case raw measurements and compare storage owners, proof and timing with funded typed profiles; never install its ordinal fit as a count Weight. These component fixtures qualify result state, not whole-Actor admission or a general missing-asset lifecycle. Mixed typed traversal remains the next independent input.

Prepared source content/modes `673bb79a606e21a101f914658ad0bb03983cf0728c50d297dd216e691ea3b2d6`, benchmark source SHA-256 `0c1f7c25e37c3abe9fe018b2c67c0dc71bf676eb420eacb808b0237760d68560`. Native benchmark test and canonical formatting pass. Measure `scripts/benchmarks.sh --extra --extrinsic opening_unfunded_snapshot_capture --output /tmp/deos-exp28-opening-unfunded.rs --json-file /tmp/deos-exp28-opening-unfunded.json pallet_deos_actors`, keeping 50 × 20/map/cache/heap controls and no competing builds/measurements. Reference ordinals 0..23 are Preservable counts 1..24, 24..35 are Target counts 1..12, 36..47 are present-receipt StakingShares counts 1..12, and 48 is the single removed-receipt surface. Require every ordinal in the raw results.

All 49 reference cases pass on Wasm `c606a51fefe3b4601af5a21a7c79fab788a8fd44d5a0d66bac515a4e645e38ce` (100-second build, 17-second run), including ordinary empty-receipt destruction. Retain 11,000 timing/550 DB samples: case zero has 440/22 and every other case has 220/11. Preservable captures have `2e` unique and `e` repeated reads; Target and present-receipt StakingShares have `2e` unique and zero repeated reads. The missing receipt has one Asset read and no Account read. All writes are zero and every expected typed key remains present with value zero, matching specification §5.3.

Upper-median time is in ns; proof is recorded bytes, not estimated ProofSize:

| Count | Preservable median / proof | Target median / proof | StakingShares median / proof |
| --- | --- | --- | --- |
| 1 | 16,274 / 747 | 14,806 / 747 | 14,178 / 586 |
| 2 | 25,981 / 1,003 | 22,908 / 1,003 | 22,280 / 813 |
| 3 | 36,737 / 1,331 | 32,337 / 1,331 | 29,823 / 974 |
| 4 | 46,236 / 1,660 | 40,438 / 1,660 | 37,785 / 1,141 |
| 5 | 54,686 / 1,982 | 46,865 / 1,982 | 45,048 / 1,372 |
| 6 | 62,439 / 2,304 | 53,011 / 2,304 | 51,054 / 1,599 |
| 7 | 71,519 / 2,632 | 60,972 / 2,632 | 58,109 / 1,766 |
| 8 | 79,201 / 2,954 | 67,189 / 2,954 | 65,792 / 2,001 |
| 9 | 87,513 / 3,276 | 73,963 / 3,276 | 73,544 / 2,236 |
| 10 | 95,684 / 3,596 | 80,528 / 3,596 | 79,550 / 2,395 |
| 11 | 103,785 / 3,919 | 86,884 / 3,919 | 85,906 / 2,620 |
| 12 | 112,166 / 4,239 | 93,868 / 4,239 | 96,731 / 2,779 |
| 13 | 125,367 / 4,567 | — | — |
| 14 | 134,586 / 4,895 | — | — |
| 15 | 142,060 / 5,217 | — | — |
| 16 | 151,139 / 5,545 | — | — |
| 17 | 162,662 / 5,867 | — | — |
| 18 | 170,764 / 6,187 | — | — |
| 19 | 179,635 / 6,507 | — | — |
| 20 | 187,178 / 6,829 | — | — |
| 21 | 197,305 / 7,157 | — | — |
| 22 | 205,546 / 7,483 | — | — |
| 23 | 215,184 / 7,811 | — | — |
| 24 | 224,264 / 8,139 | — | — |

At each family's maximum count, minimum/upper-median/p95/maximum time is Preservable `219,305 / 224,264 / 249,547 / 486,522 ns`, Target `91,563 / 93,868 / 111,050 / 144,993 ns`, and shares `92,960 / 96,731 / 114,193 / 127,812 ns`. The missing-receipt case is `6,286 / 7,682 / 9,778 / 19,975 ns` with 301 recorded proof bytes. The global largest time sample is 614,124 ns at 22 Preservable surfaces; it remains in the data.

Generated/raw SHA-256: `ce0adcd57e686aeab2786b35ba762d07bf21149c76dc23c9b4d6cbdc82986e5f` / `7d41a26022ac8cb391f3d64de4ca4636bacb49c9d3ef1995f9ddf2ca86afea3c`. The ordinal regression produces a constant 20-read model despite the 48-read endpoint, further demonstrating why this diagnostic fit is unusable as a count Weight. It remains uninstalled.

Every unfunded case median is below its corresponding funded type's pre-DB model; the smallest margins are 1,342,469 ps at three Preservable surfaces, 1,619,191 ps at four Target surfaces, and 504,780 ps at four share surfaces. Unique-read counts match those funded profiles; recorded proof is lower at every corresponding count. These are source-qualified comparisons across binaries, not isolated speed gains, p95/max containment or a complete estimated-proof envelope. The missing-account and missing-receipt result-state witnesses add no new phase or storage owner. Other host lifecycle states must be reconciled through their actual adapter paths; this run neither fabricates those states nor certifies mixed-type traversal. Native benchmark, all-feature workspace Clippy and real-host Wasm setup/capture pass without changes to production behavior or weights.

### Mixed Amount Capture Allocation Qualification

The deciding question is whether a component-wise maximum of the three funded type models can cover mixed capture at the same total count, without adding duplicate full-Contract traversal charges. For reference counts `(p, t, s)`, structural feasibility requires `p + t + s <= 24` and `ceil(p / 2) + t + s <= 12`; require at least two nonzero families. This yields 770 mixed allocations (440 using all three families). The 48 pure allocations already have range evidence and are excluded from this diagnostic.

Prepare maximum-encoded asset accounts before creating funded staking positions, then choose asset surfaces disjoint from position and receipt identities. Keep all records ordinary host state and assert exact nonzero snapshot values. Build Preservable pair Steps, then Target and share Steps; fill the remaining configured Steps with the final Preservable pair when available, otherwise the last one-surface Step. Compare that order with its complete reversal, preserving allocation, state and values. The 1,540 reference cases test two explicit orders, not every permutation, generic-host admissibility or full-Actor admission. Native/reservation and unavailable states retain their separate witnesses.

The provisional comparison uses the component-wise maximum of the preceding Preservable model at total `e` and Target/share models at `min(e, 12)`; above 12 the latter are only comparison constants, never extrapolated host ranges. Compare every case's median plus actual unique-read DB charge and recorded proof with that candidate, retain outliers and all lower counts, and inspect tracked owners before attributing a failure to timing. A categorical ordinal regression is not a count Weight. Any larger mixed owner or reproducible excess rejects the candidate; this does not reopen C1 or authorize hand-padding generated coefficients.

Prepared source content/modes `96b5b400dd3f6a15c3b909574e74424f384de9a36289d2f674aefac2ad52ca61`, benchmark source SHA-256 `e6a3de161814128f6ed374380a6d473354ae1f8ea8aa25ce89591d81a7dbcb67`. Native benchmark and canonical formatting pass. Measure `scripts/benchmarks.sh --extra --extrinsic opening_mixed_snapshot_capture --steps 1540 --output /tmp/deos-exp28-opening-mixed.rs --json-file /tmp/deos-exp28-opening-mixed.json pallet_deos_actors`, retaining 20 repeats and the preceding map/cache/heap controls with no competing build/measurement. Allocations are ordered by total, Preservable count, then Target count; `c / 2` selects that tuple and `c % 2` selects reversal. Require all ordinals 0..1539 and exact `2e` unique reads, `p` repeated reads and zero writes before interpreting timing. The expanded step count enumerates the reference domain instead of sampling only 50 categorical points.

Pinned host-tool inspection adds a stronger proof check before acceptance. `frame-omni-bencher 0.22.0` uses `frame-benchmarking-cli 58.0.0`; its `process_storage_results` adds map-depth overhead to recorded proof per storage prefix before fitting and selecting component-wise maxima. The backend groups unique keys by their 32-byte prefix, excluding repeated overlay reads. For these two non-native prefixes, each with `e` unique reads, default map size 1,000,000 contributes `5 * 495 = 2,475` bytes per read and two additional trie layers contribute 990 bytes. Thus reconstruct each point's pre-regression estimator as `recorded_proof + 2,475e + 990`, conditional on the expected owner trace. Compare that quantity as well as recorded proof with the candidate; raw proof alone cannot establish containment of the tool's conservative estimate. This is interpretation of the pinned writer, not a new production coefficient or a claim that the estimator equals actual PoV.

The full run passes every setup/value/trace assertion on Wasm `c54190bb45fd148be4662a82981b2024840948ed66a866b97f804cb0b722634e` (100-second build, 89-second run). All 1,540 cases have 20 timing samples and one DB sample: 30,800/1,540 total. Every case has exactly `2e` unique reads, `p` repeated reads and zero writes. Generated/raw SHA-256: `d9b9db44de6674d0d94071ce071bfcb6006c06b23d9067c8d881526733098e08` / `5af60496afeb041d2447994c40428c0c7fac1a078f2d6062c92a77f5c7f51577`.

The provisional candidate is not accepted: 148 medians exceed its RefTime and 22 reconstructed pre-regression proof estimates exceed its ProofSize. Recorded proof alone is below the candidate everywhere, illustrating why that weaker check is insufficient. Worst time is case 815, reverse `(8, 3, 1)`, total 12: minimum/median/p95/maximum `122,085 / 190,181 / 215,184 / 470,529 ns`, excess `64,620,487 ps` after equal DB charges. Forward case 814 has median 121,246 ns. The largest proof excess is six bytes at cases 1538/1539, `(22, 1, 0)`, total 23: raw proof 8,685, reconstructed estimate 66,600 versus candidate 66,594 bytes. Sixteen total-16 cases and four total-22 cases exceed by one byte; neither rounding-sized excess nor timing variation authorizes silently padding coefficients.

Before interpreting the time excess, reuse this exact Wasm for `opening_mixed_snapshot_capture` controls at `--low 814 --high 815 --steps 2` and `--low 1536 --high 1539 --steps 4`, each with 20 repeats and the same heap/map/trie/cache defaults. Run through `bin/frame-omni-bencher v1 benchmark pallet --runtime template/target/benchmarks/release/wbuild/deos-runtime/deos_runtime.compact.compressed.wasm --pallet pallet_deos_actors --extrinsic opening_mixed_snapshot_capture --extra`, retaining `/tmp/deos-exp28-opening-mixed-{control,tail}.{rs,json}`. The first pair tests the largest order/time discrepancy; the second covers both full-tail mixtures and orders. Workspace/all-target/all-feature Clippy has completed successfully before these controls; no competing build or measurement runs.

Per-total full-run comparison (time is ns, proof is bytes). Case IDs retain the exact tuple/order mapping above. The proof column is the reconstructed pre-regression estimator, not actual PoV:

| e | Cases | Time / proof excess cases | Maximum median case / ns | Maximum proof case / estimate | Candidate proof |
| --- | --- | --- | --- | --- | --- |
| 2 | 6 | 0 / 0 | 3 / 26,680 | 4 / 7,019 | 7,044 |
| 3 | 14 | 3 / 0 | 17 / 38,692 | 14 / 9,860 | 9,878 |
| 4 | 24 | 6 / 0 | 42 / 48,750 | 32 / 12,702 | 12,712 |
| 5 | 36 | 6 / 0 | 71 / 58,248 | 60 / 15,537 | 15,546 |
| 6 | 50 | 6 / 0 | 126 / 68,725 | 100 / 18,372 | 18,382 |
| 7 | 66 | 2 / 0 | 192 / 77,665 | 154 / 21,213 | 21,218 |
| 8 | 84 | 2 / 0 | 253 / 103,506 | 224 / 24,048 | 24,054 |
| 9 | 104 | 0 / 0 | 381 / 96,313 | 312 / 26,884 | 26,890 |
| 10 | 126 | 1 / 0 | 393 / 111,189 | 420 / 29,717 | 29,726 |
| 11 | 150 | 1 / 0 | 651 / 116,637 | 550 / 32,552 | 32,562 |
| 12 | 176 | 9 / 0 | 815 / 190,181 | 704 / 35,385 | 35,398 |
| 13 | 154 | 8 / 0 | 987 / 138,427 | 858 / 38,226 | 38,234 |
| 14 | 130 | 13 / 0 | 1081 / 152,117 | 1010 / 41,067 | 41,070 |
| 15 | 108 | 9 / 0 | 1215 / 158,403 | 1138 / 43,902 | 43,906 |
| 16 | 88 | 6 / 16 | 1299 / 168,670 | 1244 / 46,743 | 46,742 |
| 17 | 70 | 21 / 0 | 1365 / 185,920 | 1330 / 49,578 | 49,578 |
| 18 | 54 | 16 / 0 | 1437 / 190,391 | 1398 / 52,411 | 52,414 |
| 19 | 40 | 10 / 0 | 1464 / 203,242 | 1450 / 55,244 | 55,250 |
| 20 | 28 | 9 / 0 | 1493 / 224,194 | 1488 / 58,079 | 58,086 |
| 21 | 18 | 10 / 0 | 1519 / 224,474 | 1514 / 60,920 | 60,922 |
| 22 | 10 | 7 / 4 | 1533 / 230,969 | 1530 / 63,759 | 63,758 |
| 23 | 4 | 3 / 2 | 1536 / 237,534 | 1538 / 66,600 | 66,594 |

Both same-Wasm controls complete. The first has 7,240 timing/362 DB samples (3,620/181 per case); the tail has 4,880/244 (1,220/61 per case). Owner traces and proof exactly match the full run. No sample is discarded:

| Case | Tuple / order | Min / upper-median / p95 / max (ns) | Median minus candidate pre-DB RefTime (ps) |
| --- | --- | --- | --- |
| 814 | `(8,3,1)` / Forward | 117,755 / 121,107 / 134,587 / 284,818 | −4,453,513 |
| 815 | `(8,3,1)` / Reverse | 118,243 / 121,596 / 135,145 / 468,643 | −3,964,513 |
| 1536 | `(22,0,1)` / Forward | 226,150 / 232,784 / 248,010 / 535,970 | +302,211 |
| 1537 | `(22,0,1)` / Reverse | 227,477 / 233,972 / 248,569 / 338,387 | +1,490,211 |
| 1538 | `(22,1,0)` / Forward | 224,543 / 230,480 / 245,147 / 539,742 | −2,001,789 |
| 1539 | `(22,1,0)` / Reverse | 224,962 / 231,528 / 247,591 / 554,270 | −953,789 |

Control generated/raw SHA-256: `0aec8924676d6ceaf7c140d94c8fd69c139208541b0bcd50c291297bd66e2504` / `c6ca7192ca14e5c1e6ae795b297ee731a72e65520e690df7bfbd0a1c9d69b98d`; tail: `bd31e71a7cdba3a736daab3da8d88ccddb8bf813501885f7529762df6a82ab34` / `e384947b4b06a270150910e58cb5563b68ee432ba6dc2c633fe5a4a7518de6fa`. These direct control outputs are retained as generated, without treating their categorical slopes as production methods.

The 68,935-ns forward/reverse median gap at 814/815 contracts to 489 ns in the control: the large excess is not stable, and the long-run samples remain valid variance evidence. The share-containing tail retains a smaller positive median excess in both orders; measure its count-shaped owner before asserting that pure Preservable timing dominates all mixtures. Do not repeat the unchanged 1,540-case topology enumeration to investigate that narrower question.

The small proof residuals are not a new mixed storage owner. Applying the same writer reconstruction to the retained pure data already exposes positive fitted residuals: Preservable at counts 1/16/22/23/24 by 48/1/1/6/11 bytes, Target at 1/9/10/11/12 by 40/2/1/2/1 bytes, and shares at 1/2/6/8/9/11 by 12/36/10/6/38/17 bytes. A fitted FRAME model and every pre-regression observation are distinct objects. Review that approximation under the governing generated-model contract; do not promote an agent-added strict per-sample fit criterion into a new consensus invariant, hide the residuals, or manually pad output. Maximum/actual reservation dominance concerns the runtime's complete generated Weight owners and selectors; physical timing and proof observations remain separately classified evidence.

This slice closes the declared mixed allocation/value/read-topology enumeration for two orders. It does not accept the pure-only timing maximum, certify every permutation or whole-Actor resource admission, or install a generated Weight fragment. The next bounded input is a count-shaped one-share/remaining-Preservable capture profile using this exact host preparation and the measured order, followed by model/selector reconciliation. Frozen C1, the reference bounds and runtime execution stay unchanged.

### Share-Mixed Count Model

The retained tail discrepancy selects one concrete count profile, not another allocation sweep: one funded StakingShares surface plus `e - 1` distinct Preservable surfaces, full configured Contract length, and the reversed Step order. Its range is `1..min(MaxOpeningSnapshotEntries, 2 * MaxContractSteps - 1)` (reference 1..23); one is a pure share boundary and 24 cannot contain a share under the reference Step bound. Extract the existing mixed fixture preparation for reuse without changing its state, values or measured capture body. Review the generated count model against its exact two-prefix reads and the retained mixed/pure observations, keeping fitted residuals distinct from runtime maximum/actual accounting. No physical optimization or new Actor semantics are proposed.

Prepared source content/modes `73123d39a006cc4c9a6da557262a0c3b9f057ecc0e324842e1f4388baeeb3eb2`, benchmark source SHA-256 `0f3ee212078a1609461538cb11b98710a58b195778ae2ac1788619df8918bbd9`. Native tests for both the extracted mixed diagnostic and the new count profile pass; canonical formatting passes. Run `scripts/benchmarks.sh --extra --extrinsic opening_share_mixed_capture --output /tmp/deos-exp28-opening-share-count.rs --json-file /tmp/deos-exp28-opening-share-count.json pallet_deos_actors` with ordinary 50 × 20/map/cache/heap controls and no competing build/measurement. Require every reference count 1..23, `2e` unique reads, `e - 1` repeated reads and no writes. The existing 1,540-case fixture body, input values and capture owner remain equivalent after extraction; do not repeat that enumeration merely because preparation moved into a helper.

The count profile passes on Wasm `29eb45cb47ae722dc8315c578d542095fc93584020b3082c5c317217fa604211` (100-second build, 16-second run). All 23 counts are present among 8,000 timing/400 DB samples. Counts 1/5/9/14/18 have 480/24 samples, count 23 has 160/8, and the others have 320/16. All traces match `2e` unique reads, `e - 1` repeated reads and zero writes. Generated RefTime is `7,075,624 + 9,847,218e ps` plus `2e` database reads; estimated proof is `1,308 + 2,838e bytes`, with slope error `5,020 ps`. Generated/raw SHA-256: `34f950c983a65f988da89d88356f37824341c0fc9e0deeb884562e5b8db6904d` / `bd2e2256dd687c3e23d1a48c8ce3fe0606ff32eab0f6572da85f7762f177d0b3`.

| e | Upper-median time (ns) | Recorded proof (bytes) |
| --- | --- | --- |
| 1 | 15,086 | 690 |
| 2 | 26,680 | 984 |
| 3 | 38,343 | 1,352 |
| 4 | 48,680 | 1,786 |
| 5 | 57,760 | 2,146 |
| 6 | 68,026 | 2,513 |
| 7 | 76,826 | 2,872 |
| 8 | 85,417 | 3,232 |
| 9 | 94,426 | 3,594 |
| 10 | 103,366 | 3,951 |
| 11 | 112,028 | 4,312 |
| 12 | 125,367 | 4,676 |
| 13 | 135,285 | 5,042 |
| 14 | 143,876 | 5,401 |
| 15 | 154,002 | 5,768 |
| 16 | 163,082 | 6,127 |
| 17 | 176,003 | 6,486 |
| 18 | 184,593 | 6,844 |
| 19 | 193,254 | 7,203 |
| 20 | 203,311 | 7,570 |
| 21 | 211,972 | 7,896 |
| 22 | 224,474 | 8,301 |
| 23 | 234,949 | 8,667 |

At one entry, minimum/upper-median/p95/maximum are `13,759 / 15,086 / 18,019 / 31,988 ns`; at 23 they are `228,455 / 234,949 / 257,160 / 263,306 ns`. The global maximum is 507,824 ns at 19 entries. Samples remain unfiltered. The new count model exceeds the Preservable RefTime model at counts 15..23, while Preservable regains the maximum at 24 because the share model is capped at its actual 23-entry domain. Target owns the slightly larger generated proof at 1..4; Preservable owns proof from five onward. The earlier pure share model remains below these generated alternatives over its measured domain.

Stage the three generated methods unchanged: replace the obsolete Native-first `opening_snapshot_capture` model with the maximum-encoded full 1..24 profile, and add `opening_target_snapshot_capture` plus `opening_share_mixed_capture` to the required Weight interface and regular generation. The runtime's shared `opening_snapshot_models` defines the three inputs, capped at each model's domain; `opening_snapshot_weight` takes their component-wise maximum for nonzero capture and retains the independent traversal at zero. Composed maximum and actual consume this same owner; direct inclusive paths receive no additional charge. V9 binds all three raw model inputs, the selected result and zero alternatives. Generic fallback implementations remain explicit unmeasured estimates.

This is a scoped generated-model binding, not a claim of literal per-observation timing containment. Nine new profile medians exceed the combined fitted RefTime, with largest residual 1,524,670 ps at 17 entries; none of this profile's reconstructed pre-regression proof estimates exceed the combined model. Earlier residuals remain recorded and no coefficient is padded or comment repaired. The governing runtime invariant is component-wise maximum/valid-actual ownership and settlement; full release capacity and observed proof remain subject to exact production/runtime evidence. Nonzero amount capture now has the declared model/selector inputs, while the complete phase matrix, reference-admitted domain and G5 artifacts remain open.

Exact comparison verifies every staged method and annotation against its generated artifact. The expanded existing selector regression checks all 24 amount counts, model-domain caps, maximum containment and replacement of the zero traversal without addition. The pinned default runtime suite passes 493 tests with 22 intentionally ignored; workspace/all-target/all-feature Clippy passes, as do the updated generic Weight-interface parity test and 27 independent embedding tests with DEX and TryRuntime. Retained source content/modes `098d8d77c8242eb29034e20caa2de6aafb3f2c6a4e45fa2904a9c855887cf366`, benchmark source `01fedae0d66a422390a6b27ddb23547723d84e5872e64c19b71151dae6cd98b8`, provider `78c8ed7297dbd661ece19bed0783cce46f5eff98f7f727e5f223a4e407958144`, staged Weight file `e3debef345458fa392e906af0596eb45173ca329c16268db8d13cf007fba0db8`. Promotion and interface/binding edits preserve the measured capture body and setup values; final production-Wasm and complete file regeneration belong to G5.
<!-- extracted:end -->

<!-- partitioned: EXP-0028:755-772:34 -->
### Opening Capture Component Ranges

EXP-0029's independent close boundaries motivate qualifying the existing capture components over the current reference cardinalities. The bound file still models `opening_snapshot_capture` over `1..16` and `opening_predicate_capture` over `1..32`, while the current benchmark constructors expose `1..24` and `1..48`. Measure both existing profiles at 50 steps × 20 repeats on the unchanged independent-partition benchmark Wasm. The decision is whether these component inputs and storage models cover those full current ranges; no partial fragment will be installed as a complete production envelope.

These are isolated function measurements: funded distinct assets, authored two-leg/one-leg Opening amount positions or packed Opening BalanceAbove occurrences, setup outside the measured block, and exact nonzero snapshot/true-result postconditions. They do not create Actors or certify full lifecycle admission, User fees, dependency-fragment loading or complete Opening. The amount profile exercises System `PreservableAsset`; `TargetAsset`, `StakingShares` and other Predicate/Observation adapter branches require their own coverage or dominance proof before a generic maximum is accepted.

Both initial profiles pass on source content/modes `1f53c465c7db0844e73f2905b607b0287859cc6c2ea692c671be078f1fd4cf15`, benchmark source `171e68cf5e186b51d79cbedf709f937d2100181c3757163d41b331d6fa93b0e2`, and benchmark Wasm `068c40b5263e66ed0669fe12fee2569d57f549d200064fbf69fc20be229f50f3`. The prior successful compilation and known stale-storage preflight apply unchanged. Commands use `scripts/benchmarks.sh --skip-build --extrinsic PROFILE --output /tmp/deos-exp28-opening-KIND-current.rs pallet_deos_actors`, with PROFILE=`opening_snapshot_capture`/`opening_predicate_capture`, KIND=`snapshot`/`predicate`. Both use 50 × 20, compiled Wasm, 4096 heap pages, one-million worst-case map size and 1024 cache; no competing measurement/build process.

| Component / full sampled range | RefTime before DB, ps | Proof bytes | Database model |
| --- | --- | --- | --- |
| Preservable amount capture, `e = 1..24` | `3,064,882 + 10,042,874e`; slope error `5,485`; minimum `6,984,000` | Estimated `4,373 + 2,437e`; recorded `288 + 310e` | `1 + 2e` reads, zero writes |

Snapshot fragment SHA-256 `061c4a1c51400f4ec153dcabcaad70387143eccce45c57ee4c99ef5feb538e14`; predicate fragment `fa5b4638d560c1e0ee9cdd9d831ec3b07299da1ccc193cfa3529b247d8868664`. The storage trace contains one native System account and `n−1` Asset/Account pairs at maximum; generated read models retain their conservative intercept. These fragments remain uninstalled. Their larger estimated proof slopes (`2,437` versus `2,260`, `2,613` versus `2,525`) do not support a cheaper maximum admission claim; old ranges/configuration and binary differ, so this is range qualification, not an isolated regression or optimization comparison.

The follow-on predicate comparison uses an extra diagnostic `opening_observation_capture`, sharing the same packed Opening constructor with BalanceAbove, but reading distinct host-published feeds with fresh positive observations. Measure the complete `1..48` range on one rebuilt Wasm alongside the balance profile, retaining 50 × 20 and measured proof. A higher Observation cost in either resource dimension rejects BalanceAbove-only coverage of the generic capture owner; even a lower cost proves only this configured feed shape, not maximum Oracle configuration or unavailable/stale/error branches. Setup and exact `Ok(true)`/count assertions stay outside measured capture.

Both paired profiles pass on source content/modes `6847b15a6225cccc37f6e8b11c19006ce9a73f99f2dceac6efd57e486596e6f5`, benchmark source `e0ecee744fe7e32061e42b09be7d5fcc27f59f9baae50925ce0c4e68a6cafc28`, and benchmark Wasm `eaf6012430160ba32a5cd8156e26de6519ecacefc48917541da07fa0b9542971`. Commands: `scripts/benchmarks.sh --extra --extrinsic opening_observation_capture --output /tmp/deos-exp28-opening-observation.rs pallet_deos_actors`, then `scripts/benchmarks.sh --skip-build --extrinsic opening_predicate_capture --output /tmp/deos-exp28-opening-balance-paired.rs pallet_deos_actors`. Build takes 110 seconds; measurements take 17/16 seconds without competing builds/tests. Same host, 50 × 20, cache, map and heap controls as above.
<!-- extracted:end -->

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

The retained V9 evidence closes the reference Opening amount capture component. Source-derived Task algebra permits exactly three typed surfaces: PreservableAsset contributes at most two distinct entries per Step, while TargetAsset and StakingShares each contribute at most one. The 48 pure-family range points plus all 770 feasible mixed `(preservable, target, shares)` allocations, each measured in forward and reverse order, cover the complete non-native reference count/allocation domain under 12 Steps and 24 entries. Every one of the 1,540 mixed cases has exactly `2e` unique reads, `p` repeated reads and zero writes.

The host-ledger qualifications preserve the same owner partition. The 18 Native class/family/reservation cases use one System account owner and remain below the non-native candidates at the corresponding one/two-entry counts. The 49 unfunded or missing-receipt cases add no owner: non-native surfaces retain their Asset/Account reads, present staking receipts retain their Asset/Account reads, and a missing receipt stops after Asset existence. Maximum-encoded funded Asset and staking records, exact zero/nonzero values and distinct surface identities are asserted in reference Wasm. The source path performs only these typed lookups before inserting into the bounded map; Actors-local reservation and minimum arithmetic add no storage owner.

For each nonzero reference count `e`, the staged selector takes the component-wise maximum of generated Preservable `8,919,121 + 9,720,116e` RefTime / `1,366 + 2,836e` ProofSize, Target `10,563,163 + 7,873,507e` / `1,376 + 2,834e` capped at 12, and share-mixed `7,075,624 + 9,847,218e` / `1,308 + 2,838e` capped at 23, with `2e` reads in each measured model. At 24 it selects `242,201,905` RefTime before database charge, `69,430` ProofSize and 48 reads; under the pinned reference RocksDB charge the complete selected RefTime is `1,442,201,905`. Raw timing and fitted-proof residuals remain classified observations rather than manually padded consensus coefficients.

Decision-time identity checks reproduce staged Weight/provider SHA-256 values `2d9c60ac7764d1e7dc366531dd4a409444daf66cf3e37ed7feb922bb614e8f1a` / `0858133d39eaabb1d2591def2454f1b72d7ee6746b3c1f5ecd720f49a37750f9`. The focused default-runtime `composed_opening_capture_models_replace_traversal_without_double_charge` regression passes on the retained tree.

## Interpretation

The sound owner is a component-wise generated envelope, not a pure-family endpoint, categorical allocation regression, recorded-proof maximum or literal maximum of every timing sample. The complete allocation enumeration certifies reference typed read topology and the source/branch qualifications certify that Native, unavailable and missing-receipt states add no larger owner. This accepts the capture component and selector for the reference binding while leaving whole-transition admission and complete generated-file identity to their own owners.

## Decision

Accepted for the reference binding. For each nonzero captured entry count `e`, select the component-wise maximum of `opening_snapshot_capture(e)`, `opening_target_snapshot_capture(min(e, MaxContractSteps))` and `opening_share_mixed_capture(min(e, 2 * MaxContractSteps - 1))`, with each method additionally bounded by `MaxOpeningSnapshotEntries`. Zero selects only `opening_snapshot_traversal`. Use the same mapping in maximum and actual Opening Control Weight, and do not add it to inclusive direct owners. EXP-0065 owns complete regeneration and exact final artifact identity; no coefficient may be manually combined or repaired.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. In particular, reject pure-family or endpoint dominance, categorical allocation fits as count weights, literal containment of every raw timing/proof observation, and any manual subtraction, padding or coefficient repair.

## Validity

- `Establishes`: A generated component-wise nonzero Opening amount capture envelope over the reference 12-Step/24-entry typed allocation domain, including maximum-encoded non-native records, Native reservation/class variants and qualified funded/unfunded/missing-receipt states, with a shared maximum/actual selector and separate zero traversal.
- `Does not establish`: Whole-transition admission, arbitrary embedding hosts, complete production Weight-file identity, exact release artifact closure or integrated throughput.
- `Known limitations`: The three methods are staged from recorded generated artifacts rather than a complete final-tree regeneration; raw generated-model residuals remain explicit, and EXP-0064/EXP-0065 retain admission and exact-artifact closure.
- `Invalidation triggers`: A changed Task-to-surface mapping, host ledger adapter, configured Step/entry bound, selector, measured body, generated Weight, source/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Component-wise selection preserves independent time/proof owners while Native and unavailable states reduce physical ownership and zero-result traversal remains mutually exclusive.

## Follow-Up / Next Gradient

Stop measuring this satisfied component. EXP-0035 and EXP-0036 own the independent zero-result traversal decisions, EXP-0064 composes admission, and EXP-0065 regenerates exact artifacts. Reopen only for the declared falsifier, a changed reference host/typed amount domain or a changed relevant identity; broader release closure alone is not a reopen trigger.

## Architecture Impact

The existing V9 runtime selector and three staged generated methods are accepted as the scoped Opening amount capture input. No new runtime, semantic or storage change is introduced by this decision; C1 and A0 remain frozen, and complete production regeneration remains G5 work.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0028](./EXP-0028.md) O5
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0028](./EXP-0028.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
