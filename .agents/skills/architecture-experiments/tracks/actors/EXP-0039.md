# EXP-0039 — Crossing Opening Rearm Ownership

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0028](./EXP-0028.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | Crossing Opening rearm has a sound inclusive bound for its declared reachable membership, cursor and availability domain. |
| Physical mechanism | Crossing Opening Rearm Ownership |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0028](./EXP-0028.md) |


## Decision Question

Crossing Opening rearm has a sound inclusive bound for its declared reachable membership, cursor and availability domain.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns crossing opening rearm ownership. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0028 / “Crossing Availability and FIFO Progress Prerequisites”, “Live Crossing Range Cursor Repair”, “Armed Rearm Between Populated Leaves”, “Full Crossing Source and Tail Pages”, “Observation Zero-Step Rearm and Close”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. The deciding reference binding partitions Crossing Opening by current-observation availability and authored terminal disposition. An available observation selects one complete persistent or authored-close owner; a reachable unavailable observation atomically refuses before collection or topology mutation. Post-admission `Uninitialized` is outside the reference host domain.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Pre-fission baseline `a60de7748132211d9e58cecd5be82b41f76ce230`. Each extracted block retains its own exact source/Wasm/output identity. Shared pinned hardware/toolchain/cache controls follow [EXP-0030](./EXP-0030.md#environment-and-artifact-identity) only where the original comparison explicitly reuses them. A shared environment never substitutes for the block’s own source or Wasm identity. No runtime artifact was rebuilt during migration.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: More than six proof subsections; multiple consumers reviewed.
- `Decomposition review`: The declared Crossing Opening rearm owner includes membership removal/reinsertion and necessary live-cursor repair at the same Opening boundary. Armed/WaitingForRearm, full pages and availability profiles qualify that one inclusive operation. Public close has its own independently charged owner in EXP-0042; Observation subscription unlink is EXP-0040; Waiting heap cost is EXP-0063. None may be investigated here as another branch of rearm.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Crossing Opening rearm has a sound inclusive bound for its declared reachable membership, cursor and availability domain. | One admitted Crossing Opening removing a target before a live cursor and rearming into a populated leaf | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Satisfied for the reference Crossing domain: available persistence/close envelopes dominate all attained membership and cursor profiles; reachable unavailability atomically selects the retained refusal owner |

## Measurements

<!-- extracted: EXP-0028:508-529:39 -->
### Crossing Availability and FIFO Progress Prerequisites

Question: qualify unavailable current observations after a paid Crossing latch. Cleanup preflights removal using a Manual target and does not query the current observation; rearm instead requires an authoritative value/revision under specification §4.5. Strict FIFO §8.3 forbids bypass, while §9.2 supplies no observation-loss terminal reason. The former §8.5 prerequisite list omitted the necessary host observation and fee-collection conditions. This is a contract omission derived from those existing normative constraints, not authority to ratify arbitrary behavior or invent a phase, close reason or bypass. Specification §5.2/§8.3/§8.5 now state atomic refusal, head preservation, existing terminal precedence and conditional liveness explicitly.

Native `crossing_rearm_unavailability_preserves_paid_head_until_host_recovers` partitions generic host `Unavailable` and `Uninitialized` after real paid Crossing readiness. Each retains complete Actor state, head, events and payer/sink balances while an eligible System follower receives no cycle. Restoring the same authoritative value/revision permits both zero-Step cycles and exactly one User Pipeline fee. `crossing_rearm_unavailability_does_not_block_pipeline_insolvency_close` separately proves one-unit-short paid readiness closes custody-neutrally without rearm. An initial test expected `starved == false`; `execute_cycle` classifies any refused head with no execution as starved. That expectation was a test defect: sparse starvation does not prove Weight exhaustion.

The reference provider returns retained canonical values for Paused feeds and old observations, independently of age-sensitive `observe`. Crossing admission requires an existing initialized feed; ordinary Oracle lifecycle calls never delete its identity or last value. Consequently post-admission `Uninitialized` is outside this reference domain, while Root deactivation reaches `Unavailable` and has no ordinary resume path. Generic mock recovery is not evidence that a DEOS deactivated feed can recover. EXP-0029 records that qualification.

`scheduler_paged_zero_step_user_crossing_unavailable` constructs a funded paid zero-Step User Crossing and an eligible zero-Step System follower through ordinary admission, then calls the real host Oracle deactivation before measurement. The measured full FIFO pass consumes control but no effect and preserves Actor/queue/holds/membership/events/custody/sink. Outside measurement, authorized Mutable owner close removes the head and the follower completes; no new Immutable close authority is implied. The generic benchmark compares SCALE-encoded event vectors because FRAME's Wasm `EventRecord` lacks native `PartialEq`; the first preflight exposed and rejected the native-only assertion before measurement.

Earlier available-observation and cleanup measurements remain applicable within their declared domains. No production execution, C1 geometry or configured bounds change; this profile is not full Executive or an all-context numeric bound.

| Refused FIFO pass | Generated RefTime / minimum ps | Estimated / recorded proof B | Reads / writes |
| --- | --- | --- | --- |
| `scheduler_paged_zero_step_user_crossing_unavailable` | 191,857,000 / 173,908,000 | 6,186 / 2,721 | 13 / 0 |

Source content/modes `f5a5b7023aeda57de1023e939585fc05d9fb6410fce75c4de10cde70b926aa4c`; benchmark source SHA-256 `70817400fb7c81e8f254992ef47a0c9756f629420269fc4b0c8b20c14c9328e2`; reference helper SHA-256 `3bb54c35ed7db1cdfdf4f36e3f3ec8acb34d70de04db9c975b3d16f1fdef3e25`; benchmark Wasm SHA-256 `6cde806c7f6c81f3b756d2e32fdec265f8f78fcf23ff0c1e37e6356981783cf4`. Same pinned reference toolchain/hardware/cache/heap/map controls as above, 50 steps × 20 repeats, no concurrent agent build/test/benchmark job during measurement. Fresh build took 109 seconds and generation 14 seconds.

Reproduce with `scripts/benchmarks.sh --extrinsic scheduler_paged_zero_step_user_crossing_unavailable --output /tmp/deos-exp28-crossing-unavailable.rs pallet_deos_actors`. Temporary fragment SHA-256 `0bf3095ae741ef17a1288f75c5438fcee47be1e1578e4af0956b4ce5788ecb46`, uninstalled and excluded from default production generation. Measured proof mode covers this exact admitted head/follower geometry; the trace reads `Oracle::Feeds` but no `Oracle::Observations`, matching terminal deactivation refusal. No writes, collection or effect occur. The profile does not isolate the rearm probe from preceding FIFO, fee-liability and source reads; its inclusive figure must not be added to those owners again.

Validation: native `crossing_rearm_unavailability` properties pass after shared follower extraction; the final native `crossing_unavailable` benchmark passes (13 seconds). Fresh reference Wasm verifies the measured profile. Workspace all-features Clippy (27 seconds), canonical formatting, workspace check and changed-scope completion pass. Completion first rejected a 514-character changelog entry; shortening it preserves source-bound evidence and the final gate passes. Runtime/default package evidence is reused because production execution is unchanged and the runtime helper addition is benchmark-only; completion uses `--skip-runtime-tests`. Wiki consolidation and Russian-style guards pass, and independent review approves the one changed EN/RU pair out of 46 page IDs. Record normalization/graph and diff checks pass. Source identity remains equal after context synchronization. Preflight compilation passes (60 seconds), while the known stale generated-storage rejection remains the open G5 gate.
<!-- extracted:end -->

<!-- extracted: EXP-0028:628-688:39 -->
### Live Crossing Range Cursor Repair

Earlier full-page rearm profiles read an absent range cursor, leaving the rewind-write branch unqualified. The new profiles start from ordinary populated Armed-source/destination preparation at value two, publish value one in the next external phase and run the bounded Crossing worker once. It skips the latched target, rearms a nonzero guard prefix and retains a live cursor ahead of the target but no later than the surviving tail. Complete User Opening moves that tail guard into the target slot and rewinds the existing cursor; authored close preserves the repair. No synthetic cursor, manual counter reset, repeated same-block worker pass or injected observation is used.

Both native and reference-Wasm profiles pass. The initial feed has 384 Actors, source page size P=128 and a full destination page. After the worker, source membership is exactly `2P − (candidates − 1)`: one candidate was the disabled target and all remaining candidates moved to their own Armed threshold-two leaf without activation. Two source pages remain. Assertions qualify target position `< cursor ≤ surviving tail`, exact cursor revision/traversal/threshold preservation with only page/offset rewound, moved inverse ownership, guard readiness, User collection and destination allocation or terminal reclamation. A subsequent block's bounded worker pass actually rearms the moved guard from the repaired position with zero faults, activations or closes. That consequence check is outside the measured Opening; native TryRuntime also checks the resumed state.

| Profile suffix after `scheduler_inner_zero_step_user_crossing_` | RefTime before DB additions / minimum (ps) | Estimated / recorded proof (bytes) | Reads / writes | Generated fragment SHA-256 |
| --- | --- | --- | --- | --- |
| `cursor` | 358,641,000 / 335,803,000 | 40,712 / 32,297 | 21 / 16 | `eb79352a4f198fdc44a123063f01cc4edac75569700ccbe0491387eb85371237` |
| `cursor_close` | 463,125,000 / 438,401,000 | 42,457 / 34,042 | 28 / 25 | `750919b8c358ee17f2e57983a3f7e2124c4a13c27cd5cb924eeefca2aa786f6f` |

Source content/modes `00ee34e8309cd0050e2618436fa0276ebc23b1d85f6b0eb116cebe6f9112eabf`; benchmark source SHA-256 `aff0fb70cd8ff7c0bb6a6177d4cc408db2a0224583701b3e29312eb9752993b3`; benchmark Wasm SHA-256 `4ce6e92b4f263b6f6e8b5ffe6dca55cff6d5f745f3fc85c031ea7990ebc124e7`. Same pinned reference hardware/toolchain/cache/heap/map controls as above, 50 steps × 20 repeats, no concurrent agent build/test/benchmark job. Fresh build took 104 seconds and each generation 15 seconds. Reproduce with `scripts/benchmarks.sh --extrinsic scheduler_inner_zero_step_user_crossing_cursor --output /tmp/deos-exp28-zero-step-crossing-cursor.rs pallet_deos_actors`, then the `_cursor_close` selector and `cursor-close` output with `--skip-build` on that unchanged binary. Both fragments remain temporary, uninstalled and excluded from default production generation.

The traces now bind `CrossingRangeCursors` as one read and one write, alongside the complete populated-leaf User atom. Earlier absent-cursor points have one fewer write, but the new worker also changes population/trie context and the binary differs; do not infer an isolated CPU/proof constant by subtracting those aggregates. The repaired cursor preserves the original transition rather than restarting or duplicating observation work. This qualifies the real rewind branch and its next-block consequence, not maximal joint cursor/page/radix/header/Waiting geometry, every detector availability branch or full Executive execution. All collection, hold and cleanup work inside the inclusive atom remains counted once.

Retained-source validation: native `zero_step_user_crossing_cursor` all-features tests pass including next-block resumption (18 seconds), both fresh-Wasm profiles pass, and Actors all-features Clippy passes (22 seconds). Canonical formatting, workspace check, changed-scope completion (14 seconds), record normalization/dependency graph and diff checks pass. Production/runtime/mock behavior is unchanged; prior unaffected behavioral evidence is reused and completion skips runtime tests. Preflight compilation passes (57 seconds), then retains the known stale generated-storage rejection. Source content/modes remain identical after context updates. Complete Weight envelopes and G5 acceptance remain open.

### Armed Rearm Between Populated Leaves

Question: qualify cross-phase zero-Step rearm where both Crossing leaves survive. Specification §4.5 derives phase from current authoritative value, independently of latched history. A target with Rising threshold four/rearm one fires through real publication, remains latched while the current value returns to two, then opens as Armed. Ordinary guards at threshold two populate its old WaitingForRearm leaf; guards at threshold four populate the Armed destination. The source and its tail are full; destination occupancy is page size minus one or page size, separating full-page append from new-page allocation. No actor state, observation, membership or cursor is injected.

The shared observation constructor now accepts the qualified fire threshold and Opening value; its previous wrapper preserves threshold two/value one for all earlier profiles. In the reference EMA, sample 1,000 publishes value nine and seven decreasing publications of sample two reach value two. Publication occurs before the next bounded deferred-worker pass, and the latched User pays no duplicate Trigger fee. Value two need not cross rearm threshold one: Opening installs phase from the current value, intentionally discarding latched history. Benchmark guard creation is shared with the prior same-phase profiles without changing their ordinary creation semantics.

All four native and reference-Wasm profiles pass. With page size 128, their feed contains 383/384 Actors: target plus 255 source guards plus 127/128 destination guards. Complete inner Opening starts after canonical primary consumption. Assertions verify exact User fee collection, receipt and lifecycle, moved source inverse ownership, unchanged guard nonce/readiness, destination append/allocation or its terminal reclamation, and coherent source/tail cardinality. Native TryRuntime checks the complete state before and after execution; existing host System Actors remain intact.

| Profile suffix after `scheduler_inner_zero_step_user_crossing_` | RefTime before DB additions / minimum (ps) | Estimated / recorded proof (bytes) | Reads / writes | Generated fragment SHA-256 |
| --- | --- | --- | --- | --- |
| `armed_pages` | 356,615,000 / 337,269,000 | 40,549 / 32,134 | 21 / 15 | `e4ad71637449848098906977b3c671f6699f6dc8a2f815b5ac8e89e206694a2e` |
| `armed_new_page` | 351,238,000 / 333,079,000 | 40,613 / 32,198 | 21 / 15 | `a45f056d4025f0f75d4f844aa1018027877cb10d2af232c2dba3e0d83d8ffed2` |
| `armed_pages_close` | 465,430,000 / 446,083,000 | 42,294 / 33,879 | 28 / 24 | `f0c4168196bef54eb92e3491ccf5bc6df9ff5ac0976bfb9e9592ee1cad45b00d` |
| `armed_new_page_close` | 452,649,000 / 433,092,000 | 42,358 / 33,943 | 28 / 24 | `909126b2764f4229ed260427cc3e86ef208e219e50f7507c90ed4a0d83bb4e58` |

Source content/modes `8fdbe1dd702f18e0f65d687f8384ae257dc4bdaa676632e4d0b8885fcbccc337`; benchmark source SHA-256 `661b9db3d617cd658486173d7959e21b12df8d7edfabb577b90a3611edd92df7`; benchmark Wasm SHA-256 `1c6f361cad09847b59d0c3d1b0cc323fac2fa5c24c23d30933d6a488ecc5cea8`. Same pinned reference toolchain/hardware/cache/heap/map controls as above, 50 steps × 20 repeats, no concurrent agent build/test/benchmark job. Fresh build took 95 seconds; each generation took 15 seconds. Reproduce in table order with `scripts/benchmarks.sh --extrinsic scheduler_inner_zero_step_user_crossing_SUFFIX --output /tmp/deos-exp28-zero-step-crossing-KIND.rs pallet_deos_actors`, replacing underscores in SUFFIX with hyphens for KIND; only the last three use `--skip-build` on the unchanged fresh binary. Temporary fragments remain uninstalled and excluded from default production generation.

Both persistent traces read/write two leaf states and report three member-page reads/writes. For existing-page append, source, source tail and destination are read and written. For new-page allocation, the full destination is read but not rewritten, while the new page is written; identical counts therefore do not imply identical keys. Terminal work removes the inserted target again inside the same transaction, preserving the destination guards and reclaiming the newly allocated page where applicable. Both complete atoms already include rearm, User collection, hold reconciliation and terminal cleanup; no aggregate subtraction or duplicate addition is justified.

The allocation points have slightly lower fitted CPU but 64 more estimated/recorded proof bytes than their append peers. This is a measured branch tradeoff, not a selected architecture or evidence that one dominates. Both leaves survive, so these profiles omit radix deletion/recreation and live range-cursor repair. They qualify the populated-leaf Armed append/allocation and close geometry, not every combination of source reclamation, threshold-tree context, Oracle availability, maximum header or Waiting/hold state. Earlier single-leaf and same-phase profiles remain complementary source-bound evidence; full production binding and numerical acceptance remain open.

Validation: the complete native `zero_step_user_` all-features benchmark family passes after shared constructor extraction (30 seconds), covering earlier profiles and all four additions. A later assertion-message-only edit preserves that behavioral evidence; fresh Wasm verifies the retained source. Actors all-features Clippy passes (21 seconds), as do canonical formatting, workspace check (10 seconds), record normalization/dependency graph and diff checks. The initial completion pass rejected an overlong changelog entry; after prose shortening the remaining completion audits pass (5 seconds), preserving the measured source. Runtime and unaffected package behavior reuse prior evidence because no production code or host/mock behavior changed. Preflight compilation passes (52 seconds), retaining the known stale generated-storage rejection. Source content/modes remain identical after context synchronization; G5 and complete numerical acceptance stay open.

### Full Crossing Source and Tail Pages

Coverage question: complete User zero-Step rearm removes and reinserts Crossing membership. The earlier single-member profiles exercise radix removal/recreation but cannot prove a full non-tail page rewrite, moved-member inverse update or singleton-tail removal/reallocation. Persistent and nonce-close diagnostics retain the real published WaitingForRearm User setup and ordinarily admit zero-Step System guards on its leaf. The target occupies source page zero/offset zero; the final guard moves from either a singleton or full tail. These endpoints distinguish page deletion/reallocation from maximum two-page decode/rewrite. No membership, cursor, Contract or observation state is injected.

All four native and reference-Wasm profiles pass. The reference and benchmark-enabled mock page size is 128, producing 129 or 256 members on the target leaf. Complete inner Opening starts after canonical primary consumption. Assertions require exact User fee accounting, the final guard moved to page zero/offset zero with unchanged nonce/readiness, a full surviving source page and either a retained/recreated tail containing the target or the precise terminal tail reduction. Native TryRuntime verifies the full state before and after execution; the reference host's existing System Actors remain intact.

| Profile suffix after `scheduler_inner_zero_step_user_crossing_` | RefTime before DB additions / minimum (ps) | Estimated / recorded proof (bytes) | Reads / writes | Generated fragment SHA-256 |
| --- | --- | --- | --- | --- |
| `page` | 302,977,000 / 289,566,000 | 20,044 / 14,104 | 19 / 13 | `8ea289a016c3aaefff4432c1b5c5aab3e140d0db093ec1dc2fa20a9e24ff32d1` |
| `page_close` | 406,972,000 / 391,187,000 | 23,847 / 15,432 | 26 / 22 | `cc9919cc9dc0c15772552b455d0a981c1b2c4ec84d64a9476dec21b8f1211db5` |
| `page_full` | 333,567,000 / 319,529,000 | 28,946 / 23,006 | 19 / 13 | `d2fbb8c29f37b3635b2612a4bad560b479b537f3f2043f9a2fef8acab1b5a50b` |
| `page_full_close` | 448,039,000 / 422,965,000 | 32,869 / 24,454 | 26 / 22 | `5cbfbf24f3be585ca636df7fd01bbd55856c10333cc454d3e10128bd1ca3a58e` |

Source content/modes `63c9966831b5895886f6c6abf2cb9c8a0d621152a0e2cda6c6862004746bf57d`; benchmark source SHA-256 `69b5c73d8711b85b1ad2eb9b6bf9b7c0e6a746ff7fa6a97f6bffed87a00ee606`; benchmark Wasm SHA-256 `4af6d3490e9c4f6b587beb3fcf257482a31711b18a6591cc213355c2e4c9866c`. Same pinned toolchain/hardware/cache/heap/map controls as above, 50 steps × 20 repeats and no concurrent agent build/test/benchmark job during measurement. Fresh build took 95 seconds and generation 13/14/14/14 seconds. Reproduce in table order with `scripts/benchmarks.sh --extrinsic scheduler_inner_zero_step_user_crossing_SUFFIX --output /tmp/deos-exp28-zero-step-crossing-KIND.rs pallet_deos_actors`, mapping underscores in SUFFIX to hyphens in KIND; only the last three use `--skip-build` on that unchanged binary. All fragments remain temporary and uninstalled; exact-selector exclusions prevent accidental production generation.

Both persistent profiles touch two member pages and two membership locators, including the moved guard's inverse locator. The surviving leaf avoids the single-member profiles' radix deletion/reinsertion. Singleton-tail deletion/recreation and full-tail rewrite have identical unique-key read/write counts but distinct serialized proof and CPU cost; those counts do not enumerate repeated logical accesses inside one transaction. Terminal atoms include their own rearm, collection, holds and cleanup. Do not add those owners again or subtract aggregate measurements.

Within this paired binary, full-tail points increase both CPU and proof over the corresponding singleton-tail points. Relative to the earlier single-member measurements, these points have lower CPU/estimated proof but higher recorded proof; that historical comparison spans different fixture binaries and topology, so it cannot isolate a single byte-cost delta or establish a global Pareto winner. Retain both radix and page geometries in the coverage matrix. This qualifies two-page WaitingForRearm remove/reinsert and close, not Armed phase movement into populated leaves, live range-cursor repair, ObservationChange page geometry, Oracle unavailability or complete head/Waiting/hold bounds. C1 and production behavior/Weight remain unchanged.

Validation on the retained source: `scripts/ci-local.sh --only tests --package pallet-deos-actors --all-features --test-filter zero_step_user_crossing_page` passes all four profiles (29 seconds), and Actors all-features Clippy passes (20 seconds). Canonical formatting, workspace check, changed-scope completion, record normalization/dependency graph and diff checks pass. Only benchmark fixture/setup code changed; prior package/runtime behavioral evidence remains applicable and completion skips runtime tests. Preflight compiles the final source (52 seconds) but retains the known stale generated-storage rejection; source content/modes remain identical after context synchronization. These checks do not close G5 or constitute full Executive/production-Wasm acceptance.
<!-- extracted:end -->

<!-- partitioned: EXP-0028:432-457:39 -->
### Observation Zero-Step Rearm and Close

Five admitted User profiles use a real host publication, ordinary Crossing/fanout readiness and the same consumed-primary inner boundary as Manual/Cadenced. Persistent profiles verify latch consumption and retained subscription or exact Crossing phase/revision; terminal profiles require AutoCloseNonceReached and absent identity, head, subscription/membership and state holds. All check one nonzero Pipeline receipt, exact sovereign/Fee Sink deltas and no Run. The native publication adapter tracks revisions only for benchmark publications, preserving the previous default for other mock cases and clearing its state on fixture reset.

The first reference-Wasm probe falsified sample/value equality: the reference factory creates an EMA feed, and publishing sample two after value one produced value zero at revision two under its rounded aggregation. This is a fixture defect, not permission to replace the reference feed or alter Oracle semantics. Failed source `1bdeaa67754d461f4eef864b4f0ce81219c2ca4063c83fea5f1dc846f5d6371e`, benchmark Wasm `18334f869dbb27045e8ade689f9552cc8f89114a5fcd3f169c141ec124ca5aef`, log SHA-256 `4f34931f15b0d8656cf75efcd0b19c1b0c1728c03b9f53231d80523f1829ad54`; the `scheduler_inner_zero_step_user_crossing_armed` generation route failed before measurement. The corrected setup publishes sample 1,000, verifies the actual value crosses two, and reaches Armed through bounded decreasing publications of sample one while latched. Every publication checks the authoritative value/revision relation; threshold two and rearm one remain unchanged.

Armed preparation advances to the next block after the first latch. It publishes decreasing samples during that block's external phase, then services the deferred worker once. The reference EMA half-life 100 maps the initial sample 1,000 to value nine, and eight samples of one reach value one within MaxCrossingTransitionsPerBlock=8. The benchmark checks actual values/revisions and no second Trigger fee while latched; it does not substitute a LastValue feed or inject detector state.

| User zero-Step profile | RefTime before DB additions / minimum (ps) | Estimated / recorded proof (bytes) | Reads / writes | Generated fragment SHA-256 |
| --- | --- | --- | --- | --- |
| `scheduler_inner_zero_step_user_crossing_armed` | 453,486,000 / 432,673,000 | 163,961 / 4,571 | 82 / 77 | `4dbf90b63a3f5d1c09e10565042add43dda4922b26a4de6cbbe80346a9cf5086` |
| `scheduler_inner_zero_step_user_crossing_waiting` | 413,677,000 / 393,912,000 | 84,642 / 4,452 | 48 / 43 | `dd576fc6f2b2b5d178eead188c2944ee095961e84c098c4ebd3255054b098f31` |
| `scheduler_inner_zero_step_user_crossing_close` | 606,092,000 / 583,393,000 | 85,360 / 5,170 | 57 / 53 | `d562caa3a3c79a4bd8b160efe43158a97f8eeb82ffcfe90b9c7a18e530ced18b` |

All five pass reference-Wasm admission, measurement and postconditions at 50 steps × 20 repeats on one fresh binary. Source content/modes `f9190bb04b4c99e4f032b1264fc71657ae361328301ea8e758d598f8d928da4b`; benchmark source SHA-256 `85aea2cd4777265cb5b326679f021d0e1edbceb5c700fdbbb0ca3a2cb382ff42`; reference helper source SHA-256 `ab4c5eb0d496b55c8ea459b48f549fd01a69a80dfd0f24a02f5ae5dd4b28fe5d`; benchmark Wasm SHA-256 `737feb8338b38d05ebae05efda65a66c636cd7f88f19a4b8f28357116f2587e8`. Same pinned toolchain/hardware/cache/heap/map controls as above; no concurrent agent build/test/benchmark job during measurement.

Reproduce with `scripts/benchmarks.sh --extrinsic PROFILE --output /tmp/deos-exp28-zero-step-KIND.rs pallet_deos_actors`. Use the table's profile order and KIND=`crossing-armed`, `crossing-waiting`, `crossing-close`, `observation`, `observation-close`; add `--skip-build` only for the last four on the unchanged fresh binary. The corrected runtime build took 96 seconds and profiles took 13/14/13/13/13 seconds. Exact-selector exclusions keep every new diagnostic out of default production generation. Fragments remain temporary and uninstalled.

Crossing's trace includes Oracle feed/observation reads, leaf/member pages and radix-index work. Armed rearm touches 64 unique CrossingRadixNodes keys, while same-phase WaitingForRearm touches 32; both remove and reinsert membership. Close repeats cleanup inside the same transaction, so unique-key DB counts do not enumerate every logical access. ObservationChange persistence deletes indexed disablement; its close additionally removes subscription topology and lifecycle state. These are complete inclusive atoms: do not add collection, rearm, holds or cleanup again, or select a global bound from the pre-DB CPU coefficient alone. Compare the explicit DB terms and both proof dimensions.

This fresh single-member feed geometry does not establish full member-page/threshold-tree coverage, non-tail removal, concurrent cohort/range-cursor cases, maximum zero-Step head, unavailable observations or every terminal branch. Complete numerical qualification and production binding remain open; C1, monetary rules, Oracle aggregation and production execution are unchanged.

Validation: full Actors all-features tests passed after the shared mock revision change (73 seconds). After fixture-only EMA and phase-order corrections, the focused `zero_step_user_` all-features family passed (20 seconds); unaffected suite evidence is reused because its mock owner is unchanged. Actors/runtime all-features Clippy, canonical formatting, workspace check, record normalization/graph and changed-scope completion pass. Completion uses `--skip-runtime-tests` for the benchmark-only runtime helper diff, reusing unchanged default-runtime behavioral evidence. Preflight compilation passes while the known stale generated-storage audit still rejects G5; the final corrected fixture is separately compiled and verified by native and fresh-Wasm runs. Source content/modes remain identical to the measurement identity after context updates. No full Executive, production-Wasm or metadata acceptance is claimed.
<!-- extracted:end -->

### Deciding Crossing Branch Partition

The retained reference-Wasm profiles attain the declared topology branches through ordinary admission and publication. The single-member profiles exercise full radix removal/recreation; the page profiles exercise full non-tail source and tail rewrites; the Armed profiles exercise populated destination append and new-page allocation; and the cursor profiles directly attain the smallest falsifier by moving a tail guard before a live cursor while rearming into a populated destination leaf. The cursor consequence check proves resumed traversal visits the moved guard rather than skipping it.

For available observations, every retained persistent profile is contained component-wise by `scheduler_inner_zero_step_user_crossing_armed`: `453,486,000 / 163,961` plus 82 reads / 77 writes. Every retained authored-close profile is contained component-wise by `scheduler_inner_zero_step_user_crossing_close`: `606,092,000 / 85,360` plus 57 reads / 53 writes. Values are generated RefTime before database additions / estimated ProofSize. These two attained complete envelopes already include current-observation lookup, membership removal/reinsertion, necessary cursor repair, one Pipeline collection and applicable User hold reconciliation; the close envelope additionally includes authored terminal cleanup. Maximum and actual select by authored terminal disposition after availability succeeds, rather than adding topology fragments.

A reachable reference-host `Unavailable` result selects the separately measured state-preserving full-FIFO refusal owner `191,857,000 / 6,186` plus 13 reads / zero writes. It performs no collection, membership mutation, receipt or follower service and must not be added to either successful Opening owner. Post-admission `Uninitialized` is unreachable because reference admission requires an initialized feed and ordinary lifecycle retains its identity and last value. Generic mock recovery therefore remains a construction control, not a reference-host liveness claim. EXP-0058 must reconcile the refusal profile's outer FIFO/source boundary during complete scheduler composition; EXP-0065 owns complete generated-file regeneration.

## Derived Metrics

The two available envelopes are attained profiles, not aggregate subtraction or cross-artifact fitted deltas. Coordinate-wise containment is used only to select a conservative branch owner over the retained legal witness set; no timing-causality or architecture ranking is inferred.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

The retained available profiles cover singleton radix, full source/tail pages, populated destination append/allocation and live-cursor repair for both persistence and authored close. Their complete resource vectors are contained by the attained persistent and close envelopes respectively. The reference-Wasm unavailable profile covers the only reachable missing-current-value branch and proves exact atomic refusal.

## Interpretation

Availability and authored terminal disposition are the required selector dimensions. Membership page, radix and cursor shapes change physical work but do not require additive owners: each is already inside the matching complete available envelope. Unavailability is a refusal alternative, not a partially completed rearm; it preserves the paid FIFO head and cannot charge Pipeline collection or let a follower bypass it.

## Decision

Accepted for the reference Crossing binding. After an available current observation, select exactly one complete owner: persistence `453,486,000 / 163,961` plus 82 reads / 77 writes, or authored close `606,092,000 / 85,360` plus 57 reads / 53 writes. A reachable unavailable observation selects the state-preserving full-FIFO refusal owner `191,857,000 / 6,186` plus 13 reads / zero writes and no Pipeline collection. Post-admission `Uninitialized` is outside the reference domain. EXP-0058 owns outer source-boundary composition and EXP-0065 owns exact production regeneration.

## Rejected Alternatives and Tradeoffs

Reject adding page, radix, cursor, collection, hold or cleanup fragments to the complete available envelopes; subtracting aggregates to invent disjoint coefficients; treating unavailable refusal as successful Opening; allowing FIFO bypass; claiming generic mock recovery as reference-host recovery; and inflating the reference domain with unreachable post-admission `Uninitialized`.

## Validity

- `Establishes`: The reference available persistence/authored-close Crossing Opening envelopes over singleton radix, full-page, populated-destination and live-cursor geometry, plus the reachable unavailable refusal owner.
- `Does not establish`: Generic-host recovery, an `Uninitialized` reference state, outer source/Waiting/heap composition, public close, final production Weight identity or integrated throughput.
- `Known limitations`: Diagnostic branch fragments remain uninstalled; Root-deactivated reference feeds have no ordinary resume path, and immutable liveness implications remain outside this Weight decision. EXP-0058 and EXP-0065 retain composition and regeneration.
- `Invalidation triggers`: A changed Crossing rearm/removal/insertion/cursor path, host availability lifecycle, branch selector, collection/hold/cleanup path, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Crossing page/radix/cursor shapes are physical alternatives inside one complete Opening owner; unavailable current state refuses before that owner mutates topology.

## Follow-Up / Next Gradient

Stop measuring this satisfied Crossing Opening owner. EXP-0058 owns outer FIFO source/refusal composition, EXP-0042 owns public close, EXP-0063 owns Waiting/heap work, EXP-0064 composes admission and EXP-0065 regenerates exact artifacts. Reopen only for the declared falsifier or a changed relevant path, host domain, selector or identity.

## Architecture Impact

The reference zero-Step input now requires available persistence/authored-close Crossing owners and a distinct unavailable refusal owner, with no duplicate Trigger or Pipeline charge. No runtime, semantic or storage change is introduced; C1 and A0 remain frozen, and complete production regeneration remains G5 work.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0028](./EXP-0028.md) O10
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0028](./EXP-0028.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
