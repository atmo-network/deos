# EXP-0065 — Production Binding Closure

| Field | Value |
| --- | --- |
| Status | Invalidated |
| Record kind | Leaf |
| Former IDs | actors/EXP-0032@a60de7748132211d9e58cecd5be82b41f76ce230 |
| Parent question | None |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-07 |
| Affected domain | Exact production artifact and consumer identity |
| Physical mechanism | Source-to-generated production binding |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0066](./EXP-0066.md) |

## Decision Question

Can one exact final source tree generate self-consistent Weight, Wasm, metadata, ABI, client bounds and storage identities?

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.
- `Scope`:

- Own complete Actors Weight regeneration and Router/TMC cross-pallet regeneration where Actors storage names changed.
- Bind benchmark source, benchmark Wasm, production source/Wasm, metadata, ABI/PAPI, client descriptors, cost vectors, runtime bounds and storage-name audit.
- Reconcile 12/24/48, maximum-tail/genesis and host-configurable package bounds, ActorSimulationApi v2 with explicit synthetic budgets and one-record result bound.
- Verify canonical browser and staking-bootstrap consumers against regenerated metadata and live reference composition, including finalized identity and corruption-versus-absence.
- Never manually edit generated coefficients/comments to evade stale-storage checks.

## Context and Hypothesis

- `Entry condition`: Satisfied: EXP-0028, EXP-0029, EXP-0063 and EXP-0064 are Accepted. Complete generation began only after those decisions, and the final bundle is accepted below.
- `Hypothesis`: One closed source/configuration/toolchain identity set can reproduce all production and client artifacts without stale scalar storage annotations or mismatched resource bounds.
- `Materiality`: Publish one exact source ↔ benchmark source ↔ generated Weight ↔ benchmark Wasm ↔ production Wasm ↔ metadata ↔ ABI/client-bounds identity set, with justified feature/build differences and no stale generated owners. All accepted upstream envelopes and canonical consumers must agree on that set.
- `Dependency boundary`: Satisfied by accepted complete Weight/admission/domain/Waiting outputs. This is the explicitly requested binding-consistency question, not a reason to open experiments for routine generation chores.

## Baseline and Candidates

| ID | Comparison | Source tree | Runtime/config identity |
| --- | --- | --- | --- |
| B0 | Retained frozen C1 with incomplete production evidence | Commit `5b46b19004b62a723d6c45a610dfe1e5c79982b0`; template `98af72ffbb55fe8840b5bb44b5b01d65c8e775e1` | Reference 12/24/48, equal thirds; current artifacts not accepted as a complete set |
| A | One closed source/configuration/toolchain identity set can reproduce all production and client artifacts without stale scalar storage annotations or mismatched resource bounds. | Accepted post-binding source/configuration identity `c5345d6ba9574b9f16d0c62541400df027edd954039167f413f9b0aa9297c26d` | Same frozen C1 and semantic policy; accepted dependency outputs identified |

## Controls

- `Controlled variables`: Freeze source/configuration, host bounds, toolchain/SDK, database/cache, legal state and workload for each comparison; name any changed input and refresh dependent evidence.
- `Changed variable`: Only the question-specific model, fixture, accounting or binding under test; no macrogeometry or authored semantics.
- `Confounders`: Historical native, direct-path, partial-heap and old generated evidence are narrower than production acceptance. [EXP-0026](./EXP-0026.md) and [EXP-0027](./EXP-0027.md) reject ceiling-only and state-hold-cache-only proof improvements; do not reintroduce them without changed evidence.

## Benchmark Class, Workloads, and Criteria

- `Smallest falsifier`: Run the storage-name/binding audit over a complete generated Actors/Router/TMC set, then compare metadata/ABI/client bounds and production Wasm against the same source identity. One retired storage owner or divergent 12/24/48/simulation bound rejects the set.
- `Evidence class`: Exact-tree generated identity and consumer integration validation.

| Identity edge | Required artifact / check |
| --- | --- |
| Source ↔ benchmark source ↔ Weight | Exact content/mode/config/toolchain and complete generated files |
| Benchmark Wasm ↔ production Wasm | Separate hashes and declared feature/build difference, no accidental reuse |
| Production Wasm ↔ metadata ↔ ABI/PAPI | Regenerated runtime APIs, simulation bounds and storage identities |
| Runtime bounds ↔ client/cost vectors | 12/24/48, maximum-tail/genesis fit and canonical consumer paths |
| Actors ↔ Router/TMC | Changed Actors storage names and complete cross-pallet generated provenance |

- `Acceptance criteria`: Publish one exact source ↔ benchmark source ↔ generated Weight ↔ benchmark Wasm ↔ production Wasm ↔ metadata ↔ ABI/client-bounds identity set, with justified feature/build differences and no stale generated owners. All accepted upstream envelopes and canonical consumers must agree on that set.
- `Rejection criteria`: Partial Weight fragment presented as full regeneration, handwritten annotation/coefficient cleanup, stale scalar/32-Step artifacts, mock-only consumer closure or unidentified Wasm reuse.

## Environment and Artifact Identity

| Artifact | Identity / required binding |
| --- | --- |
| Baseline source | Commit `5b46b19004b62a723d6c45a610dfe1e5c79982b0`; template tree `98af72ffbb55fe8840b5bb44b5b01d65c8e775e1` |
| New experimental source/config/toolchain | Accepted post-binding content-and-mode identity `c5345d6ba9574b9f16d0c62541400df027edd954039167f413f9b0aa9297c26d`; pre-generation identity `b9688a4f7259cda08f3b6360ba735e848df9d23999e72e6bf2c3d696eeb6aa06`; Rust `1.98.0 (88d9e12ae 2026-08-18)`; `frame-omni-bencher 0.22.0`, benchmark CLI 58.0.0; reference 12/24/48 and equal-thirds policy retained |
| Benchmark / production Wasm | Corrected Actors measurement Wasm `584565e3a340890333746882bb599b0b106caa761cb21466295b50740a2a4b40`; final converged Router/TMC benchmark compressed Wasm `3004ac8553962cb73c34c6adc0a063f3ae8fa10d56ae621a1ad855a647c72be5`; separate production compressed Wasm `155749e22a6235570d2bb97d4a42b1327b86f4f5d82742df36494fca0fd88c1d` |
| Generated Weight / metadata / ABI | Actors `20d56df225eca3ff52857f2170eccf50587c5ca17f997d18d083e61e8afbeed9`; Router `4f663e5925ebd187d15414f61439e6fb97ba61380aa1d29e946afdba0f5c34f6`; TMC `a1e792e801bc5f64be3024b7e29ba3a0acb70303addeabb6d787eea1d6eda6fa`; live-matching metadata `75868ea75fd85e7b79db12650a7407e89e249f5564b2157d55ac46cd26cb5d12`; descriptor package `2f9c6de877f2f4d575c49992a262109d1635f68e6b586cb9b34844cbf3849da2`; Actors ABI `7c645cde7c5405ee805281dace5cddd0e80fa60be6fdf45ac06fbeabe3403e71`; bounds `0cd7716d1e694b516a33a47c40f16cf97ba90510e8b4792feddac89826e29d15`; cost vectors `df65f158ba755e42f6487cac46525ce5f3a910b643172169054b9cb755030e33`; fee-envelope vectors `e7457d02450ba257253c325805c11b636650d1a8262796f622157c5fc64671e9` |
| Commands | `./scripts/benchmarks.sh --check`; complete Actors/Router/TMC generation at 50 × 20 in `/tmp/deos-exp-0065-actors-50x20-r3`, `/tmp/deos-exp-0065-router-50x20-final` and `/tmp/deos-exp-0065-tmc-50x20-final`; `./scripts/03-build-runtime.sh`; canonical-ParaId `./scripts/export-papi-metadata.sh`; runtime/client artifact generators; pinned-Node `./scripts/actors-assurance.sh --quick` and `npm run validate`; live reference Zombienet browser adapter/runtime identity probe and `bootstrap-native-staking-local.sh check --json`; dependent runtime compilation and changed-scope completion gate |
| Evidence owner | Baseline source and the question-specific facts below; new measurements must carry exact source/artifact identities in this record |

## Proof Obligations

- `Freeze`: Frozen at Measuring to O1 and the finite identity edges above; the accepted decision remains frozen. Newly independent selectors, owners or evidence questions transfer rather than expanding this Leaf.
- `Review triggers`: None.
- `Decomposition review`: One indivisible artifact-consistency decision: any stale member rejects the complete bundle. Regeneration and consumer checks verify that same identity contract; they are not new component-bound research. Any changed physical selector or independent benchmark question transfers to its own Leaf before generation continues.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | One exact complete source, generated Weight, benchmark/production Wasm, metadata and canonical-consumer identity set realizes all accepted upstream contracts. | One mismatch in the regenerated complete source/Weight/Wasm/metadata/consumer bundle prevents exact binding closure | Exact-tree production artifact and canonical-consumer integration closure | Self | Required | Named parent decision and declared downstream consumers | Satisfied; exact artifacts and both live canonical consumers converge on the recorded reference identity |

## Measurements

The initial preflight compiled the benchmark runtime in 73 seconds, then rejected the installed complete set for retired Actors storage annotations in Actors, Router and TMC. On one fresh compiled-Wasm cohort with 50 steps × 20 repeats, Router regenerated in 75 seconds after a 109-second build and TMC regenerated in 24 seconds by reusing exactly that benchmark Wasm. Generated files were installed only through the script's staged normalization and contract checks; no coefficient or storage comment was edited manually.

| Pallet | Previous Weight SHA-256 | Generated Weight SHA-256 | Cross-pallet storage result |
| --- | --- | --- | --- |
| Router | `ca94deb1801f9ef2af45bb3914805a8d75e228c9836e0a7981fbd32de1ccf379` | `2d3a941cabf5ffe24cd18eed5c9909a114813ed1eabec1ce9823822332c1b7bb` | Retired ActorHot/Admission/Queue names removed; current split control, Run payload and Ready owners measured |
| TMC | `851d61b5f1e421630701b3c5cea63886b23d548dd1ad7d880eac5a077d08ab0b` | `97a5d76a2272514fd926e4bc80847c6f61f7f4bc36ce3bdaee57df284151baa1` | Retired ActorHot removed; current locator/control/activation/Run/tail owners measured |

The repeated preflight compiled in 47 seconds and now reports stale names only in `pallet_deos_actors.rs`: ActorAdmissionCertificate, ActorHot, NextQueueTicket, QueueHead, QueueOccupancy, QueuePages, QueueTail, WakeupBuckets and WakeupPages. This is an expected incomplete-bundle falsifier, not acceptance. Cargo formatting, `cargo check --locked -p deos-runtime`, and diff checks pass for the retained cross-pallet generation.

The first complete Actors attempt rebuilt the benchmark runtime in 121 seconds from the post-Router/TMC tree, producing compressed benchmark Wasm `c43d01fd41f69912be3459ff43f6cc8d906765bd4315fb326af6a83a36286d5a`. Before the 3,600-second execution bound terminated the run at 77%, 23 production benchmark paths failed at five shared setup constructors with `ContractStepsExceedOnIdleBudget`: deactivate/close/update, current-Step plan, scheduler inner Opening/Running/Suspended, and Run progress/suspend/complete/cancel paths. Each failed setup still requested a dense maximum Contract that the accepted component-wise G4 admission frontier intentionally rejects. No generated Actors file was installed; its SHA-256 remains `2d9c60ac7764d1e7dc366531dd4a409444daf66cf3e37ed7feb922bb614e8f1a`.

Fixture correction now retains the requested structural Step/tail geometry and each measured Step, then reduces only unrelated setup payload until the complete Contract passes the same ordinary component-wise admission used in production. Crossing retry setup uses real bounded rearm plus admitted transition notification; successful Suspended-tail branches arise from a real unfunded fixed Transfer retry followed by spendable custody, not direct state installation. The focused regression preserves length/protected-Step identity and proves the resulting maximum against the service floor. A fresh benchmark build produced compressed Wasm `c9f31b7da31f9eb4415c16df2c47351c0ac30a4e8e53946139aafea6537f03e7`; every one of the 23 formerly failing production benchmarks executed successfully from it at 3 steps × 1 repeat. This is reachability smoke evidence, not generated production Weight.

The next complete run reached 96% before `control_frame_locator_transition_batch32` exposed a benchmark-verification defect: its measured map writes were valid, but its postcondition assumed an empty `ActorControlLocators` map while the reference genesis already activates two System Actors. No output was installed. The fixture now selects fresh IDs from `NextActorId` and verifies each requested location exactly without treating unrelated genesis entries as benchmark output. Fresh compressed benchmark Wasm `cf19dd830d7297821fb29bfa1164fd1f43e74bf47c9c8121cd12b559994eb129` passed the focused path at 3 steps × 1 repeat with the expected `0 + n` writes.

The corrected complete run then succeeded in 4,337 seconds and installed Actors output `9926c2320691ec44444ef5d61599f5fbdf08467e91637aab1aacdcdf34eaa880`, but the first dependent runtime build correctly rejected it with 19 interface errors. Thirteen EXP-0064 candidate-only control probes had not been marked `extra`, so they leaked into a production trait implementation; the six Suspended-head benchmarks generated their deliberately coupled `n,p` or `p` signatures while `WeightInfo` and runtime selectors still claimed the retired independent Opening/result/funding axes explicitly rejected by EXP-0048. The source now marks all candidate probes extra, binds `WeightInfo` to the measured coupled signatures, commits their bounded endpoints in the production identity and retains the complete admitted maximum rather than issuing an unsupported independent-axis Suspended-head refund. A bootstrap-only removal of the leaked methods from the rejected output restored compilation; no generated coefficient or proof annotation is accepted from that cleanup, and the corrected complete run must supersede it.

`./scripts/benchmarks.sh --check`, runtime compilation, the retained allocation-boundary test, production-identity regression and exact funding-retry regression pass. All six corrected Suspended-head profiles execute at 3 × 1 from benchmark Wasm `584565e3a340890333746882bb599b0b106caa761cb21466295b50740a2a4b40`. Corrected benchmark source is `6afc5421deb8a37011e4a12499427ee7d2944025513fba6bbd41b0c4c9c52b76`.

The corrected complete Actors run succeeded in 3,952 seconds at 50 × 20 and installed Weight `20d56df225eca3ff52857f2170eccf50587c5ca17f997d18d083e61e8afbeed9`. Dependent runtime compilation and the storage-name audit passed without candidate-only methods or stale Suspended-head signatures. Rebuilding the benchmark runtime from that installed Weight produced compressed Wasm `3004ac8553962cb73c34c6adc0a063f3ae8fa10d56ae621a1ad855a647c72be5`; Router then completed in 74 seconds and TMC in 24 seconds at 50 × 20, installing `4f663e5925ebd187d15414f61439e6fb97ba61380aa1d29e946afdba0f5c34f6` and `a1e792e801bc5f64be3024b7e29ba3a0acb70303addeabb6d787eea1d6eda6fa`.

The first full behavioral gate against this final cohort exposed 15 stale pre-final Weight snapshots, including an assertion that still expected an independent Suspended-head receipt refund. The retained tests now bind the final generated owners and explicitly preserve the conservative coupled-head no-refund contract. All 206 focused Actors integration tests pass, followed by the complete changed-scope alignment gate; the converged three-Weight set therefore passes behavioral validation, dependent compilation and the generated storage-name audit.

The deterministic production build completed in 404 seconds and published compressed Wasm `155749e22a6235570d2bb97d4a42b1327b86f4f5d82742df36494fca0fd88c1d`, distinct from the benchmark Wasm. Metadata export completed in 306 seconds, descriptor generation in 17 seconds and runtime-evidence generation in one second. The retained metadata, descriptor, ABI, 12-Step bound, fee-envelope and cost-vector hashes are listed above.

The regenerated metadata falsified stale browser assumptions: direct `ActorHot` active reads no longer compile, cost and authoring fixtures still claimed 32 Steps, and the matching-Wasm codec still required five-input `ActorSimulationApi` v1. The retained client now resolves one bounded canonical control cell through `ActorControlLocators`, distinguishes NotRegistered, Dormant and corrupt split/mismatched authority, binds `ActorSimulationApi` v2's explicit synthetic Actor Control/Shared Economic budget into request/response provenance, accepts `Continued`, and rejects more than one result Step. Pinned Node 22.22.0 Actors quick assurance passed in 75 seconds; all 151 browser automation tests, Svelte checking and formatting pass. Operator evidence scripts use the same canonical control projection; staking bootstrap already consumes bounded `ActorEligibilityApi` state.

The first live reference-composition pass correctly rejected the candidate metadata identity. Production `:code` matched `155749e22a6235570d2bb97d4a42b1327b86f4f5d82742df36494fca0fd88c1d`, but Metadata API bytes were `75868ea75fd85e7b79db12650a7407e89e249f5564b2157d55ac46cd26cb5d12` while the native exporter had produced `4b566e3df9e0f5633e89862c60bc8c0c311566c605193ec78ee603f051cfaf90`. Four bytes differed: the exporter ran under empty externalities and encoded storage-backed `ParachainSystem::SelfParaId` as 100 rather than the reference 2000, with the corresponding `PolkadotXcm::UniversalLocation` difference. The exporter now builds canonical ParaId externalities, its regression passes, and regenerated metadata exactly matches all 288,278 live bytes.

The corrected live pass used the generated chain spec and owned Zombienet topology. The Vite-loaded production `BlockchainAdapter` resolved all 14 known System Actors through canonical control (3 Active, 11 Dormant), read Actor resource APIs and the finalized authoring context, retained the 12-Step bound, and matched both finalized runtime code and metadata hashes exactly. `bootstrap-native-staking-local.sh check --json` exposed a second stale assumption: `NativeSecurityView` is mode-shaped, not a struct containing `mode`. After correction it decoded finalized TrustedSet epoch 0, both genesis staking assets, bounded Dormant Actor 14, and honestly reported `missing-staking-assets` because the ephemeral reference state had no exchange rate or AMM pool. Every owned process was terminated and ports 9944/9988/9999 were clear.

## Derived Metrics

No new derived metric. Inherited numbers cannot be promoted beyond their original source and evidence class.

## Statistical / Variance Notes

For generated FRAME evidence, pin 50 steps × 20 repeats where applicable and review model/branch fit, not merely a mean. Identity and constructive correctness checks are deterministic; no wall-clock capacity inference. Declare a different method before measurement if the bounded question requires it.

## Result

Accepted. Corrected Actors and converged Router/TMC generation completed at 50 × 20. Production Wasm, live-matching metadata, ABI/PAPI, descriptors, 12-Step bounds, fee-envelope and cost vectors form the recorded exact bundle. Pinned source/type/behavioral assurance and live finalized browser/staking consumers converge on it.

## Interpretation

The converged Weight and downstream artifact cohort removes stale generated bindings without changing C1. Fixture and locator corrections remove false blockers without weakening production admission. Live execution rejected storage-default metadata and mode-shape client drift before acceptance, and the retained fixes bind generated metadata constants and both consumers to the canonical reference composition. O1 is satisfied; this establishes exact binding closure only, not W0–W9 throughput.

## Decision

Accepted. Permanently reject the first complete Actors output and the empty-externalities metadata candidate. Retain the corrected Weight, production Wasm, canonical-ParaId metadata, ABI/PAPI, descriptor, bounds and vector cohort as one exact bundle. EXP-0066 is now unblocked on this identity; no throughput or release-Geometry claim is imported.

## Rejected Alternatives and Tradeoffs

| Shortcut | Disposition | Reason |
| --- | --- | --- |
| Reopen C1 because this question is expensive | Excluded | Requires the explicit invariant/impossibility test in EXP-0025 |
| Reuse direct historical C1 as production acceptance | Excluded | Source/path/artifact authority differs |
| Manually repair generated annotations | Excluded | Partial Weight fragment presented as full regeneration, handwritten annotation/coefficient cleanup, stale scalar/32-Step artifacts, mock-only consumer closure or unidentified Wasm reuse. |

## Validity

- `Establishes`: Historically, Accepted G4 inputs were ready; all 23 admission-blocked fixture paths execute through ordinary admission; the locator-write fixture passes against non-empty reference genesis; candidate probes are extra-only; all six coupled Suspended-head signatures compile and execute; complete Actors/Router/TMC 50 × 20 Weight generation passes; and the exact production Wasm, live-matching metadata, ABI/PAPI, descriptors, 12-Step bounds, vectors and finalized browser/staking consumers converge.
- `Does not establish`: Integrated W0–W9 throughput, release acceptance or permission to change frozen C1.
- `Known limitations`: Earlier complete attempts stopped at 77% and 96%, the first successful Actors output failed dependent compilation, and the first metadata candidate encoded empty-externalities ParaId 100. They remain rejected evidence.
- `Invalidation triggers`: Triggered on 2026-09-08 by EXP-0067's User completion selector and regenerated Actors Weight/Wasm. EXP-0095 owns replacement binding closure.
- `Residual qualitative insight`: This accepted exact artifact set unlocks EXP-0066. Any upstream correction invalidates affected generated descendants; refresh those dependencies without reopening physical C1.

## Follow-Up / Next Gradient

- `Produces`: Historical exact artifact set `c5345d6ba9574b9f16d0c62541400df027edd954039167f413f9b0aa9297c26d` with production Wasm `155749e22a6235570d2bb97d4a42b1327b86f4f5d82742df36494fca0fd88c1d` and metadata `75868ea75fd85e7b79db12650a7407e89e249f5564b2157d55ac46cd26cb5d12` unlocks EXP-0066. Any upstream correction invalidates affected generated descendants; refresh those dependencies without reopening physical C1.
- `Blocking evidence required`: Satisfied by exact live canonical-browser and staking-bootstrap identity checks.
- `Stop condition`: Decide only this question against the stated criteria. Transfer materially distinct hypotheses to dedicated records; BACKLOG alone owns release-blocking work.
- `Residual trigger`: A production experiment must name a measured owner and changed assumption before any bitmap/page/shared-body/key-locality/compression/PoV candidate begins.

## Architecture Impact

- `Affected physical truth`: Source-to-generated production binding, within frozen C1.
- `Architecture update`: Historical exact binding invalidated as current by changed generated descendants; no change to frozen C1 physical geometry.
- `Provenance citation`: `actors/EXP-0065`, Accepted for exact production binding only, with the identities recorded above.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: None.
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0028](./EXP-0028.md), [EXP-0029](./EXP-0029.md), [EXP-0063](./EXP-0063.md), [EXP-0064](./EXP-0064.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None; current authority is explicitly invalidated by [EXP-0095](./EXP-0095.md).
- `Uses evidence from`: [EXP-0064](./EXP-0064.md), [EXP-0028](./EXP-0028.md), [EXP-0029](./EXP-0029.md), [EXP-0063](./EXP-0063.md)
- `Transfers question to`: [EXP-0095](./EXP-0095.md) — refreshed exact production binding.
- `Produces input for`: [EXP-0095](./EXP-0095.md); prior EXP-0066 evidence retains historical identity only.
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
