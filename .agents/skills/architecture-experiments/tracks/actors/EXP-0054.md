# EXP-0054 — Native Admission Refusal and Precedence

| Field | Value |
| --- | --- |
| Status | Measured |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0064](./EXP-0064.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | Native resource refusal and terminal precedence preserve the admitted authority, custody and FIFO state in the declared bounded partition. |
| Physical mechanism | Native Admission Refusal and Precedence |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0064](./EXP-0064.md) |


## Decision Question

Native resource refusal and terminal precedence preserve the admitted authority, custody and FIFO state in the declared bounded partition.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns native admission refusal and precedence. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission former actors/EXP-0030@a60de7748132211d9e58cecd5be82b41f76ce230 / Measurements subsection at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: former actors/EXP-0030@a60de7748132211d9e58cecd5be82b41f76ce230 / “Measurements”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. No candidate is introduced by this migration.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Extraction baseline: `a60de7748132211d9e58cecd5be82b41f76ce230`. The migration manifest records exact original content/mode identities and source spans. Question-specific source/Wasm/output identities remain inline. Historical fragments remain historical; no new runtime or production artifact was built.

Use the identified environment attached to each extracted source. Where a shared control is needed, follow its explicit evidence source; absent identity requires refresh before acceptance.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Native resource refusal and terminal precedence preserve the admitted authority, custody and FIFO state in the declared bounded partition. | One resource refusal at the last allowed retry that reads cold payload, mutates FIFO/Run, or reverses terminal precedence | Native trie/storage-root correctness under the declared refusal partition | Self | Required | Named parent decision and declared downstream consumers | Satisfied with scope; original binding/host limitations retained; complete release binding remains open |

## Measurements

<!-- extracted: former actors/EXP-0030@a60de7748132211d9e58cecd5be82b41f76ce230:82-102:54 -->
### Native Refusal and Terminal Precedence

The retained Idle User trie witness on source identity `97e4e1a3aa0593d2801945121e458bd612594dedbee868e25685e9326f0fea42` checks native liability before Funding/Run and leaves Suspended cold-read proof open. `idle_weight_refusal_reads_only_header_and_user_fee_prerequisite` owns that witness. The baseline actual selector was `DEOS_ACTOR_CONTROL_ACTUAL_V3`; EXP-0028 owns its later receipt-composition correction. The withdrawn maximum-Opening shortcut must not return, and an Action-collection allowance cannot cover missing System base control. Full 12-Step/24/48 admissibility remains a question for the complete production envelope, not a reason to raise budgets.

The native Suspended falsifier against `17de6e546a7115321b2e5eca83a6442035d3b38d` observed an avoidable Funding read after an admitted SwapIn temporarily failed and the retry returned to Ready. The repaired negative preflight reads coherent Run-head/current-Step authority and current User Action capacity before resource refusal; exhausted retry policy and exceptional paths retain full classification. No storage or scheduler authority is added.

`suspended_weight_refusal_avoids_run_payload_and_funding_reads` covers Temporary/FundingUnavailable, User/System, cursor 0/1, aggregate/control/effect lanes and independently scarce RefTime/ProofSize (48 refusal cases). Each fixture is immediately before the last allowed attempt (`max_attempts = 2`). Trie evidence excludes Funding/RunPayload, requires native balance only for User, preserves the storage root and FIFO head across repeated calls, and resumes successfully with ample capacity and restored Task funding. Scheduler SHA-256 remains `8ed61e1a5c009eb7a940e4fc9f816c74d131a502ffa49d13d88db9e7099a95cd`; scheduling-test SHA-256 is `5a539a8d223d2007071a3b683423ab7ca581ae59717ad749ddf6adc72d0028b8`. The quick assurance composition still rejects stale fee-envelope vectors at G5; this evidence is not production Weight or release acceptance.

`suspended_expiry_and_breaker_precede_liability_and_effect_deferral` constructs a legal window and one committed suspension, then services its retained Ready entry after expiry. Its 16 User/System × paused/unpaused × breaker on/off × scarce effect RefTime/ProofSize cases prove WindowExpired cleanup before insolvent User Action liability and effect deferral, while the breaker preserves Run. Custody, fee collection and effect usage remain unchanged; TryRuntime checks topology after each outcome. The native Suspended correctness boundary is reconciled with these existing owners:

| Boundary | Authority and evidence |
| --- | --- |
| Solvent/insolvent Action capacity | Shared `action_capacity_sufficient`; `user_retry_insolvency_closes_before_effect_capacity_deferral` covers both effect dimensions, exact Run retention and custody-neutral insolvency |
| Paused nonterminal retry | Preflight excludes pause; `pause_and_breaker_gate_scheduler_owned_retry` preserves attempt count through owner pause/resume and breaker |
| Last allowed retry and repeated resource refusal | The 48-case trie property preserves the exact Run and current FIFO head, then completes the last allowed attempt |
| Local/global exhaustion | `prepare_loaded_resuspension` checks both bounds before choosing suspension; terminal failure removes Run instead of persisting an exhausted Suspended state. `frame_only_retry_attempt_exhaustion_closes_from_consumed_authority`, `global_failure_limit_can_close_before_retry_later_local_limit` and `retry_local_limit_precedes_simultaneous_global_failure_limit` cover the terminal owners and precedence |
| Same-block/future eligibility | Valid suspension schedules after its last attempt; inline eligibility rejects early service. Same-block attempt/commit and malformed nonce/authority guards fall through to the existing full classifier and cannot admit an effect |
| Other lifecycle reasons | Nonce exhaustion and auto-close nonce are Idle-only classifier reasons; failure-streak and window termination bypass the negative preflight. Cold binding validation, cleanup admission and effect reservation retain their ordinary owners |

Validation: package default suite 808 passed/5 ignored; TryRuntime suite 826 passed/5 ignored before the property expanded from four to 24 cases, then the expanded TryRuntime property passed. Independent embedding with `dex-fixture,try-runtime` passed 27 tests. Clippy for Actors/runtime/embedding all targets passed with `-D warnings`; the changed-scope completion gate including workspace check passed. No production Wasm or Weight regeneration was performed for this native correction.

The subsequent test-only expansion to 48 refusals and 16 expiry cases passed the seven `suspended_` TryRuntime tests. The reference runtime's non-ignored `actors_integration_tests` passed through `scripts/ci-local.sh --only tests --package deos-runtime --test-filter actors_integration_tests --default-features`; the listed total of 224 includes ignored profiles and is not a passed-test count. The CI route skips Wasm building, so this is native integration evidence. Execution source and the earlier unaffected package/embedding evidence remain unchanged. G3's native Suspended correctness reconciliation is complete; production reachability, measured envelopes and exact bindings stay with G4/G5.
<!-- extracted:end -->

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

The extracted observations and counterexamples above remain valid only within their original source-bound scope. No new observation was made during migration.

## Interpretation

A scoped witness may satisfy its declared obligation without accepting every production consumer. Existing rejected fits, admission refusals, missing host domains and uninstalled fragments remain explicit evidence boundaries.

## Decision

No new Accepted/Rejected decision during migration. The declared scoped proof is measured; stop repeating its satisfied witnesses and let the parent consume their stated limits.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. Do not infer a new selection from record relocation or from aggregate coefficient subtraction.

## Validity

- `Establishes`: Only the source-qualified observations and constraints above.
- `Does not establish`: Complete production Weight, exact release artifact closure or integrated throughput.
- `Known limitations`: Existing missing domains, diagnostic sample counts, stale bindings and host restrictions remain effective.
- `Invalidation triggers`: A changed measured body, host domain, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Follow the owning physical boundary, not a benchmark name.

## Follow-Up / Next Gradient

Stop when the declared proof is satisfied. Transfer any independent owner, selector, parameter domain or artifact-identity question before investigating it beyond the smallest classifier. A hypothetical corner case alone is not a measurement mandate.

## Architecture Impact

No runtime, semantic, storage or production binding change. C1 and A0 remain frozen; this record owns only its declared proof evidence.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0064](./EXP-0064.md) O4
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0064](./EXP-0064.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
