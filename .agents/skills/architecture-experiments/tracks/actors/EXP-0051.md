# EXP-0051 — Legal Waiting Reference Population

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0029](./EXP-0029.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | Ordinary lifecycle construction realizes the declared mixed and final-Tick Waiting Reference populations without injected reference state. |
| Physical mechanism | Legal Waiting Reference Population |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0029](./EXP-0029.md) |


## Decision Question

Ordinary lifecycle construction realizes the declared mixed and final-Tick Waiting Reference populations without injected reference state.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns legal waiting reference population. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0029 / “Mixed Waiting Reference Cleanup Qualification”, “Final Tick Reference and Saturated Heap Qualification”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. No candidate is introduced by this migration.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Extraction baseline: `a60de7748132211d9e58cecd5be82b41f76ce230`. The migration manifest records exact original content/mode identities and source spans. Question-specific source/Wasm/output identities remain inline. Historical fragments remain historical; no new runtime or production artifact was built.

Use the identified environment attached to each extracted source. Where a shared control is needed, follow its explicit evidence source; absent identity requires refresh before acceptance.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Ordinary lifecycle construction realizes the declared mixed and final-Tick Waiting Reference populations without injected reference state. | One ordinarily constructed cleanup removing the final Tick reference or reclaiming a mixed Reference page | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Accepted for the declared mixed-page and saturated final-Tick mock/reference histories |

## Measurements

<!-- extracted: EXP-0029:121-157:51 -->
### Mixed Waiting Reference Cleanup Qualification

The next legal-state prerequisite carries the existing native `mixed_waiting_pages_reclaim_both_entry_kinds_through_real_close` construction across the host clock boundary. Create 32 ordinary Cadenced primary guards, then one two-Step System Actor whose Current `BlockNumberBelow(0)` predicate skips Step zero. Advance through the host's clock helper, bounded due worker and ordinary cycle to retain a real Running suffix and rearmed Tick reference. Append 63 further Cadenced primary guards at that same deadline. The three linked pages are full; the middle begins with the target reference and continues with 31 primary guards. No Contract, Run, pointer or page is injected or rewritten after admission.

Compare public Root `close_actor` while the mixed page is full against closing its 31 primary guards first, then removing the target as its last remaining entry. The latter must unlink the middle page between two full neighbors. Both cases retain the same target Run across preparation, preserve the neighbors' encoded entries, remove the target's complete process state and keep positive native sovereign custody unchanged. Full native TryRuntime audits own pre/post bijection; Wasm checks use the constructive transitions and explicit state assertions. The fixture has 96 Actors before optional cleanup, plus retained host genesis; it does not claim saturated global population, maximal encoded cells, User hold/fee coverage, final-key removal or a complete close/Waiting envelope.

Native all-feature `mixed_waiting_reference_close_uses_real_opening` passes both branches in 36 seconds including compilation. Prepared source content/modes `856d8ac72b307ff24c8b3d713ec010a7fbc2440d1a8ab4a51e2a3588779ea1c9`, benchmark SHA-256 `8c45cf9cbe2882c5cdc95ce2ec9eb495d79ae050fa713873527b74d5d2e6deed`. The extra diagnostics are `close_actor_mixed_waiting_reference` and `close_actor_mixed_waiting_unlink`; they measure only the final public close, including its already-owned Run, Ready-source, contract and lifecycle work. Qualify each with `./scripts/benchmarks.sh --extra --steps 2 --repeat 1 --min-duration 0 --extrinsic NAME --output /tmp/deos-exp29-mixed-BRANCH.rs --json-file /tmp/deos-exp29-mixed-BRANCH.json pallet_deos_actors`, substituting the exact name and `reference`/`unlink`, reusing one built Wasm with `--skip-build` for the second. One time and one database sample decide constructive shape/access coverage; statistical RefTime and production-envelope decisions require later declared sampling and owner reconciliation.

Both reference-Wasm probes pass on SHA-256 `081df3d8fae3f8ba8351a9058976e0c47df70b45cfa30edc5294d72c155a0da9`: build 107 seconds, each execution four seconds. The current host clock admits the same real two-Step Running suffix, rearmed Tick reference and three-page geometry. The full-page branch changes one Waiting page and leaves 95 primary entries at the deadline; the last-reference branch deletes the middle page, links its two full neighbors and leaves 64 primary entries. Native custody remains positive and unchanged, and the target's Run, Ready locator, Contract and funding state are absent after close.

| Branch | Time sample / storage-root ns | DB sample / storage-root ns | Unique / repeat reads | Unique / repeat writes | Recorded / estimated proof bytes | Generated model SHA-256 | Raw JSON SHA-256 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Reference in full mixed page | 493,227 / 4,400 | 450,414 / 4,749 | 23 / 36 | 16 / 0 | 15,200 / 18,665 | `433fcbabf747fd9b9444e54c77c3182c6930e5ef6adf0b8a36393a161f2e1256` | `d26e38bb23b93786a646eb28663cc1bbdb186ad3136ff8f879f7a25643252203` |
| Last reference, middle unlink | 437,563 / 6,216 | 457,608 / 4,749 | 25 / 36 | 18 / 0 | 25,149 / 33,564 | `3b21ceb0f6eb0c686fe5d17439b8f213ce6a0b2581441df252c251bf62cfb019` | `328b3e8b42773a9d98d79c0240f517a05d5176e73fd2ec981ac6de2a7c22060d` |

Each probe retains one timing and one database sample. Both traces include Ready-frame/occupancy removal, Run head/payload removal and the Waiting directory; the unlink branch reads/writes three Waiting frames instead of one. Neither touches heap pages/length because the deadline survives. The different preparation histories also change retained population, so these complete public-close observations are not isolated unlink coefficients. Their Run, source, Contract and lifecycle work must not be charged again through a second Waiting allowance. Generated fragments remain uninstalled; statistical RefTime, maximal cells, User accounting, final-key and other heap branches remain outside this witness. A subsequent help-text correction describes `--extra` as all diagnostic benchmarks; execution and benchmark source are unchanged.

### Final Tick Reference and Saturated Heap Qualification

Extend the mixed-reference constructor through ordinary lifecycle history: close its remaining 64 primary guards, preserving the target's real Running suffix and sole Tick reference. Reuse all 95 known retired guard sovereigns through `create_system_actor_at_sovereign_id`, then create fresh Cadenced guards until the active/identity/sovereign capacity intersection is exhausted. Retain host genesis and custody. Group the minimum number of future deadlines needed to place exactly one key on the final heap page; the target remains the minimum. Public target close must delete its final Waiting page/directories/key, repair the heap downward, reclaim the heap tail page and preserve positive sovereign custody.

This prerequisite tests a complete legally constructed Tick heap with slot reuse, not the global maximum over all lifecycle histories or maximal encoded control cells. Native TryRuntime owns full pre/post state validation; explicit Tick forward/reverse, parent-order, cardinality, Run and cleanup checks also execute in Wasm. The prior index helper checked Block only; its clock-specific implementation now allows the mixed-reference diagnostics to check Tick without changing their measured close bodies or original artifact bindings.

Qualification command: `./scripts/benchmarks.sh --extra --steps 2 --repeat 1 --min-duration 0 --extrinsic close_actor_mixed_waiting_deadline --output /tmp/deos-exp29-mixed-deadline.rs --json-file /tmp/deos-exp29-mixed-deadline.json pallet_deos_actors`. One time and one database sample are sufficient for constructive/access qualification only. Keep the existing host/toolchain, compiled Wasm and heap/cache controls; do not overlap heavy work. The measured public close includes Run, Ready-source, Contract and lifecycle cleanup, so no independent additive heap coefficient or statistical RefTime dominance follows. No generated fragment is installed.

Native all-feature `mixed_waiting_` tests pass in 75 seconds, including the new saturated heap and existing mixed-reference branches. The mock reuses 95 sovereigns and fills 1,023 background Actors; 993 Tick keys become 992 and the 32nd heap page is reclaimed. Prepared source content/modes `4e84cad96fce654264d38e9483479dfa6feb224feaa8c19353bc02f05b168555`, benchmark SHA-256 `9629cd9dd7cab7ef87cdf691bb38b2ba0e8c267deba1794e5bef85a525bf7c1d`. Background desktop/user processes were present; this diagnostic does not control host timing sufficiently for a RefTime comparison or envelope.

Reference Wasm `bd80b7eb860766630fa46e269b7e95f8a4d8813f0e80f5880c26c5c4f10b6e58` passes: build 110 seconds, execution 16 seconds. Public creation, sovereign reuse, saturated capacity, retained Run, root-key removal, final-page reclamation, Tick index/order and custody postconditions all hold. Retained reference genesis and the constructor's capacity intersection imply 9,984 background guards, including 95 reused sovereigns; this is not a new global bound over all possible lifecycle histories. The successful compact log is ephemeral; exact source, command, Wasm and normalized sample/artifact bindings below preserve the evidence scope.

| Branch | Time sample / storage-root ns | DB sample / storage-root ns | Unique / repeat reads | Unique / repeat writes | Recorded / estimated proof bytes | Generated model SHA-256 | Raw JSON SHA-256 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Last Tick reference, root repair and heap-tail reclamation | 1,833,363 / 27,588 | 3,494,006 / 7,473 | 70 / 80 | 44 / 0 | 51,201 / 96,741 | `0d409cd13ea33e1f363352f42ed36a132589917c49d78826274d754046130457` | `4d01cac25a7f4ea5607a8ce58589a3ab05c11d0155d65001aa184b79fba38f53` |

The single database trace reads 18 heap pages and writes ten, reads/writes 15 reverse indices, reads 15 Waiting occupancies and deletes the target occupancy/head/tail and sole Waiting page. It includes heap length, Ready-source, Run head/payload, Contract and lifecycle work. `scheduler.rs::wakeup_cursor_remove_inner` starts at index zero, so its upward loop exits immediately and the populated repair follows the downward branch. This qualifies a complete legal final-key/reclamation path; it neither isolates its heap cost by subtraction from older Wasm probes nor decides competing upward/tail-only, User/maximal-cell or complete production Weight envelopes.

Canonical formatting, workspace/all-target/all-feature Clippy (27 seconds), 28-record normalization/dependency validation, diff checks and changed-scope completion (three seconds) pass. The benchmark source matches the prepared digest. Production code, Weight and metadata are unchanged; existing unrelated release gates remain in BACKLOG.
<!-- extracted:end -->

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

Ordinary Cadenced creation, Opening, bounded due work, sovereign reuse and public close construct a full mixed Primary/Reference page, a last-entry middle-page unlink, and a sole final Tick reference at the root of a saturated heap. The final close repairs downward and reclaims the heap-tail page while preserving custody and every surviving owner. Current-tree all-feature `waiting_` validation passes all 37 selected tests, including both mixed-reference and saturated-deadline controls.

## Interpretation

The declared mixed and final-Tick Reference populations are lifecycle-reachable without direct Run, pointer, cursor or page injection. Complete close observations overlap Run, Ready, Contract and heap owners and therefore remain indivisible reachability evidence rather than additive cost components.

## Decision

Accepted for the declared mixed-page and saturated final-Tick mock/reference histories. Ordinary lifecycle construction and public close realize mixed Reference removal, middle unlink and final-Tick root/downward-repair reclamation. This satisfies EXP-0029 O6; EXP-0056/EXP-0063 retain numeric envelope ownership.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. Do not infer a new selection from record relocation or from aggregate coefficient subtraction.

## Validity

- `Establishes`: Lifecycle reachability of the declared mixed Waiting Reference and sole final-Tick saturated-heap cleanup histories.
- `Does not establish`: Complete production Weight, exact release artifact closure or integrated throughput.
- `Known limitations`: These complete close traces do not isolate additive page/heap costs or establish maximal encoded-cell and User-hold combinations.
- `Invalidation triggers`: A changed measured body, host domain, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Follow the owning physical boundary, not a benchmark name.

## Follow-Up / Next Gradient

Stop when the declared proof is satisfied. Transfer any independent owner, selector, parameter domain or artifact-identity question before investigating it beyond the smallest classifier. A hypothetical corner case alone is not a measurement mandate.

## Architecture Impact

No runtime, semantic, storage or production binding change. C1 and A0 remain frozen; this record owns only its declared proof evidence.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0029](./EXP-0029.md) O6
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0029](./EXP-0029.md), [EXP-0055](./EXP-0055.md), [EXP-0056](./EXP-0056.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
