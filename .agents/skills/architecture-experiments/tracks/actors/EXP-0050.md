# EXP-0050 — Legal Waiting Primary Population

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0029](./EXP-0029.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | Ordinary lifecycle construction realizes the declared Waiting Primary publication and removal populations without injected scheduler state. |
| Physical mechanism | Legal Waiting Primary Population |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0029](./EXP-0029.md) |


## Decision Question

Ordinary lifecycle construction realizes the declared Waiting Primary publication and removal populations without injected scheduler state.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns legal waiting primary population. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0029 / “Complete Waiting Population Qualification”, “Saturated Upward and Tail Removal Qualification”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. No candidate is introduced by this migration.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Extraction baseline: `a60de7748132211d9e58cecd5be82b41f76ce230`. The migration manifest records exact original content/mode identities and source spans. Question-specific source/Wasm/output identities remain inline. Historical fragments remain historical; no new runtime or production artifact was built.

Use the identified environment attached to each extracted source. Where a shared control is needed, follow its explicit evidence source; absent identity requires refresh before acceptance.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Ordinary lifecycle construction realizes the declared Waiting Primary publication and removal populations without injected scheduler state. | One public retry publishing into a full Waiting Primary page or new minimum key under the declared saturated host population | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Accepted for the declared ordinary-construction mock/reference populations |

## Measurements

<!-- extracted: EXP-0029:102-120:50 -->
### Waiting Primary Publication Qualification

| Branch | Time sample / storage-root ns | DB sample / storage-root ns | Unique / repeat reads | Unique / repeat writes | Recorded / estimated proof bytes | Generated model SHA-256 | Raw JSON SHA-256 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Existing key | 2,350,197 / 7,613 | 1,070,335 / 6,705 | 19 / 43 | 10 / 0 | 16,846 / 20,311 | `07bb36060fbd93ee7bfecb9793412165dcc0d1f1edeadf64770c05f2002868ea` | `b12a53c7aaa98320c7f9a59369445de9f6cfd1c687258fbad8e7b6a189d0013b` |
| New minimum | 1,534,298 / 8,521 | 1,813,458 / 8,102 | 56 / 59 | 36 / 0 | 46,277 / 81,917 | `2f91f3b8d424b6b260cfd62a4cfa013bb0cf6c43df24fe545e037584e442b35c` | `2429fb8730c1a8d3ac1385cfcd298706be2c98ebf62f61b6fb0ccbfaccde6944` |

The new-key trace reads/writes ten `WakeupCursorPages`, fourteen reverse indices, and the heap length, while reading fourteen per-key occupancies to validate repair. Existing-key publication reads one reverse index and one occupancy, with no heap page/length access. The recorded-proof difference is 29,431 bytes and database work differs by 37 unique reads and 26 writes on the same identified Wasm; those are this complete cycle's observed branch differences, not an independently chargeable heap allowance. One existing-key timing sample exceeding the new-key sample underscores why the probes decide reachability and access topology rather than RefTime dominance. Native all-feature regression with the final shared constructor passes in 42 seconds; no production Weight is installed.

The next occupancy falsifier, `scheduler_opening_retry_full_waiting_page`, shares the same saturated population but assigns 32 ordinary guards to the target deadline. Public readiness must fill its first Waiting page; the measured full cycle must allocate the second page, preserve reciprocal links and place the retry target at page one/slot zero. All other guards retain distinct future deadlines. The constructor now takes an explicit destination-entry count (`0`, `1`, `32`) and returns distinct key count; the earlier `0`/`1` construction and measured operations are semantically unchanged, so their qualified reachability evidence is reused without claiming a new numeric artifact binding. The native comparison covers all three cases and requires identical source, Run and control results.

This adds full Primary-page occupancy, not maximum encoded control-cell content, mixed Primary/Reference pages, reference reclamation or a complete Waiting envelope. The native three-case comparison passes in 71 seconds including compilation. Bind benchmark SHA-256 `2231ee1c50d05ad4aca71866ad404262f88d469fdbecee1849b8e81b49a81d1a`, including explicit post-measurement byte equality of the original page entries and unchanged scan cursor. Qualify the new point using `./scripts/benchmarks.sh --extra --steps 2 --repeat 1 --min-duration 0 --extrinsic scheduler_opening_retry_full_waiting_page --output /tmp/deos-exp29-waiting-page.rs --json-file /tmp/deos-exp29-waiting-page.json pallet_deos_actors`. Keep the earlier two artifacts at their own Wasm identity, and do not infer RefTime ordering or isolated costs across these probes.

The full-page Wasm probe passes: build 111 seconds, execution 23 seconds. Source content/modes `152a67f24b282b93e0cbf710c7a2fa5f483ef632f0e246f87fd81df093b9168a`, benchmark hash above, Wasm SHA-256 `281afd5fd952a1cdadbf37a88f292fafad83fa82632b23da43a385b117fb18cb`. The same 9,984 guards now own 9,953 keys because 32 share the target deadline. The target raises that deadline's occupancy to 33 on two linked pages without inserting a heap key. Verification preserves every encoded original page entry and its scan cursor, checks both end links and the target's new locator. The trace reads/writes two `ActorWaitingFrameChunks` and one occupancy/tail, reads one head/reverse index, and performs no heap-page/length access. This is the full-primary-page allocation witness under the declared ordinary guard Contract.

| Full-page probe | Time sample / storage-root ns | DB sample / storage-root ns | Unique / repeat reads | Unique / repeat writes | Recorded / estimated proof bytes | Generated model SHA-256 | Raw JSON SHA-256 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Existing deadline, full first page | 910,466 / 7,683 | 1,134,240 / 7,193 | 20 / 43 | 11 / 0 | 27,125 / 33,065 | `c94c7cb3cda807d0e0f805db393f0b6570365d651e5cbdea0d14cbee86a30ec5` | `080ae96ffc9db413e23d9a9c74cf31b114cf6d0ac94f1c963cce711f78f31b63` |

One timing and one database sample are retained; the generated fragment stays uninstalled. Its different Wasm and key population prohibit an isolated timing/proof subtraction against the earlier pair. Production coverage still requires maximal admitted control-cell content, mixed Primary/Reference pages, competing removal/reclamation and Tick branches, and EXP-0028 / EXP-0063 ownership reconciliation.
<!-- extracted:end -->

<!-- extracted: EXP-0029:158-180:50 -->
### Saturated Upward and Tail Removal Qualification

The existing native upward-removal witness supplies the next legal-state construction: create every remaining System Actor through ordinary admission and Manual readiness into future windows. Assign unique Block deadlines in heap-index order, with one low-valued root subtree and one high-valued opposite subtree; no heap insertion needs a swap. Select the subtree containing the last key as low, then remove a deep opposite-subtree key. The low tail must move to the opposite subtree root, shifting only its ancestor path down. A second public-close probe removes the heap tail itself, requiring no repair. Both preparations retain genesis and fill the same active/identity/sovereign capacity intersection.

Share only enough Actors at the first deadline to leave exactly one key on the final heap page. Both closes must remove that page, the target's sole Primary Waiting page and its directory/key, preserve every expected surviving heap entry, and pass native full-state plus Wasm explicit index/order checks. The target is an Idle System Actor with a pending Manual latch and an authored window; this differs from the Running Tick-reference cleanup above and does not supply User hold, maximal-cell or isolated heap-cost evidence.

Ordinary admission rejects the old witness's large numerical deadlines with `ExecutionDelayTooLong`: the mock horizon is 5,000 blocks, whereas its direct wakeup placement did not install those deadlines as authored windows. Preserve the same heap order using root 10, low-subtree `K + 10 + index` and high-subtree `2K + 10 + index`, where `K` is the distinct key count; require `3K + 10` to fit the host horizon. This corrects fixture reachability without changing temporal bounds, population, heap branches or protocol semantics.

Qualification uses one freshly built reference Wasm for `close_actor_full_waiting_upward` and `close_actor_full_waiting_tail`: `DEOS_VERBOSE=1 ./scripts/benchmarks.sh --extra --steps 2 --repeat 1 --min-duration 0 --extrinsic NAME --output /tmp/deos-exp29-fullheap-BRANCH.rs --json-file /tmp/deos-exp29-fullheap-BRANCH.json pallet_deos_actors`, substituting the exact name and `upward`/`tail`, with `--skip-build` for the second. Retain verbose logs for population assertions. One timing and one database sample per branch decide constructive reachability and access coverage, not statistical RefTime ordering or a production envelope; desktop background activity remains uncontrolled and no own heavy work overlaps sampling.

The corrected all-feature native `full_waiting_heap_close_covers_upward_and_tail_removal` passes both branches in 43 seconds including compilation. It admits 1,024 ordinary windowed Actors, with 993 distinct keys and 32 Actors sharing the root deadline; either close removes the one-entry 32nd heap page, leaving 992 keys. Prepared source content/modes `c4d04a84da191699ec761aa7765955581e2ccc9be4a032cafad44dcbfbc67efb`, benchmark SHA-256 `1eaf096801b18de020a3e3e8422058f8cd251e2f7d2969ce31d73f50e2d76190`.

Both reference probes pass on Wasm `0d907492eb8c9c924269cb52e73eed4ae80a22a6e04b7c205981a5a099286b98`: build 104 seconds, each execution 24 seconds. Each of the three preparation passes logs 9,985 guards and 9,985 keys; upward removal starts at index 8,190, tail-only at 9,984. Both leave 9,984 keys and reclaim heap page 313. The upward tail moves to index two through eleven swaps; the tail-only profile preserves every other key's position. Exact surviving pages, directories and inverse indices satisfy the postconditions. Verbose log SHA-256: upward `3d8bf1bd4c183bf8975a334169481b70c6d2cb1adf7a575d9c0897fab12a13ed`, tail `da7ba525446c915fecf118bf00d1b9f22081e3d87acffff25901961023e04160`.

| Branch | Time sample / storage-root ns | DB sample / storage-root ns | Unique / repeat reads | Unique / repeat writes | Recorded / estimated proof bytes | Generated model SHA-256 | Raw JSON SHA-256 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Upward repair and tail-page reclamation | 1,457,471 / 7,613 | 1,730,625 / 8,101 | 55 / 69 | 39 / 0 | 42,947 / 76,112 | `90c79138e75e3e547c3e06400ab9208eb460ca404f970d43024c3d8f638cfccf` | `7bf8844b4ada69b196d4205a13097b767757e139ccb92da08372fdd8dd22ffdc` |
| Tail-only reclamation | 673,909 / 8,032 | 888,744 / 7,194 | 22 / 43 | 18 / 0 | 17,565 / 21,030 | `f5515d4532f461d4b17bed01ff1f481efaa74f614793bdda0bd2e1eeb4f37718` | `872cf5750cc84754841185d305a8cfad9e0363ab4df542c0c7c3ec68e6ec4874` |

The upward trace reads/writes ten heap pages and 13 reverse indices, with 13 occupancy reads; tail-only reads/writes one of each. Both delete one Primary Waiting page and its occupancy/head/tail/key and include public-close Contract/lifecycle work. These same-Wasm branch observations qualify complete legal population and the chosen repair histories. One sample cannot establish RefTime dominance or a maximum over every index/page layout; no fragment is installed or combined additively with other close work. Together with the prior Tick-root witness they provide complete-state upward/downward/tail prerequisites later consumed by Accepted EXP-0063's numeric envelope and page/clock/phase reconciliation.

Canonical formatting, workspace/all-target/all-feature Clippy (27 seconds), 28-record normalization/dependency validation and diff checks pass on the prepared benchmark digest. Existing unrelated benchmark preflight binding failures remain G5 work; the fresh full Wasm build verifies this changed diagnostic compilation without repairing generated storage annotations manually.
<!-- extracted:end -->

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

Ordinary creation, readiness, retry and public close construct existing-key, new-minimum, full-first-page/second-page, upward-repair and tail-only Primary histories at the declared saturated host populations. Every witness preserves reciprocal pages, locators, directories, heap order and surviving entries; no scheduler state is injected. Current-tree all-feature `waiting_` validation passes all 37 selected tests.

## Interpretation

These witnesses close legal Primary population reachability and removal topology. Their complete-cycle timings are not isolated heap coefficients, and maximal encoded-cell publication plus numeric branch composition remain EXP-0055/EXP-0056/EXP-0063 concerns.

## Decision

Accepted for the declared ordinary-construction mock/reference populations. Waiting Primary publication reaches existing, new-minimum and full-page destinations, while public close reaches upward-repair and tail-only reclamation histories. This satisfies EXP-0029 O5 without installing diagnostic fragments or claiming a production envelope.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. Do not infer a new selection from record relocation or from aggregate coefficient subtraction.

## Validity

- `Establishes`: Legal Waiting Primary publication and removal populations for the declared saturated mock/reference histories, constructed through ordinary lifecycle operations.
- `Does not establish`: Complete production Weight, exact release artifact closure or integrated throughput.
- `Known limitations`: Maximal encoded control-cell content, mixed Reference pages and numeric production composition remain owned by EXP-0055, EXP-0056 and EXP-0063.
- `Invalidation triggers`: A changed measured body, host domain, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Follow the owning physical boundary, not a benchmark name.

## Follow-Up / Next Gradient

Stop when the declared proof is satisfied. Transfer any independent owner, selector, parameter domain or artifact-identity question before investigating it beyond the smallest classifier. A hypothetical corner case alone is not a measurement mandate.

## Architecture Impact

No runtime, semantic, storage or production binding change. C1 and A0 remain frozen; this record owns only its declared proof evidence.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0029](./EXP-0029.md) O5
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md), [EXP-0049](./EXP-0049.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: [EXP-0049](./EXP-0049.md)
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0029](./EXP-0029.md), [EXP-0055](./EXP-0055.md), [EXP-0056](./EXP-0056.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
