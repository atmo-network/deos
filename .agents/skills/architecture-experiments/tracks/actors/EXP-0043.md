# EXP-0043 — Window Expiry Close Ownership

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0028](./EXP-0028.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | Window expiry source consumption and custody-neutral close have one complete terminal Control owner. |
| Physical mechanism | Window Expiry Close Ownership |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0028](./EXP-0028.md) |


## Decision Question

Window expiry source consumption and custody-neutral close have one complete terminal Control owner.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns window expiry close ownership. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0028 / “Window Expiry Source and Custody-Neutral Close”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

The retained reference-Wasm `scheduler_wakeup_zero_step_user_expiry` profile directly attains the declared unsignaled Immutable zero-Step User expiry through its singleton Block primary. It is the candidate complete source-consumption and custody-neutral terminal owner for this admitted domain; outer clock selection and competing populated Waiting/heap envelopes remain independently owned.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Pre-fission baseline `a60de7748132211d9e58cecd5be82b41f76ce230`. Each extracted block retains its own exact source/Wasm/output identity. Shared pinned hardware/toolchain/cache controls follow [EXP-0030](./EXP-0030.md#environment-and-artifact-identity) only where the original comparison explicitly reuses them. A shared environment never substitutes for the block’s own source or Wasm identity. No runtime artifact was rebuilt during migration.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | Window expiry source consumption and custody-neutral close have one complete terminal Control owner. | One unsignaled Immutable zero-Step User expiring through its Block primary before Pipeline admission | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Satisfied for the admitted reference domain by the attained singleton-Block expiry witness |

## Measurements

<!-- extracted: EXP-0028:556-573:43 -->
### Window Expiry Source and Custody-Neutral Close

The profile qualifies the complete single-key Block source-consumption/WindowExpired atom used by the due worker, excluding its outer clock selection and Weight meter. It ordinarily creates an unsignaled Immutable zero-Step Manual User with a finite window, advances the real host clock to expiry, preserves other host deadlines, and transacts canonical key drain, loaded expiry classification and consumed-state close. Postconditions require released process/hold/deadline state, no Run or Pipeline receipt and unchanged sovereign/Fee Sink balances. The singleton Block key is a declared geometry, not a maximum heap/Waiting claim; EXP-0063 retains selection and competing physical envelopes.

The first Wasm setup failed before measurement: advancing Timestamp across Aura slots without a matching header left CurrentSlot at zero while the timestamp required slot eight. Failed source `ed755398490b4dabe13c4dc01f8791b590f4c42aeaa6db2ef3e96849f68b103e`, Wasm `49e888bc4d779f9191f3f1c7eaffebef1fd7dd90153fbddf480dfb9b9f6b4b25`, log SHA-256 `ffc90b7c6031a640b7320173b0d870bafe2290889963878e89eceb19bbe4be87`. No numeric result from that run is accepted. The reference benchmark clock helper now begins the next fixture block through public System initialization with an Aura pre-runtime digest, then invokes Aura's ordinary initialization hook before Timestamp; it never writes CurrentSlot directly. Callers leave block advancement to that helper, and the mock requires strictly increasing clock/block time.

| User expiry profile | RefTime before DB additions / minimum (ps) | Estimated / recorded proof (bytes) | Reads / writes | Generated fragment SHA-256 |
| --- | --- | --- | --- | --- |
| `scheduler_wakeup_zero_step_user_expiry` | 261,001,000 / 239,140,000 | 6,367 / 2,902 | 24 / 21 | `f04d2a4a1b4b84e5062ce5579e9fe9e491438b214bf22e7ad1f9564b138460d9` |

Fresh reference-Wasm admission, measurement and postconditions pass at 50 steps × 20 repeats. Source content/modes `1ee040699041b0cb305f7892359de7ec0ebf48a29312681b3eb310f6e7899765`; benchmark source SHA-256 `4bf787dcca8a8ac8bf7e17e3a2e88eb379de68bda991b8ab23e18ae922019fca`; reference helper source SHA-256 `c75d1ac5e2ae0a842518d2b5d076d9749d9e1cb33e1e927b46ab98d07bcc7af9`; benchmark Wasm SHA-256 `bff209117e546d373142e7a86204903a8bcf58d482f82125bea416c76e4e1847`. Same pinned toolchain/hardware/cache/heap/map controls as above and no concurrent agent build/test/benchmark job during measurement. Reproduce with `scripts/benchmarks.sh --extrinsic scheduler_wakeup_zero_step_user_expiry --output /tmp/deos-exp28-zero-step-window-expiry.rs pallet_deos_actors`; build took 107 seconds and measurement 14 seconds. The fragment is uninstalled and excluded from default production generation.

The trace includes Waiting chunk/head/tail/occupancy and cursor removal, full lifecycle/index cleanup and User hold release. Its sole System account read/write belongs to hold release, with no sovereign-to-Fee-Sink collection. Source removal and cleanup are already inside this inclusive atom and must not be added again. An unsignaled Idle expiry does not establish Ready/Running/Suspended expiry bounds, maximum header or detector cleanup, nor competing populated-heap branches; complete envelopes remain open.

Shared clock validation on this same fresh Wasm separately passes `scheduler_inner_zero_step_user_at_time` and `scheduler_inner_zero_step_user_cadenced` with `--skip-build --steps 2 --repeat 1`, using `/tmp/deos-exp28-clock-at-time-check.rs` and `/tmp/deos-exp28-clock-cadenced-check.rs`. Each took 14 seconds. These are verification-only runs for repeated-clock/no-recurrence and same-slot/rearm behavior; no new performance coefficients are inferred, and earlier measurements retain their original source identity. Clock setup stays outside all measured atoms, and none of these fixtures claims full Executive execution.

Retained-source validation: native all-features `zero_step_user_` tests pass (32 seconds), workspace all-features Clippy passes (25 seconds), and canonical formatting, workspace check, changed-scope completion (47 seconds), record normalization/dependency graph and diff checks pass. Completion skips runtime tests for the benchmark-only runtime diff, reusing unchanged production behavior. Preflight compilation passes (61 seconds), retaining the known stale generated-storage rejection. Source content/modes remain identical after context synchronization; G5 and complete numerical acceptance remain open.
<!-- extracted:end -->

### Deciding Window-Expiry Owner

The attained reference-Wasm profile directly exercises O1's smallest falsifier. An ordinarily created unsignaled Immutable zero-Step Manual User reaches expiry through its singleton Block primary before Pipeline admission. The measured transaction consumes the Block source and its Waiting chunk/head/tail/occupancy/cursor authority, classifies the loaded state as expired, removes lifecycle/process/deadline state and releases the applicable User hold. It produces no Run or Pipeline receipt, preserves sovereign and Fee Sink balances and performs no custody movement.

The generated fixed vector is `261,001,000` pre-database RefTime, `6,367` estimated ProofSize, 24 reads and 21 writes. Under the pinned reference RocksDB charge, complete RefTime is `2,961,001,000`. Source consumption and close cleanup are already inside this inclusive atom and must not be added again. The clock helper, outer due-selection work and competing populated Waiting/heap branches are excluded; EXP-0063 owns those envelopes and their overlap attribution. Ready/Running/Suspended lifecycle branches remain owned by their respective phase experiments rather than broadening this expiry owner.

## Derived Metrics

The selected RocksDB total is `261,001,000 + 24 × 25,000,000 + 21 × 100,000,000 = 2,961,001,000` RefTime. This is an inclusive owner, not a source-removal or hold-release component inferred by subtraction.

## Statistical / Variance Notes

The fixed model comes from the retained fresh reference-Wasm run at 50 steps × 20 repeats. Its `239,140,000` minimum and `2,902` recorded proof remain diagnostic observations; the generated upper model and estimated proof define the selected reference owner. No samples are removed.

## Result

The attained singleton-Block witness satisfies the declared window-expiry falsifier and establishes custody-neutral source consumption plus terminal cleanup over the admitted unsignaled Immutable zero-Step User domain.

## Interpretation

Window expiry in this domain is one inclusive terminal Control atom. Outer clock selection and populated Waiting/heap alternatives are separate composition inputs, not missing parts of this inner owner and not permission to duplicate its source or cleanup work.

## Decision

Accepted for the admitted reference window-expiry domain. Select `261,001,000` pre-database RefTime, `6,367` estimated ProofSize, 24 reads and 21 writes (`2,961,001,000` RefTime under the pinned RocksDB charge) for the unsignaled Immutable zero-Step User singleton-Block expiry owner. Maximum and actual use the same inclusive atom. EXP-0065 owns exact regeneration; the diagnostic fragment remains uninstalled.

## Rejected Alternatives and Tradeoffs

Reject the failed Aura-clock setup and its absent numeric result; treating the singleton source as a maximum populated-heap claim; broadening this owner to Ready/Running/Suspended expiry; adding separate generic source removal, lifecycle cleanup or hold release to the inclusive atom; inferring components by aggregate subtraction; and manually patching production Weight before EXP-0065.

## Validity

- `Establishes`: One attained inclusive expiry owner over the admitted unsignaled Immutable zero-Step Manual User, singleton Block primary, canonical source removal, lifecycle/process/deadline cleanup and applicable User hold release.
- `Does not establish`: Outer clock selection, populated Waiting-page/heap alternatives, Ready/Running/Suspended expiry, maximum headers or detector cleanup, exact final production Weight identity or integrated throughput.
- `Known limitations`: The selected model remains diagnostic and uninstalled; EXP-0063/EXP-0064 must preserve outer-envelope attribution before EXP-0065 regeneration.
- `Invalidation triggers`: A changed expiry body, source/cleanup/hold path, admitted state domain, source/Weight/Wasm identity or selector rule requires scoped requalification.
- `Residual qualitative insight`: A narrow attained terminal atom can settle its owner while independently larger outer scheduler envelopes remain separate.

## Follow-Up / Next Gradient

Stop measuring this satisfied window-expiry owner. EXP-0063 owns outer clock and populated Waiting/heap alternatives, EXP-0064 composes admission and EXP-0065 regenerates exact artifacts. Reopen only for the declared falsifier or a changed relevant path, domain, selector or identity.

## Architecture Impact

The reference window-expiry input now requires one inclusive owner covering singleton Block-source consumption, expiry classification, terminal cleanup and applicable User hold release without duplicate charges. No runtime, semantic or storage change is introduced; C1 and A0 remain frozen, and complete production regeneration remains G5 work.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0028](./EXP-0028.md) O14
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: None.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0028](./EXP-0028.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
