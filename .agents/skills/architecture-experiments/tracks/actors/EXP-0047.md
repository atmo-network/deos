# EXP-0047 — Funding Snapshot Consumption Bound

| Field | Value |
| --- | --- |
| Status | Accepted |
| Record kind | Leaf |
| Former IDs | None |
| Parent question | [EXP-0028](./EXP-0028.md) |
| Architecture release / experiment campaign | 0.7.25 frozen C1 production evidence |
| Date | 2026-09-06 |
| Affected domain | The funding snapshot consumption component covers the legal authored tracked-asset domain with certified ingress. |
| Physical mechanism | Funding Snapshot Consumption Bound |
| Primary track | [actors](./experiments.md) |
| Related tracks | None |
| Baseline experiment | [EXP-0025](./EXP-0025.md) — Accepted, physical architecture only |
| Successor experiment | [EXP-0028](./EXP-0028.md) |


## Decision Question

The funding snapshot consumption component covers the legal authored tracked-asset domain with certified ingress.

## Governing Specification

- `Owners`: `template/pallets/actors/docs/specification.en.md` (sole ownership, lifecycle/Trigger/Pipeline/Action semantics, placement and scheduler); `docs/actors-resource-policy.specification.en.md` (component-wise admission/actual settlement, equal-thirds block policy); `docs/actors-performance-assurance.specification.en.md` (evidence and target).
- `Frozen physical input`: [EXP-0025](./EXP-0025.md) C1 Architecture Freeze. No change to FIFO, ticket chronology, Q1, causal floor, primary/reference ontology, source/successor atomicity or sole execution owner.
- `Semantic oracle`: A0 SHA-256 `8746e423168309975e509be91e4eb06fdf0eb0b43369d75f1ee62202a4341678` remains frozen; use only for relevant semantic equivalence, never as a Weight oracle.

## Context and Hypothesis

This Leaf owns funding snapshot consumption bound. Its boundary is the single claim above. Evidence was extracted from the explicitly marked pre-fission parent subsections at Git baseline `a60de7748132211d9e58cecd5be82b41f76ce230`; this migration changes navigation and evidence ownership only. Source, Wasm, commands, rejection outcomes and evidence limitations retain their original meaning.

Extraction provenance at the recorded Git baseline: EXP-0029 / “Retained Correction Evidence”, “Environment and Artifact Identity”. Original directional prose (above/below) belongs to these pre-fission subsections; current cross-owner navigation follows the parent proof map.

## Baseline and Candidates

Retained measured implementations and rejected candidates are identified in the extracted evidence. No candidate is introduced by this migration.

## Controls

Only the declared witness input varies within each source-bound comparison. Preserve the exact host, toolchain, database, Wasm and sampling controls printed with each evidence item; do not compare unlike artifacts as a controlled performance result.

## Benchmark Class, Workloads, and Criteria

Acceptance requires the single claim to hold over its declared admitted domain, with each physical operation charged once and every retained limitation resolved before broader production use. One legal omitted-owner, domain or containment counterexample falsifies that claim. Existing workloads below are bounded witnesses, not permission to add independent questions.

## Environment and Artifact Identity

Extraction baseline: `a60de7748132211d9e58cecd5be82b41f76ce230`. The migration manifest records exact original content/mode identities and source spans. Question-specific source/Wasm/output identities remain inline. Historical fragments remain historical; no new runtime or production artifact was built.

Use the identified environment attached to each extracted source. Where a shared control is needed, follow its explicit evidence source; absent identity requires refresh before acceptance.

## Proof Obligations

- `Freeze`: Frozen by explicit migration reconstruction from the pre-fission decision and retained witnesses at a60de7748132211d9e58cecd5be82b41f76ce230. No new measurement or historical decision is implied.
- `Review triggers`: None.
- `Decomposition review`: Boundary reviewed during provenance-preserving fission. Workloads within this Leaf share the declared claim, physical owner, changed variable, evidence domain and acceptance rule. Independently decidable owners were transferred; a new owner or selector requires another node.

| Obligation ID | Claim | Smallest falsifier | Evidence class | Owning Experiment | Required/conditional | Downstream consequence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O1 | The funding snapshot consumption component covers the legal authored tracked-asset domain with certified ingress. | One admitted tracked asset funded by certified Signed ingress, then consumed by funding_snapshot_open | Reference-Wasm FRAME evidence in the declared owner/domain; native checks are construction controls | Self | Required | Named parent decision and declared downstream consumers | Satisfied for the reference 1..24 authored-source domain by certified Signed ingress and the complete generated component model |

## Measurements

<!-- extracted: EXP-0029:301-318:47 -->
### Pre-fission EXP-0029 lines 301–318

Reference probe command: `./scripts/benchmarks.sh --extrinsic funding_snapshot_open --output /tmp/deos-exp29-funding-snapshot.rs pallet_deos_actors`, 50 steps × 20 repeats. The first run built successfully and failed before a Weight model was generated; failure-log SHA-256 `4c715e70f2220fd4f025c608cee9771b687f66617ae56959874b0b6379b3a5c7`. Signed-origin setup models the certified producer, not full Executive/transaction-fee composition. Unsupported embedding hosts fail closed through the benchmark-helper default instead of assigning Signed provenance to arbitrary internal transfers.

The corrected reference-Wasm run passed the complete declared `a = 1..24` domain, including the maximum 12-Step, 24-source Contract. It used compiled Wasm on AMD Ryzen 7 4800H, DB cache 1024, 4096 heap pages and worst-case map size 1,000,000. Build and measurement followed completed native checks without competing test/Clippy jobs. Native `deos-runtime --all-features --test-filter signed_` and workspace Clippy also passed after the host-helper correction.

| Component / legal domain | Generated RefTime, ps | ProofSize, bytes | Database work |
| --- | --- | --- | --- |
| `funding_snapshot_open(a)`, `1 ≤ a ≤ 24` | `14,154,244 + 288,800a`; slope standard error 4,245; minimum observed execution 12,432,000 | Measured `631 + 26a` (657..1,255); estimated 4,531; ActorFunding MaxEncodedLen 1,066 | ActorFunding: one read, one write; configured DB Weight added separately |

This proves the component's input construction on the reference benchmark runtime. It does not certify the complete Opening envelope, replace User hold/fee work, prove values `25..40` reachable, or demonstrate a performance gain from smaller logical cardinality: the estimated proof still follows the storage encoding bound. EXP-0028 / EXP-0064 must reconcile this component with complete phase ownership and admission before EXP-0065 installs regenerated complete files.

| Additional verification | Outcome / scope |
| --- | --- |
| Same all-feature test route with `--test-filter reachable_update_allocation_boundaries_match_authored_contract` | Passed after adding funding-boundary rejection; accepted allocations reuse existing lifecycle construction |
| From `template`: `SKIP_WASM_BUILD=1 cargo test --locked -p pallet-deos-actors --lib --features try-runtime try_state_ --message-format=short` | 22 passed, one ignored; audit tests compile and execute without runtime-benchmarks |
| `./scripts/ci-local.sh --only clippy` | Workspace/all-target/all-feature `-D warnings` passed after the last Rust edit |
| `./scripts/benchmarks.sh --check` | Compilation passed; final storage-name audit failed on unchanged generated Actors/Router/TMC Weight referencing retired storage. This remains EXP-0065/G5, not candidate compilation failure or permission to repair generated comments manually |
| Changed-scope completion | Passed, including the complete default-feature runtime test suite; the retained audit-loop refinement also passed the focused all-feature `try_state_` tests |
<!-- extracted:end -->

<!-- partitioned: EXP-0029:67-83:47 -->
### Funding Measurement Environment

| Artifact | Identity / required binding |
| --- | --- |
| Baseline source | Commit `5b46b19004b62a723d6c45a610dfe1e5c79982b0`; template tree `98af72ffbb55fe8840b5bb44b5b01d65c8e775e1` |
| Measurement source/config/toolchain | Rust 1.94.1, frame-omni-bencher 0.22.0; worktree source identity `33ca329a7e40d826f7ec38afa72fbb51ab92dca7a19e693ef4169d6b4769737d` from `scripts/actors-assurance.sh::worktree_evidence_identity`; content and modes, independent of commit packaging |
| Measured fixture source | SHA-256: pallet `src/lib.rs` `78d113fb1592c516fc29a713601af8edc0780c014f8653e26abeaabff0a3b44f`, `src/benchmarking.rs` `4adba7cc4918f8c22d0209592caf7f2db855099e818032d5f11e8d844e626e52`, runtime `configs/actor_config.rs` `7216c72353c9ce45abaf4392e26019e76fa3fdbe88b7f1dd271884191237e4d5` |
| Funding audit-only refinement | Source `009d6cd04232b3c712ae9e7cd01713e855dfc1f5fc78a39524e1e5946f27f059`; pallet lib `4d081f6cb7cf39f6b30c95f023d826acbe9c17e977e977081c865bc859e7d56b`. Re-expressed the same zero/untracked check as bounded borrowed iteration inside the final `cfg(try-runtime)` function. Reversing only this change reconstructs the measured lib hash exactly; the benchmark profile excludes that function, so the original funding evidence remains applicable to that refinement |
| Benchmark Wasm | Successful signed-producer candidate: `2346a0865075cdefc710b1e056739dfde5b57282a30c362535af58e5ef43f5c4`; first failed probe: `8fa6ab9b0887e3edeb5902205f1176cc7b66b4c6eea44b6bbe822c590f272248` on source `6edb38cffbc9b62831cc7685765da2e851d79e76368cead2b1679f8abb6516ec` |
| Production Wasm | Not produced; absent at the canonical `template/target/release/wbuild/deos-runtime/deos_runtime.compact.compressed.wasm` path during this measurement |
| Generated component fragment | `/tmp/deos-exp29-funding-snapshot.rs`, SHA-256 `b6cbfef3bbb784be419e5f5d8f69823b03c21679d84f335d0e816ce20c3336e7`; normalized model below, not installed as production Weight |
| Commands | Native commands and outcomes below; `scripts/benchmarks.sh --check` checks compilation/storage names only, not measured Weight or production capacity |
| Evidence owner | Baseline source and the question-specific facts below; new measurements must carry exact source/artifact identities in this record |
<!-- extracted:end -->

### Decision-Time Qualification

The corrected generated fragment remains available at its recorded SHA-256 `b6cbfef3bbb784be419e5f5d8f69823b03c21679d84f335d0e816ce20c3336e7`. The `funding_snapshot_open` benchmark body is byte-identical between the retained post-measurement source anchor `c2bb0c34016645a5d859684e86cb38b938ec17a4` and the decision tree (body SHA-256 `08254b3f2d57e71b5eba8f3776d8d2cc31dd8bdf5ae5468d03b9ec59e19671dd`). The production fresh-Opening execution owner and `derive_funding_tracked_assets` algebra are likewise unchanged across that interval, with scoped SHA-256 values `57b8fe64f21a85e39cd3b9e47a2791ccedb17e200982697e5f47f672c4c59af4` and `2322c89f7dc9f30934acefd34f7a7273454c16cf156c7f6d03c196cc25f7024f`.

Accepted EXP-0045 proves `F ≤ 2S` and the reference `S = 12` authored-source ceiling, so `1..24` is the complete nonzero actual funding-snapshot domain even though `MaxFundingTrackedAssets = 40` remains a conservative storage/admission capacity. The benchmark constructs each point through an admitted Contract and certified Signed producer ingress, then consumes exactly the populated `ActorFunding` map. At the attained endpoint `a = 24`, the selected component is `21,085,444` pre-DB RefTime / `4,531` estimated ProofSize plus one read and one write, totaling `146,085,444` RefTime under the pinned RocksDB charge.

This accepts only the consumption component and its actual authored domain. The configured-capacity maximum selector, complete Opening owner, installed stale `1..10` generated coefficient, and exact production-file identity remain EXP-0064/EXP-0065 work; values `25..40` must not be presented as reachable actual snapshots or used to widen this benchmark.

## Derived Metrics

Derived values are retained beside the evidence that defines their source and units. No subtraction of overlapping aggregate owners or cross-artifact timing comparison is introduced.

## Statistical / Variance Notes

Retain each item’s original sampling and uncertainty. Singleton construction probes establish only their admitted state/access witness, never statistical timing dominance or a production bound.

## Result

The corrected reference-Wasm model covers every nonzero actual snapshot cardinality in the admitted reference authored domain: `funding_snapshot_open(a) = 14,154,244 + 288,800a` pre-DB RefTime with fixed `4,531` estimated ProofSize and one `ActorFunding` read/write for `1 ≤ a ≤ 24`. Certified Signed ingress and public Contract admission construct the measured state.

## Interpretation

The legal actual domain comes from authored source algebra, not the wider 40-entry storage capacity. This component consumes and clears accumulated funding only; it excludes Opening snapshots, predicates, task execution, Pipeline collection, User holds, Run publication and outer scheduler work.

## Decision

Accepted for the reference binding's authored funding domain. Select the generated `funding_snapshot_open(a)` component only for a nonempty fresh-Opening funding snapshot with actual `1 ≤ a ≤ 24`; zero selects no component. Use `14,154,244 + 288,800a` pre-DB RefTime, fixed `4,531` estimated ProofSize and one read/write. EXP-0065 must regenerate the complete production Weight file; do not install this fragment manually or generalize it to unreachable `25..40` actual snapshots.

## Rejected Alternatives and Tradeoffs

Retain all rejected candidates and causal limits in the extracted evidence. Do not infer a new selection from record relocation or from aggregate coefficient subtraction.

## Validity

- `Establishes`: The reference 1..24 authored-source funding-snapshot consumption component, certified Signed ingress construction, generated RefTime/proof model and one-read/one-write owner.
- `Does not establish`: Reachability of storage-capacity values 25..40, the complete Opening envelope, configured-capacity maximum composition, exact production Weight, release artifact closure or integrated throughput.
- `Known limitations`: The generated fragment is intentionally uninstalled; the production file still carries a stale 1..10 coefficient and complete binding remains G5 work.
- `Invalidation triggers`: A changed measured body, authored funding algebra, host domain, source/Weight/Wasm identity or acceptance rule requires scoped requalification.
- `Residual qualitative insight`: Actual funding cardinality follows authored LastFunding sources; storage capacity remains a separate conservative input.

Pre-fission EXP-0029 / Retained Correction Evidence retains these causal qualifications:

| Witness | Observation | Classification |
| --- | --- | --- |
| First reference-Wasm funding probe | At `a = 1`, `AssetOps::transfer` moved custody but accumulation remained empty: the runtime supplies `InternalProtocol`, while the fixture's `OwnerOnly` requires `Signed`; the mock had reported Signed | Rejected fixture assumption, not a reason to weaken `OwnerOnly`. Shared funding setup now uses a host signed-transfer helper; the runtime composes the existing ingress extension's prepare, actual dispatch and consequence under one storage transaction |

## Follow-Up / Next Gradient

Stop measuring this satisfied component. EXP-0064 owns configured-capacity admission composition and EXP-0065 owns complete generated-file regeneration. Reopen only if authored funding algebra admits more than 24 actual sources, the consumption body or host encoding changes, or a legal point exceeds the selected component.

## Architecture Impact

No runtime, semantic, storage or production binding change. C1 and A0 remain frozen. The accepted component and actual-domain partition become inputs to phase composition and later complete regeneration.

## Relations

- `Decomposes into`: None.
- `Satisfies obligation`: [EXP-0028](./EXP-0028.md) O16
- `Replaces`: None.
- `Supersedes`: None.
- `Refines`: None.
- `Contradicts`: None.
- `Depends on`: [EXP-0025](./EXP-0025.md), [EXP-0045](./EXP-0045.md)
- `Baseline of`: None.
- `Confirms`: None.
- `Invalidates`: None.
- `Uses evidence from`: [EXP-0045](./EXP-0045.md) authored source algebra and reference-domain bound.
- `Transfers question to`: None; independent findings require an explicit new transfer.
- `Produces input for`: [EXP-0028](./EXP-0028.md)
- `Reopen trigger`: A falsifier of this declared owner/domain or exact artifact identity; cost alone does not reopen frozen C1.
