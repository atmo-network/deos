# DEOS Framework Delivery History

> Canonical complete delivery history for the DEOS ecosystem.
> This file retains completed work that was rotated out of `BACKLOG.md`.

## Delivered

- `GitHub CI now matches the 2603-era runtime/toolchain contract more honestly`: `.github/workflows/ci.yml` and `stress-lane.yml` now pin the runner image to `ubuntu-24.04`, install Rust directly from `template/rust-toolchain.toml` instead of trusting the ambient GitHub image, include the toolchain file in cargo cache keys, use `--locked` cargo invocations, skip unnecessary wasm builds on the ordinary lint/test/stress lanes, and split the benchmark lane into explicit governance-benchmark tests plus runtime-benchmark compilation so SDK/runtime-bench regressions fail earlier and with clearer blame.

- `Governance benchmarking formatting now matches rustfmt again`: `template/pallets/governance/src/benchmarking.rs` now follows the formatter's expected single-line wrapping for the two `ProposalAuthorsByItem::<T>::insert(...)` benchmark setup calls that still diverged locally, so the CI `cargo fmt --all -- --check` gate passes again on the current line.
- `Runtime-benchmark CI is back in sync with the current governance/runtime surfaces`: `template/pallets/governance/src/benchmarking.rs` now seeds the widened invoice-capable `ProposalVotes` state plus current proposal metadata/authorship more honestly, removes the stale `ProposalClass` benchmark residue, and `template/runtime/src/apis.rs` now implements the newer `cumulus_pallet_session_benchmarking::Config::generate_session_keys_and_proof` hook so the benchmark-compilability workflow can build against the current SDK/runtime-benchmarks contract again.

- `Governance overview wiki summaries now follow the stricter one-colon summary rule`: `wiki/overview/governance-overview.{en,ru}.md` now avoid an extra value-side colon in the frontmatter `summary:` line, and `wiki/_meta/navigation.json` plus `wiki/log.{en,ru}.md` were refreshed so the same stricter summary formatting stays aligned across the governance wiki entrypoint.

- `The governance wiki cluster now has a more even terminology handoff`: `wiki/glossary/core-terms.{en,ru}.md` now defines the main governance vocabulary (`Governance Domain`, `Primary Track`, `Protection Track`, `Proposal Payload Kind`, `Execution Authority`, `GovXP`) more explicitly, while `wiki/_meta/{state,aliases}.json` and `wiki/log.{en,ru}.md` were refreshed so governance pages, glossary lookups, and alias search all point at the same newcomer-facing terminology line.
- `Governance wiki pages now separate overview-vs-concept roles much more clearly`: `wiki/overview/governance-overview.{en,ru}.md` now acts as the wide-angle newcomer map for the governance subsystem, while `wiki/concepts/governance-domains.{en,ru}.md` now stays much more strictly focused on domains as typed governance cells that bind subject, power surfaces, payload families, cadence, and execution authority; `wiki/_meta/navigation.json` plus `wiki/log.{en,ru}.md` were refreshed so the governance wiki cluster advertises that sharper split consistently.
- `Governance wiki pages now describe the live governance contract much more concretely`: `wiki/overview/governance-overview.{en,ru}.md` and `wiki/concepts/governance-domains.{en,ru}.md` now explain the current dual-track governance topology, the shipped `3/7/7/3` public cadence, payload-kind semantics, current signed-submission scope, bounded runtime-upgrade authority, and the bounded governance query/read-model surface more explicitly for newcomers, while `wiki/_meta/{navigation,state,aliases}.json` and `wiki/log.{en,ru}.md` were refreshed so governance discovery, summaries, and alias search stay aligned with the current docs/runtime line.

- `Root context entrypoints now describe the shipped 2603 baseline more honestly`: `README.md`, `template/pallets/README.md`, `scripts/README.md`, and the shipped-baseline section of `BACKLOG.md` now say explicitly that the current line is on `Polkadot SDK 2603 / node 1.22.0`, that `system_version >= 3` staged runtime-upgrade behavior is part of the shipped baseline, that the ordinary public governance cadence is already live, and that local binary-download defaults now track `polkadot-v1.22.0`.
- `Pallet source code no longer keeps stale frame::deps upgrade residue`: the proactive 2603 audit found pallet-local legacy dependency shims only in `pallet-tmc`, `pallet-asset-registry`, and `pallet-axial-router`, so those crates now use direct `polkadot_sdk` / `frame_support` / `sp_runtime` imports and trait names instead of `frame::deps::*` compatibility paths in their core source and tests. The rest of the pallet set needed no source-level cleanup beyond the already landed baseline/docs pass, and the full workspace still passes fmt/check/test/clippy after the modernization.
- `Every pallet README now states its 2603 baseline explicitly`: the local README entrypoints for `pallet-aaa`, `pallet-asset-registry`, `pallet-axial-router`, `pallet-governance`, `pallet-staking`, and `pallet-tmc` now all state that they are maintained against the `Polkadot SDK 2603 / node 1.22.0` line, and they also record whether the 2603 upgrade required pallet-local changes or only runtime/session/XCM integration work. This makes the pallet-local upgrade posture explicit instead of leaving the SDK migration story visible only from workspace/runtime files.
- `The obsolete SDK 2512 redirect doc is gone`: `docs/polkadot-sdk-2512.insights.en.md` was removed after the repository-wide switch to `docs/polkadot-sdk-2603.insights.en.md`, since no live docs/wiki entrypoints still depended on the old compatibility stub and the remaining mentions are historical notes only.
- `The reference runtime now targets Polkadot SDK 2603 / node 1.22.0`: `template/Cargo.toml` now tracks the `2603.0.0` umbrella with aligned direct crate pins (`polkadot-sdk-frame 0.15.0`, `cumulus-pallet-parachain-system 0.26.0`, `substrate-wasm-builder 32.0.0`, `sp-arithmetic 28.0.1`), the runtime itself now compiles against the 2603 API surface, `system_version` is raised to `3` so the new staged `:pending_code -> :code` runtime-upgrade path is active, the session-keys runtime API now follows the owner/proof-aware signature, the AAA-aware XCM transactor now matches the new `AssetsInHolding` / transfer return contracts, the stale `AssetClaims` executor hook is gone, and docs/scripts/wiki entrypoints now point at the new `polkadot-sdk-2603.insights.en.md` canonical guidance plus `polkadot-v1.22.0` operator tag defaults.
- `Pane top chrome is now one component again`: the shared top row for tab dragging plus pane dragging now lives in `src/lib/layout/PaneTopChrome.svelte`, replacing the split `PaneTabs.svelte` + `PaneGripPlate.svelte` pair after the grip moved behind the tabs. The pane-drag background and tab-strip interaction now share one explicit component boundary instead of one visual contract being split across two files.
- `Pane grip now lives behind the tab bar instead of below it`: pane tabs and pane-drag grip now share one top chrome row, empty space behind the tabs can drag the whole pane stack, dragging a tab still works from the tab itself, and the grip no longer consumes a second layout row or extra pane height.
- `PaneWidgetHost now shows its outer border only when vertical scrolling is actually active`: the pane widget host now measures `overflow-y-auto` state client-side and keeps the rounded border only while a real vertical scrollbar is needed, removing it again once the content fits without vertical scrolling.
- `PanelHeader and widget-level top header rows were removed from the web client`: the shared UI-kit component is gone, the stale `SwapWidget` import is gone, and the affected widgets no longer keep dedicated top title/subtitle header rows just to host local actions; any still-useful controls now live in the widget body or section actions instead, while the shared UI barrel plus docs/readme no longer advertise `PanelHeader` as a reusable primitive.
- `AutomationWidget now stops crowding its actor headers in the narrowest panes`: dense actor cards now stack the title/role above the status badge before that header row becomes cramped, and the top-level actor-count badge now shortens in compact panes.
- `StatisticsWidget route-mix cards now stop cramping in the densest panes`: the route-mix detail cards now stack to one column before their labels/details collapse into overly narrow mini-columns.
- `SettingsWidget now adapts more honestly inside narrow sidebar lanes`: the endpoint field now stays full-width as the main control, the smaller `Domain ID` / `Sidebar edge` controls share one row once width supports it, and the apply action now uses sidebar width more honestly instead of wasting vertical space by default.
- `AccountWidget now adapts more honestly inside narrow sidebar lanes`: the sidebar account surface now shortens the selected address with full-detail fallback in dense lanes, switches preset and injected-account lists to denser multi-column grids earlier once width supports them, and keeps the custom-account handoff row horizontal only when the sidebar is actually wide enough instead of wasting vertical space by default.
- `WikiWidget now gives the generated wiki metadata a bounded discovery-and-provenance layer`: the web client now adds a client-side filter over `wiki/_meta/navigation.json` plus `wiki/_meta/aliases.json`, matching localized title/summary/path text and canonical term aliases with explicit live result counts and empty-state messaging while still telling the user that the current surface is generated-manifest search rather than full-text archive search; search results now also expose alias-hit hints so that metadata contract stays legible, trusted internal wiki links now also surface generated-summary hover previews so nearby concepts can be inspected without losing reading context, the selected page now exposes graph-derived related-page navigation with relation labels from `wiki/_meta/graph.json`, the reader now surfaces compact compiled provenance (`status`, `confidence`, generation time, source-doc copy targets) from `wiki/_meta/state.json`, and non-dual reader mode now keeps the markdown body ahead of those secondary context cards so content stays first in tight panes.
- `Template workspace entrypoints now mirror the latest AAA framing`: `template/README.md` and `template/pallets/README.md` now describe `pallet-aaa` as a deterministic actor runtime with bounded scheduling, triggers, lifecycle, and execution plans, so the local Rust-workspace entrypoints stay aligned with the updated AAA contract language.
- `AAA documentation framing is now more balanced again`: the latest README/wiki/spec wording now presents event-driven triggers and actor-graph composition as one important dimension of AAA while restoring equal emphasis on deterministic scheduling, bounded execution, lifecycle, fee admission, and the scheduler contract.
- `The AA-Actor wiki overview now shows simplified single-actor configuration examples`: `/wiki/overview/aa-actor.en.md` and `.ru.md` now include conceptual pseudo-config examples for timer-driven, balance-ingress-driven, and graph-composed actors so the single-instance mental model is easier to grasp.
- `AAA docs now explain the actor system as event-driven graph reconfiguration rather than mere scheduled repetition`: `template/pallets/aaa/README.md`, the root/docs entrypoints, both AAA specifications, and the `AAA System` wiki overview now state more clearly that balance ingress can function as trigger-messages, actor graphs compose into larger protocol behavior, and many workflow/topology changes should be expressed as on-chain actor-graph reconfiguration rather than runtime rewrites.
- `Root context entrypoints now acknowledge the generated wiki layer explicitly`: `README.md` and `docs/README.md` now link the generated `/wiki` as the newcomer-facing navigation layer while keeping `/docs` as the authoritative source of truth, so the human entrypoint graph matches the repository's current docs/wiki reality.
- `Runtime-core wiki wording now matches two critical protocol truths better`: the `Axial Router` overview now states explicitly that canonical swap execution cannot bypass the router, while the `AAA System` overview now emphasizes balance-ingress events as trigger-messages plus the emergent behavior that appears when actor graphs are composed.
- `The Russian wiki now reads much more naturally across nearly the entire knowledge surface`: the remaining major `/wiki/**/*.ru.md` entrypoints and concept/overview/process pages were rewritten into more idiomatic, human-facing prose — including governance, asset identity, randomness, runtime patterns, repository/tooling pages, formulas, development workflow, scripts, and the root index — while keeping the same contracts, sources, and navigation graph.
- `Wiki concept pages now explain the economic model in more natural language`: `/wiki` now rewrites `TMCTOL Standard`, `Governance Domains`, and `Staking Pools` in clearer newcomer-facing prose, links them more explicitly to `Axial Router`, `Token Minting Curve`, and the broader DEOS framework story, and refreshes the shared wiki navigation summaries to match.
- `Wiki onboarding prose is now more natural and newcomer-oriented across the main entry pages`: `/wiki` now explains `/docs` vs `/wiki`, `AAA` vs `AA-Actor`, read-model honesty, and the reference-client contract in clearer language across `First Steps`, `Newcomer FAQ`, `Token-Driven Automation`, `Read-Model Split`, `Reference Client`, and `Core Terms`, while the shared wiki metadata summaries were refreshed to stay aligned.
- `Wiki newcomer coverage and wording are now stronger on the runtime core path`: `/wiki` now includes first-class overview pages for `AAA System`, `Axial Router`, and `Token Minting Curve`, the main overview/index pages were rewritten in more natural newcomer-facing language, and the shared wiki metadata (`navigation`, `aliases`, `graph`, `state`, `locales`) now reflects the clearer AAA system-vs-instance distinction plus the new runtime subsystem entrypoints.

- `Wiki now comprehensively covers missing repository context areas`: the generated wiki has been expanded with new detailed EN/RU documentation blocks covering technical implementation (`Repository Structure`, `Tech Stack`), mathematical formulas (`TMCTOL Formulas`), practical usage (`Scripts Layer`), development/process (`Three-Layer Validation`, `Development Status`), and community governance (`Contributing Guidelines`). The new structure and `_meta/navigation.json` align perfectly with the authoritative `/docs` specifications and repository layout.
- `WikiWidget now renders trusted repo-local markdown directly in the SPA client`: the web client now uses `marked` to load/render selected wiki pages from the generated repo-local markdown tree, `.agents/skills/wiki-sync/scripts/validate-wiki-trust.sh` now enforces the no-raw-HTML / no-dangerous-URL / no-inline-handler trust contract for `/wiki/**/*.md`, and the web-client/wiki-sync/AGENTS docs now record that repo-validation boundary explicitly instead of implying runtime sanitization.
- `The generated wiki no longer uses the redundant /wiki/pages wrapper`: wiki taxonomy directories now live directly under `/wiki` (`overview/`, `concepts/`, `faq/`, `glossary/`, `comparisons/`, `getting-started/`), metadata paths were rewritten to match, and the wiki-sync/AGENTS contracts now explicitly prefer direct taxonomy directories over an extra content wrapper.
- `The generated wiki now uses locale-suffixed markdown plus mirrored Russian pages`: `/wiki` now stores human-facing pages as `*.en.md`, mirrors the current graph as `*.ru.md`, keeps `_meta/*.json` shared with language-keyed localized fields instead of per-locale JSON duplication, and the wiki-sync skill now treats locale mirroring as part of the generated contract.
- `The generated wiki now covers the main first-class newcomer clusters`: `/wiki` now also includes `Asset Identity`, `Randomness Strategy`, and `Runtime Patterns`, while `BACKLOG.md` retargets the remaining wiki work away from raw coverage expansion and toward future taxonomy additions only when real frontend/navigation pressure appears.
- `The generated wiki now has dedicated AAA and governance overview pages`: `/wiki` now includes first-class `AA Actor` and `Governance Overview` nodes, `BACKLOG.md` now tracks the remaining wiki coverage work as an explicit open slice, and the wiki index/backlinks/metadata graph were refreshed so newcomers can navigate core automation and governance contracts without jumping straight into raw specs.
- `Wiki terminology now avoids the redundant doubled-actor label`: the generated wiki overview node, links, and metadata now use `AA Actor` for the human-facing page title while preserving `AAA` as the runtime acronym for `Account Abstraction Actor`.
- `Wiki wording now follows the uppercase list-item style more consistently`: after the `AA Actor` rename, the remaining human-facing wiki prose lists were normalized so visible markdown bullets start with uppercase wording across the generated onboarding/concept/overview pages.
- `The browser now ships the first tactical treasury submit surface too`: `GovernanceWidget` no longer stops at advisory-only public submission once tactical `$BLDR` `L2TreasurySpend` became signed. It now exposes a minimal invoice-composition form with beneficiary / payout-asset-id / base-amount inputs, derived payload bytes/hash, and the same preimage-review path used by the advisory flow.
- `Tactical L2 treasury spend is now publicly submittable on the current line`: the runtime now treats tactical `$BLDR` `L2TreasurySpend` as a signed public submission kind with the same opening-fee contract as the earlier advisory slices, while the browser still truthfully keeps any yet-unsupported signed public kinds out of the active submit form until a dedicated composition surface lands.
- `GovernanceWidget now stays honest if signed submission authority broadens ahead of browser composition`: the public-submit surface still composes only advisory payload kinds, but it now filters its picker to those supported kinds and explicitly tells the user when future signed non-advisory kinds become visible on-chain before the browser can compose them.
- `The remaining shared web-client bootstrap cost is now an explicit accepted trade-off`: after the earlier bundle-honesty cuts, the next honest choice for the current line is to accept the remaining shared `tmctol` / metadata bootstrap weight as part of the on-chain-first/eager startup contract rather than keep treating it as unowned lazy-loading debt.
- `The remaining active bounded web-client query surfaces now expose explicit provenance`: after the earlier widget provenance rollout plus the latest wiki/widget integration work, the current live browser surfaces no longer leave an active bounded query path visually ambiguous about its realization; the remaining open provenance work is now only the future materialized/archive contract.
- `WikiWidget now consumes the generated wiki navigation manifest`: the web-client no longer hardcodes a small repo-doc shortcut list for the Wiki pane. It now reads `wiki/_meta/navigation.json`, highlights featured entrypoints from that manifest, and exposes explicit repo-local `wiki/...` paths so the browser-facing knowledge surface follows the generated wiki section/page contract directly.
- `The generated wiki now exposes a frontend-facing navigation manifest`: `/wiki/_meta/navigation.json` now provides deterministic top-level sections plus featured page order so frontend knowledge surfaces do not need to reconstruct their primary navigation purely from prose or low-level graph edges.
- `The generated wiki now has broader onboarding and subsystem coverage`: `/wiki` now also includes routing/minting, staking, reference-client, manifesto-comparison, and FAQ pages, while `index.md`, related-link sections, and `_meta/{state,graph,aliases}.json` were expanded so the knowledge graph is more useful for newcomer navigation and frontend rendering.
- `The repository now has an initial generated wiki layer`: `/wiki` was bootstrapped as a provenance-aware derivative of `/docs` with a compact `index.md`, append-only `log.md`, newcomer-oriented overview/getting-started/concept/glossary pages, and `_meta/{state,graph,aliases}.json` for frontend/agent navigation.
- `Web-client DEOS adapter imports now match the renamed blockchain module`: the remaining web-client import/dynamic-import paths that still pointed at `src/lib/adapters/blockchain/tmctol` were updated to `src/lib/adapters/blockchain/deos`, which restores both `npm run build` and `npm run check` after the TMCTOL -> DEOS file rename without widening the existing low-level `Tmctol*` type surface.
- `TMC launch-physics docs now describe curve immutability consistently`: `docs/tmc.architecture.en.md`, `docs/tmctol.specification.en.md`, `docs/tmctol.specification.ru.md`, and `template/pallets/tmc/README.md` now all state that curve parameters are configured at creation and remain immutable on the current TMCTOL launch line, while also clarifying that forks may deliberately widen that surface without making post-launch slope mutation part of the default contract.
- `TMCTOL now uses a typed specification filename and no longer embeds governance constitution details`: `docs/tmctol.specification.en.md` and `docs/tmctol.specification.ru.md` now replace the old bare `tmctol.*.md` filenames, repo links were updated accordingly, the TMCTOL standard docs no longer carry the bootstrap / veto governance section, and the remaining governance dependency language stays at the economic-condition level instead of duplicating constitutional rules already owned by the governance docs.
- `Web-client PAPI install path is back to the direct happy path`: the local `web-client/scripts/papi-generate.mjs` workaround was removed, `web-client/package.json` now points `papi:generate` directly at upstream `papi generate` again, and the README no longer documents a local CLI patch layer.
- `Swap execution log wording now matches user-facing intent better`: `SwapWidget` no longer logs terse uppercase `BUY` / `SELL` arrows after execution, and the blockchain adapter no longer falls back to internal-sounding `Live ... quote unavailable` / `Live router swap failed` wording. Successful swap logs now read as user-facing `Bought ... for ...` / `Sold ... for ... via ...`, and quote/submission fallbacks now describe route or submission availability more honestly.
- `Swap quote fetching now respects invalid-slippage blocker state too`: `SwapWidget` no longer keeps fetching live route quotes while the current draft is already blocked on invalid slippage. The quote-fetch eligibility path now uses the same slippage-validity gate as the warning stack and disabled submit action, which closes the remaining buy-side honesty mismatch between visible blockers and background quote activity.
- `Wallet transfer UX now keeps in-flight draft state honest across account/signer changes`: `WalletWidget` no longer treats every pending transfer as if the currently visible draft were the one submitting. Pending state is now keyed to the initiating draft, success only clears recipient/amount when the user is still on that same draft, errors no longer overwrite a newer draft after account/signer changes, and the surface now tells the user when a previous draft is still in flight instead of pretending the new watch-only or signer-changed draft is the one being submitted.
- `The local web-client probe harness is now more truthful and self-terminating`: `/scripts/08-seed-web-client-state.sh` and `/scripts/09-web-client-live-probe.sh` now force the inline Node probes to exit cleanly instead of leaving lingering websocket handles behind, and both now retry stale local transactions before failing so bounded local wallet/swap probes do not hang on transient nonce races.
- `The live web-client probe now covers tracked-asset transfer too`: `/scripts/09-web-client-live-probe.sh` no longer stops at a native transfer plus swap probe; it now also exercises a bounded tracked-asset keep-alive transfer between the same Alice/Bob dev identities, and `BACKLOG.md` closes the corresponding wallet hardening audit slice instead of leaving non-native transfer coverage implicit.
- `Governance advisory payload hashing no longer pins util-crypto on the base viewer path`: the web-client bundle report identified `GovernanceWidget -> advisory-payload -> @polkadot/util-crypto` as the next honest cut, `advisory-payload.ts` now loads the Blake2 helper on demand, `GovernanceWidget` now treats payload-hash derivation as an explicit async review-step state instead of assuming that heavy crypto support must exist on the base viewer route, and `BACKLOG.md` now records that the remaining bundle-growth hotspot has shifted back to the shared `tmctol` / metadata bootstrap fan-out.
- `The next truthful L2 parameter-delegation search space is now narrower and explicit`: `docs/governance.architecture.en.md` now records that the current Root-only custom-pallet surfaces in TMC launch physics, staking onboarding/recovery/admin reward bootstrap, AAA global controls, and asset-registry registration/migration are not honest next `L2ParameterChange` targets for DEOS Governance, and `BACKLOG.md` now closes the rejected-surface inventory slice while retargeting the remaining open work toward either a genuinely delegated/domain-owned parameter surface or a richer treasury authority topology.
- `Governance terminology now leans on DEOS identity instead of inventing a separate public brand`: root/docs governance entrypoints and the governance specification now introduce `DEOS Governance` as the DEOS-specific bounded dual-track alternative to Polkadot `OpenGov`, while `AGENTS.md` records the same rule that public docs may use that identity but runtime/UI implementation names stay generic unless a future rename epic explicitly targets them.
- `Documentation honesty now matches the shipped TMC/runtime reality better`: `template/pallets/tmc/README.md` no longer claims post-launch curve updates in the current slice, `simulator/README.md` now reports the live `57`-test regression count consistently, and `template/runtime/src/configs/tmc_config.rs` now describes the shipped slope constant with the correct `0.000001` comment instead of the stale `0.0001` wording.
- `First-class subsystem docs now use the full-form dotted filename taxonomy`: the repository now uses readable typed filenames such as `aaa.specification.en.md`, `governance.architecture.en.md`, `randomness.strategy.en.md`, `read-model.contract.en.md`, and `polkadot-sdk-2512.insights.en.md` instead of both the older hyphenated filenames and the short-form `*.spec.*` / `*.arch.*` experiment. In-repo links were updated across root/docs/template/web-client/agent surfaces and wiki paths, and `AGENTS.md` now records the same full-form dotted taxonomy as durable documentation protocol.
- `Backlog epics are now decomposed into concrete remaining slices`: `BACKLOG.md` no longer leaves several active governance/web-client items as giant mixed status-history paragraphs. The open work for governance submission/product UX, wallet UX, swap UX, bundle growth, and read-model provenance is now split into parent epics plus explicit remaining child tasks so execution boundaries are clearer.
- `Swap submit fallback messages now match live guidance better`: `SwapWidget` now uses the same richer user-facing wording in submit-time fallback checks for watch-only accounts, missing signer support, missing foreign collateral, and missing native curve, instead of falling back to shorter internal-sounding errors.
- `Swap sell-side pool blocker now uses one explicit label`: `SwapWidget` no longer mixes `No liquidity` with `Pool not initialized yet` for the same structural state. The warning stack, disabled button, and submit path now all use the more explicit pool-readiness wording.
- `Swap minimum-buy guidance now appears in the warning stack too`: `SwapWidget` no longer keeps the runtime minimum-buy rule only in the disabled button and submit path. The same bounded threshold is now surfaced in the main warning stack during composition.
- `Swap submit path now revalidates its own action gates`: `SwapWidget` no longer relies only on disabled-button state before dispatch. The submit handler now rechecks chain snapshot readiness, signer state, minimum-buy, funded balance, native safe-max, and sell-side liquidity before attempting the live swap.
- `Swap warnings now match executable chain state too`: `SwapWidget` no longer treats `no pool` as late route-state noise. Its warning stack now uses the same executable-state predicate as quote fetching, and impossible sell-side pool state is surfaced directly as a structural warning.
- `Swap quote eligibility now matches executable chain state too`: `SwapWidget` no longer fetches live quotes when the bounded snapshot already says swapping is impossible, such as no native curve, no registered foreign collateral, or no sell-side pool. Quote fetching now respects those same structural action gates in addition to funded size and native safe-max rules.
- `Swap quote eligibility now matches funded input sizes`: `SwapWidget` no longer fetches live quotes or surfaces route-state noise for amounts above the currently held input balance. Quote eligibility now respects actual funded size in addition to the existing minimum-buy and native safe-max rules.
- `Swap native sell blockers now distinguish balance from fee headroom`: `SwapWidget` no longer explains every oversized native sell as a fee-headroom issue. Amounts above the full held balance now surface as `Insufficient ... balance`, while only the narrower band above native `Safe max` uses the fee-headroom warning, and the submit handler now revalidates that same split instead of trusting button disable state alone.
- `Swap native safe-max handling is now more explicit`: `SwapWidget` no longer mixes a hidden native fill-max haircut with a separate rough near-max warning. The input leg now labels `Safe max`, the fill-max action uses that same fee-headroom-aware amount, the disabled primary action enforces it, the warning copy refers to that same explicit threshold, the helper control disables itself when nothing spendable exists, zero-balance input state now names the real blocker directly instead of pointing the user toward unrelated missing fields first, the remaining insufficient-balance gate now also uses the parsed bigint amount instead of a float comparison, and quote fetching now also respects that same safe-max contract instead of retrieving sell quotes for sizes the action path already rejects.
- `Wallet known-asset cards are now less noisy`: `WalletWidget` no longer appends a meaningless always-on `send-ready` tag to every known-asset card on the current line, leaving only balance and genuinely distinguishing asset traits.
- `Wallet send-surface provenance is now explicit`: `WalletWidget` now shows a `ReadModelBadge` on the send surface too, so the transfer UI itself is visibly anchored to the same bounded portfolio projection as the known-asset panel instead of leaving that client-bounded read path unlabeled.
- `Wallet native blockers now distinguish balance from fee headroom`: `WalletWidget` no longer explains every oversized native transfer as a safe-max issue. Amounts above the full held balance now surface as insufficient balance, while only the narrower band above native `Safe max` uses the fee-headroom message, and the submit handler revalidates that same split.
- `Wallet native safe-max enforcement now matches its own guidance`: `WalletWidget` no longer leaves the primary send action enabled for native amounts above the fee-headroom-aware spendable balance. Those amounts are now blocked up front, explained as exceeding the spendable balance after fee headroom, and revalidated in the submit handler instead of trusting button disable state alone.
- `Wallet native helper copy is now honest at zero safe max`: `WalletWidget` no longer prints a fake `Safe max 0` when native fee headroom consumes all spendable balance. It now says explicitly that no spendable balance remains after fee headroom.
- `Wallet send-balance labels are now more honest`: `WalletWidget` now distinguishes native `Spendable now` from the full held balance when fee headroom matters, instead of labeling the whole native balance as freely available for transfer.
- `Wallet zero-balance guidance is now more explicit`: `WalletWidget` now tells the user when the currently selected transfer asset has no spendable balance instead of staying quiet until they start constructing a doomed send, the primary action now names that zero-balance blocker directly instead of pointing the user toward unrelated missing fields first, and native dust below fee headroom is now treated as unsendable under that same contract instead of being presented as transferable balance.
- `Wallet transfer-asset selection now stays aligned with the live bounded asset set`: `WalletWidget` now normalizes the armed transfer key when the bounded transferable-asset set changes, so the UI cannot keep a stale selection while derived state silently falls back to a different asset behind the scenes.
- `Wallet selected transfer asset is now explicit in the send form`: `WalletWidget` now shows the currently armed transfer asset and its kind/id directly beside the send controls instead of relying only on the selected tile state and submit-button text.
- `Wallet native safe-max handling is now internally consistent`: `WalletWidget` now uses one bigint-safe `safe max` value for native fee-headroom UX, so the helper copy, near-max warning, and fill-max action all agree on the same spendable amount instead of mixing a hidden smaller max with a separate coarse warning threshold, and the max-fill control now disables itself when nothing spendable exists.
- `Wallet transfer amount handling is now more consistent`: `WalletWidget` now parses the entered transfer amount through one derived bigint path and uses that same parsed amount for disable-state checks, guidance, near-max native warnings, and the eventual submit call instead of mixing float-only balance comparisons with later bigint conversion.
- `Wallet native max-fill copy is now honest`: `WalletWidget` no longer labels the native fee-headroom helper as `Fill balance` when it intentionally leaves keep-alive fee room. The control now says `Fill safe max`, matching the actual spendable-max behavior.
- `Wallet transfer errors now clear when the draft changes`: `WalletWidget` now ties send-error visibility to the exact failed draft, including signer source/status for the same address, so once the user changes recipient, amount, asset, selected account, or signer context, stale failure banners are cleared and the current pre-submit guidance can surface again instead of being masked by an old error.
- `Governance browser submission review is now more explicit`: the public advisory submit flow in `GovernanceWidget` now includes an explicit `Submission review` area with separate `Submit proposal` and `Note preimage` action panels, each showing its own readiness/status plus opening-fee vs note-cost consequence, resulting chain effect, and exact bounded item/hash identifiers before click, each panel now owns its own action button so the two browser-side actions no longer read like one blended step, and the referenced-payload field can now be quick-filled from the currently visible bounded proposal set while keeping provenance visible for both bounded and valid manual/external hashes plus one-click clear control.
- `Governance retained execution receipts are now more explicit in the browser`: `GovernanceWidget` no longer compresses every retained execution detail into one summary line. Recent finalized cards now expand per-kind bounded receipt rows for runtime-upgrade code hashes, parameter-change surfaces, treasury funding source / beneficiary / payout asset / base amount / scalar / final amount / settlement kind, and advisory/failure epochs, the same retained execution/rejection states now render human-readable reason labels instead of raw enum codes, the top-level retained outcome/state labels now also include failure reasons plus advisory payload identity where that bounded detail exists, retained account/asset identifiers are now labeled explicitly as `Account …` / `Asset #…`, retained winning-primary plus compact receipt summary labels now also use human-facing option/surface/scalar wording, the UI now hides the winning-primary row when no bounded retained winner exists, executed-state labels now tell the user which bounded execution slice actually ran, and opening-fee rows are hidden for admin-only proposals so fee semantics appear only when the signed public-submission contract actually applies.
- `TMCTOL alignment memory now rejects low-signal ledger noise`: the local alignment skill now explicitly bans bare gate/tool-failure entries from canonical ledgers, `log-ledger.sh` rejects low-information titles/summaries and requires scope plus remedy, `while-true-gate.sh` no longer appends generic gate-failure telemetry to the hallucination ledger, stale low-signal gate-only entries were pruned from `hallucinations.jsonl`, and `AGENTS.md` now records the same low-signal-ledger ban as durable repo protocol.
- `Web-client local bootstrap docs now match the post-Sudo dev chain reality`: `web-client/README.md` no longer teaches that the plain development chain still lacks the foreign asset / router tracking / native curve by default. It now says honestly that current dev/local chain specs provide those surfaces in genesis and that `/scripts/08-seed-web-client-state.sh` mainly tops up Alice funding plus missing pool/liquidity state.
- `Governance advisory composition is now more self-explanatory in the browser`: the public advisory submit form now adds payload-kind-specific purpose guidance plus dynamic summary placeholders/button labels for `Intent` vs `L2SignalToL1`, while the surrounding governance surfaces continue to expose the exact bounded payload bytes/hash, advisory scope, and non-dispatch execution truth.
- `Post-bootstrap runtime-upgrade governance is now fully landed on the current line`: `pallet_sudo` is gone from the runtime and genesis/chain-spec wiring, dev/local bootstrap no longer depends on Sudo for the web-client foreign-asset/router/curve setup, protocol `L1RootAction` now has the chosen unanimous raw `$VETO` fast path, `/scripts/check-authorized-upgrade-local.sh` and `/scripts/apply-authorized-upgrade-local.sh` now cover the off-browser verification/relay operator flow, and docs/browser copy now describe the post-bootstrap contract honestly: governance authorizes the code hash, any operator may relay matching bytes, and that relay remains ministerial rather than a second governance decision.
- `Repository context entrypoints are now better connected for humans and agents`: the root `README.md` now links the canonical backlog, delivery history, and simulator entrypoint directly, `docs/README.md` now exposes the simulator README as part of the local tooling plane, and `template/pallets/README.md` now links each pallet's local README explicitly so subsystem entrypoints stay reachable inside the repository context graph.
- `DEOS identity, naming, and durable context are now explicit and coherent`: `README.md`, `docs/README.md`, and `AGENTS.md` now explain the DEOS acronym, keep the DEOS-vs-TMCTOL framework/standard boundary explicit, and organize durable project memory around clear concept, naming, topology, architecture, conventions, integration, and task-lifecycle layers instead of incremental context drift.
- `Launch-line tactical governance is now invoice-native end-to-end`: governance storage, family-aware tally/query surfaces, retained winning-primary-option identity, discrete `Amplify / Approve / Reduce / Nay` resolution, scalar-aware treasury execution, and browser invoice-family voting/receipt rendering now all agree on the live `$BLDR` tactical invoice contract.
- `L1 runtime-upgrade governance now uses a dedicated bounded payload`: the first `L1RootAction` slice no longer decodes an arbitrary `RuntimeCall` preimage. The runtime now decodes a dedicated runtime-upgrade payload carrying only `code_hash`, dispatches `System::authorize_upgrade { code_hash }`, and rejects malformed payload bytes as `InvalidPreimage`, which keeps the launch-line runtime-upgrade path aligned with the same explicit-payload discipline now used by tactical invoice execution.
- `The reference runtime now uses the public ordinary governance cadence`: runtime governance timing is no longer pinned to the earlier tiny local values. The ordinary line now runs with a `3 day` lead-in, `7 day` ordinary protection window, `7 day` ordinary primary window, `1 day` urgent window, and `3 day` enactment delay, while urgent policy on the current line is now narrow rather than fully deny-by-default: strategic `L1RootAction` is the one live exception, using unanimous raw protection-track `Pass` as its acceleration path. Runtime integration coverage now also regresses the shipped timing contract explicitly, and the staking integration suite now uses direct governance epoch jumps instead of stale tiny-window assumptions.
- `The mock governance provider no longer teaches the old protection activation rule`: the browser mock now keeps protection open through its mock protection window instead of pretending a first-touch `Veto / Pass` activation deadline still exists, and its error copy now matches that simpler bounded window.
- `GovXP is now counters-only on the canonical runtime read surface`: the governance pallet no longer exports a live `govxp_multiplier(...)` view, the runtime/browser/docs now treat v1 GovXP as bounded on-chain input counters plus `reward_coefficient(...)` only, and the stale first-iteration multiplier plumbing was removed instead of being quietly left as a misleading query surface.
- `Submission authority is now explicit in the governance read model`: the pallet/runtime now expose `proposal_submission_authority(domain, payload_kind)` as a canonical bounded query, and the first signed public advisory paths are live: `Intent` proposals plus tactical `$BLDR` `L2SignalToL1` proposals can now be submitted by a signed origin while executable kinds remain admin-only. Signed advisory submission also burns the configured native opening fee on-chain, `proposal_opening_fee(domain, payload_kind)` now exposes that cost as a bounded runtime query, and the runtime now also exposes `authorized_runtime_upgrade()`, `payload_hash_preimage_status(payload_hash)`, and `payload_preimage_note_cost(payload_len)` so browser-side advisory composition plus runtime-upgrade status can stay on canonical governance query surfaces without hardcoding deposit constants or reading raw storage layout directly. The browser governance surface now renders both submission authority and public opening fee alongside payload/execution semantics, the browser submit adapter/store contract no longer pretends proposal creation is an admin-only `proposerAccountId` path, and the Governance widget now exposes a visible signed-advisory submit flow that derives the exact bounded advisory payload bytes plus hash from `summary`, optional `doc_cid`, and optional referenced payload hash, surfaces those bytes directly for later reuse, keeps the chain-vs-preimage truth explicit, uses distinct signed write-surface readiness for submit vs optional preimage noting instead of piggybacking on vote readiness, waits for the canonical preimage-status and note-cost quote to resolve before enabling the optional note action, queries the canonical runtime preimage-status surface for the derived hash before the user clicks that path, now renders exact live execution-path summaries for the currently shipped governance payload matrices, surfaces when a governance-authorized runtime upgrade is still pending authorized code relay through the canonical `authorized_runtime_upgrade()` view, reflects that same pending relay truth on retained `RuntimeUpgradeAuthorized` cards, explicitly tells the user that `apply_authorized_upgrade { code }` remains a separate system-level step not exposed in the browser, reflects the verified second-step system reality that invalid authorized code clears the pending authorization explicitly, and now also ships a plan-only `/scripts/check-authorized-upgrade-local.sh` helper that compares a local WASM blob against the chain's pending authorized runtime-upgrade hash, can optionally build the local runtime first, and can emit offline `apply_authorized_upgrade` call data without submitting any live upgrade call.
- `AccountWidget no longer advertises injected Connect when discovery is unavailable`: the sidebar wallet selector now hides the Connect action entirely unless injected-wallet discovery is actually available, keeping the unavailable-extension/browser states honest instead of showing a dead disabled affordance while the existing inline notices continue to explain what is missing.
- `Governance lifecycle now pairs the public ordinary cadence with explicit urgent-policy truth`: the runtime uses the shipped public timing contract, urgent fast-track authorization mechanics are fully wired, `proposal_urgent_eligibility(domain, item_id)` now truthfully exposes the narrow current-line policy (`L1RootAction` only), and unanimous raw protection-track `Pass` now immediately resolves and executes the runtime-upgrade authorization path for that strategic payload instead of waiting for a separate primary ballot.
- `Governance browser surfaces now reflect bounded runtime truth instead of UI inference`: the web client exposes vote readiness, bounded finalized-retention semantics, execution-path and payload state, proposal timing, active proposal metadata, and retained finalized execution detail directly from canonical governance views.
- `Governance execution observability is now a first-class on-chain contract`: recent finalized execution detail, typed failure reasons, treasury settlement kind, and payload-kind-aware finalization events are retained or emitted explicitly so clients and audits no longer need event archaeology or metadata joins to understand outcomes.
- `Curve parameters are now treated as immutable launch physics on the current line`: the shipped runtime no longer exposes `TokenMintingCurve.update_curve` through governance or through the pallet call surface at all, and the docs/backlog now treat post-launch curve-slope mutation as out of contract rather than as an example L2 parameter path. The remaining honest `L2ParameterChange` line is now narrower and stricter: delegated/domain-owned surfaces only, not economic-physics rewrites after launch.
- `Governance payload admission, semantics, and the remaining execution gap are now synchronized with reality`: `docs/governance.specification.en.md` now fixes the v1 preimage-admission rule that executable payload kinds may be submitted before their full preimage is noted as long as readiness stays queryable, while advisory payload kinds may remain hash-only and actual enactment still requires runtime-visible payload availability; `docs/governance.architecture.en.md` now records both that the current runtime exposes payload readiness but still keeps admission soft beyond that scaffold, that no canonical payload executor yet maps `(domain, payload_kind, payload_hash)` into bounded enactment behavior, and that the current pending-enactment slice is still status-only scaffolding without bounded servicing; and `BACKLOG.md` now closes the stale preimage-admission-definition, payload-kind-matrix, advisory-payload-semantics, and invoice-payload-semantics slices while narrowing the remaining dispatch-semantics umbrella into concrete executor, bounded enactment servicing, executable-kind wiring, advisory-finalization, and execution-observability slices.
- `Durable context now matches the shipped governance and AAA vocabulary more honestly`: `AGENTS.md` now records the bootstrap-to-protection handoff, uses the corrected simulator ratio vocabulary, lifts legacy manager language to the AAA actor abstraction, scopes provisioning traits correctly, clarifies the runtime-adapter term, and routes evergreen fragilities into durable context instead of backlog noise.
- `Governance proposal semantics are now explicit and typed at the kernel boundary`: proposals require explicit `cadence_mode + payload_kind + payload_hash`, distinguish in-domain `Intent` from upward `L2SignalToL1`, expose payload-preimage readiness plus metadata/authority surfaces on-chain, emit semantically rich proposal/vote events, and standardize the governance model around `GovernanceDomain + CadenceMode + ProposalPayloadKind` instead of proposal classes.
- `Governance specification now closes its biggest contract gaps`: the spec now defines proposal payload/execution-authority semantics, exact approval and turnout math for binary and invoice tracks, invoice payout execution requirements, live vote-power settlement under transferable staking receipts, and a minimum event/observability contract.
- `Governance specification is now contract-pure again`: `docs/governance.specification.en.md` no longer mixes target governance rules with shipped/runtime-status narration, `docs/governance.architecture.en.md` remains the implementation-status surface, and `AGENTS.md` now codifies that spec/architecture split explicitly as durable documentation protocol.
- `Governance Declining Power now follows the canonical 7x curve in runtime`: the runtime governance weight helper now uses the agreed piecewise `7x -> 1x` temporal weighting shape, fresh runtime unit tests pin the curve endpoints, and the affected staking-governance integration assertions now match the new weights.
- `Governance protection admission now follows an explicit protection window`: the old first-touch `2/3` activation deadline is gone, and protection-track ballots are now simply admissible from submission until the configured protection close.
- `Governance now enforces lead-in admission for ordinary ballots`: ordinary `Aye / Nay` votes are rejected before `primary_open`, while protection-track `Veto / Pass` ballots remain admissible during any configured lead-in window.
- `Governance weighting windows are now split by track`: the pallet now computes ordinary-ballot and protection-ballot vote weights against separate internal timing windows, so future lead-in and early-open protection behavior can diverge honestly without forcing both tracks to share one hidden clock.
- `Governance status now models pending enactment explicitly`: successful proposals can now schedule a bounded pending-enactment epoch when `ProposalEnactmentDelay > 0`, `proposal_status(domain, item_id)` now reports `PendingEnactment { outcome, enactment_epoch }` until that delay elapses, and finalized-outcome expiry now also clears the additive urgent/enactment timing markers.
- `Governance timing scaffolding is now live in pallet-governance`: the pallet config now exposes additive timing hooks for lead-in, protection duration, urgent primary duration, enactment delay, and raw fast-track `Pass` threshold; the pallet now keeps additive `ProposalUrgentAuthorizedAt` / `ProposalPendingEnactmentAt` storage for future fast-track and enactment scheduling, exports the new canonical `proposal_timing(domain, item_id)` runtime view, and ships regression coverage for ordinary timing plus scaffolded urgent/enactment timing without changing the current live resolution behavior yet.
- `Governance target docs now align on the v1 public cadence instead of the older extension wishlist`: `docs/governance.specification.en.md`, `docs/governance.architecture.en.md`, `docs/README.md`, and `BACKLOG.md` now agree that the target public contract is a `3 day` lead-in with protection open at submission, a `7 day` ordinary protection window plus `7 day` ordinary primary window, canonical `7x -> 1x` Declining Power by the end of day 6 with day 7 flat at `1x`, a default `3 day` ordinary enactment delay, payload-gated urgent handling with runtime-configured raw protection-track `Pass` thresholds, first-class `$BLDR` invoice voting, governance-owned runtime upgrades, a burned proposal-opening fee instead of proposal bonds, and GovXP counters-first v1 policy while adaptive curves drop out of the target contract.
- `Adaptive approval and support curves are now implemented in pallet-governance`: `ProposalApprovalCeiling` and `ProposalTurnoutCeiling` config constants enable monotonically decaying thresholds over the voting window through shared `voting_progress()`, `approval_threshold_at()`, and `turnout_threshold_at()` helpers; when ceiling equals the corresponding floor value, the curve degenerates to a flat threshold (current behavior); both `resolve_or_reject_from_current_votes` and `proposal_resolution_state` now use the adaptive helpers instead of fixed lookups; the mock sets ceiling = floor for backward compatibility, and the runtime config defaults to the same flat policy; the protection-track raw-supply guards remain independent of the curves.
- `Confirm period is now implemented in pallet-governance as a runtime-configurable option`: when `ProposalConfirmPeriod > 0`, a matured passing proposal enters a `Confirming` state and must sustain its passing tally for the full confirm duration before finalization; if a new vote during confirm flips the tally, the confirm timer resets and the proposal is re-scheduled for maturity servicing; the implementation reuses the existing `ProposalMaturityBuckets` infrastructure, adds `ProposalConfirmStartedAt` storage and a `Confirming` variant to `ProposalResolutionState`, emits `ProposalConfirmStarted` / `ProposalConfirmReset` events, cleans up confirm state on resolve/reject/veto-cancel paths, and remains fully backward-compatible (`ProposalConfirmPeriod = 0` preserves the current immediate-finalization behavior); storage version bumped to `7`; all 46 existing governance tests pass; both pallet and runtime mock default to period `0` for the current launch line.
- `Governance specification now reserves three OpenGov-inspired lifecycle extensions as target contract`: lead-in period (Section 3.1) equalizes timing before Declining Power activates, confirm period (Section 3.2) requires sustained passing before finalization, and adaptive approval/support curves (Section 4.3) replace the fixed-threshold cliff with continuous temporal gradients that compose with Declining Power. Section 14 and Section 15 were updated to acknowledge the new gap items and explicitly fence off conviction-style locks as incompatible with liquid staking, while per-track delegation and decision deposits are reserved as future extensions. The governance architecture doc now lists the missing lifecycle extensions as divergence item 6, and `BACKLOG.md` now carries three concrete implementation tasks for the reserved extensions.
- `StatusWidget footer strip now carries explicit provenance`: the persistent reserved-footer status lane now shows a `ReadModelBadge` instead of looking like raw chain truth without context, making one more always-visible client projection honest about being assembled from connection, snapshot, and wallet session state.
- `Bundle-honesty work has now pushed the app-owned route/bootstrap chunk down to roughly ~54 KiB`: after the sidebar-shell deferral plus governance/sidebar action-path lazy-loading slices, the latest fresh bundle report shows the startup app chunk nearly halved again while the remaining heavyweight artifacts stay the crypto `verify` chunk and dynamic `tmctol_metadata` payload.
- `Lazy sidebar opening now has an explicit loading shell instead of a blank lane`: `WorkspaceFrame` now renders `SidebarPanelSkeleton` while the deferred sidebar shell is still loading, so bundle-honesty no longer costs the user a blank first-open sidebar.
- `WalletWidget now tells the user what kind of account is selected`: the selected-account summary now exposes the current mode (`Injected`, `Local dev`, `Watch-only`, or `Custom`) plus whether signing is ready, so users no longer have to infer account capability only from later send/swap failure states.
- `SettingsWidget no longer pins governance store state at module load`: it now reads lightweight governance/session helpers up front and only imports the full governance/system stores on Apply, and the fresh bundle graph shows that shared governance/index chunk now reaches `SettingsWidget` through a dynamic path too.
- `SettingsWidget no longer pins system refresh code at module load`: the sidebar settings surface now loads `systemStore` only when Apply actually runs, matching the same action-path lazy-loading rule used in `AccountWidget`.
- `GovernancePapiProvider no longer pins the tmctol connection class at provider construction time`: it now lazy-instantiates `TmctolPapiConnection`, and the fresh bundle graph shows governance PAPI reaching the `tmctol` connection chunk through a dynamic import path instead of a direct static one.
- `AccountWidget no longer pins governance/system refresh code at module load`: the sidebar-owned account surface now loads those refresh dependencies only when the user actually switches accounts, and the latest bundle graph shows that shared governance/index chunk reaching `AccountWidget` through a dynamic import path rather than a direct static one.
- `WorkspaceFrame now lazy-loads the sidebar shell itself`: the reserved sidebar lane no longer statically imports `SidebarPanel`, so the latest fresh bundle build shows the app-owned route/bootstrap chunk down around ~109 KiB while the remaining heavyweight slices stay the crypto `verify` chunk and dynamic `tmctol_metadata` descriptor payload.
- `Sidebar-lane widgets now follow the same bundle-honesty rule as center panes`: `SidebarPanel` now lazy-loads `AccountWidget` and `SettingsWidget`, and the fresh bundle report after that plus the layout decomposition pass shows the app-owned route/bootstrap chunk down around ~110 KiB while the remaining heavyweight slices stay the crypto `verify` chunk and dynamic `tmctol_metadata` descriptor payload.
- `SettingsWidget no longer duplicates account selection at all`: the stale account/signer override was removed from settings so the sidebar-owned `AccountWidget` remains the single authority for account selection and signer onboarding, matching the documented widget boundary instead of competing with it.
- `Layout decomposition is now delivered as a concrete web-client architecture pass instead of an endless cleanup itch`: shared persistence, legacy normalization, tree helpers, tile-id sequencing, widget loading, pane DnD/projection math, overlay rendering, tab-strip rendering, pane-grip chrome, and resize/FLIP suppression were all extracted out of the original `layout/index.svelte.ts` and `PaneHost.svelte` hotspots, so the old open decomposition epic was removed from `BACKLOG.md` in favor of future concrete slices only if a new layout pain point appears.
- `AccountWidget now adapts more honestly in narrow sidebar widths`: account-status cards no longer force a two-column layout at every width, dev presets can collapse to one column before widening, and injected-account lists no longer wait until extreme width before using two columns.
- `Selected custom addresses now promote into injected-account identity once a matching signer is discovered`: after injected account discovery runs, the wallet session now upgrades the selected label/source away from stale custom/watch-only presentation when the same address is present in the connected wallet extension, keeping the sidebar-owned account surface honest without forcing the user to reselect the account manually.
- `Widget loading/content hosting is no longer embedded inside PaneHost`: `src/lib/layout/PaneWidgetHost.svelte` now owns active-panel loading, load-error handling, and content hosting for the mounted widget, so `PaneHost.svelte` no longer has to mix pane coordination with widget lifecycle/render state.
- `Resize-driven FLIP suppression is no longer embedded inside PaneHost`: `src/lib/layout/tab-flip.ts` now owns the resize observer, suppression timer, and tab FLIP animation policy, so `PaneHost.svelte` no longer has to mix pane coordination with its own mini animation-control engine.
- `Tile-id sequencing is no longer embedded inside the layout store`: `src/lib/layout/tree-ids.ts` now owns tile id generation, sequence reset, and persisted-tree id recalculation, so `layout/index.svelte.ts` no longer has to carry hidden engine-sequencing state inline.
- `Pane grip / merge-target chrome is no longer embedded inside PaneHost`: `src/lib/layout/PaneGripPlate.svelte` now owns the draggable pane-stack grip and merge-target highlight markup, so `PaneHost.svelte` no longer has to inline that full render block just to coordinate pane-level drag events.
- `Tab-bar rendering is no longer embedded inside PaneHost`: `src/lib/layout/PaneTabs.svelte` now owns preview-tab rendering, active-tab chrome, and tab-drag markup, so `PaneHost.svelte` no longer has to inline the full tab-strip UI just to coordinate tab state and events.
- `Pane drop/merge overlay rendering is no longer embedded inside PaneHost`: `src/lib/layout/PaneDropOverlay.svelte` now owns the merge highlight and edge-drop overlay rendering, so `PaneHost.svelte` no longer has to inline the full projection markup just to coordinate drag state.
- `Pane drag/projection math is no longer embedded inside PaneHost`: `src/lib/layout/pane-dnd.ts` now owns preview-tab construction, edge detection, tab insert-index calculation, and edge-projection labeling/shell logic, so `PaneHost.svelte` no longer has to mix render coordination with the core drop-geometry helpers.
- `Widget lazy-loading is no longer embedded inside PaneHost`: `src/lib/layout/widget-loader.ts` now owns widget import wiring and cache reuse, so `PaneHost.svelte` no longer has to mix render coordination with widget-registry bootstrapping.
- `Tree mutation logic is no longer embedded inside the layout store`: `src/lib/layout/tree-ops.ts` now owns tab moves, leaf extraction/merge/split, active-tab updates, split resizing, and empty-collapse behavior, so `layout/index.svelte.ts` no longer has to carry the full tile-tree mutation engine inline.
- `Tree search and validation logic is no longer embedded inside the layout store`: `src/lib/layout/tree-utils.ts` now owns leaf lookup, leaf counting, and whole-tree validity checks, so `layout/index.svelte.ts` is one step closer to being a state coordinator rather than the full layout engine.
- `Legacy layout normalization is no longer embedded inside the layout store`: `src/lib/layout/legacy-normalization.ts` now owns old panel-id and persisted-layout/frame migration logic, so `layout/index.svelte.ts` no longer has to mix live workspace state with backward-compatibility decoding.
- `Shared browser persistence primitive now exists and layout is no longer the only owner of local-storage wiring`: the new `src/lib/shared/persistence.ts` now owns safe string/JSON local-storage helpers, `src/lib/layout/index.svelte.ts` now consumes that shared primitive for workspace/frame persistence, and `src/lib/wallet/index.svelte.ts` now reuses the same helper for persisted account selection instead of duplicating browser-storage handling.
- `Custom account handoff in the sidebar is no longer mouse-only`: `AccountWidget` now lets a valid custom dev-alias/address selector submit on Enter, preserving the same validation guardrails while removing the extra click from keyboard-driven account switching.
- `Wallet and swap signer guidance now distinguishes environment failure from generic signer setup`: when no injected wallet extension exists, the action surfaces now say so explicitly instead of always repeating one generic sidebar prompt, while still preserving the separate watch-only account guidance for valid unsigned custom addresses.
- `WalletWidget send guidance now distinguishes watch-only accounts from generic missing-signer state`: the disabled send action and inline guidance now preserve the new readonly/watch-only account model instead of flattening valid unsigned custom addresses into the same copy used for ordinary signer-setup gaps.
- `SwapWidget now distinguishes watch-only accounts from generic missing-signer state before submit`: the disabled primary action and warning stack now tell users when the selected account is valid but watch-only, pointing them toward connecting an injected signer for that same address instead of flattening the state into one generic signer-missing message.
- `Custom addresses now surface as watch-only instead of fake-broken signer state`: the wallet session layer now uses the existing `readonly` signer state for valid unmatched custom addresses, and `AccountWidget` presents that state as `watch-only` so users can tell the difference between an invalid selector and a valid address that simply lacks a connected signer.
- `Injected-wallet onboarding now tells the truth before the user clicks Connect`: `AccountWidget` now surfaces browser/extension availability inline and disables the injected-wallet Connect action unless injected discovery is actually available, preventing the sidebar-owned account surface from advertising a path the current environment cannot execute.
- `Sidebar-owned account selection now rejects invalid custom selectors before it can poison live wallet context`: `AccountWidget` now disables the custom-account handoff until the input is either a built-in dev alias (`Alice`, `//Alice`, etc.) or a valid DEOS ss58 address, surfaces that guidance inline before refresh/signing confusion happens, and keeps the broader transaction-grade wallet UX epic open only for further live-tested polish instead of leaving this sharp edge implicit.
- `DEOS pivot is now closed as a framework-positioning pass rather than left as a fuzzy rename campaign`: the remaining user/operator-facing labels were audited to the point where only intentional standard references and low-level technical identifiers remain, `AGENTS.md` now codifies an explicit `Artifact-Name Tail Policy`, and the old open pivot epic was removed from `BACKLOG.md` because the residual `tmctol-*` names are now a conscious deferred migration choice rather than unresolved framework-brand drift.
- `Context evolution now locks the DEOS-vs-TMCTOL naming boundary into durable protocol memory`: `AGENTS.md` now carries an explicit `Framework-vs-Standard Naming` rule so future work uses `DEOS` for the repository/framework/reference-stack/runtime-substrate identity and reserves `TMCTOL` for the concrete tokenomic standard running on top of DEOS, preventing future blind-renames from collapsing framework identity into one shipped economic configuration.
- `Framework-facing architecture docs now complete the next DEOS pivot slice`: the remaining DEOS-layer docs for AAA, governance, staking, and randomness now describe DEOS as the framework/runtime substrate while explicitly treating TMCTOL as the current shipped standard or reference topology where appropriate, so the pivot no longer stops at the root entrypoints and docs hub.
- `Framework naming pivot started: DEOS is now the framework identity while TMCTOL is explicitly preserved as the flagship economic standard`: root entrypoints (`README.md`, `AGENTS.md`, `BACKLOG.md`, `CHANGELOG.md`) now frame the repository as DEOS, canonical docs/readmes (`docs/README.md`, `template/README.md`, `web-client/README.md`, `scripts/README.md`) now distinguish framework-vs-standard roles more explicitly, framework-level architecture notes (`core-architecture`, `read-model-contract`, `web-client-architecture`, manifesto) now anchor themselves in DEOS while the TMCTOL EN/RU specification is preserved and clarified as a DEOS standard rather than the repository-wide identity, the scripts layer now describes DEOS reference-runtime operations more honestly, and the default browser signer dapp label now presents itself as `DEOS Web Client` instead of `TMCTOL Web Client`
- `PaneHost now supports whole-pane dragging via the grip plate under the tab bar and one unified directional drop system`: each pane stack now exposes a dedicated draggable grip plate beneath its tabs, so users can move the entire leaf by grabbing that plate instead of moving one tab at a time. Dragging a pane onto another pane's grip plate merges the two tab stacks into one pane, while dragging toward the left, right, or bottom edge now goes through the same directional projection system for both tabs and whole panes: one projected landing pane is rendered at the target side, with either a ghost tab or a ghost pane grip inside it depending on the dragged payload.
- `PaneHost tab dragging now previews landing with a projected tab shadow, elastic tab spreading, and full pane-resize calmness`: the old narrow vertical insertion line in pane tab bars is replaced by a real in-bar landing projection that renders the dragged tab as a translucent shadow placeholder at the computed drop slot, while neighboring tabs reflow around it with an elastic FLIP transition so the tab destination reads spatially before drop instead of only after reorder. The tab FLIP path now also suppresses resize-driven motion briefly when either the tab bar or the surrounding pane container changes size, preventing split-handle width or height updates from injecting parasitic tab jitter into the drag preview.
- `SettingsWidget now uses the same Bits-UI select language for sidebar-edge choice`: the remaining raw native `<select>` in the sidebar settings surface is replaced by a Bits-UI dropdown with the same selected-row affordance already used in the upgraded swap asset selectors, keeping the local control vocabulary more consistent without introducing another shared component prematurely.
- `SwapWidget now uses progressive in-file snippet decomposition plus Bits-UI asset dropdowns`: the trade widget no longer keeps its whole execution form as one large flat template. It now decomposes the major sections through local snippets that sit beside the main template and consume explicit typed view-model arguments instead of implicitly reading the whole widget through closure, the old hardcoded token buttons are replaced by Bits-UI dropdown selectors for the active input/output asset pair, and the dropdown rows now show the currently selected asset explicitly instead of relying only on subtle row styling.
- `PaneHost now gives chart-first widgets a real full-height box`: the pane body wrapper now uses an explicit full-height grid contract instead of only a loose `min-h-full` child wrapper, and `ChartWidget` now opts into that contract with `h-full` so the graph can actually consume the available pane height instead of stopping short while the outer pane host still owns the only scroll surface.
- `ChartWidget now behaves like an elastic chart-first pane`: the chart canvas now consumes the remaining widget height as a special no-internal-scroll case, while the existing series toggle handles move into a bottom control rail opposite the pane-stack tabs instead of stealing space above the graph. That bottom toggle rail now also stays center-aligned regardless of the latest-series summary width.
- `ChartWidget compact summary now follows width-first morphology`: the latest-series summary row is no longer triggered only after chart height is already scarce, which was self-defeating because that extra row consumed even more vertical room. The compact summary now appears for narrow panes instead, while height-sensitive rendering stays internal to the chart canvas margins/ticks.
- `Wallet and swap signer guidance now matches the sidebar-owned account surface`: the missing-signer guidance in `WalletWidget` and `SwapWidget` no longer tells users to configure signing in a stale header-era location; the wallet guidance, swap warning stack, and swap disabled primary action now all point users to open the sidebar and connect an injected or local dev signer there, keeping pre-submit execution copy aligned with the reserved-lane account architecture.
- `Sidebar lane toggling now animates as one desktop motion instead of two disconnected phases`: `WorkspaceFrame` still uses a directional fly transition for the reserved sidebar lane in stacked mode, but the desktop path now keeps a stable flex row with a width-animated sidebar shell and a fixed-width inner panel clipped by that shell. That lets the adjacent center pane expand and contract in the same motion as the sidebar, while the sidebar itself collapses in place instead of visibly sliding away before the frame catches up. The clipped desktop sidebar subtree is also explicitly `aria-hidden` and `inert` while collapsed so hidden account/settings controls do not stay keyboard-focusable, the sole `AccountChip` toggle now advertises both `aria-expanded` and the concrete sidebar host id it controls, its chevron/label now follow the configured left-vs-right sidebar edge instead of assuming one hardcoded side, and the neighboring icon-only reset-layout control now also carries an explicit accessible label. `SidebarPanel` itself stays focused on rendering the lane host/content instead of owning open/close transition state, and the workspace frame also drops a stray debug-orange fill so the app now correctly rides on the intended page background instead of tinting the whole shell.
- `AppFooter now really behaves as the full-width reserved footer host`: the footer lane host no longer centers a smaller inner shell; it now spans the workspace width as documented, while `StatusWidget` remains inline content inside that full-width reserved lane.
- `AGENTS completion now includes the repo-local TMCTOL alignment gate`: the task completion protocol now requires autonomous repo-changing passes to run `./.agents/skills/alignment/scripts/while-true-gate.sh` after local validation and knowledge sync, treating a failing gate as not done while keeping the skill file itself as the canonical detail surface.
- `AccountChip is now a strict sidebar-toggle widget`: the header-lane `AccountChip` no longer supports a non-toggle mode and now expresses only one responsibility — opening and closing the sidebar.
- `PaneHost and SidebarPanel now own the top-level overflow hosts`: the pane host now provides the outer overflow/scroll container for tabbed center panes, and the affected widgets no longer self-own their top-level vertical scroll wrappers. In parallel, `SidebarPanel` now owns both the outer frame and the inner overflow container for sidebar widgets instead of relying on an outer scroll wrapper in `WorkspaceFrame`, while `StatusWidget` continues to render only the inline footer content and `AppFooter` owns the full-width outer footer frame.
- `Width-first morphology now prevents pane-resize flicker in the main adaptive widgets`: the current width-sensitive widgets (`SwapWidget`, `WalletWidget`, `GovernanceWidget`, `StatisticsWidget`, `AutomationWidget`, `WikiWidget`, and `LogWidget`) now switch their structural compact/dense modes from width thresholds only rather than coupling width and height in the same breakpoint, which removes oscillation during width-resize feedback loops where one layout shape was changing the measured height used to select the next shape.
- `Reserved-lane specs now define the shipped header/footer/sidebar widget composition`: `SettingsWidget` moved into `src/lib/widgets/`, `SidebarPanel` now hosts `AccountWidget` plus `SettingsWidget` according to `RESERVED_LANE_SPECS`, and `AccountWidget` fully replaces the old shell popover account flow. The lane specs now also carry explicit desktop/mobile widget mappings plus `userMutable: false`, which fixes the rule that reserved-lane widgets are developer-configured rather than user-reorderable. `AccountChip` moved into `src/lib/widgets/` as the sole header-lane sidebar toggle and is now part of the same lane-spec contract as the other reserved-lane widgets, the center tile tree is now explicitly capped at `4 x 4` capacity (`16` leaves) so future growth stays bounded, and the root barrel now keeps widget exports out of the layout section so the `widgets` vs `layout` split stays structurally honest.
- `Layout now owns the active reserved edge lanes while preserving mobile ribbon morphology`: the route shell now mounts a dedicated `WorkspaceFrame`, `AppHeader` / `AppFooter` plus the first `SidebarPanel` all live directly under `src/lib/layout/`, `AccountChip` now lives under `src/lib/widgets/` as the sole sidebar toggle surface in the header lane, `AccountWidget` now lives in the sidebar lane instead of behind a shell popover, and the temporary `src/lib/chrome/` wrappers are gone. The web-client docs/backlog now describe those surfaces as reserved edge lanes around the center tile tree rather than as a separate shell subsystem, while keeping the key behavioral distinction intact: edge lanes are still not tabbed panes, future sidebars should follow the same reserved-lane model, and the mobile-width linear ribbon still applies only to the center tile tree rather than flattening the whole frame.
- `WalletWidget and SwapWidget now surface clearer pre-submit execution guidance`: the wallet send form now shows inline guidance for missing signer setup, invalid TMCTOL recipient addresses, invalid amounts, and insufficient balance instead of relying only on the disabled primary-button label, and native sends near full balance now also warn that some balance must remain for fees because the live transfer path is keep-alive based. `SwapWidget` now adds an explicit warning when no signer is available instead of leaving that state visible only in the disabled submit button, and native near-max sell attempts now also warn that some native balance must remain for fees. `WalletWidget` now also labels the selected account as coming from the sidebar account widget rather than from an older header-auth flow. Together these changes make the early execution surfaces more legible and more honest during live use without changing the bounded transfer/swap contracts.
- `Premature performance work is now explicitly fenced off in durable context`: `AGENTS.md` now states a `Premature Optimization Discipline` rule so future work prioritizes business-facing correctness, product flows, and contract honesty over speculative lazy-loading, bundle-shaping, or similar micro-optimizations while the project is still raw. The TMCTOL alignment dead-end ledger also records this pattern explicitly so failed performance indirection experiments are remembered as coordination memory rather than repeated later.
- `Bundle-honesty tooling and entrypoint docs got sharper together`: `AGENTS.md` now frames TMCTOL as `Standard + Reference Stack` instead of collapsing the framework layer into `/template`, moves project topology and support priority ahead of deep architecture detail, distinguishes stable engineering architecture from current launch-line constraints, renames the Rust-only style section more honestly, removes the stale monorepo version literal from subsystem-versioning guidance, and aligns validation/pre-task routing with the real `/docs -> /template -> /web-client -> /scripts` maintenance order while keeping `/simulator` authoritative for tokenomic and invariant changes. `README.md` now mirrors that same repository posture by describing the full reference stack, reframing project structure as project topology with active support priority, making the documentation hub the first development entrypoint, and surfacing `Node.js` plus the `web-client` dev path in getting-started. `docs/README.md` now states explicitly that the docs hub is the current conceptual control plane for day-to-day work, orders local tooling in the same runtime-kernel -> reference-client -> automation sequence, and keeps the client/tooling descriptions consistent with that stack. `template/README.md` now treats `/template` as the runtime kernel inside that wider reference stack and demotes `/simulator` to the math-reference role it still owns for tokenomic changes, while `web-client/README.md` and `scripts/README.md` now describe themselves more honestly as downstream client and automation layers rather than protocol-truth owners. `scripts/10-web-client-bundle-report.sh` now prints both inbound importer chains and outbound static/dynamic dependencies for the top emitted client assets, which exposed that `tmctol.ts` still owns the descriptor/runtime dynamic fan-out and that extra async indirection around `.papi/descriptors` does not materially slim the emitted `tmctol` chunk. `bootstrap-client.ts` also dropped an unnecessary `marketStore` dependency that had been used only to stamp log-step metadata during startup, keeping the bootstrap path slightly cleaner even though the measured `tmctol` hotspot itself remains.
- `StatisticsWidget, AutomationWidget, WikiWidget, LogWidget, StatusWidget, GovernanceWidget, ChartWidget, SwapWidget, and WalletWidget now read and resize more honestly`: the statistics surface now tracks viewport size directly, collapses protocol/liquidity stat grids more aggressively in dense panes, stacks the lower route/liquidity split instead of assuming one roomy desktop matrix at every size, and now labels its bounded protocol/liquidity projections separately instead of only badging the session-derived trace/router sections, `AutomationWidget` now reads actor health through the system adapter and labels that panel as a bounded client projection over multiple storage reads instead of a raw storage surface, `WikiWidget` now uses the shared panel chrome plus a pane-aware one-vs-two-column docs-card layout instead of a one-off static list, `LogWidget` now collapses receipt rows and grouped block entries into stacked mobile-style cards in narrow panes instead of preserving one desktop timeline shape, the footer-hosted `StatusWidget` in what is now the reserved footer lane at that stage used a denser 2-up presentation before the later minimal-strip refactor, `GovernanceWidget` now marks its active-proposals panel as the bounded client projection it actually is instead of only labeling the recent-finalized side while recent-finalized retention itself is now honestly tagged as a governance runtime view, governance write failures now persist through the store/widget/log path instead of being wiped by the next refresh or disappearing into console-only noise, and governance refresh failures now clear stale canonical-chain proposal/runtime-view panels instead of leaving outdated data visible under an error banner, `ChartWidget` now uses the shared panel header/provenance chrome instead of a one-off top strip, and both `SwapWidget` and `WalletWidget` now keep max-fill form inputs at cleaner bounded precision while `WalletWidget` also rejects obviously invalid recipient addresses and no longer duplicates successful receipt truth outside the canonical `LogWidget`.
- `Web-client shell and governance/system boundaries got lighter again`: `AppFooter` now lazy-loads `StatusWidget` after first paint behind a compact shell skeleton, `AppHeader` now lazy-loads `AccountChip` instead of statically importing it into the route shell, the footer now reads the live system adapter connection state directly instead of booting governance just to render connection status, governance bootstrap now starts from the governance surfaces rather than the generic client bootstrap/footer path, the governance store now lazy-loads the heavy PAPI provider instead of importing it at module load, has dropped dead provider-mirror fields, and now keeps durable write-error feedback separate from refresh errors, the governance PAPI provider now imports the narrow blockchain signer/connection modules instead of the full blockchain barrel, the blockchain adapter now lazy-loads its `tmctol` connection core internally instead of statically pinning that module through the system/bootstrap path, now reads shared endpoint state instead of importing the full governance store into the system/bootstrap path, and still rebuilds that connection when the settings endpoint changes so the live system adapter no longer stays stuck on the old websocket, the startup bootstrap now waits for the first system refresh before it emits connected/error feedback so the log starts from real chain state, `AutomationWidget` now reads actor health through the live system adapter instead of owning a second direct chain connection, the live `system` slice has dropped stale simulator-era config ghosts and the last exported config wrapper so it now keeps only chain snapshot/connection/refresh state plus one local bootstrap-side foreign-balance seed constant, the current on-chain-first startup contract stays eager because the canonical default workspace opens `Swap`, `Statistics`, and `Automation` immediately, and the measured shared client hotspot dropped from roughly ~259 KiB to ~156 KiB, then ~139 KiB, and now ~122 KiB in the latest bundle reports while the remaining dominant heavy artifacts stay the crypto `verify` chunk and dynamic PAPI metadata chunk.
- `SwapWidget now waits for a live quote before it lets the user sign`: the trade form now treats live quote readiness as part of the execution contract, blocks submission while a quote is still loading or unavailable, validates slippage bounds explicitly instead of silently falling back, removes the dishonest local-config fee fallback from the quote panel, and shows both input-fee and minimum-receive summaries before submission in regular and compact panes.
- `WalletWidget now discovers and transfers the bounded tracked-asset set for the selected account`: the blockchain adapter now reads the current bounded `AxialRouter.TrackedAssets` set plus the primary route asset, resolves live balances and metadata for the selected wallet, `portfolioStore` now keeps that broader bounded asset projection with explicit primary-route semantics, and `WalletWidget` now surfaces those additional tracked assets in both its known-assets panel and the live send surface instead of pretending the wallet only knows about Native plus one primary route asset.
- `Web-client footer status and statistics split landed`: the compact `StatusWidget` moved out of the pane tree into what is now the reserved footer lane so chain/session/account/finalized-block state stays persistently visible without consuming a resizable pane, the old status panel atom was retired in favor of the new `statistics` panel atom with legacy layout normalization from `status -> statistics`, the canonical desktop/tablet workspace now uses `Swap | Wallet` over `Log | Statistics`, and the new `StatisticsWidget` now owns protocol snapshot metrics plus recent system traces/gauges/sparklines instead of overloading the old status surface.
- `Web-client now carries explicit read-model provenance in the browser`: the new `web-client/src/lib/shared/read-model.ts` layer now preserves the project-wide two-class `canonical-chain` vs `materialized` contract while separately tagging client realization (`direct`, `session-cache`, `session-derived`, `provider`), `marketStore` now marks both the live quote path and chart history as canonical direct/runtime-view vs bounded recent chain sample surfaces with a request guard against stale quote overwrites, `logStore` now marks the network panel as a live session feed built from finalized events, `portfolioStore` now marks known assets as a bounded client projection over the live snapshot plus balances, governance recent-finalized retention now carries canonical provenance in blockchain mode, the visible `Swap` / `Chart` / `Log` / `Wallet` / `Governance` surfaces now expose that distinction through the shared `src/lib/shared/ui/ReadModelBadge.svelte` primitive instead of ad-hoc widget-local pills, the governance viewer store no longer carries dormant materialized archive/timeline state or unused proposal-selection scaffolding before a real archive/search surface exists, the route shell dropped an always-true workspace wrapper, and dead governance/system helper methods that no current UI called were removed.
- `Web-client bundle report now explains what the biggest chunks actually are`: `scripts/10-web-client-bundle-report.sh` now annotates the top emitted client assets back to their Vite manifest entries/sources plus static and dynamic manifest importers instead of reporting only raw file sizes, which turns future chunk-weight work into a traceable perf-honesty loop rather than manual manifest archaeology.
- `Web-client startup now defers more runtime/bootstrap weight out of the route shell`: `src/routes/+page.svelte` now lazy-loads the new `src/lib/system/bootstrap-client.ts` bootstrap module on mount instead of statically importing all runtime stores into the route component, `AccountChip.svelte` now reads asset symbols from `portfolioStore` instead of pulling `systemStore` into the header path, the active governance viewer store is blockchain-only, and the measured client route chunk dropped from the previous post-provenance line (~258 KiB) to roughly ~155 KiB while the remaining heavier follow-up hotspot shifted into a shared post-bootstrap `index.svelte` chunk.
- `Web-client portfolio state is now split out of system ownership`: the new top-level `src/lib/portfolio/` slice now owns balances, bounded asset projection, and transfer/deposit bridging, `WalletWidget` plus account/product surfaces now read that slice directly, the old standalone `assets` slice is gone, and `src/lib/system/` is correspondingly narrower around chain snapshot and refresh ownership.
- `Web-client market state is now split out of system ownership`: the new top-level `src/lib/market/` slice now owns swap direction, quote helpers, swap execution orchestration, and live price/history state, `SwapWidget` / `ChartWidget` / market-aware status surfaces now read that slice directly, and `src/lib/system/` is correspondingly narrower around chain snapshot, balances, refresh ownership, and transfer bridging.
- `Web-client system/log state is now less entangled`: account log history, live network-feed state, and shared tx-progress / receipt state were extracted into the new top-level `src/lib/log/` slice, `LogWidget` plus the action widgets now read/write that slice directly, and `src/lib/system/` is correspondingly narrower around snapshot, balances, quotes, trade direction, and live market history.
- `Web-client src/lib no longer hides stateful slices under an entities umbrella`: `assets`, `governance`, `layout`, `system`, and `wallet` now live directly under `src/lib/`, import surfaces and docs were retargeted accordingly, and the workspace/layout subsystem now reads more honestly as a first-class peer of chrome, adapters, shared UI, and widgets instead of pretending every stateful slice is the same kind of entity.
- `Organic customization is now explicit in both web-client docs and layout behavior`: the web-client architecture/README/backlog now lock the rule that widgets and layout must adapt to arbitrary pane sizes, the root tile tree now collapses into a vertical ribbon/feed only on mobile-width viewports instead of clinging to desktop splits, tablet/desktop preserves the user-customizable split-tree workspace, the canonical default desktop/tablet tree is now fixed in code as `Swap | Wallet` over `Log | Status` on the left plus `Chart | Automation | Governance | Wiki` on the right, that arrangement now lives in the named `src/lib/layout/default-layout.ts` spec instead of ad-hoc store assembly, legacy persisted copies of the previous shipped default auto-migrate to that new canonical arrangement, pane tab bars now stay usable inside tighter widths, shared panel/section/stat chrome wraps more honestly in narrow panes, `ChartWidget` now compresses its chrome and surfaces the latest visible series values in tight panes, `SwapWidget` now collapses into a denser pane-aware trade form instead of preserving one roomy desktop arrangement, `WalletWidget` now shortens account chrome and turns asset selection into a ribbon in tight panes, `GovernanceWidget` now collapses proposal internals into a denser vote-ready card in tight panes, `AutomationWidget` now collapses actor details into denser summary blocks in tight panes, and stale widget copy was retargeted from the removed Activity surface to the canonical Log surface so user-driven resizing/dragging can morph the workspace into new readable forms instead of breaking it.
- `LogWidget now owns execution feedback and network history`: the standalone `TransactionActivityWidget` was removed, shared tx receipt/finalization feedback moved into `LogWidget` account mode, the same widget now exposes a live `network` mode backed by finalized `System.Events` captured during the current session, and legacy persisted layouts normalize the retired `activity` panel atom back into `log` on load.
- `Web-client shared UI-kit extraction started in earnest`: repeated section/stat/notice/row patterns were extracted into `web-client/src/lib/shared/ui/` as `SectionCard`, `StatCard`, `DetailRow`, `Notice`, and `SelectableTile`; the next layer now also wraps Bits-UI 2 primitives behind project-local `Button`, `TextField`, `IconButton`, `SidePanelDialog`, `PopoverPanel`, and `PanelHeader` components; those pieces are already consumed by header/settings/wallet/governance/log/automation/swap surfaces, `AppHeader` now lazy-loads the heavier account-menu and settings chrome on demand, the layout host now lazy-loads panel widgets by tab id, and the client now has a clearer reusable UI-kit layer instead of duplicating local micro-UI inside widgets.
- `Web-client widgets now bias harder toward product-facing domain surfaces`: `SwapWidget` dropped the signer-card and developer-heavy compare/status slabs in favor of a smaller trade-core surface, `StatusWidget` dropped address/endpoint/bucket/buffer detail slabs in favor of top-line chain/protocol state, `GovernanceWidget` dropped the internal GovXP/policy/counter overview so the widget now centers proposals, voting, and recent outcomes, `LogWidget` now owns both current-account receipt/history and network-event history instead of splitting those concerns across separate surfaces, `AutomationWidget` now shows only actor health, trigger, last cycle, and native balance instead of operator-heavy runtime internals, `ChartWidget` dropped the supply overlay/right-axis mix so it now reads as a cleaner price-history surface rather than a mixed metric dashboard, and the layout/runtime vocabulary now uses honest `status` / `automation` panel atoms with legacy persisted layouts normalized on load.
- `Web-client header/auth shell and wallet boundaries are now cleaner`: the route shell at that stage delegated titlebar logic to the dedicated header surface that later evolved into today's reserved header lane, account selection/injected-wallet onboarding/dev presets moved out of `WalletWidget` and into the shell-level account surface, `WalletWidget` now focuses on balances plus receive/send for the account already selected in that shell layer, `InfoPanel.svelte` was honestly renamed to `StatusWidget.svelte`, `ConfigPanel` / `WalletChip` were moved out of the widget namespace into the shell-owned settings/account surfaces that later evolved into today's `SettingsWidget` / `AccountChip`, the bounded client-known asset/portfolio projection sourced from the current on-chain snapshot was moved out of hardcoded wallet labels and later evolved into today's dedicated `portfolio` slice, and `SwapWidget` was retuned back to a more compact/responsive 2D layout after the oversized-card regression from the earlier simplification pass.
- `Web-client transaction feedback first converged into one shared surface`: in the earlier phase the workspace introduced a dedicated receipt surface so `SwapWidget` and `WalletWidget` could stop duplicating full receipt/status blocks, `WalletWidget` dropped its stray session/status chrome in favor of the dedicated `Status` surface, and `SwapWidget`, `WalletWidget`, plus `GovernanceWidget` were simultaneously simplified toward coarser internal 2D grid composition instead of long flat stacks; that receipt surface has since been folded into today's canonical `LogWidget`.
- `Web-client UI architecture boundary is now documented and reflected more honestly in code`: `docs/web-client.architecture.en.md` now locks the client vocabulary and postulates (`widgets = economic-functional units`, dynamic tiling = separate layout subsystem, DRY/KISS, 2D grid composition), `web-client/README.md` now surfaces those rules directly, the existing `src/lib/layout/` subsystem is now the only home for `TileContainer`, `PaneHost`, and `SplitHandle`, the misleading `LeafPane` / `ResizeHandle` / `TileContainer` files were removed from `src/lib/widgets/`, and the backlog now tracks the remaining UI cleanup as explicit client-architecture work instead of leaving it as vague discomfort.
- `Web-client wallet/auth execution surface is now materially real`: the browser client now has a dedicated wallet session layer with persisted account selection, Zombienet-oriented Alice/Bob/Charlie/Dave presets, in-browser dev signing for those local identities, injected-wallet account discovery, signer-readiness classification, live native / bounded tracked-asset transfers from `WalletWidget`, shared last-transaction signed/broadcasted/best/finalized status, event counts, decoded dispatch-error text, parsed receipt highlights, clearer injected-vs-local-dev signer onboarding copy, governance vote signing routed through the same wallet layer, lazy-loaded signer bridge/dev-signer codepaths, and a `SwapWidget` that now exposes explicit slippage tolerance, explicit chain-bootstrap diagnostics, runtime minimum-buy enforcement, quote-ready submit gating, minimum-receive preview, and a provisional-best-vs-finalized execution distinction instead of hardcoded execution-only defaults. Live local-network probing also verified that native transfers execute correctly, undersized swaps fail with the real `AmountTooLow` runtime error, properly bootstrapped plus sufficiently large swaps succeed end-to-end on the parachain, `scripts/08-seed-web-client-state.sh` now codifies the minimum local foreign-asset / curve / liquidity bootstrap needed for reproducible wallet+swap testing, `scripts/09-web-client-live-probe.sh` now turns the ad-hoc transfer/swap probes into a repeatable live validation path, and `scripts/10-web-client-bundle-report.sh` now turns bundle-growth observations into a reproducible chunk-size report instead of one-off build-log archaeology.
- `Framework-vs-product boundary is now explicit in the canonical entrypoints`: `README.md`, `docs/README.md`, and `AGENTS.md` now state that TMCTOL is the reusable framework/foundation layer, while actual product value is expected to emerge in downstream forks that add a concrete ecosystem thesis, product philosophy, and live dApps.
- `Alignment ledger query script implemented`: added `get-ledger.sh` to allow agents and developers to effectively query, filter (by date, tag, scope, status), and format the contents of JSONL ledgers compactly, preserving context window.
- `Materialized history chart contract implemented`: added `MaterializedHistoryProvider` in `web-client` to separate bounded on-chain sampling from indexed long-range history, aligning with the project read-model contract.
- `Runtime integration tests stabilized`: updated `next_aaa_id` assertions in `aaa_integration_tests` and `tmctol_integration_tests` to match the current system actor topology (offset shifted from 2005 to 14), restoring 100% test pass rate.
- `Alignment auditor refined`: logged O(N) iteration bans for `try_state`, `on_runtime_upgrade`, and `BoundedVec` as false positives in the hallucination ledger to protect valid Substrate patterns.
- `TMCTOL alignment skill now keeps a memetic memory taxonomy instead of run telemetry`: `.agents/skills/alignment/hallucination_ledger.jsonl` was moved to `.agents/skills/alignment/ledgers/hallucinations.jsonl`, the skill now canonically separates `hallucinations`, `ambiguities`, `dead_ends`, and `boundary_drifts`, `SKILL.md` explains when each ledger should absorb a pattern, `auditor.sh` now hints which memory surface fits each caught rule, `log-ledger.sh` writes explicit high-order records such as the foreground-dev-server trap instead of polluting canonical memory with low-order execution traces, and `AGENTS.md` now lifts the durable-memory-over-telemetry rule plus the foreground-service trap into repo-wide cognitive-infrastructure guidance.
- `Manifesto EN/RU now carries one sharper doctrine layer`: `docs/manifesto.en.md` and `docs/manifesto.ru.md` now frame TMCTOL as a physics-first coordination system, preserve the `L0 / L1 / L2` separation, contrast politics-first vs physics-first DAOs directly, and close on a harder contract/test framing instead of a soft invitation.
- `Governance is now a bounded but explicit domain-scoped contract`: former standalone L2 TOL governance content was consolidated into the canonical governance specification, the live runtime now ships domain-scoped `primary + protection` hierarchy (`$NTVE + $VETO` for protocol governance, `$BLDR + $NTVE` for the canonical tactical domain), Declining Power, explicit `Veto / Pass` protection semantics, bounded proposal lifecycle, proposal-authorship GovXP counters, bounded recent finalized outcomes, exported read views, and a queryable `governance_domain_policy(domain)` surface, with docs/readmes/web-client metadata kept in lockstep.
- `Web-client is now fully on-chain-first`: the local simulator backend was removed from the browser path, governance/DEX/wallet/chart surfaces now consume live on-chain state through PAPI, chart history now bootstraps from bounded recent finalized state, live balances/asset identity/route previews/swap execution align with runtime truth, and the committed metadata/descriptors are refreshed against the runtime query surface.
- `Staking is now live as a multi-asset share-vault with tokenized receipts`: `pallet-staking` is runtime-integrated, `stXXX` receipts are canonical for native/local and foreign staking namespaces, native `$NTVE -> stNTVE` remains the operator-aware security path, legacy position-to-receipt migration exists for live chains, governance-conditioned reward snapshots remain sparse and one-epoch-lagged, same-asset auto-compound claim paths are live, and AAA staking automation mirrors the native vs non-native split honestly.
- `AAA now serves as the hardened launch-time execution platform`: the canonical system actor map, fee-sink role, close-path semantics, scheduler wakeup/storage invariants, telemetry surface, release-gate evidence, and local try-runtime/operator workflows are aligned around the current launch line, with docs/spec/runtime terminology kept in lockstep.
- `VRF/randomness is now crystallized as the bounded launch candidate with explicit research boundaries`: the shipped line includes commit/reveal randomness, bounded reveal committees, delegated-native weighting, processed-block reward routing, observability and emergency runbooks, launch-gate rationale, and concentration/liveness modeling, while threshold/hidden/same-block lines are explicitly kept in research with separate evidence harnesses.
- `Runtime/core integration is consolidated around the TMCTOL launch architecture`: active custom pallets (`aaa`, `axial-router`, `tmc`, `asset-registry`, `staking`, `vrf`), reserve-aware zap policy, canonical total-burned view, hybrid asset registration, token-domain coupling, canonical system AAA IDs, and Polkadot SDK 2512 hygiene now align code, tests, docs, and operator workflows.
- `Repository tooling and documentation now act as explicit cognitive infrastructure`: the scripts layer shares one shell skeleton and common harness, repo-local skills reuse that harness through local bridges, diff-aware validation gates judge changed scope rather than inherited debt, and `README.md`, `docs/README.md`, `AGENTS.md`, `BACKLOG.md`, and `CHANGELOG.md` now operate as one coordinated project-memory system.
- `TMCTOL threshold vocabulary is now stricter in spec, simulator, test mirror, and durable context`: `docs/tmctol.specification.en.md`, `docs/tmctol.specification.ru.md`, `simulator/tests.js`, `simulator/tests.md`, and `AGENTS.md` now distinguish `Elasticity Inversion Threshold`, `Relative Compression Parity Threshold`, `Absolute-Gap Compression`, and `Arbitrage Reversal / Overtake Threshold`, separate burn-time bidirectional compression from expanding-supply threshold analysis, and back the distinction with executable taxonomy coverage plus dual-metric (`C/F` and `C - F`) burn-time compression assertions.

## [1.0.1] - 2025-12-12

### Fixed

#### Documentation Errata (v1)

- `Floor Formula Correction`: Updated floor price formula to correctly reflect constant product invariant ($P_{floor} = k / (R_{native} + S_{sold})^2$) in EN/RU docs
- `Approximation Logic`: Replaced misleading $(1+1.5s)^2$ approximation with formally derived $P_{floor} / P_{ceiling} \approx 1 / (1 + s/a)^2$ where $a$ is floor support fraction
- `Protection Semantics`: Clarified min/max protection scenarios (25% = all buckets supporting, 11% = only Bucket_A supporting) correcting inverted definitions
- `Artifact Removal`: Cleaned up stray text in L2 TOL documentation and duplicate entries in Test Mirror

#### Semantic Clarification

- `Scenario Definitions`: Renamed scenarios to "User Exit" and "System Exit" to reflect actor behavior rather than emotional states
- `Supply Logic`: Explicitly defined "33% Sold" as total user exit and "66% Sold" as catastrophic failure requiring treasury leakage
- `Dependency Clarity`: Added explicit $a=33\%$ assumption to scenario tables

#### Mechanism Formalization

- `Ratchet Effect`: Replaced vague proportional claims with rigorous derivation showing how supply contraction reduces max potential pool balance, forcing floor elevation ($P'_{floor} > P_{floor}$)
- `Bidirectional Compression`: Explicitly defined the simultaneous lowering of Ceiling (via curve) and raising of Floor (via burn)

#### Equilibrium Logic

- `Dimensional Correction`: Fixed $P_{eq}$ formula to $P \approx \sqrt{R_{foreign} \cdot m}$, resolving unit mismatch ($[Price] = \sqrt{[Foreign] \cdot [Foreign/Native^2]}$)
- `Backing Definition`: Defined Equilibrium as the point of 100% Foreign Reserve backing for the curve-implied capitalization

#### Verification & Testing

- `Floor Logic Validation`: Added Test 41 (`Floor Formula & Scenario Verification`) confirming simulator behavior matches theoretical floor derivation ($P_{floor} = k/(R+S)^2$)
- `Scenario Ratio Check`: Verified that the floor/ceiling ratio scales correctly with support fraction ($1/(1+s/a)^2$ approximation verified within 0.5% tolerance)
- `Test Synchronization`: Updated `simulator/tests.md` mirror to include new validation coverage

---

## [1.0.0] - 2025-11-01

### Added

#### Core Framework

- `Consolidated Specification`: Unified `tmctol.specification.en.md` integrating Foundation → Architecture → Mathematics → Economics → Dynamics → Implementation → Trade-offs
- `L2 TOL Governance`: Constant protection model with declining voting power (10x → 1x linear decay for direct holders)
- `Axial Router Specification`: Comprehensive routing gateway with fee burning and price discovery
- `Emergent Properties Documentation`: Bootstrap gravity well (~15% TOL/market-cap stability threshold), supply elasticity inversion, price ratchet effect
- `Dimensional Type System`: Physical types encoding units (`Price [Foreign/Native]`, `Slope [Foreign/Native²]`) preventing categorical errors

#### Mathematical Guarantees

- `Fee Consistency Principle`: XYK quote and execution apply fees identically using "fee on input" model (amount_in × (1 - fee)); prevents routing logic breakage when fees activated from default zero
- `Fair Rounding Strategy`: Largest remainder method eliminates systematic distribution bias; remainder allocated to party with maximum fractional part
- `Internal Slippage Protection`: Fee manager foreign→native conversion uses 10% slippage tolerance; prevents price manipulation attacks on burn mechanism with graceful degradation (buffering)
- `XYK Constant Product Necessity`: Validated that constant product (x·y=k) guarantees liquidity at all price levels; XYK "inefficiency" is precisely its strength for floor protection

#### Testing & Validation

- `Simulator as Ground Truth`: JavaScript/BigInt formal verification environment defining implementation truth (54 comprehensive tests)
- `Structural Symmetry Testing`: RADB (Recursive Abstraction Decomposition) test organization mirroring system architecture
- `Economic Attack Simulation`: Governance resilience, cross-chain independence, sandwich attack resistance scenarios
- `Legitimacy Phase Documentation`: Academic rigor with conditional guarantees throughout entire specification

#### Architectural Patterns

- `Architectural Refactoring Patterns`: New section 6.9 in AGENTS.md documenting symmetry violations, rounding bias, YAGNI in architecture, state ownership questions, distribute-collect anti-pattern, return value redundancy, and single source of truth
- `Buffer Ownership Clarity`: Documented principle that buffers live where concepts live—Tol buffer (awaiting zap) vs Bucket LP (owned liquidity)
- `Method Return Minimalism`: Principle that methods should return only what callers actually use; side-effect methods returning detailed decomposition create redundancy when state is directly queryable
- `Layered Abstraction Insights`: Cognitive scaffolding patterns for progressive comprehension

---

_Changelog maintained according to [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) standards._
